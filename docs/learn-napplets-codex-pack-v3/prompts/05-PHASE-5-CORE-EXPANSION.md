# Codex Prompt — Phase 5: Core Course and Workbench Expansion

Execute only Phase 5.

Add:

- cast and mental model;
- optional Nostr primer;
- identity and distribution;
- Architecture Atlas;
- Manifest Identity Lab;
- expanded Workbench panes;
- broader deterministic faults;
- first browser-safe conformance path.

Implement the `verified-loader` host profile only if Phase 0/ADR evidence permits it. Otherwise document the blocker and build a clearly labeled fixture walkthrough without claiming verified loading.

Use real hashes and current source-defined verification steps. Do not invent a resolution for manifest identity or metadata drift.

Run source, loader/fixture, browser, accessibility, and conformance tests.

Stop after the Phase 5 PR.
