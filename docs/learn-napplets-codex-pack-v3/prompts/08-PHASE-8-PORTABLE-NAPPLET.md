# Codex Prompt — Phase 8: Optional Portable Napplet Target

Execute this phase only when accepted ADR 0007 approves a Workbench, selected-course, or full-course napplet.

Implement only the approved scope:

- separate guest entry;
- current verified artifact contract;
- no teaching-host imports;
- optional capability feature gating;
- embedded approved content/tools;
- session fallback;
- external-host composition for real sibling labs;
- bundle/startup/accessibility budgets;
- compatible-runtime test;
- content/source parity;
- publication metadata and release process.

Document limitations and tested source/runtime revisions.

Stop after the Phase 8 PR.
