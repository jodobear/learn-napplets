# Codex Prompt — Phase 4: Real End-to-End Vertical Slice

Execute only Phase 4.

Build the content MVP with actual napplet mechanics:

- homepage;
- why napplets;
- sandbox boundary;
- one request;
- capabilities;
- glossary essentials;
- source/status baseline.

Implement:

- the minimal production `boundary-harness` teaching host approved by ADR;
- one focused note-tool lab napplet, preferably the composer selected by Phase 0;
- actual current sandbox/loading subset;
- actual pre-script domain injection;
- actual `postMessage` and source-window mapping;
- deterministic selected-domain service;
- actual envelope/timeline observer;
- capability removal and failure path.

Build Monolith Splitter, Threat Lens, Envelope Journey, and Capability Lab. The Envelope Journey and Capability Lab must be driven by the real lab napplet, not a duplicate simulation. Do not invent a teaching-only protocol domain when the preferred composer operation is blocked; use the approved current fallback operation and record the limitation.

Display evidence class, maturity, source revision, and host profile.

Run content, browser integration, accessibility, architecture, and example-build tests.

Stop after the Phase 4 PR.
