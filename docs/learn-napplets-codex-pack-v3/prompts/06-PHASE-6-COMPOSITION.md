# Codex Prompt — Phase 6: Composition, Patterns, and Application Design

Execute only Phase 6.

First refresh current intent, archetype, convention, manifest, and catalog sources.

Then:

- build two focused note-viewer napplets;
- implement only the resolved composition-host subset;
- build Composition Lab;
- build pattern gallery and anti-pattern catalog;
- build Architecture Clinic;
- add boundary/capability assessments;
- attempt independent-runtime interoperability.

Real sibling napplets are hosted and selected by the teaching host. Do not present private nesting as canonical composition.

Where current sources conflict, expose the conflict rather than inventing behavior.

Stop after the Phase 6 PR.
