---
phase: 01-research-and-truth-baseline
plan: 30
type: execute
wave: 2
depends_on: [01-29]
files_modified: []
autonomous: false
gap_closure: true
requirements: [EVID-01, EVID-03, EVID-04, OPER-03]
must_haves:
  truths:
    - "A designated human reviewer, not automation, records six distinct dated role determinations—product, protocol/technical, security, accessibility, content/learning, and release—for every proposed evidence-intake scope, even when one person fills all roles (D-27, D-28)."
    - "Each determination has a role-specific decision, actor, RFC 3339 timestamp, scoped subject/evidence IDs, impact map, and either evidence references or an explicitly scoped dated N/A rationale; records cannot be merged or inferred."
    - "Missing, merged, stale, or scope-mismatched determinations preserve the affected evidence as impact-scoped blocked material."
  artifacts:
    - path: .planning/research/reports/upstream-acquisition-20260728.md
      provides: "Evidence packet reviewed before canonical source ingestion"
    - path: .planning/research/phase-governance.yaml
      provides: "Six separate dated authority-determination records consumed fail-closed by Plan 01-31"
  key_links:
    - from: acquisition receipt and queue
      to: subsequent canonical SRC and compatibility records
      via: "six independent dated role determinations keyed to subject, evidence IDs, freshness, and impact scope"
  prohibitions:
    - statement: "Human review does not accept an ADR, residual risk, package install, production scaffold, or Phase 2 transition."
      category: authority
      status: unresolved
      flagged: true
      verification: "The reviewer supplies only classifications or corrections for later evidence records."
---

<objective>
Obtain the required human authority boundary for canonical source ingestion after public read-only collection.

Purpose: Honor D-15, D-25, D-27, and D-28 so automation does not authenticate its own upstream or package conclusions.
Output: A clear reviewer response that the next plan can apply or preserve as blockers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/research/reports/upstream-acquisition-20260728.md
@.planning/research/upstream-acquisition-queue.yaml
@.planning/research/reports/upstream-refresh-synthesis-2026-07-28.md
@.planning/research/source-registry.yaml
@.planning/research/candidate-source-manifest.yaml
@.planning/adr/0010-package-versioning.md
</context>

<tasks>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Review authority classifications for acquired upstream and package evidence</name>
  <read_first>.planning/research/reports/upstream-acquisition-20260728.md; .planning/research/upstream-acquisition-queue.yaml; upstream-refresh-synthesis-2026-07-28.md; source-registry.yaml; claims.yaml; compatibility-matrix.yaml; package-map.md; 0010-package-versioning.md</read_first>
  <action>Present the acquired immutable locators and collector receipts for review per D-27 and D-28. Require six separate dated determinations, one each for `product`, `protocolTechnical`, `security`, `accessibility`, `contentLearning`, and `release`; the same person may sign all six but no one record, copied date, or generic approval may satisfy another role. Each determination must contain `role`, `decision` (`approved`, `blocked`, `rejected`, or `not-applicable`), `determinedBy`, RFC 3339 `determinedAt`, nonempty `subjectIds`, nonempty `evidenceIds`, nonempty `impact` with affected requirements/phases/ADRs/lessons, `freshnessCheckedAt`, and either nonempty `evidenceRefs` or a `notApplicableRationale`. A `not-applicable` decision additionally requires a role-specific bounded `notApplicableScope`; it is never a blanket approval. Require the decision scope to name the candidate/source purpose and authority class. Do not infer, merge, clone, or repair a classification that the reviewer has not supplied. Plan 01-31 alone records the determinations in the versioned governance contract and fails closed on absent, merged, stale, or scope-mismatched records.</action>
  <what-built>Public read-only acquisition has recorded candidate identity and immutable repository bytes for `kehto/web`, `napplet/web`, and `napplet/naps`; the receipt distinguishes the eight merge observations from missing normative protocol and published-package evidence.</what-built>
  <how-to-verify>
1. For every proposed `kehto/web`, `napplet/web`, and `napplet/naps` item, write six separate dated determinations using the exact fields in Task 1, including a role-specific scoped dated N/A determination where a role has no applicable decision.
2. For the protocol/technical determination, classify each candidate only as observed implementation/release, authoritative specification/governance for its named purpose, or insufficient/unavailable and blocked; reject PR narrative or merged implementation as normative authority.
3. For product, security, accessibility, content/learning, and release separately, record the bounded impact and decision for the same candidate/evidence scope; do not merge their evidence, rationale, timestamp, or approval into protocol/technical.
4. Confirm whether public registry metadata is enough to record a package artifact as observed release evidence only. Do not approve package installation or conformance execution here.
5. Verify every record retains proposed ADRs, Firefox limitations, deterministic fallback, and the no-scaffold boundary.
  </how-to-verify>
  <acceptance_criteria>For every intake scope, six distinct records contain the exact required fields and individual RFC 3339 timestamps; a generic, merged, undated, stale, or scope-free response cannot satisfy any role. Any missing determination remains an explicit impact-scoped blocker rather than assumed approval.</acceptance_criteria>
  <verify><automated>test -s .planning/research/reports/upstream-acquisition-20260728.md && test -s .planning/research/upstream-acquisition-queue.yaml && tools/phase1-python tools/validate-research.py validate-governance .planning/research/phase-governance.yaml</automated></verify>
  <done>A dated six-role response identifies each candidate as limited observed evidence, qualified authority for its named purpose, scoped N/A, or blocked; no candidate is silently approved.</done>
  <resume-signal>Type `approved classifications` with any corrections, or describe which candidates remain blocked.</resume-signal>
</task>
</tasks>

## Artifacts this phase produces
- No new implementation artifact; this blocking review records authority input consumed by Plan 01-31.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| collected repository bytes → authority classification | Immutable code bytes do not prove the repository is normative for a protocol claim. |
| package metadata → package admission | Public metadata alone must not authorize execution or dependency use. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-30-01 | Spoofing | repository-purpose authority | high | mitigate | Blocking reviewer classifies each purpose before canonical use. |
| T-30-02 | Tampering | source-to-protocol inference | high | mitigate | Reviewer rejects PR prose and observed implementation as normative proof. |
| T-30-03 | Elevation of Privilege | package admission | high | mitigate | Review explicitly excludes installation and execution authorization. |
| T-30-04 | Repudiation | approval boundary | high | mitigate | Dated, role-specific response is required; absent response remains blocked. |
</threat_model>

<verification>
The executor must halt at this checkpoint until a reviewer supplies the resume signal and any corrections.
</verification>

<success_criteria>
Canonical ingestion can rely on explicit human classification or preserves an explicit blocker; neither outcome advances an ADR or phase transition.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-30-SUMMARY.md` when done.
</output>
