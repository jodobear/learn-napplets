---
phase: 01-research-and-truth-baseline
plan: 30
type: execute
wave: 4
depends_on: [01-45]
files_modified:
  - .planning/research/authority-determinations.yaml
  - .planning/research/authority-determinations.receipt.yaml
autonomous: false
gap_closure: true
requirements: [EVID-01, EVID-03, EVID-04, OPER-03]
must_haves:
  truths:
    - "A designated human reviewer, not automation, persists six distinct dated role determinations—product, protocol/technical, security, accessibility, content/learning, and release—for every proposed evidence-intake scope, even when one person fills all roles (D-27, D-28)."
    - "Every scope has exactly six unique `(scopeId, role)` decisions, each with its role-specific decision, actor, RFC 3339 timestamp, subject/evidence IDs, receipt-bound impact map, and evidence reference or bounded dated N/A rationale."
    - "A separate immutable receipt records the exact authority-artifact SHA-256, acquisition-receipt SHA-256, scope IDs, and reviewer identity; Plan 01-31 rejects an altered or scope-mismatched artifact."
    - "Before human review, the acquisition queue and receipt pass `validate-reviewed-acquisition` and expose the exact Plan 01-29 reviewed commit and four-report SHA-256 binding; the reviewer receives no unbound refresh input."
    - "Missing, merged, stale, or scope-mismatched determinations preserve only the affected evidence as impact-scoped blocked material."
  artifacts:
    - path: .planning/research/reports/upstream-acquisition-20260728.md
      provides: "Evidence packet with a Plan 01-29 reviewed-source binding, reviewed before canonical source ingestion"
    - path: .planning/research/authority-determinations.yaml
      provides: "Human-authored per-intake-scope authority decisions with exactly six unique roles per scope"
    - path: .planning/research/authority-determinations.receipt.yaml
      provides: "Digest-bound handoff that Plan 01-31 validates before canonical intake"
  key_links:
    - from: Plan 01-29 reviewed source-input map through Plan 01-45's validated acquisition receipt and queue
      to: authority-determinations receipt and subsequent canonical SRC and compatibility records
      via: "same reviewed commit/four-report digest binding, receipt SHA-256, and six independent dated role determinations per declared scope"
  prohibitions:
    - statement: "Human review does not accept an ADR, residual risk, package install, production scaffold, or Phase 2 transition."
      category: authority
      status: unresolved
      flagged: true
      verification: "The reviewer supplies only classifications or corrections for later evidence records."
---

<objective>
Obtain the required human authority boundary for canonical source ingestion after public read-only collection.

Purpose: Honor D-15, D-25, D-27, and D-28 so automation does not authenticate its own upstream or package conclusions.
Output: A clear reviewer response that the next plan can apply or preserve as blockers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/research/reports/upstream-acquisition-20260728.md
@.planning/research/upstream-acquisition-queue.yaml
@.planning/research/source-registry.yaml
@tools/acquire-sources.py
@tools/validate-planning.py
@.planning/research/candidate-source-manifest.yaml
@.planning/adr/0010-package-versioning.md
</context>

<tasks>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Review authority classifications for acquired upstream and package evidence</name>
  <read_first>01-29-SUMMARY.md; 01-REVIEWS.md; tools/validate-planning.py; tools/acquire-sources.py; .planning/research/reports/upstream-acquisition-20260728.md; .planning/research/upstream-acquisition-queue.yaml; source-registry.yaml; claims.yaml; compatibility-matrix.yaml; package-map.md; 0010-package-versioning.md</read_first>
  <action>Addresses Codex HIGH source-grounding finding and the review concern: before presenting any evidence, run Plan 01-29's `--verify-phase1-source-inputs --review .planning/phases/01-research-and-truth-baseline/01-REVIEWS.md` and Plan 01-45's `validate-reviewed-acquisition --queue .planning/research/upstream-acquisition-queue.yaml --receipt .planning/research/reports/upstream-acquisition-20260728.md --review .planning/phases/01-research-and-truth-baseline/01-REVIEWS.md`; halt this checkpoint if either fails. Present only the receipt/queue records whose same reviewed commit and four-report digest map the commands return, never direct refresh-report prose or mutable source data. Persist a digest-bound per-scope authority handoff rather than a conversational response. Present the acquired immutable locators and collector receipts for review per D-27 and D-28. The reviewer authors `.planning/research/authority-determinations.yaml` with named intake scopes; each scope identifies the candidate/source purpose, authority class, nonempty subject/evidence IDs, acquisition-receipt SHA-256, and exactly six role decisions: `product`, `protocolTechnical`, `security`, `accessibility`, `contentLearning`, and `release`. Each unique `(scopeId, role)` decision contains `decision` (`approved`, `blocked`, `rejected`, or `not-applicable`), `determinedBy`, RFC 3339 `determinedAt`, nonempty impact with affected requirements/phases/ADRs/lessons, `freshnessCheckedAt`, and either nonempty evidence references or a role-specific bounded dated N/A rationale. The same person may sign all six roles, but no generic approval, copied decision, or merged record may satisfy more than one role. After authoring, create `.planning/research/authority-determinations.receipt.yaml` recording the exact artifact SHA-256, acquisition receipt SHA-256, sorted scope IDs, reviewer identity, and generated time. Do not infer, merge, clone, or repair a classification that the reviewer has not supplied. Plan 01-31 consumes these exact bytes, not chat text, and fails closed on absent, altered, stale, merged, or scope-mismatched records.</action>
  <what-built>Public read-only acquisition has recorded candidate identity and immutable repository bytes for `kehto/web`, `napplet/web`, and `napplet/naps`; a verified receipt distinguishes the complete bound observation window from missing normative protocol and published-package evidence.</what-built>
  <how-to-verify>
1. For every proposed `kehto/web`, `napplet/web`, and `napplet/naps` item, write six separate dated determinations using the exact fields in Task 1, including a role-specific scoped dated N/A determination where a role has no applicable decision.
2. For the protocol/technical determination, classify each candidate only as observed implementation/release, authoritative specification/governance for its named purpose, or insufficient/unavailable and blocked; reject PR narrative or merged implementation as normative authority.
3. For product, security, accessibility, content/learning, and release separately, record the bounded impact and decision for the same candidate/evidence scope; do not merge their evidence, rationale, timestamp, or approval into protocol/technical.
4. Confirm whether public registry metadata is enough to record a package artifact as observed release evidence only. Do not approve package installation or conformance execution here.
5. Verify every record retains proposed ADRs, Firefox limitations, deterministic fallback, and the no-scaffold boundary.
  </how-to-verify>
  <acceptance_criteria>For every declared intake scope, the canonical artifact contains exactly six distinct `(scopeId, role)` records with required fields and individual RFC 3339 timestamps; its digest and acquisition-receipt binding match the immutable receipt. The receipt's reviewed commit and four-report digest map must first pass Plan 01-29/01-45 validation. A generic, merged, undated, stale, altered, scope-free, or source-unbound response cannot satisfy any role. Any missing determination remains an explicit impact-scoped blocker rather than assumed approval.</acceptance_criteria>
  <verify><automated>tools/phase1-python tools/validate-planning.py --verify-phase1-source-inputs --review .planning/phases/01-research-and-truth-baseline/01-REVIEWS.md && tools/phase1-python tools/acquire-sources.py validate-reviewed-acquisition --queue .planning/research/upstream-acquisition-queue.yaml --receipt .planning/research/reports/upstream-acquisition-20260728.md --review .planning/phases/01-research-and-truth-baseline/01-REVIEWS.md && test -s .planning/research/authority-determinations.yaml && test -s .planning/research/authority-determinations.receipt.yaml && sha256sum .planning/research/authority-determinations.yaml</automated></verify>
  <done>A digest-bound, human-authored six-role decision set exists for every declared scope; no candidate is silently approved.</done>
  <resume-signal>Type `approved classifications` with any corrections, or describe which candidates remain blocked.</resume-signal>
</task>
</tasks>

## Artifacts this phase produces
- `.planning/research/authority-determinations.yaml` — per-scope six-role human authority record.
- `.planning/research/authority-determinations.receipt.yaml` — exact artifact/acquisition digest handoff consumed by Plan 01-31.

## Created-symbol inventory
- `authorityDeterminations[].scopeId` — stable intake-scope identifier.
- `authorityDeterminations[].determinations[]` — six-role decision set unique on `(scopeId, role)`.
- `authority-determinations.receipt.yaml.authorityArtifactSha256` — exact-byte binding for Plan 01-31.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| collected repository bytes → authority classification | Immutable code bytes do not prove the repository is normative for a protocol claim. |
| package metadata → package admission | Public metadata alone must not authorize execution or dependency use. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-30-01 | Spoofing | repository-purpose authority | high | mitigate | Blocking reviewer classifies each purpose before canonical use. |
| T-30-02 | Tampering | source-to-protocol inference | high | mitigate | Reviewer rejects PR prose and observed implementation as normative proof. |
| T-30-03 | Elevation of Privilege | package admission | high | mitigate | Review explicitly excludes installation and execution authorization. |
| T-30-04 | Repudiation | approval boundary | high | mitigate | Dated, role-specific response is required; absent response remains blocked. |
| T-30-05 | Tampering | review evidence packet | high | mitigate | Task 1 requires the Plan 01-29 Git-blob source-input verifier and Plan 01-45 receipt/queue binding before human authority work begins. |
</threat_model>

<verification>
The executor must halt at this checkpoint until a reviewer supplies the resume signal and any corrections.
</verification>

<success_criteria>
Canonical ingestion can rely on explicit human classification or preserves an explicit blocker; neither outcome advances an ADR or phase transition.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-30-SUMMARY.md` when done.
</output>
