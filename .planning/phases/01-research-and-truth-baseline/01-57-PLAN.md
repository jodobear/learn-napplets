---
phase: 01-research-and-truth-baseline
plan: 57
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/collect-registry-evidence.py
  - tests/phase1/test_compatibility.py
autonomous: true
gap_closure: true
requirements: [EVID-03, EVID-04, OPER-01]
must_haves:
  truths:
    - "Live registry collection uses one absolute monotonic deadline spanning connection, headers, and every response chunk; repeated progress cannot extend the ten-second total bound."
    - "Each connect or read socket timeout is the smaller of its declared five-second operation cap and the remaining total deadline."
    - "A slow-chunk, stalled-read, elapsed-deadline, redirect, oversized-response, or socket-timeout outcome becomes an impact-scoped blocker with no partial observed-live response."
    - "Receipt limits remain exactly the limits the collector enforces: connect 5 seconds, read 5 seconds, total 10 seconds, and 65,536 response bytes."
  artifacts:
    - path: tools/collect-registry-evidence.py
      provides: "Absolute monotonic deadline and remaining-budget socket timeout enforcement."
      contains: "_remaining_deadline_seconds"
    - path: tests/phase1/test_compatibility.py
      provides: "Deterministic slow-chunk/deadline/timeout and receipt-limit regressions."
      contains: "test_registry_total_deadline_stops_slow_chunks_and_records_enforced_limits"
  key_links:
    - from: "tools/collect-registry-evidence.py::_read_bounded_response"
      to: "time.monotonic"
      via: "One deadline is created before opener.open and checked before/after every blocking operation."
      pattern: "deadline"
    - from: "remaining total budget"
      to: "connect/read socket timeout"
      via: "Each blocking call receives `min(operation_cap, remaining)` rather than a fresh total timeout."
      pattern: "min"
---

<objective>
Enforce the registry receipt's claimed connect, read, and total wall-clock bounds.

Purpose: Prevent a slow response that yields occasional chunks from extending collection beyond the declared ten-second evidence boundary.
Output: Monotonic deadline enforcement and deterministic fake-transport tests with accurate receipt limits.
</objective>

<execution_context>
@/home/at/.codex/gsd-core/workflows/execute-plan.md
@/home/at/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-41-PLAN.md
@tools/collect-registry-evidence.py
@tests/phase1/test_compatibility.py
@tests/phase1/fixtures/registry-response.json
@.planning/research/reports/package-registry-fixture-receipt-20260728.yaml
@.planning/research/reports/package-registry-live-or-blocker-receipt-20260728.yaml
</context>

<scope_boundary>
Change only live registry timeout enforcement and deterministic tests. Preserve the one allowlisted canonical URL, redirect refusal, response-size cap, credential-free public-read gate, immutable exclusive receipt paths, fixture/live/blocker separation, existing receipt field names/values, package non-admission, and all current retained receipts. Tests use fakes; no live registry request, package command, install, import, or external write is authorized.
</scope_boundary>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Enforce one monotonic total deadline across connect and chunk reads</name>
  <read_first>tools/collect-registry-evidence.py timeout constants, `_limits_record`, `_read_bounded_response`, `_live_receipt`, `_blocker_receipt`, and `collect_live_or_blocker`; tests/phase1/test_compatibility.py registry command/tests</read_first>
  <files>tools/collect-registry-evidence.py, tests/phase1/test_compatibility.py</files>
  <behavior>
    - The deadline is `time.monotonic()` plus ten seconds and is created before opening the request.
    - `opener.open` receives at most five seconds and never more than the remaining total budget.
    - Before each `response.read`, the underlying response socket receives at most five seconds and never more than the remaining total budget.
    - The remaining budget is checked before and after every open/read; zero or negative remaining time raises a stable ReceiptError even if the last read returned bytes.
    - A fake response returning sub-limit chunks while advancing the fake monotonic clock beyond ten seconds is refused before an observed-live payload can be returned.
  </behavior>
  <action>Write direct deterministic tests with a fake opener, fake HTTPS response/socket, and injected monotonic clock; no sleep or network is permitted. Import `time` and `socket`. Add `_remaining_deadline_seconds(deadline, monotonic=time.monotonic)` that returns a positive remaining value or raises a stable total-deadline ReceiptError. Create the absolute deadline before `opener.open`, pass `min(CONNECT_TIMEOUT_SECONDS, remaining)` to open, and recheck immediately after it returns. Add a narrow response-socket timeout setter for the stdlib HTTPS response shape; refuse a response whose socket cannot be bounded. Before each chunk read, set `min(READ_TIMEOUT_SECONDS, remaining)` on that socket; after the read, recheck the absolute deadline before retaining the chunk. Translate socket/timeout failures into ReceiptError so `collect_live_or_blocker` produces a blocker. Preserve exact allowlist, redirect, status, and byte-cap checks. Do not reset the deadline when data arrives.</action>
  <verify>
    <automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_compatibility.CompatibilitySchema.test_registry_total_deadline_stops_slow_chunks_and_records_enforced_limits test_compatibility.CompatibilitySchema.test_registry_connect_and_read_timeouts_use_remaining_deadline_budget -v</automated>
  </verify>
  <acceptance_criteria>Fake-clock tests prove a ten-second absolute ceiling despite repeated chunks, and every recorded connect/read timeout equals the applicable operation cap or smaller remaining budget.</acceptance_criteria>
  <done>The registry reader cannot extend its wall-clock budget through per-operation timeout resets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve blocker routing and truthful immutable receipt limits</name>
  <read_first>Task 1 deadline helpers/tests; existing fixture, immutable-attempt, live-unavailable, receipt-rehash, and package-blocker tests; `_limits_record` and `validate_receipt` exact map check</read_first>
  <files>tools/collect-registry-evidence.py, tests/phase1/test_compatibility.py</files>
  <behavior>
    - Deadline and read-timeout failures yield an impact-scoped blocker, not observed-live response bytes.
    - The blocker reason identifies bounded live collection unavailability without claiming a package fact.
    - New fixture/live/blocker receipts retain exact connect=5, read=5, total=10, max-bytes=65,536 fields, and validator mutations of any limit still fail.
    - Existing immutable retained receipts remain valid and byte-unchanged.
  </behavior>
  <action>Add an injected slow-response `collect_live_or_blocker` regression that writes to a fresh repository-confined temporary receipt, then assert impact-scoped blocker classification, absent response, exact affected requirements/ADRs/questions/drift, exact `_limits_record`, and successful receipt revalidation. Record SHA-256 for both retained registry receipts before the test cluster and require exact equality afterward. Run the existing fixture/attempt/live-unavailable/package-history tests. Do not broaden exception handling beyond timeout/transport errors already routed to a blocker, and do not rewrite existing receipt history.</action>
  <verify>
    <automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_compatibility.CompatibilitySchema.test_registry_deadline_failure_routes_impact_scoped_blocker_without_partial_response test_compatibility.CompatibilitySchema.test_registry_collector_fixture_receipt_rehashes_exact_response_bytes test_compatibility.CompatibilitySchema.test_registry_attempt_receipts_are_separate_immutable_and_revalidated test_compatibility.CompatibilitySchema.test_registry_live_unavailable_routes_impact_scoped_blocker -v</automated>
  </verify>
  <acceptance_criteria>Deadline failure creates only a valid blocker receipt with truthful enforced limits; existing receipt validation/history tests pass and both retained receipt files remain byte-identical.</acceptance_criteria>
  <done>Timeout enforcement remains evidence-safe and cannot silently promote partial registry data.</done>
</task>

</tasks>

## Artifacts this phase produces

| Path | New symbol/test | Contract |
|---|---|---|
| `tools/collect-registry-evidence.py` | `_remaining_deadline_seconds` and response-socket timeout helper | Applies one monotonic total deadline and remaining-budget connect/read timeouts. |
| `tests/phase1/test_compatibility.py` | Slow-chunk and remaining-budget tests | Deterministically proves the total budget cannot reset per chunk. |
| `tests/phase1/test_compatibility.py` | Deadline-to-blocker receipt test | Proves timeout yields no partial observed response and retains exact impact/limit fields. |

No retained receipt, package evidence, live response, dependency, external action, or approval record is produced.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| registry socket → bounded collector | A peer can drip chunks inside per-read timeouts and exceed the declared total collection budget. |
| transport failure → evidence receipt | Partial bytes must not become observed package evidence. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-57-01 | Denial of Service | live registry reads | high | mitigate | Enforce one monotonic deadline plus remaining-budget socket timeouts before/after every blocking operation. |
| T-01-57-02 | Repudiation | receipt limits | medium | mitigate | Keep exact limit map and deterministic tests proving each declared value is enforced. |
| T-01-57-03 | Tampering | partial live response classification | medium | mitigate | Deadline/socket failures route only to impact-scoped blocker receipts with no response field. |

No package-manager install occurs; supply-chain package legitimacy gate is not applicable.
</threat_model>

<verification>
Run direct fake-clock deadline tests, blocker-receipt routing, then the complete existing registry receipt/history cluster through `tools/phase1-python`.
</verification>

<success_criteria>
Registry collection cannot exceed its declared absolute deadline through slow chunks, every socket operation uses its remaining budget, timeout produces only a scoped blocker, and immutable receipt limits/history stay truthful and unchanged.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-57-SUMMARY.md` when done.
</output>
