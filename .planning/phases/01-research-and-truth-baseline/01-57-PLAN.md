---
phase: 01-research-and-truth-baseline
plan: 57
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/collect-registry-evidence.py
  - tests/phase1/test_compatibility.py
autonomous: true
gap_closure: true
requirements: [EVID-03, EVID-04, OPER-01]
must_haves:
  truths:
    - "Live registry collection uses one parent-enforced absolute monotonic deadline spanning DNS/connect, TLS, status/header receipt, and every response chunk; repeated socket progress cannot extend the ten-second total bound."
    - "Each connect or read socket timeout is the smaller of its declared five-second operation cap and the remaining total deadline."
    - "The blocking urllib transaction runs in an interruptible isolated worker; at the absolute deadline the parent terminates then kills/reaps it if necessary and accepts no partial payload, so drip-fed headers cannot evade the bound."
    - "A slow-chunk, stalled-read, elapsed-deadline, redirect, oversized-response, or socket-timeout outcome becomes an impact-scoped blocker with no partial observed-live response."
    - "Receipt limits remain exactly the limits the collector enforces: connect 5 seconds, read 5 seconds, total 10 seconds, and 65,536 response bytes."
  artifacts:
    - path: tools/collect-registry-evidence.py
      provides: "Parent-watchdog absolute deadline plus remaining-budget socket timeout enforcement across connect, headers, and body."
      contains: "_remaining_deadline_seconds"
    - path: tests/phase1/test_compatibility.py
      provides: "Deterministic slow-chunk/deadline/timeout and receipt-limit regressions."
      contains: "test_registry_total_deadline_stops_slow_chunks_and_records_enforced_limits"
  key_links:
    - from: "tools/collect-registry-evidence.py::_run_live_worker"
      to: "time.monotonic"
      via: "Parent creates one deadline before worker start and interrupts/reaps the entire urllib transaction when remaining wall-clock budget expires."
      pattern: "deadline"
    - from: "remaining total budget"
      to: "connect/read socket timeout"
      via: "Each blocking call receives `min(operation_cap, remaining)` rather than a fresh total timeout."
      pattern: "min"
---

<objective>
Enforce the registry receipt's claimed connect, read, and total wall-clock bounds.

Purpose: Prevent slow connect, drip-fed status/headers, or occasional body chunks from extending collection beyond the declared ten-second evidence boundary.
Output: Interruptible parent-watchdog deadline enforcement and deterministic slow-header/body fixtures with accurate receipt limits.
</objective>

<execution_context>
@/home/at/.codex/gsd-core/workflows/execute-plan.md
@/home/at/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-41-PLAN.md
@tools/collect-registry-evidence.py
@tests/phase1/test_compatibility.py
@tests/phase1/fixtures/registry-response.json
@.planning/research/reports/package-registry-fixture-receipt-20260728.yaml
@.planning/research/reports/package-registry-live-or-blocker-receipt-20260728.yaml
</context>

<scope_boundary>
Change only live registry timeout enforcement and deterministic tests. Preserve the one allowlisted canonical URL, redirect refusal, response-size cap, credential-free public-read gate, immutable exclusive receipt paths, fixture/live/blocker separation, existing receipt field names/values, package non-admission, and all current retained receipts. Tests use fake openers plus a loopback-only slow-header server; no external registry request, package command, install, import, or external write is authorized.
</scope_boundary>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Enforce one interruptible wall-clock deadline across connect, headers, and body</name>
  <read_first>tools/collect-registry-evidence.py timeout constants, `_limits_record`, `_read_bounded_response`, `_live_receipt`, `_blocker_receipt`, and `collect_live_or_blocker`; tests/phase1/test_compatibility.py registry command/tests</read_first>
  <files>tools/collect-registry-evidence.py, tests/phase1/test_compatibility.py</files>
  <behavior>
    - The parent deadline is `time.monotonic()` plus ten seconds and is created before starting the isolated urllib worker, so process startup, DNS/connect, TLS, status/headers, and body all consume the same budget.
    - Worker `opener.open` and body socket timeouts receive at most five seconds and never more than the remaining budget communicated by the parent start/deadline contract.
    - The parent waits only for the remaining deadline, then sends TERM, waits a short fixed reap grace, sends KILL if needed, waits to reap, closes pipes, and raises one stable total-deadline ReceiptError.
    - Worker output uses a length-delimited success/error envelope emitted only after status, headers, redirect, size, and complete-body validation; EOF, malformed/multiple frames, stderr, signal exit, or partial frame is refusal.
    - A local slow-header fixture sends header bytes frequently enough to stay below a per-socket timeout but beyond a shortened test-only absolute deadline; the parent interrupts/reaps it and no observed-live payload is returned.
    - A slow-body fixture retains the existing chunk-progress case and is refused by the same watchdog.
  </behavior>
  <action>Addresses external Codex review concern: urllib socket timeouts are per operation, so drip-fed headers can keep `opener.open` alive beyond an absolute deadline. Keep deterministic fake-opener tests for timeout values, and move the entire blocking live transaction into an isolated worker process invoked with an argv list through the verified interpreter—never a shell. Parent `_run_live_worker` creates the absolute monotonic deadline before `Popen`, passes fixed URL/limits and remaining budget to a private worker mode, and calls `communicate(timeout=_remaining_deadline_seconds(deadline))`. On timeout, send TERM, wait a short fixed grace, send KILL if still live, wait/reap unconditionally, close pipes, discard all bytes, and raise stable total-deadline ReceiptError; do the same fail-closed cleanup for malformed protocol or unexpected exit. Worker owns DNS/connect/TLS/status/header/body urllib operations, applies `min(CONNECT_TIMEOUT_SECONDS, initial_remaining)` to `opener.open`, applies remaining-budget read socket timeouts when available, validates allowlist/redirect/status/headers/size, and buffers body privately. It writes exactly one length-delimited JSON metadata plus base64/content frame only after complete validation; partial/multiple frames, stderr, nonzero/signal exit, or trailing bytes never become evidence. Production constants remain connect=5, read=5, total=10, max=65,536. Add a test-only constructor/direct-call seam for a shorter absolute deadline that cannot be selected by CLI/env/config. Add a deterministic loopback slow-header fixture whose server accepts one connection then drips incomplete header bytes at intervals below the per-operation cap beyond the shortened absolute deadline; assert worker interruption/reaping, bounded elapsed time, no partial response, and unchanged receipt targets. Retain the slow-body fixture and fake remaining-budget assertions. Preserve exact URL allowlist, redirect, status, credential-free, and byte-cap checks; data arrival never resets parent deadline.</action>
  <verify>
    <automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_compatibility.CompatibilitySchema.test_registry_absolute_deadline_interrupts_slow_headers_and_reaps_worker test_compatibility.CompatibilitySchema.test_registry_total_deadline_stops_slow_chunks_and_records_enforced_limits test_compatibility.CompatibilitySchema.test_registry_connect_and_read_timeouts_use_remaining_deadline_budget test_compatibility.CompatibilitySchema.test_registry_worker_rejects_partial_or_malformed_result_frames -v</automated>
  </verify>
  <acceptance_criteria>Slow-header and slow-body fixtures prove the parent interrupts and reaps the worker at one absolute ceiling despite progress; no partial frame becomes evidence; fake-clock tests prove every connect/read timeout equals the applicable cap or smaller remaining budget.</acceptance_criteria>
  <done>The registry reader cannot extend its wall-clock budget through connect, header, or body progress.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve blocker routing and truthful immutable receipt limits</name>
  <read_first>Task 1 deadline helpers/tests; existing fixture, immutable-attempt, live-unavailable, receipt-rehash, and package-blocker tests; `_limits_record` and `validate_receipt` exact map check</read_first>
  <files>tools/collect-registry-evidence.py, tests/phase1/test_compatibility.py</files>
  <behavior>
    - Deadline and read-timeout failures yield an impact-scoped blocker, not observed-live response bytes.
    - The blocker reason identifies bounded live collection unavailability without claiming a package fact.
    - New fixture/live/blocker receipts retain exact connect=5, read=5, total=10, max-bytes=65,536 fields, and validator mutations of any limit still fail.
    - Existing immutable retained receipts remain valid and byte-unchanged.
  </behavior>
  <action>Add injected slow-header and slow-body `collect_live_or_blocker` regressions that write only to fresh repository-confined temporary receipts, then assert impact-scoped blocker classification, absent response, exact affected requirements/ADRs/questions/drift, exact `_limits_record`, successful receipt revalidation, and no live worker remaining. Record SHA-256 for both retained registry receipts before the test cluster and require exact equality afterward. Run existing fixture/attempt/live-unavailable/package-history tests. Route only the stable worker deadline/transport/protocol failures to blocker; do not accept child partial bytes, broaden evidence classification, or rewrite receipt history.</action>
  <verify>
    <automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_compatibility.CompatibilitySchema.test_registry_slow_header_deadline_routes_blocker_without_partial_response test_compatibility.CompatibilitySchema.test_registry_deadline_failure_routes_impact_scoped_blocker_without_partial_response test_compatibility.CompatibilitySchema.test_registry_collector_fixture_receipt_rehashes_exact_response_bytes test_compatibility.CompatibilitySchema.test_registry_attempt_receipts_are_separate_immutable_and_revalidated test_compatibility.CompatibilitySchema.test_registry_live_unavailable_routes_impact_scoped_blocker -v</automated>
  </verify>
  <acceptance_criteria>Deadline failure creates only a valid blocker receipt with truthful enforced limits; existing receipt validation/history tests pass and both retained receipt files remain byte-identical.</acceptance_criteria>
  <done>Timeout enforcement remains evidence-safe and cannot silently promote partial registry data.</done>
</task>

</tasks>

## Artifacts this phase produces

| Path | New symbol/test | Contract |
|---|---|---|
| `tools/collect-registry-evidence.py` | `_remaining_deadline_seconds` and response-socket timeout helper | Applies one monotonic total deadline and remaining-budget connect/read timeouts. |
| `tools/collect-registry-evidence.py` | `_run_live_worker` and private worker framing mode | Makes the entire urllib connect/header/body transaction interruptible and accepts bytes only after complete validation. |
| `tests/phase1/test_compatibility.py` | Slow-chunk and remaining-budget tests | Deterministically proves the total budget cannot reset per chunk. |
| `tests/phase1/test_compatibility.py` | Slow-header and malformed-frame tests | Proves header progress cannot defeat the watchdog and partial worker output never becomes evidence. |
| `tests/phase1/test_compatibility.py` | Deadline-to-blocker receipt test | Proves timeout yields no partial observed response and retains exact impact/limit fields. |

No retained receipt, package evidence, live response, dependency, external action, or approval record is produced.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| registry socket → bounded collector | A peer can drip connect/header/body bytes inside per-operation timeouts and exceed the declared total collection budget. |
| transport failure → evidence receipt | Partial bytes must not become observed package evidence. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-57-01 | Denial of Service | live registry connect/header/body | high | mitigate | Parent watchdog bounds and interrupts/reaps the whole urllib worker; remaining-budget socket timeouts are defense in depth, not the absolute-deadline mechanism. |
| T-01-57-02 | Repudiation | receipt limits | medium | mitigate | Keep exact limit map and deterministic tests proving each declared value is enforced. |
| T-01-57-03 | Tampering | partial live response classification | medium | mitigate | Deadline/socket failures route only to impact-scoped blocker receipts with no response field. |

No package-manager install occurs; supply-chain package legitimacy gate is not applicable.
</threat_model>

<verification>
Run slow-header/slow-body watchdog, worker-frame, fake-clock remaining-budget, blocker-receipt routing, then the complete existing registry receipt/history cluster through `tools/phase1-python`.
</verification>

<success_criteria>
Registry collection cannot exceed its declared absolute deadline through slow connect, headers, or body chunks; the worker is interrupted/reaped with no partial payload; socket operations use remaining budget; timeout produces only a scoped blocker; immutable receipt limits/history stay truthful and unchanged.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-57-SUMMARY.md` when done.
</output>
