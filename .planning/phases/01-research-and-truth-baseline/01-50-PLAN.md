---
phase: 01-research-and-truth-baseline
plan: 50
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/validate-research.py
  - tests/phase1/test_spike_consolidation.py
  - tests/phase1/test_spikes.py
autonomous: true
gap_closure: true
requirements: [EVID-04, OPER-01]
must_haves:
  truths:
    - "Each retained impact fragment resolves its declared source-registry digest to the current regular registry bytes or the exact bytes of a regular Git blob reachable from repository history; source IDs and immutable fields are validated against that resolved generation per D-13, D-20, and D-23."
    - "A legitimate truth refresh does not require or permit changing the four retained impact-fragment provenance digests."
    - "Consolidation remains idempotent for duplicate fragments and deterministic across input order."
    - "Concurrent, interrupted, semantic-failure, dry-run, duplicate, and conflict paths preserve coherent canonical bytes and explicit failure/review outcomes."
  prohibitions:
    - requirement_id: EVID-04
      category: transparency
      status: unresolved
      verification: null
      statement: "MUST NOT silently rebind retained spike provenance to newer source-registry bytes."
    - requirement_id: OPER-01
      category: transparency
      status: unresolved
      verification: null
      statement: "MUST NOT rewrite accepted evidence or erase historical source identity during refresh or consolidation."
  flagged_assumptions:
    - "EVID-04 unclassified edge probe remains unresolved beyond the exact historical-registry predicate exercised here; no other mandatory-spike semantics are inferred."
  artifacts:
    - path: tools/validate-research.py
      provides: "Exact current-or-Git-historical source-registry generation resolver shared by completed-spike and impact-fragment validation."
      contains: "resolve_retained_source_registry"
    - path: tests/phase1/test_spike_consolidation.py
      provides: "Impact-fragment historical-generation acceptance and rejection regressions plus restored consolidation matrix."
      contains: "test_fragment_accepts_git_reachable_historical_registry_generation"
    - path: tests/phase1/test_spikes.py
      provides: "Completed-spike parity regression for the shared resolver."
      contains: "test_completed_spike_accepts_reachable_historical_registry_digest_only"
  key_links:
    - from: "tools/validate-research.py::validate_impact_fragment"
      to: "tools/validate-research.py::resolve_retained_source_registry"
      via: "sourceLinks and source evidenceLinks resolve and parse the one digest-declared registry generation instead of reopening current bytes."
      pattern: "resolve_retained_source_registry"
    - from: "tools/validate-research.py::discover_consolidation_inputs"
      to: "tools/validate-research.py::validate_impact_fragment"
      via: "Every retained fragment crosses historical provenance validation before merge or journaled publication."
      pattern: "validate_impact_fragment"
---

<objective>
Restore retained impact-fragment provenance after the legitimate truth refresh without altering historical evidence.

Purpose: Close IMP011/IMP016 at their root cause so EVID-04 spike evidence can be revalidated and consolidation can reach its existing atomicity, semantic, idempotency, contention, dry-run, duplicate, and conflict contracts.
Output: One exact-byte historical-registry resolver used consistently by completed-spike and impact-fragment validation, with focused regression coverage.
</objective>

<execution_context>
@/home/at/.codex/gsd-core/workflows/execute-plan.md
@/home/at/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-FINAL-VERIFICATION.md
@.planning/phases/01-research-and-truth-baseline/01-46-PLAN.md
@.planning/phases/01-research-and-truth-baseline/01-47-PLAN.md
@tools/validate-research.py
@tests/phase1/test_spikes.py
@tests/phase1/test_spike_consolidation.py
@.planning/spikes/spk-c-boundary-harness/impact-fragment.yaml
@.planning/spikes/spk-d-verified-loader/impact-fragment.yaml
@.planning/spikes/spk-g-package-conformance/impact-fragment.yaml
@.planning/spikes/spk-h-browser-egress/impact-fragment.yaml
</context>

<scope_boundary>
Repair historical source-registry resolution only. The four impact fragments are immutable inputs and must not be edited, regenerated, or rebound. Preserve journaled publication, blocked SPK-G/package state, Firefox and runtime blockers, WR-02, PRE118, ADR status, and current research records. No new research, external fetch, package operation, schema change, phase transition, or state/roadmap mutation is authorized.
</scope_boundary>

<assumption_delta_decision>
Generalized noun: retained registry generation. Decision: no-change. The phase already models current and historical immutable generations; this plan connects impact fragments to that existing model rather than introducing a second provenance identity.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Resolve and validate the exact registry generation named by each fragment</name>
  <read_first>tools/validate-research.py `validate_impact_fragment`, `canonical_source_registry_digest_is_retained`, `validate_complete_spike_evidence`, and `discover_consolidation_inputs`; the four impact fragments' sourceLinks/evidenceLinks; existing historical completed-spike test</read_first>
  <files>tools/validate-research.py, tests/phase1/test_spike_consolidation.py, tests/phase1/test_spikes.py</files>
  <behavior>
    - A declared digest matching the current confined regular source-registry file returns those exact bytes.
    - A declared digest matching a regular `.planning/research/source-registry.yaml` blob in a Git-reachable commit returns those historical bytes even when current bytes differ.
    - An unreachable digest, malformed digest, non-blob object, Git failure/timeout, symlink/nonregular current path, malformed historical YAML, missing declared source ID, or incomplete historical source record fails closed.
    - Impact-fragment sourceLinks and source evidenceLinks are checked against the same resolved bytes; current registry contents cannot stand in for a historical digest.
  </behavior>
  <action>Write copied-Git-repository tests that commit one valid registry generation, advance the working registry legitimately, and validate a retained fragment against the earlier digest; add mutations for unreachable digest and historical source-ID/content mismatch. Refactor the boolean helper into `resolve_retained_source_registry(planning_root, expected_digest) -> bytes | None`. Check the current path is confined and regular; otherwise enumerate only commits reachable by `git rev-list --all -- .planning/research/source-registry.yaml`, validate revision syntax, require `git cat-file -t revision:path` to report `blob`, retrieve with bounded `git show`, and compare SHA-256 over exact stdout bytes. Parse source_index from those resolved bytes once. Use it for IMP010, IMP011, and source-kind IMP016 checks, while keeping all non-source evidence links confined to current spike-local regular files. Make `validate_complete_spike_evidence` consume the same resolver so both validators implement D-13/D-20 history identically. Never update a fragment digest per D-36.</action>
  <verify>
    <automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_spike_consolidation.SpikeConsolidationTests.test_fragment_accepts_git_reachable_historical_registry_generation test_spike_consolidation.SpikeConsolidationTests.test_fragment_rejects_unreachable_or_mismatched_historical_registry_generation test_spikes.SpikeValidationTests.test_completed_spike_accepts_reachable_historical_registry_digest_only -v</automated>
  </verify>
  <acceptance_criteria>Historical acceptance proves exact reachable blob bytes and historical source content; every nonexact or unprovable path fails without changing current records or fragment bytes.</acceptance_criteria>
  <done>Impact fragments and completed spike metadata share one safe current-or-historical registry-generation contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restore the complete consolidation regression matrix after refresh</name>
  <read_first>Task 1 resolver/tests; tests/phase1/test_spike_consolidation.py dry-run, duplicate, conflict, publish-interruption, semantic-failure, and contention tests; tools/validate-research.py consolidation call graph</read_first>
  <files>tools/validate-research.py, tests/phase1/test_spike_consolidation.py</files>
  <behavior>
    - Dry-run accounts for all spikes without mutating canonical targets.
    - Duplicate fragments collapse to one stable no-op with order-independent output.
    - Conflicting stable IDs produce deterministic review work without replacing compatibility or drift state.
    - Semantic failure never enters publication; every injected publication interruption recovers coherently.
    - Concurrent contention and injected precommit failure preserve byte-identical canonical state.
    - IMP011 and IMP016 no longer preempt these behaviors for the checked-in legitimate historical fragments.
  </behavior>
  <action>Run the six existing consolidation contracts after Task 1. If a failure remains, change only historical-registry data flow before merge; do not relax fragment path confinement, source-ID completeness, semantic validation, lock behavior, conflict routing, or journaled publication. Add assertions that the checked-in fragment files retain their pre-task SHA-256 values and that successful validation used the declared historical generation rather than current registry bytes.</action>
  <verify>
    <automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_spike_consolidation.SpikeConsolidationTests.test_dry_run_accounts_for_all_spikes_and_preserves_canonical_targets test_spike_consolidation.SpikeConsolidationTests.test_duplicate_fragment_is_a_single_no_op_and_order_is_stable test_spike_consolidation.SpikeConsolidationTests.test_conflicting_stable_id_is_blocked_without_replacing_canonical_state test_spike_consolidation.SpikeConsolidationTests.test_consolidation_generation_recovers_after_each_publish_interruption test_spike_consolidation.SpikeConsolidationTests.test_consolidation_semantic_failure_does_not_enter_publication test_spike_consolidation.SpikeConsolidationTests.test_contention_and_injected_precommit_failure_preserve_byte_identical_state -v</automated>
  </verify>
  <acceptance_criteria>All six consolidation regressions reach their intended assertions after the truth refresh, and no retained fragment byte or declared provenance digest changes.</acceptance_criteria>
  <done>IMP011/IMP016 no longer block legitimate retained evidence; consolidation safety and review semantics remain intact.</done>
</task>

</tasks>

## Artifacts this phase produces

| Path | New symbol/test | Contract |
|---|---|---|
| `tools/validate-research.py` | `resolve_retained_source_registry` | Returns exact current or Git-reachable regular-blob registry bytes for one lowercase SHA-256, else fails closed. |
| `tests/phase1/test_spike_consolidation.py` | `test_fragment_accepts_git_reachable_historical_registry_generation` | Proves legitimate retained provenance survives a later truth refresh. |
| `tests/phase1/test_spike_consolidation.py` | `test_fragment_rejects_unreachable_or_mismatched_historical_registry_generation` | Proves reachability, digest, source ID, and content must all agree. |

No new path, record field, CLI flag, package, or external service is added. Existing four `impact-fragment.yaml` files are asserted unchanged.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| fragment-declared digest → Git object database | Untrusted provenance text requests historical bytes from repository history. |
| resolved historical bytes → source index | A digest match is insufficient unless the exact generation parses and contains the declared complete sources. |
| validated fragments → canonical publication | Provenance failure must stop before merge and journaled publication. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-50-01 | Tampering | historical registry resolution | high | mitigate | Require lowercase SHA-256 match over a regular Git blob reachable from repository history; this HIGH threat blocks completion until acceptance/rejection tests pass. |
| T-01-50-02 | Repudiation | retained impact provenance | high | mitigate | Parse source IDs and immutable fields from the exact digest-selected bytes and assert fragment bytes remain unchanged. |
| T-01-50-03 | Denial of Service | Git history lookup | medium | mitigate | Use fixed path, bounded subprocess timeouts, validated revisions, and fail-closed Git errors before consolidation. |
| T-01-50-04 | Elevation of Privilege | path authority in evidence links | medium | mitigate | Preserve canonical-path, regular-file, no-symlink, and spike-local checks; digest selection never becomes arbitrary path selection. |

No package-manager install occurs; supply-chain package legitimacy gate is not applicable.
</threat_model>

<verification>
Run the historical-generation tests first, then all six named consolidation regressions through `tools/phase1-python`. Any IMP011/IMP016, changed fragment digest, HIGH-threat provenance ambiguity, or bypass of journaled publication fails the plan.
</verification>

<success_criteria>
The four retained fragments validate against their exact Git-reachable historical registry generation, remain byte-unchanged, and allow every required consolidation safety regression to execute and pass.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-50-SUMMARY.md` when done.
</output>
