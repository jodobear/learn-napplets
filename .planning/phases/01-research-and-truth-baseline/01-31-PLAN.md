---
phase: 01-research-and-truth-baseline
plan: 31
type: execute
wave: 5
depends_on: [01-30]
files_modified:
  - .planning/research/source-registry.yaml
  - .planning/research/candidate-source-manifest.yaml
  - .planning/research/acquisition-log.yaml
  - .planning/research/claims.yaml
  - tests/phase1/test_evidence.py
  - .planning/research/schemas/phase-governance.schema.json
  - .planning/research/phase-governance.yaml
  - tools/acquire-sources.py
  - tests/phase1/test_governance.py
autonomous: true
gap_closure: true
requirements: [EVID-01, EVID-02, EVID-03, EVID-04, OPER-01]
must_haves:
  truths:
    - "Canonical records retain independently pinned observed repository evidence and, only when directly acquired and reviewer-classified, authoritative NIP/NAP source evidence."
    - "Every unavailable normative source remains an impact-scoped blocker with a retrieval record, affected claims, and a refresh trigger."
    - "Source and normative intake consumes the exact Plan 01-30 authority artifact and receipt, validates their SHA-256 binding, and fails closed on missing, altered, merged, stale, or scope-mismatched six-role determinations while leaving only impacted evidence blocked."
    - "Published package metadata is handled separately by dependent Plan 01-41 without installing a package."
  artifacts:
    - path: .planning/research/source-registry.yaml
      provides: "Reviewed observed and, where qualified, normative immutable source records"
    - path: .planning/research/authority-determinations.yaml
      provides: "Plan 01-30 human-authored per-scope authority decisions consumed only with the exact receipt digest"
    - path: .planning/research/authority-determinations.receipt.yaml
      provides: "Immutable digest binding for the authority artifact and acquisition receipt"
    - path: .planning/research/phase-governance.yaml
      provides: "Validated authority-intake audit record referencing the exact per-scope Plan 01-30 artifact"
    - path: tools/acquire-sources.py
      provides: "Fail-closed source/normative intake guard with impact-scoped blocker emission"
  key_links:
    - from: source-registry SRC records
      to: claims and later compatibility entries
      via: "exact commit/path/content digest plus authority/evidence/maturity and review status"
  prohibitions:
    - statement: "No source or normative record automatically verifies a claim, accepts an ADR, or turns Firefox observations into protocol behavior."
      category: authority
      status: unresolved
      flagged: true
      verification: "State and review fields remain pending or blocked until required reviewer approval."
---

<objective>
Ingest reviewed immutable observed evidence and directly pursue normative NIP/NAP inputs under a fail-closed six-role authority boundary.

Purpose: Make the upstream refresh actionable while preserving the EVID-03/EVID-04 blockers wherever qualified evidence is absent.
Output: Canonical source/normative records or explicit impact-scoped unavailable outcomes; Plan 01-41 separately owns package records.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/research/reports/upstream-acquisition-20260728.md
@.planning/research/upstream-acquisition-queue.yaml
@.planning/research/source-registry.yaml
@.planning/research/candidate-source-manifest.yaml
@.planning/research/acquisition-log.yaml
@.planning/research/claims.yaml
@.planning/research/compatibility-matrix.yaml
@.planning/research/open-work-snapshot.json
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Ingest one reviewed observed source and one direct normative acquisition outcome without rewriting history</name>
  <precondition>The Plan 01-30 reviewer response classifies at least one candidate or explicitly records it as insufficient; read-only collection receipt and queue are present.</precondition>
  <read_first>Plan 01-30 reviewer response; source-registry.yaml; candidate-source-manifest.yaml; acquisition-log.yaml; claims.yaml; tools/acquire-sources.py; tests/phase1/test_evidence.py; tests/phase1/test_governance.py; upstream-refresh-synthesis-2026-07-28.md; upstream-refresh-napplet-naps-2026-07-28.md</read_first>
  <files>.planning/research/source-registry.yaml, .planning/research/candidate-source-manifest.yaml, .planning/research/acquisition-log.yaml, .planning/research/claims.yaml, .planning/research/schemas/phase-governance.schema.json, .planning/research/phase-governance.yaml, tools/acquire-sources.py, tests/phase1/test_evidence.py, tests/phase1/test_governance.py</files>
  <behavior>
    - A reviewed `napplet/web`, `kehto/web`, or `napplet/naps` commit:path receipt becomes a separate `SRC-*` observed-implementation or repository-history record with exact bytes/digest, retrieval date, limited authority, material uncertainty, impacts, and pending review; existing `SRC-POLICY-*` and discovery IDs remain unchanged.
    - The collector directly follows authoritative references from the pinned upstream content one hop to exact NIP-5A/NIP-5D and NAP registry/projection/governance candidates, resolving commit:path bytes and status when available.
    - Missing, unauthenticated, draft-only, or unreviewed normative material creates an explicit acquisition blocker linked to `CLM-UPSTREAM-BASELINE-001`, affected DRF/OQ IDs, and a refresh trigger; it cannot make a normative claim verified.
    - `GovernanceValidationTests.test_validate_governance_receipt_binding` proves `acquire-sources.py validate-authority-receipt --determinations PATH --receipt RECEIPT` accepts an exact authority-determinations/receipt binding and rejects a missing receipt, malformed receipt, altered determination bytes, altered bound acquisition-receipt digest, or scope/digest mismatch with a nonzero result.
  </behavior>
  <action>Addresses review concern: consume a durable, digest-bound six-role decision set per intake scope rather than six global rows. Write RED fixtures for accepted observed, unavailable normative, and each missing, altered, merged, stale, receipt-digest-mismatched, or scope-mismatched authority determination. Add `SourceEvidenceTests.test_reviewed_source_or_normative_acquisition_preserves_scope`, `GovernanceValidationTests.test_authority_determination_requires_current_complete_scope`, and `GovernanceValidationTests.test_validate_governance_receipt_binding`. Extend the versioned authority schema and canonical governance audit with `authorityDeterminations[]`, where every `scopeId` references the exact Plan 01-30 artifact SHA-256 and acquisition-receipt SHA-256 and contains exactly six unique role values (`product`, `protocolTechnical`, `security`, `accessibility`, `contentLearning`, `release`). Reject reused determination IDs, duplicate `(scopeId, role)`, generic/shared evidence or rationale text, absent roles, stale `freshnessCheckedAt` relative to the bound receipt, candidate/evidence/impact scope mismatch, and any byte or digest mismatch between `authority-determinations.yaml` and `authority-determinations.receipt.yaml`. Add `validate_authority_determinations()` and `require_current_authority_determinations()` to `tools/acquire-sources.py`; source/normative intake must call the latter before creating a canonical record and, on failure, append one deterministic impact-scoped blocked attempt without changing unrelated records. Add the owned `tools/acquire-sources.py validate-authority-receipt --determinations PATH --receipt RECEIPT` subcommand. It parses both YAML inputs safely, computes the determinations SHA-256 from `PATH`, and fails nonzero unless the receipt identifies that exact path/digest and its bound acquisition-receipt digest matches the determination record. Its argparse help must expose both required `Path` options; it calls `validate_authority_determinations()` for the complete six-role/scope checks, prints stable receipt-binding diagnostics, and returns nonzero before any source/normative intake attempt. Apply the Plan 01-30 reviewer classifications exactly: add distinct source records for reviewed material `napplet/web` PR #184/#186/#188, `kehto/web` PR #204/#209/#211, and the `napplet/naps` checkpoint only at their stated limited observed/repository-history authority. Preserve #208/#210 and every former candidate/discovery pointer as history rather than replacing them. Use the collector to enumerate official repository trees and directly resolve the exact NIP-5A/NIP-5D plus NAP registry, projection, and governance paths referenced by the acquired upstream sources; record actual immutable bytes/digests when a reviewer-approved official purpose and locator are available. Otherwise append an unavailable/blocked attempt with response evidence, cause, affected claim/DRF/OQ/ADR scope, safe fallback, and retry trigger. Add only source relations and wording that the collected bytes support; preserve `CLM-UPSTREAM-BASELINE-001` as blocked unless all its required evidence and human review conditions are independently satisfied. Enforce D-01 through D-14, D-16 through D-20, and D-29 through D-32, including additive source history under D-13.</action>
  <verify><automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_evidence.SourceEvidenceTests.test_reviewed_source_or_normative_acquisition_preserves_scope && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_governance.GovernanceValidationTests.test_authority_determination_requires_current_complete_scope && tools/phase1-python tools/acquire-sources.py validate-authority-receipt --determinations .planning/research/authority-determinations.yaml --receipt .planning/research/authority-determinations.receipt.yaml && tools/phase1-python tools/validate-research.py validate --root .planning/research --report /tmp/phase1-source-ingestion.md</automated></verify>
  <acceptance_criteria>The test proves separate observed versus normative/unavailable outcomes; the owned `acquire-sources.py validate-authority-receipt --determinations PATH --receipt RECEIPT` CLI accepts only an exact receipt binding and rejects each named negative fixture with a nonzero exit; every ingested source has complete schema fields and immutable bytes, and every unavailable normative target has a concrete blocker/impact/retry record instead of an invented source or verified claim.</acceptance_criteria>
  <done>Repository evidence and direct normative-source attempts are traceable, limited to their real authority, and ready for compatibility review.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove authority-intake failure is limited to its evidence scope</name>
  <read_first>.planning/research/phase-governance.yaml; phase-governance.schema.json; tools/acquire-sources.py; tests/phase1/test_governance.py; tests/phase1/test_evidence.py; upstream-acquisition-20260728.md</read_first>
  <files>tools/acquire-sources.py, tests/phase1/test_governance.py, tests/phase1/test_evidence.py</files>
  <behavior>- Missing, merged, stale, or scope-mismatched determinations reject only the affected candidate with a stable blocked impact record; a qualifying six-role set permits only its declared limited intake.</behavior>
  <action>Test the six independent determination records and fail-closed intake guard under fixture transport. Assert blocked outcomes retain candidate/source IDs, affected requirements/phases/ADRs/lessons, safe fallback, and refresh trigger, preserve historic IDs and acquisition history, and do not block independent candidates. Keep packages entirely out of this plan; Plan 01-41 owns package registry intake. Apply D-13, D-27, D-28, D-30, D-31, and D-32.</action>
  <verify><automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_governance.GovernanceValidationTests.test_authority_determination_requires_current_complete_scope && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_evidence.SourceEvidenceTests.test_reviewed_source_or_normative_acquisition_preserves_scope && tools/phase1-python tools/validate-research.py validate-governance .planning/research/phase-governance.yaml</automated></verify>
  <acceptance_criteria>Malformed authority records fail before canonical intake, blocked evidence retains its exact impact scope and history, and qualifying evidence does not permit ADR acceptance or production work.</acceptance_criteria>
  <done>Source and normative intake is authority-gated, fail-closed, and history-preserving; package intake remains a separate dependent concern.</done>
</task>
</tasks>

## Artifacts this phase produces
- `.planning/research/source-registry.yaml` — additive `SRC-*` source records with restricted authority classes.
- `.planning/research/phase-governance.yaml` — six independent, dated, scope-bound authority determinations.
- `tools/acquire-sources.py` — fail-closed source/normative authority-intake guard.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| reviewer classification → canonical source record | A reviewer-approved limited purpose must not be broadened by automation. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-31-01 | Spoofing | protocol authority | high | mitigate | Task 1 records only reviewer-classified official purpose and retains unavailable blockers. |
| T-31-02 | Tampering | immutable source provenance | high | mitigate | Task 1 stores commit:path bytes and SHA-256 with retrieval evidence. |
| T-31-05 | Repudiation | historic discovery state | medium | mitigate | Additive snapshots and acquisition log preserve old IDs and outcomes. |
| T-31-06 | Tampering | false source claim verification | high | mitigate | Tests require explicit blocked outcomes for incomplete normative source evidence. |
</threat_model>

<verification>
Run source-focused tests and canonical research validation through the approved wrapper.
</verification>

<success_criteria>
The refresh establishes source and normative evidence where available, and otherwise leaves every dependent requirement visibly and accurately blocked.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-31-SUMMARY.md` when done.
</output>
