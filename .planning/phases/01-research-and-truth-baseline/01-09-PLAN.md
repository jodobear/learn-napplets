---
phase: 01-research-and-truth-baseline
plan: 09
type: execute
wave: 7
depends_on: [01-07]
files_modified:
  - .planning/spikes/spk-a-workspace/metadata.yaml
  - .planning/spikes/spk-a-workspace/recipe.md
  - .planning/spikes/spk-a-workspace/fixture.md
  - .planning/spikes/spk-a-workspace/environment.json
  - .planning/spikes/spk-a-workspace/measurements.yaml
  - .planning/spikes/spk-a-workspace/report.md
autonomous: true
requirements: [EVID-04]
must_haves:
  truths:
    - "SPK-A compares workspace structures in an isolated non-production directory against public-site, shared-content, host-only, and multi-napplet boundary criteria."
    - "SPK-A reports predeclared measurements and a proposed repository/workspace recommendation or a blocker, never creates production workspace markers."
  artifacts:
    - path: .planning/spikes/spk-a-workspace/metadata.yaml
      provides: "SPK-A experiment envelope"
    - path: .planning/spikes/spk-a-workspace/report.md
      provides: "Workspace evidence and recommendation"
  key_links:
    - from: SPK-A metadata
      to: teaching-scope and ADR-0001 evidence
      via: "source IDs, comparison criteria, measurements, and disposition"
  prohibitions:
    - statement: "Phase 1 MUST NOT promote a disposable workspace comparison into a production `package.json`, workspace file, `src`, `apps`, or `packages` scaffold."
      category: safety
      status: unresolved
      flagged: true
      verification: null
      reason: "The workspace experiment is isolated research only."
---

<objective>
Produce reproducible evidence for the independent-workspace ADR recommendation without initializing a production workspace.

Purpose: Test the repository boundary assumptions that later phases will need while preserving the Phase 1 no-scaffold gate.
Output: SPK-A contract, isolated comparison fixture, environment/measurements, and report.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-RESEARCH.md
@.planning/research/teaching-scope.yaml
@.planning/research/compatibility-matrix.yaml
@.planning/research/schemas/spike.schema.json
@.planning/spikes/_shared/toolchain-environment.json
@.planning/validation/required-artifacts.json
@docs/learn-napplets-codex-pack-v3/docs/04-RESEARCH-PLAN.md section 9A
@docs/learn-napplets-codex-pack-v3/docs/10-DECISIONS-AND-ADRS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Specify and fixture the isolated workspace comparison</name>
  <read_first>.planning/research/teaching-scope.yaml; .planning/research/compatibility-matrix.yaml; .planning/research/schemas/spike.schema.json; .planning/validation/required-artifacts.json; .planning/phases/01-research-and-truth-baseline/01-CONTEXT.md (D-21 through D-24); docs/learn-napplets-codex-pack-v3/docs/04-RESEARCH-PLAN.md section 9A</read_first>
  <files>.planning/spikes/spk-a-workspace/metadata.yaml, .planning/spikes/spk-a-workspace/recipe.md, .planning/spikes/spk-a-workspace/fixture.md</files>
  <action>Create the explicitly non-production `SPK-A` envelope, recipe, and comparison fixture. Predeclare candidate workspace structures, variables, source/compatibility IDs, commands, measurements, success threshold, failure outcome, and blocked outcome against static-site boundaries, shared content, host-only isolation, multiple guest entries, testability, bundle isolation, and deployment compatibility. Keep all candidate layouts as text/temporary fixture material beneath the spike directory and state that no fixture may create any path listed in `productionMarkersBlockedBeforePhase3`, per D-21 through D-24.</action>
  <verify><automated>python3 tools/validate-research.py validate-spike .planning/spikes/spk-a-workspace</automated></verify>
  <acceptance_criteria>The envelope has `SPK-A` ID, non-production marker, source IDs, predeclared threshold/outcomes, deterministic fixture/replay instructions, and no production marker appears at repository root.</acceptance_criteria>
  <done>The workspace decision can be tested from a reviewable, isolated experiment contract.</done>
</task>

<task type="auto">
  <name>Task 2: Execute SPK-A and report a recommendation or blocker</name>
  <read_first>.planning/spikes/spk-a-workspace/metadata.yaml; .planning/spikes/spk-a-workspace/recipe.md; .planning/spikes/spk-a-workspace/fixture.md; .planning/spikes/_shared/toolchain-environment.json; .planning/research/schemas/environment.schema.json; docs/learn-napplets-codex-pack-v3/templates/RESEARCH-REPORT-TEMPLATE.md</read_first>
  <files>.planning/spikes/spk-a-workspace/environment.json, .planning/spikes/spk-a-workspace/measurements.yaml, .planning/spikes/spk-a-workspace/report.md</files>
  <action>Run only the declared isolated comparison commands. Record actual environment fields, all measurement values and computation method, input/output digests, and replay result. Write the report with research question, pinned sources, observations, conflicts, inference, measurement, recommendation, uncertainty, affected requirements/phases/ADR 0001, and disposition. A measured recommendation remains proposed; a failed or unavailable comparison creates an impact-scoped blocker instead of a scaffold or unmeasured preference, per D-22, D-23, D-25, and D-26.</action>
  <verify><automated>python3 tools/validate-research.py validate-spike .planning/spikes/spk-a-workspace && python3 tools/validate-planning.py</automated></verify>
  <acceptance_criteria>Environment, measurements, and report reference the same `SPK-A` ID; the report labels the result proposed/blocked and repository validation confirms no production marker was created.</acceptance_criteria>
  <done>ADR 0001 has a reproducible workspace comparison or an explicit evidence blocker.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.planning/spikes/spk-a-workspace/{metadata.yaml,recipe.md,fixture.md}` — SPK-A contract and isolated comparison fixture.
- `.planning/spikes/spk-a-workspace/{environment.json,measurements.yaml,report.md}` — actual environment, values/digests, and research report.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace fixture → repository root | A disposable comparison must not create an authorized production structure. |
| measured comparison → ADR 0001 | A local prototype result must remain a proposed recommendation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Elevation of Privilege | workspace spike | high | mitigate | Confine all candidate structures to the spike directory and validate the production-marker gate. |
| T-09-02 | Tampering | workspace measurements | high | mitigate | Predeclare criteria, record environment/digests, and validate the SPK envelope. |
| T-09-03 | Repudiation | ADR evidence | medium | mitigate | Report sources, observations, uncertainty, affected ADR, and proposed/blocked disposition. |
| T-09-SC | Tampering | research tooling | high | mitigate | Use only the approved isolated toolchain and no new package installation. |
</threat_model>

<verification>
Validate SPK-A and run the existing planning validator to prove the experiment did not introduce a production scaffold.
</verification>

<success_criteria>
The project has measurable workspace evidence for ADR 0001 with no production-framework creation.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-09-SUMMARY.md` when done.
</output>
