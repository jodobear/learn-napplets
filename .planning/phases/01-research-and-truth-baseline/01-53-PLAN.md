---
phase: 01-research-and-truth-baseline
plan: 53
type: execute
wave: 4
depends_on: [01-49, 01-50, 01-51, 01-52, 01-54, 01-55, 01-56, 01-57]
files_modified: []
autonomous: false
gap_closure: true
requirements: [EVID-01, EVID-02, EVID-03, EVID-04, OPER-01, OPER-03]
must_haves:
  truths:
    - "After Plans 01-49 through 01-52 and review-remediation Plans 01-54 through 01-57 complete, every focused regression passes together through tools/phase1-python: CR-08, IMP011, IMP016, WR-04, reduced-motion replay, live observed-refresh attestation, complete static output ownership, reviewed Git-blob identity, transactional acquisition publication, and the registry total deadline."
    - statement: "The complete retained Phase 1 suite is rerun as a backstop; failure IDs equal the declared direct-ID set, error IDs equal the declared wrapper-ID set, their union equals the complete TestCase.id() allowlist, each direct traceback and the direct probe has its own exact parsed line set, each wrapper has its exact sole exception line, and skips, expected failures, unexpected successes, duplicate IDs, extra ERROR lines, and every other unexpected result category are rejected."
      verification: backstop
    - "Before canonical baselines and again immediately before the human checkpoint/closeout, STATE.md frontmatter is parsed with safe YAML and must equal status `gaps_found` with integer `progress.completed_phases` 0; pre-transition or malformed state is rejected even when its SHA-256 is stable."
    - "Pre-task SHA-256 values for STATE.md, ROADMAP.md, and REQUIREMENTS.md remain exact through the human checkpoint and verification-only closeout; only 01-53-SUMMARY.md is created and committed, and the standard execute-plan state/roadmap/requirements mutation and metadata-commit steps are explicitly bypassed per D-25 and D-33 through D-35."
    - "Human visual/accessibility judgment occurs only after automated blockers close and can approve only the local static site's reading hierarchy, focus visibility, responsive layout, motion comfort, and evidence-label clarity."
    - "One foreground lifecycle controller keeps manifest, focused/full logs, and classifier output only in anonymous mode-0600 TemporaryFile descriptors with link count zero; it creates no named private directory and performs no pathname-based recursive cleanup."
  prohibitions:
    - requirement_id: EVID-02
      category: transparency
      status: unresolved
      verification: null
      statement: "MUST NOT collapse conflict, uncertainty, drift, or blocked evidence into settled upstream fact."
  flagged_assumptions:
    - "EVID-02 adjacency probe remains unresolved: this scoped repair set does not define whether equal/touching conflict records merge, collide, or remain parallel."
    - "EVID-02 empty probe remains unresolved: this gap set does not redefine empty conflict/drift collection semantics."
    - "EVID-02 ordering probe remains unresolved: this gap set does not create a new stable-order contract for equal conflict records."
    - "OPER-03 adjacency probe remains unresolved: this gap set does not define how coincident approval/governance events compose."
    - "OPER-03 empty probe remains unresolved: absence of a required human approval remains non-passing, but no broader empty-governance model is inferred."
    - "OPER-03 ordering probe remains unresolved: existing role/date/order rules are retained without inventing a new tie-break contract."
  artifacts:
    - path: .planning/phases/01-research-and-truth-baseline/01-53-SUMMARY.md
      provides: "Focused pass matrix, exact category/line-set PRE118 attribution, three-file pre/post SHA-256 equality, exact summary-only closeout commit path, and explicit human visual/accessibility judgment outcome."
  key_links:
    - from: "Plans 01-49 through 01-52 and 01-54 through 01-57 focused regressions"
      to: "complete tests/phase1 retained-suite backstop"
      via: "Wave 4 begins only after all eight dependencies; the integration tracer runs focused checks first, then requires exact equality between unittest TestCase.id() results and the declared PRE118 allowlist."
      pattern: "PRE118"
    - from: "automated site checks"
      to: "human visual/accessibility checkpoint"
      via: "The checkpoint is unreachable until the automated prerequisite command passes."
      pattern: "blocking"
    - from: "Task 1 controller-emitted canonical digest map"
      to: "summary-only closeout commit"
      via: "Task 2 and closeout record the exact emitted three-file digest map; pre-closeout and post-closeout guards recompute it and require the closeout commit's NUL-parsed path set to equal only 01-53-SUMMARY.md."
      pattern: "verification_only_closeout"
---

<objective>
Prove the authorized gap and external-review repairs are closed together, preserve PRE118 as explicit deferred non-passing evidence, then obtain bounded human visual/accessibility judgment.

Purpose: Prevent a locally green focused fix from hiding an integration regression, including every accumulated exact external-review defect, or crossing the owner-authorized phase/approval boundary.
Output: A summary-only evidence record containing focused/full-suite results, anonymous namespace-detachment and descriptor-close proof, parsed locked-state proof, exact PRE118 attribution, unchanged canonical status, and the human judgment result.
</objective>

<execution_context>
@/home/at/.codex/gsd-core/workflows/execute-plan.md
@/home/at/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-RESEARCH.md
@.planning/phases/01-research-and-truth-baseline/01-FINAL-VERIFICATION.md
@.planning/phases/01-research-and-truth-baseline/deferred-items.md
@.planning/phases/01-research-and-truth-baseline/01-49-SUMMARY.md
@.planning/phases/01-research-and-truth-baseline/01-50-SUMMARY.md
@.planning/phases/01-research-and-truth-baseline/01-51-SUMMARY.md
@.planning/phases/01-research-and-truth-baseline/01-52-SUMMARY.md
@.planning/phases/01-research-and-truth-baseline/01-54-SUMMARY.md
@.planning/phases/01-research-and-truth-baseline/01-55-SUMMARY.md
@.planning/phases/01-research-and-truth-baseline/01-56-SUMMARY.md
@.planning/phases/01-research-and-truth-baseline/01-57-SUMMARY.md
</context>

<scope_boundary>
This is a verification-and-human-judgment plan, run only after all eight dependencies complete. Do not modify production, test, research, spike, STATE.md, ROADMAP.md, REQUIREMENTS.md, review-binding, package, runtime, browser pin, ADR, deployment, release, or transition files. PRE118 must remain visible and deferred. The only persistent closeout artifact is 01-53-SUMMARY.md. CR-05 through CR-07/SPK-G, WR-02, package admission, runtime/guest execution, native-runtime qualification, portable targets, deployment/release, ADR acceptance, external action, residual-risk acceptance, Phase 2 transition, and Firefox/cross-browser workaround remain excluded.
</scope_boundary>

<assumption_delta_decision>
Generalized noun: Phase 1 evidence baseline. Decision: no-change. The detector's “choose” signal describes later human use of the baseline; this gap set introduces no alternate primary key, source of truth, or architecture identity.
</assumption_delta_decision>

<retained_failure_contract>
The source refusal is produced by `tools/phase1-python tools/validate-planning.py --verify-phase1-source-inputs --review .planning/phases/01-research-and-truth-baseline/01-REVIEWS.md` through `review_manifest_errors` in `tools/validate-planning.py`. Classification is category- and line-exact; broad PRE118 substring presence is insufficient.

For each direct traceback, parse `ERROR ` evidence by taking the suffix beginning at the first literal `ERROR ` on every traceback line that contains it. Preserve one occurrence per parsed line, then require both occurrence-count equality and set equality against this exact TestCase.id() map:
- `test_consolidation_recovery.ObservedRefreshPublicationTests.test_observed_refresh_rejects_tampered_or_incomplete_attestation_before_reader_entry` → {`ERROR OBS004: observed-refresh staged validation refused: reviewed acquisition provenance refused before observed-refresh snapshot: PRE118: review plan manifest is not the exact active plan set`}
- `test_consolidation_recovery.ObservedRefreshPublicationTests.test_observed_refresh_staged_validation_binds_exact_five_target_digests` → {`ERROR OBS004: observed-refresh staged validation refused: reviewed acquisition provenance refused before observed-refresh snapshot: PRE118: review plan manifest is not the exact active plan set`}
- `test_evidence.BaseSourceEvidenceTests.test_evidence_validation_uses_temporary_report_without_canonical_mutation` → {`ERROR IO001: reviewed acquisition provenance refused before compatibility snapshot: PRE118: review plan manifest is not the exact active plan set`}
- `test_evidence.SourceEvidenceTests.test_evidence_validation_uses_temporary_report_without_canonical_mutation` → {`ERROR IO001: reviewed acquisition provenance refused before compatibility snapshot: PRE118: review plan manifest is not the exact active plan set`}
- `test_governance.GovernanceValidationTests.test_phase_one_closeout_requires_governance_and_complete_evidence_contract` → {`ERROR PRE118: review plan manifest is not the exact active plan set`}

The direct probe must return nonzero, have empty stderr, and have stdout lines exactly equal in order and cardinality to:
- `bootstrap verification passed`
- `toolchain verification passed`
- `ERROR PRE118: review plan manifest is not the exact active plan set`
- `Planning validation failed: 1 error(s), 0 warning(s).`

For every wrapper traceback below, parse stripped lines beginning `phase1_terminal_validator.` and require the one-element sequence exactly [`phase1_terminal_validator.ValidationError: Plan 01-29 source-input verifier refused terminal evidence`]. Its parsed `ERROR ` line sequence must be empty:
- `test_closeout_commands.TerminalPublicationTests.test_terminal_validator_binds_staged_three_file_set_before_publication`
- `test_closeout_commands.TerminalPublicationTests.test_terminal_validator_preserves_closed_ledger_through_post_closure_verification`
- `test_closeout_commands.TerminalPublicationTests.test_terminal_validator_requires_canonical_probe_attempt_ledger`
- `test_closeout_commands.TerminalPublicationTests.test_terminal_validator_requires_exact_reviewed_source_input_binding`
- `test_closeout_commands.TerminalPublicationTests.test_terminal_validator_requires_separate_role_bound_records_but_allows_one_principal`
- `test_closeout_commands.TerminalPublicationTests.test_terminal_validator_uses_external_target_only_aggregate_without_self_reference`
- `test_closeout_commands.VerificationAdjudicationTests.test_fresh_verification_has_exact_review_finding_adjudication`

`result.failures` TestCase.id() values must equal the five direct IDs exactly; `result.errors` IDs must equal the seven wrapper IDs exactly; their union must equal the twelve-ID allowlist with no duplicates. `result.skipped`, `result.expectedFailures`, and `result.unexpectedSuccesses` must each be empty, and no other result category may contain an entry. Any extra parsed `ERROR ` line fails even when a permitted PRE118 line is also present. An empty retained-result set, a passing direct probe, any line-order/cardinality mismatch, category migration, missing ID, or unexpected ID fails verification. PRE118 remains deferred and non-passing; this contract never counts it as phase-pass evidence.
</retained_failure_contract>

<tasks>

<task type="tracer">
  <name>Task 1: Run the repaired integration path and classify the complete retained suite</name>
  <read_first>Plans 01-49 through 01-52 and 01-54 through 01-57 summaries and focused test names; 01-FINAL-VERIFICATION behavioral checks; deferred-items.md PRE118 evidence; tools/phase1-python stdout contract; tools/validate-planning.py _parse_review_manifest and review_manifest_errors; tests/phase1/test_closeout_commands.py current test class/method IDs and terminal wrapper result; tests/phase1/test_consolidation_recovery.py live observed-refresh generation tests; tests/site/test_static_site.py complete output-tree tests; tests/phase1/test_evidence.py reviewed acquisition tests; tests/phase1/test_compatibility.py registry deadline tests; retained_failure_contract above; STATE.md frontmatter</read_first>
  <files>None — verification-only; write results only to 01-53-SUMMARY.md through normal executor output.</files>
  <behavior>
    - CR-08 positive/negative source identity tests pass with generated output current.
    - Historical impact provenance and all six consolidation safety regressions pass without fragment changes.
    - The WR-04 tracer and strict-root neighbors pass together.
    - Direct reduced/no-preference replay option tests and the complete site suites pass.
    - The live observed-refresh targetMap matches all five current live target bytes and its exact Git-blob/provenance binding revalidates.
    - Static-site check/build owns the complete output tree, removes safe stale routes/assets, and refuses symlink/nonregular entries without external mutation.
    - Reviewed acquisition rejects raw bytes whose Git blob ID differs before any side effect and publishes queue/receipt/history/candidates as one recoverable generation.
    - Registry collection enforces one monotonic total deadline across connect and slow chunk reads and routes timeout to an impact-scoped blocker.
    - Full Phase 1 discovery runs; failures equal the five direct IDs, errors equal the seven wrapper IDs, traceback/probe line sequences match retained_failure_contract exactly, no duplicate/extra ERROR line or category migration occurs, and skipped/expected-failure/unexpected-success collections remain empty.
    - One tools/phase1-python -c lifecycle controller creates each private artifact with `tempfile.TemporaryFile(dir="/tmp")`, sets mode 0600, requires current UID, regular-file type, and link count zero, then performs every read/write/hash through retained handles before closing all handles in finally.
    - Deterministic HUP/INT/TERM child probes create only anonymous descriptors, close them in the signal handler, and return exactly 129/130/143 with no named artifact.
    - A deterministic anonymous namespace-detachment probe uses only a `TemporaryFile` descriptor, proves current UID, regular mode 0600, link count zero, stable retained fstat identity, exact write/read/fsync, and successful close; because no private namespace entry exists, there is no pathname generation an attacker could replace.
    - A parser probe accepts the exact current STATE frontmatter and rejects copies with status changed from gaps_found or completed_phases changed from integer 0; production parse occurs before canonical hash baselines and reoccurs after all subprocesses.
    - STATE.md, ROADMAP.md, and REQUIREMENTS.md retain their pre-task exact SHA-256 values; parsed STATE remains status gaps_found with completed_phases 0.
  </behavior>
  <action>Addresses fresh external Codex review concerns for pathname-race cleanup and locked-state enforcement, plus exact PRE118 classification, canonical-file guarding, and all focused remediation paths. Run only after all eight dependencies complete. Invoke one lifecycle controller through tools/phase1-python -c as a normal foreground command; never place wrapper call or stdout in command substitution. Eliminate the named-directory lifecycle completely: create manifest, focused log, full log, and classifier output only with `tempfile.TemporaryFile(mode="w+b", dir="/tmp")`; immediately fchmod each descriptor to 0600 and require fstat current UID, regular-file type, mode 0600, and link count zero before use. Retain handles through every write/read/fsync/hash and close all in one finally path; a close failure forces nonzero. No private pathname is emitted or handed off. Add HUP/INT/TERM child probes that create only an anonymous descriptor, close it in the installed handler, and return exact 129/130/143. Add a deterministic namespace-detachment/close probe that creates only a `TemporaryFile` descriptor, fchmods it to 0600, requires current UID/regular type/mode 0600/link count zero, retains and rechecks its fstat identity across exact write/flush/fsync/seek/read, and then closes it successfully. The probe must create, resolve, rename, unlink, or remove no private pathname: link count zero is the proof that no namespace entry exists to replace. The controller must contain no named private root, no directory removal, and no recursive deletion path. Before computing canonical hashes, parse STATE.md frontmatter with yaml.safe_load from exact bytes and require top-level status string `gaps_found`, progress mapping, and non-boolean integer completed_phases 0; probe exact current bytes plus mutated pre-transition copies (`status: passed`, `completed_phases: 1`) and require both rejected. Hash the same accepted bytes, run builder/focused Plan 01-49 through 01-52 and 01-54 through 01-57 regressions/site/browser/full discovery/PRE118 classification, then reread/reparse STATE immediately after subprocesses and require locked values plus all three hashes unchanged. Stream diagnostics while logs remain anonymous. Emit final JSON with state assertions, canonical/artifact hashes, classifier facts, namespace-detachment facts, signal rows, `anonymousArtifacts: true`, and `privatePathHandedOff: false`. Preserve PRE118 as non-passing per D-25/D-35; stop before human judgment on any parser probe, state recheck, focused/classifier/signal/namespace-detachment/descriptor-close/hash failure.</action>
  <verify>
    <automated>PYTHONPATH=tests/site:tests/phase1 tools/phase1-python -c '
import hashlib
import io
import json
import os
import pathlib
import signal
import stat
import subprocess
import sys
import tempfile
import unittest
import yaml

ROOT = pathlib.Path.cwd()
SIGNALS = (signal.SIGHUP, signal.SIGINT, signal.SIGTERM)
SIGNAL_STATUS = {signal.SIGHUP: 129, signal.SIGINT: 130, signal.SIGTERM: 143}
artifacts = {}
active_process = None
cleanup_result = "not-run"

class SignalExit(BaseException):
    def __init__(self, status):
        self.status = status

def close_artifacts():
    global cleanup_result
    failures = []
    for name, handle in list(artifacts.items()):
        try:
            handle.close()
        except Exception as exc:
            failures.append(name + ":" + repr(exc))
    artifacts.clear()
    cleanup_result = "closed" if not failures else "refused"
    if failures:
        print("PLAN53_ANONYMOUS_CLOSE_REFUSED=" + json.dumps(failures), file=sys.stderr)
    return cleanup_result

def on_signal(signum, frame):
    global active_process
    if active_process is not None and active_process.poll() is None:
        active_process.send_signal(signum)
        try:
            active_process.wait(timeout=5)
        except subprocess.TimeoutExpired:
            active_process.kill()
            active_process.wait()
    close_artifacts()
    raise SignalExit(SIGNAL_STATUS[signum])

for item in SIGNALS:
    signal.signal(item, on_signal)

def new_artifact(name, initial=b""):
    if name in artifacts:
        raise RuntimeError("duplicate anonymous artifact")
    handle = tempfile.TemporaryFile(mode="w+b", dir="/tmp")
    os.fchmod(handle.fileno(), 0o600)
    state = os.fstat(handle.fileno())
    if not stat.S_ISREG(state.st_mode) or state.st_uid != os.geteuid() or stat.S_IMODE(state.st_mode) != 0o600 or state.st_nlink != 0:
        handle.close()
        raise RuntimeError("artifact is not an anonymous owned mode-0600 regular file")
    if initial:
        handle.write(initial)
        handle.flush()
        os.fsync(handle.fileno())
        handle.seek(0)
    artifacts[name] = handle
    return name

def write_artifact(name, data, append=False):
    handle = artifacts[name]
    state = os.fstat(handle.fileno())
    if not stat.S_ISREG(state.st_mode) or state.st_uid != os.geteuid() or state.st_nlink != 0:
        raise RuntimeError("anonymous artifact identity changed")
    handle.seek(0, os.SEEK_END if append else os.SEEK_SET)
    if not append:
        handle.truncate(0)
    handle.write(data)
    handle.flush()
    os.fsync(handle.fileno())

def read_artifact(name):
    handle = artifacts[name]
    handle.seek(0)
    return handle.read()

def parse_locked_state(payload):
    try:
        text = payload.decode("utf-8")
    except UnicodeDecodeError as exc:
        raise RuntimeError("STATE must be UTF-8") from exc
    lines = text.splitlines()
    if not lines or lines[0] != "---":
        raise RuntimeError("STATE frontmatter opener missing")
    try:
        end = lines.index("---", 1)
    except ValueError as exc:
        raise RuntimeError("STATE frontmatter closer missing") from exc
    try:
        parsed = yaml.safe_load("\n".join(lines[1:end]))
    except yaml.YAMLError as exc:
        raise RuntimeError("STATE frontmatter invalid") from exc
    if not isinstance(parsed, dict) or parsed.get("status") != "gaps_found":
        raise RuntimeError("STATE status must equal gaps_found")
    progress = parsed.get("progress")
    completed = progress.get("completed_phases") if isinstance(progress, dict) else None
    if type(completed) is not int or completed != 0:
        raise RuntimeError("STATE progress.completed_phases must equal integer zero")
    return {"status": parsed["status"], "completedPhases": completed}

def probe_state_parser(payload):
    accepted = parse_locked_state(payload)
    text = payload.decode("utf-8")
    mutations = {
        "status": text.replace("status: gaps_found", "status: passed", 1).encode(),
        "completed": text.replace("  completed_phases: 0", "  completed_phases: 1", 1).encode(),
    }
    if mutations["status"] == payload or mutations["completed"] == payload:
        raise RuntimeError("STATE mutation probe could not construct pre-transition fixtures")
    rejected = []
    for name, candidate in mutations.items():
        try:
            parse_locked_state(candidate)
        except RuntimeError:
            rejected.append(name)
        else:
            raise RuntimeError("STATE parser accepted pre-transition mutation: " + name)
    return {"accepted": accepted, "rejected": sorted(rejected)}

def anonymous_namespace_probe():
    descriptor = tempfile.TemporaryFile(mode="w+b")
    row = None
    try:
        os.fchmod(descriptor.fileno(), 0o600)
        before = os.fstat(descriptor.fileno())
        identity = (before.st_dev, before.st_ino, before.st_mode, before.st_uid, before.st_nlink)
        if before.st_uid != os.getuid() or not stat.S_ISREG(before.st_mode) or stat.S_IMODE(before.st_mode) != 0o600 or before.st_nlink != 0:
            raise RuntimeError("namespace probe is not an anonymous owned mode-0600 regular file")
        payload = b"anonymous-descriptor-owned\n"
        descriptor.write(payload)
        descriptor.flush()
        os.fsync(descriptor.fileno())
        descriptor.seek(0)
        if descriptor.read() != payload:
            raise RuntimeError("namespace probe payload changed")
        after = os.fstat(descriptor.fileno())
        if (after.st_dev, after.st_ino, after.st_mode, after.st_uid, after.st_nlink) != identity:
            raise RuntimeError("namespace probe descriptor identity changed")
        row = {
            "uidOwned": True,
            "regular": True,
            "mode": "0600",
            "nlink": 0,
            "identityRetained": True,
            "payloadExact": True,
            "namespaceEntryAbsent": True,
        }
    finally:
        descriptor.close()
    if not descriptor.closed or row is None:
        raise RuntimeError("namespace probe descriptor did not close")
    row["closed"] = True
    return row

signal_probe_code = r"""
import json
import os
import signal
import stat
import sys
import tempfile
signum = int(sys.argv[1])
status = {signal.SIGHUP: 129, signal.SIGINT: 130, signal.SIGTERM: 143}[signum]
handle = tempfile.TemporaryFile(mode="w+b", dir="/tmp")
os.fchmod(handle.fileno(), 0o600)
state = os.fstat(handle.fileno())
if state.st_nlink != 0 or not stat.S_ISREG(state.st_mode):
    raise SystemExit(70)
def handler(received, frame):
    handle.close()
    print(json.dumps({"signal": received, "status": status, "anonymous": True}, sort_keys=True))
    raise SystemExit(status)
signal.signal(signum, handler)
os.kill(os.getpid(), signum)
raise SystemExit(71)
"""

def run_captured(command, env=None):
    global active_process
    process = subprocess.Popen(command, cwd=ROOT, env=env, text=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE)
    active_process = process
    try:
        stdout, stderr = process.communicate()
        return subprocess.CompletedProcess(command, process.returncode, stdout, stderr)
    finally:
        if process.poll() is None:
            process.terminate()
            try:
                process.wait(timeout=5)
            except subprocess.TimeoutExpired:
                process.kill()
                process.wait()
        active_process = None

def run_signal_probes():
    rows = []
    for signum in SIGNALS:
        result = run_captured([sys.executable, "-c", signal_probe_code, str(signum)])
        lines = [line for line in result.stdout.splitlines() if line.strip()]
        expected = SIGNAL_STATUS[signum]
        if result.returncode != expected or len(lines) != 1 or result.stderr:
            raise RuntimeError("anonymous signal probe failed: " + repr((signum, result.returncode, result.stdout, result.stderr)))
        payload = json.loads(lines[0])
        if payload != {"signal": signum, "status": expected, "anonymous": True}:
            raise RuntimeError("anonymous signal payload mismatch")
        rows.append(payload)
        print("PLAN53_ANONYMOUS_SIGNAL_PROBE=" + json.dumps(payload, sort_keys=True))
    return rows

def run_logged(command, log_name, env=None):
    global active_process
    process = subprocess.Popen(command, cwd=ROOT, env=env, stdout=subprocess.PIPE, stderr=subprocess.STDOUT)
    active_process = process
    captured = bytearray()
    try:
        while True:
            chunk = process.stdout.read(65536)
            if not chunk:
                break
            sys.stdout.buffer.write(chunk)
            sys.stdout.buffer.flush()
            captured.extend(chunk)
        status = process.wait()
    finally:
        if process.poll() is None:
            process.terminate()
            try:
                process.wait(timeout=5)
            except subprocess.TimeoutExpired:
                process.kill()
                process.wait()
        active_process = None
    write_artifact(log_name, bytes(captured), append=True)
    if status != 0:
        raise RuntimeError("verification subprocess failed: " + repr(command))
    return status

status = 1
result_payload = None
try:
    namespace_probe = anonymous_namespace_probe()
    print("PLAN53_ANONYMOUS_NAMESPACE_PROBE=" + json.dumps(namespace_probe, sort_keys=True))
    signal_rows = run_signal_probes()

    canonical_paths = [pathlib.Path(".planning/STATE.md"), pathlib.Path(".planning/ROADMAP.md"), pathlib.Path(".planning/REQUIREMENTS.md")]
    state_bytes = canonical_paths[0].read_bytes()
    state_probe = probe_state_parser(state_bytes)
    print("PLAN53_STATE_PROBE=" + json.dumps(state_probe, sort_keys=True))
    canonical_before = {str(path): hashlib.sha256(state_bytes if path == canonical_paths[0] else path.read_bytes()).hexdigest() for path in canonical_paths}

    manifest_name = new_artifact("pre-task-sha256.json")
    write_artifact(manifest_name, (json.dumps({"sha256": canonical_before, "state": state_probe["accepted"]}, sort_keys=True) + "\n").encode())
    focused_log = new_artifact("focused.log")
    full_log = new_artifact("phase1-suite.log")
    summary_name = new_artifact("phase1-suite-summary.json")

    environment = os.environ.copy()
    run_logged(["tools/phase1-python", "tools/build-site.py", "--check"], focused_log, environment)
    focused = [
        "test_static_site",
        "test_site_browser",
        "test_consolidation_recovery.ObservedRefreshPublicationTests.test_live_observed_refresh_generation_matches_attestation",
        "test_consolidation_recovery.ObservedRefreshPublicationTests.test_re_attestation_binds_exact_git_blob_live_generation_and_preserves_provenance",
        "test_consolidation_recovery.ObservedRefreshPublicationTests.test_re_attestation_holds_one_writer_transaction_against_concurrent_refresh",
        "test_consolidation_recovery.ObservedRefreshPublicationTests.test_re_attestation_rejects_reentrant_locking_without_hang",
        "test_static_site.StaticSiteBuildTests.test_build_removes_stale_routes_and_assets_and_check_rejects_complete_tree",
        "test_static_site.StaticSiteBuildTests.test_builder_refuses_symlink_and_nonregular_output_entries_without_following",
        "test_static_site.StaticSiteBuildTests.test_output_root_symlink_is_refused_before_resolution_or_mutation",
        "test_evidence.BoundedCollectorTests.test_candidate_acquisition_tracer_blocks_incomplete_dimensions",
        "test_evidence.BoundedCollectorTests.test_reviewed_collection_rejects_raw_bytes_whose_git_blob_id_mismatches_before_cache",
        "test_evidence.BoundedCollectorTests.test_reviewed_collection_generation_recovers_after_each_replacement_and_rerun",
        "test_evidence.BoundedCollectorTests.test_reviewed_collection_production_route_requires_exact_canonical_queue_and_receipt",
        "test_evidence.BoundedCollectorTests.test_reviewed_collection_rejects_state_or_other_confined_output_before_transport",
        "test_compatibility.CompatibilitySchema.test_registry_absolute_deadline_interrupts_slow_headers_and_reaps_worker",
        "test_compatibility.CompatibilitySchema.test_registry_total_deadline_stops_slow_chunks_and_records_enforced_limits",
        "test_compatibility.CompatibilitySchema.test_registry_deadline_failure_routes_impact_scoped_blocker_without_partial_response",
        "test_spike_consolidation.SpikeConsolidationTests.test_fragment_accepts_git_reachable_historical_registry_generation",
        "test_spike_consolidation.SpikeConsolidationTests.test_dry_run_accounts_for_all_spikes_and_preserves_canonical_targets",
        "test_spike_consolidation.SpikeConsolidationTests.test_duplicate_fragment_is_a_single_no_op_and_order_is_stable",
        "test_spike_consolidation.SpikeConsolidationTests.test_conflicting_stable_id_is_blocked_without_replacing_canonical_state",
        "test_spike_consolidation.SpikeConsolidationTests.test_consolidation_generation_recovers_after_each_publish_interruption",
        "test_spike_consolidation.SpikeConsolidationTests.test_consolidation_semantic_failure_does_not_enter_publication",
        "test_spike_consolidation.SpikeConsolidationTests.test_contention_and_injected_precommit_failure_preserve_byte_identical_state",
    ]
    run_logged(["tools/phase1-python", "-m", "unittest", *focused, "-v"], focused_log, environment)

    for test_root in (ROOT / "tests" / "phase1", ROOT / "tests" / "site"):
        if str(test_root) not in sys.path:
            sys.path.insert(0, str(test_root))
    stream = io.StringIO()
    suite_result = unittest.TextTestRunner(stream=stream, verbosity=2).run(unittest.defaultTestLoader.discover("tests/phase1"))
    suite_output = stream.getvalue()
    print(suite_output, end="")
    write_artifact(full_log, suite_output.encode(), append=True)

    direct = {
        "test_consolidation_recovery.ObservedRefreshPublicationTests.test_observed_refresh_rejects_tampered_or_incomplete_attestation_before_reader_entry": "ERROR OBS004: observed-refresh staged validation refused: reviewed acquisition provenance refused before observed-refresh snapshot: PRE118: review plan manifest is not the exact active plan set",
        "test_consolidation_recovery.ObservedRefreshPublicationTests.test_observed_refresh_staged_validation_binds_exact_five_target_digests": "ERROR OBS004: observed-refresh staged validation refused: reviewed acquisition provenance refused before observed-refresh snapshot: PRE118: review plan manifest is not the exact active plan set",
        "test_evidence.BaseSourceEvidenceTests.test_evidence_validation_uses_temporary_report_without_canonical_mutation": "ERROR IO001: reviewed acquisition provenance refused before compatibility snapshot: PRE118: review plan manifest is not the exact active plan set",
        "test_evidence.SourceEvidenceTests.test_evidence_validation_uses_temporary_report_without_canonical_mutation": "ERROR IO001: reviewed acquisition provenance refused before compatibility snapshot: PRE118: review plan manifest is not the exact active plan set",
        "test_governance.GovernanceValidationTests.test_phase_one_closeout_requires_governance_and_complete_evidence_contract": "ERROR PRE118: review plan manifest is not the exact active plan set",
    }
    wrapped = {
        "test_closeout_commands.TerminalPublicationTests.test_terminal_validator_binds_staged_three_file_set_before_publication",
        "test_closeout_commands.TerminalPublicationTests.test_terminal_validator_preserves_closed_ledger_through_post_closure_verification",
        "test_closeout_commands.TerminalPublicationTests.test_terminal_validator_requires_canonical_probe_attempt_ledger",
        "test_closeout_commands.TerminalPublicationTests.test_terminal_validator_requires_exact_reviewed_source_input_binding",
        "test_closeout_commands.TerminalPublicationTests.test_terminal_validator_requires_separate_role_bound_records_but_allows_one_principal",
        "test_closeout_commands.TerminalPublicationTests.test_terminal_validator_uses_external_target_only_aggregate_without_self_reference",
        "test_closeout_commands.VerificationAdjudicationTests.test_fresh_verification_has_exact_review_finding_adjudication",
    }
    wrapper_line = "phase1_terminal_validator.ValidationError: Plan 01-29 source-input verifier refused terminal evidence"
    failures = {test.id(): detail for test, detail in suite_result.failures}
    errors = {test.id(): detail for test, detail in suite_result.errors}
    failure_ids = [test.id() for test, detail in suite_result.failures]
    error_ids = [test.id() for test, detail in suite_result.errors]
    parse_error = lambda detail: [line[line.index("ERROR "):].strip() for line in detail.splitlines() if "ERROR " in line]
    parse_wrapper = lambda detail: [line.strip() for line in detail.splitlines() if line.strip().startswith("phase1_terminal_validator.")]
    direct_bad = sorted(identifier for identifier, expected in direct.items() if parse_error(failures.get(identifier, "")) != [expected])
    wrapper_bad = sorted(identifier for identifier in wrapped if parse_wrapper(errors.get(identifier, "")) != [wrapper_line] or parse_error(errors.get(identifier, "")))
    probe = run_captured(["tools/phase1-python", "tools/validate-planning.py", "--verify-phase1-source-inputs", "--review", ".planning/phases/01-research-and-truth-baseline/01-REVIEWS.md"], environment)
    expected_probe = ["bootstrap verification passed", "toolchain verification passed", "ERROR PRE118: review plan manifest is not the exact active plan set", "Planning validation failed: 1 error(s), 0 warning(s)."]

    state_after_bytes = canonical_paths[0].read_bytes()
    state_after = parse_locked_state(state_after_bytes)
    canonical_after = {str(path): hashlib.sha256(state_after_bytes if path == canonical_paths[0] else path.read_bytes()).hexdigest() for path in canonical_paths}
    duplicate = len(failure_ids + error_ids) != len(set(failure_ids + error_ids))
    categories_clear = not suite_result.skipped and not suite_result.expectedFailures and not suite_result.unexpectedSuccesses
    summary = {
        "failureIds": sorted(failure_ids),
        "errorIds": sorted(error_ids),
        "duplicate": duplicate,
        "directBad": direct_bad,
        "wrapperBad": wrapper_bad,
        "skipped": suite_result.skipped,
        "expectedFailures": suite_result.expectedFailures,
        "unexpectedSuccesses": suite_result.unexpectedSuccesses,
        "probeStdout": probe.stdout.splitlines(),
        "probeStderr": probe.stderr.splitlines(),
        "canonicalMatch": canonical_after == canonical_before,
        "stateLocked": state_after == state_probe["accepted"],
    }
    write_artifact(summary_name, (json.dumps(summary, sort_keys=True) + "\n").encode())
    artifact_hashes = {name: hashlib.sha256(read_artifact(name)).hexdigest() for name in (focused_log, full_log, summary_name, manifest_name)}
    ok = set(failure_ids) == set(direct) and len(failure_ids) == len(direct) and set(error_ids) == wrapped and len(error_ids) == len(wrapped) and not duplicate and not direct_bad and not wrapper_bad and categories_clear and probe.returncode != 0 and probe.stdout.splitlines() == expected_probe and probe.stderr.splitlines() == [] and canonical_after == canonical_before and state_after == state_probe["accepted"]
    if not ok:
        raise RuntimeError("retained suite classifier failed: " + json.dumps(summary, sort_keys=True))
    result_payload = {
        "canonicalSha256": canonical_before,
        "artifactSha256": artifact_hashes,
        "classifier": summary,
        "stateProbe": state_probe,
        "namespaceDetachment": namespace_probe,
        "signalProbes": signal_rows,
        "anonymousArtifacts": True,
        "privatePathHandedOff": False,
    }
    print("PLAN53_CONTROLLER_RESULT=" + json.dumps(result_payload, sort_keys=True))
    status = 0
except SignalExit as exc:
    status = exc.status
except BaseException as exc:
    print("PLAN53_CONTROLLER_ERROR=" + repr(exc), file=sys.stderr)
    status = 1
finally:
    cleanup_result = close_artifacts()
    print("PLAN53_CONTROLLER_CLEANUP=" + cleanup_result)
    if cleanup_result != "closed":
        status = 1
sys.exit(status)
'</automated>
  </verify>
  <acceptance_criteria>All authorized repair groups plus site suites pass; every private artifact is anonymous mode-0600/current-UID/regular/link-count-zero; HUP/INT/TERM probes return 129/130/143; the namespace-detachment probe retains exact descriptor identity and payload through fsync, proves no namespace entry exists, and closes successfully; STATE parser accepts current gaps_found/0 and rejects passed/1 mutations before hashing, then locked STATE is re-parsed unchanged after subprocesses; failure/error IDs and exact diagnostics equal retained contract; controller emits artifact/canonical hashes without private path; finally reports closed; all three canonical files match pre-task SHA-256.</acceptance_criteria>
  <done>Automated blockers owned by this repair set are closed, private evidence is removed by its creator before return, PRE118 remains explicit deferred evidence, and Phase 1 remains gaps_found.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Judge visual hierarchy, focus visibility, responsive reading, motion comfort, and evidence-label clarity</name>
  <read_first>Task 1 controller result JSON, state/namespace-detachment/signal probe rows, anonymous close outcome, streamed focused/full-suite output, and emitted canonical/artifact SHA-256 values; site/README.md preview contract; generated home, learn, architecture, and sources pages</read_first>
  <files>None — human judgment is recorded only in 01-53-SUMMARY.md.</files>
  <what-built>Authorized gap and external-review repairs with green focused/site checks and a PRE118-only retained-suite backstop.</what-built>
  <action>Only after Task 1 passes and reports PLAN53_CONTROLLER_CLEANUP=closed, reparse current STATE.md and require status gaps_found plus completed_phases 0, then require its SHA-256 and ROADMAP.md/REQUIREMENTS.md SHA-256 to equal the controller map. Carry forward exact controller JSON; no private pathname exists. Print loopback URL plus state/namespace-detachment/signal matrices. Start generated site loopback-only through verified wrapper. Ask human to inspect all four routes at wide/narrow widths, keyboard, reduced motion, transcript replay/reset, and source/status labels. This checkpoint approves visual/accessibility judgment only; it cannot alter canonical state or accept phase/ADR/risk/Firefox/external/release/transition.</action>
  <how-to-verify>
    1. Open the executor-provided loopback URL and follow Home → Learn → Architecture → Sources at desktop and 320px-equivalent width; confirm reading hierarchy is clear and no horizontal page overflow appears.
    2. Use keyboard only from the skip link through primary navigation, calls to action, transcript disclosure, replay/reset, and source links; confirm focus is always visible and order is understandable.
    3. Enable reduced-motion emulation, click Replay transcript, and confirm no smooth animated movement or discomfort; confirm reset changes only the optional trace.
    4. Read several source/fact/blocker cards and confirm authority tier, evidence class, maturity, state, uncertainty, source identity, and refresh trigger make draft/implementation/blocked material visibly non-settled.
    5. Approve only if all four judgments pass; otherwise report exact route, viewport/preference, control/record ID, and observed defect.
  </how-to-verify>
  <verify>
    <automated>tools/phase1-python tools/build-site.py --check &amp;&amp; tools/phase1-python -m unittest discover -s tests/site -p 'test_static_site.py' -v &amp;&amp; tools/phase1-python -m unittest discover -s tests/site -p 'test_site_browser.py' -v</automated>
    <human-check>
      1. Open the executor-provided loopback URL and follow Home → Learn → Architecture → Sources at desktop and 320px-equivalent width; confirm reading hierarchy is clear and no horizontal page overflow appears.
      2. Use keyboard only from the skip link through primary navigation, calls to action, transcript disclosure, replay/reset, and source links; confirm focus is always visible and order is understandable.
      3. Enable reduced-motion emulation, click Replay transcript, and confirm no smooth animated movement or discomfort; confirm reset changes only the optional trace.
      4. Read several source/fact/blocker cards and confirm authority tier, evidence class, maturity, state, uncertainty, source identity, and refresh trigger make draft/implementation/blocked material visibly non-settled.
      5. Approve only if all four judgments pass; otherwise report exact route, viewport/preference, control/record ID, and observed defect.
    </human-check>
  </verify>
  <acceptance_criteria>Human explicitly approves all five bounded judgments only after fresh STATE parse proves gaps_found/0, controller reports anonymousArtifacts=true and cleanup=closed, namespace-detachment/signal/state probes pass, and all canonical digests still match; approval keeps Phase 1 and deferred boundaries non-passing/unaccepted.</acceptance_criteria>
  <done>Human visual/accessibility judgment is explicitly recorded without auto-accepting any phase, ADR, residual risk, browser workaround, release, or transition.</done>
  <resume-signal>Type "approved — visual/accessibility judgment only" or describe exact issues.</resume-signal>
</task>

</tasks>

<verification_only_closeout_override>
This plan intentionally overrides normal `execute-plan.md` closeout because its verified outcome is canonical non-mutation. After human approval and before any closeout write, require Task 1 result with `anonymousArtifacts: true`, `privatePathHandedOff: false`, passing state/namespace-detachment/signal probes, and `PLAN53_CONTROLLER_CLEANUP=closed`. Reread and safely parse `.planning/STATE.md`; require status `gaps_found` and integer `progress.completed_phases: 0` before hashing. Recompute exact SHA-256 for STATE.md, ROADMAP.md, and REQUIREMENTS.md and require equality with controller map. Any missing result, malformed/pre-transition state, descriptor-close refusal, or digest mismatch stops before SUMMARY creation.

When that guard passes, create only `.planning/phases/01-research-and-truth-baseline/01-53-SUMMARY.md` using standard template. Record focused results, exact failure/error category/line-set equality, anonymous descriptor invariants, namespace-detachment/signal/state probe rows, cleanup=closed, emitted hashes, human judgment, pre-closeout safe STATE parse plus three-file hash equality, and PRE118/Phase 1 non-passing status. Stage exactly SUMMARY; require NUL-parsed cached path set equals that one path; create one `docs(01-53): complete integrated gap verification plan` commit containing only it.

For this plan, explicitly skip `execute-plan.md` steps `update_current_position`, `extract_decisions_and_issues`, `update_session_continuity`, `update_roadmap`, `update_requirements`, and `git_commit_metadata`; do not call `state.advance-plan`, `state.update-progress`, `state.record-metric`, `state.add-decision`, `state.add-blocker`, `state.record-session`, `roadmap.update-plan-progress`, `requirements.mark-complete`, or the standard metadata commit. Task 1 and Task 2 produce no per-task commit; the summary-only closeout commit is Plan 01-53's sole commit.

After commit, safely reparse STATE and require gaps_found/0, recompute all three canonical SHA-256 values and require controller equality, then require closeout commit NUL-parsed changed-path set equals only SUMMARY. Any parse/hash/path failure blocks completion. No later private-artifact operation exists because anonymous handles closed before Task 1 returned.
</verification_only_closeout_override>

## Artifacts this phase produces

| Path | New record | Contract |
|---|---|---|
| `.planning/phases/01-research-and-truth-baseline/01-53-SUMMARY.md` | Execution summary | Records focused checks, exact category/line-set PRE118 attribution, three-file SHA-256 equality, exact summary-only closeout path, and bounded human judgment. |
| `PLAN53_CONTROLLER_RESULT` | Ephemeral emitted result | Records artifact/canonical SHA-256, exact classifier facts, and `privatePathHandedOff: false`; copied into the SUMMARY and never treated as canonical authority. |
| Anonymous TemporaryFile descriptors | Ephemeral runner output | Hold manifest, focused/full logs, and classifier JSON with mode 0600, current UID, regular type, and link count zero; all handles close before Task 1 returns. |
| `PLAN53_STATE_PROBE` / `PLAN53_ANONYMOUS_NAMESPACE_PROBE` / `PLAN53_ANONYMOUS_SIGNAL_PROBE` / `PLAN53_CONTROLLER_CLEANUP` | Ephemeral lifecycle evidence | Proves current locked state, pre-transition rejection, absence of any replaceable private namespace entry, stable descriptor identity and payload, exact signal exits, and final close. |

No production/test symbol, canonical path, schema/record field, CLI flag, package, external service, API integration, or ORM schema is created. Official API-coverage detector returned `detected:false`; ORM scan returned no schema file, so no coverage declaration or schema-push task applies.

<probe_coverage>
## Spec-less Edge Probe Disposition

| Requirement | Category | Disposition |
|---|---|---|
| EVID-01 | adjacency | Explicit: Plan 01-49 requires exact repository/commit/path equality; any mismatch separates into rejection. |
| EVID-01 | empty | Explicit: Plan 01-49 rejects null/empty identity, URL, digest, and retrieval fields. |
| EVID-01 | encoding | Flagged assumption in Plan 01-49; no Unicode/path normalization rule is invented. |
| EVID-01 | ordering | Flagged assumption in Plan 01-49; record order is outside CR-08 validity. |
| EVID-02 | adjacency, empty, ordering | Three explicit flagged assumptions in this plan; scoped repairs do not alter these semantics. |
| EVID-03 | unclassified | Explicit flagged assumption in Plan 01-51; fixture proves blocked dimensions only. |
| EVID-04 | unclassified | Explicit flagged assumption in Plan 01-50; exact historical provenance only. |
| OPER-01 | idempotency | Explicit: Plan 01-50 duplicate-fragment/order regression. |
| OPER-01 | concurrency | Explicit: Plan 01-50 contention/interruption/precommit regressions. |
| OPER-03 | adjacency, empty, ordering | Three explicit flagged assumptions in this plan; no governance tie-break model is invented. |

Coverage: 14 applicable; 4 explicit predicates, 10 explicitly unresolved/flagged assumptions, 0 silent drops. The retained full suite is a structured backstop and abstains wherever PRE118 prevents explicit proof.
</probe_coverage>

<prohibition_probe>
## Recall → Precision Result

Stage 1 considered silent authority inflation, provenance rebinding, compatibility qualification, conflict collapse, auto-acceptance, generic input attacks, and routine test hygiene for each Phase 1 requirement. Stage 2 dropped routine-engineering items and routed generic OWASP/path/input concerns to the ASVS threat registers rather than minting canon prohibitions. Six bespoke transparency/governance prohibitions remain descriptor-less and flagged-unverified across Plans 01-49 through 01-57—one for each EVID-01, EVID-02, EVID-03, EVID-04, OPER-01, and OPER-03. None is auto-dismissed; no `check_*` descriptor or fabricated canonical compliance item is authored.
</prohibition_probe>

<source_coverage_audit>
## Multi-Source Coverage Audit

| SOURCE | ID | Scoped feature/decision | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Dated immutable-source evidence remains reliable without presenting unsettled behavior as fact | 01-49, 01-50, 01-54, 01-56, 01-53 | COVERED | Public identity, live attestation, acquisition identity/transactionality, and retained spike provenance are repaired; final gate preserves unsettled/deferred labels. |
| REQ | EVID-01 | Immutable source traceability | 01-49, 01-54, 01-56, 01-53 | COVERED | CR-08 strict tuple/digest/time validation plus exact live Git blobs, raw-byte object-ID checks, and integrated proof. |
| REQ | EVID-02 | Conflict, uncertainty, drift, impact, refresh visibility | 01-54, 01-53 | COVERED | Live targetMap/provenance re-attestation, regression backstop, and explicit unresolved edge assumptions; no record semantics changed. |
| REQ | EVID-03 | Complete-before-acceptance compatibility | 01-51, 01-56, 01-57, 01-53 | COVERED | WR-04 tracer, atomic acquisition generation, and registry deadline reach blocked-dimension assertions. |
| REQ | EVID-04 | Reproducible mandatory-spike evidence | 01-50, 01-57, 01-53 | COVERED | Exact historical registry bytes and bounded live collection restore reproducible impact validation/consolidation. |
| REQ | OPER-01 | Detect/map drift without silent rewrite | 01-50, 01-54, 01-56, 01-57, 01-53 | COVERED | Live/historic provenance, atomic acquisition, bounded registry reads, idempotency, contention, conflict, and no-rebind assertions. |
| REQ | OPER-03 | Roles, exit evidence, GSD status, traceability | 01-52, 01-55, 01-53 | COVERED | Complete generated-tree ownership, direct browser evidence, explicit human gate, locked gaps_found state. |
| RESEARCH | immutable Git source pattern | Commit/blob/digest before claim | 01-49, 01-50, 01-54, 01-56 | COVERED | URL tuple, live target objects, collected raw bytes, and Git-object history fail closed. |
| RESEARCH | refresh detects/routes | Retain history; never rewrite meaning automatically | 01-50, 01-54, 01-56 | COVERED | Historical fragments remain byte-unchanged while staged and acquisition generations publish atomically. |
| RESEARCH | exact ignored cache | Repository-owned cache only | 01-51, 01-56 | COVERED | Reviewed bytes bind to tree identity before cache admission; fixture keeps exact ignored cache root. |
| RESEARCH | browser/accessibility evidence | Approved Chromium, direct observation, reduced motion | 01-52, 01-55, 01-53 | COVERED | Complete served tree is owned; Firefox blocker explicitly retained; no workaround. |
| RESEARCH | package legitimacy/toolchain | No new install; wrapper-only commands | all | EXCLUDED | Existing approved isolated tools reused; no package action. |
| REVIEW | finding 1 | Plan 01-53 private evidence must not cross an unsafe named-directory lifecycle | 01-53 | COVERED | Manifest/log/result artifacts use anonymous mode-0600 link-count-zero descriptors only; signal and namespace-detachment probes prove exact descriptor closure with no private namespace entry or recursive deletion. |
| REVIEW | finding 2 | Live observed-refresh attestation targetMap is stale | 01-54, 01-53 | COVERED | Exact current five-target generation is re-attested with retained provenance and integrated equality proof. |
| REVIEW | finding 3 | Static build/check ignores stale output files | 01-55, 01-53 | COVERED | Complete-set equality, safe stale deletion, refusal tests, and 404 proof. |
| REVIEW | finding 4 | Reviewed acquisition trusts tree metadata without rehashing raw bytes as the blob | 01-56, 01-53 | COVERED | Canonical Git blob rehash occurs before cache/source-record effects. |
| REVIEW | finding 5 | Acquisition publication can leave a partial canonical generation | 01-56, 01-53 | COVERED | Four outputs render and publish through one recoverable locked generation. |
| REVIEW | finding 6 | Registry per-operation timeout lacks a total deadline | 01-57, 01-53 | COVERED | One monotonic deadline covers connect and every chunk read with blocker routing. |
| REVIEW | fresh finding 1 | Plan 01-53 validate-then-rmtree pathname replacement race | 01-53 | COVERED | Named private root and every private pathname operation are eliminated; descriptor fstat proves link count zero and stable identity through exact write/read/fsync before close, so no namespace entry exists to replace or recursively remove. |
| REVIEW | fresh finding 2 | Plan 01-54 stale read-to-publish lock gap | 01-54, 01-53 | COVERED | One non-reentrant writer transaction spans registered snapshot, Git/worktree revalidation, attestation derivation, and publication; concurrent refresh regression is integrated. |
| REVIEW | fresh finding 3 | Plan 01-57 urllib per-operation timeout permits slow headers | 01-57, 01-53 | COVERED | Parent watchdog interrupts/reaps isolated connect/header/body worker; slow-header fixture is integrated. |
| REVIEW | fresh finding 4 | Plan 01-53 hashes STATE without asserting locked status | 01-53 | COVERED | Safe frontmatter parser requires gaps_found and integer completed_phases 0 before baseline, before human gate/closeout, and after commit; passed/1 probes reject. |
| REVIEW | fresh finding 5 | Plan 01-56 arbitrary confined queue/receipt overwrite | 01-56, 01-53 | COVERED | Production route requires exact role-specific canonical paths; STATE/claims/swapped-role rejection probes run with zero transport. |
| REVIEW | fresh finding 6 | Plan 01-55 resolves caller output root before no-follow check | 01-55, 01-53 | COVERED | Caller root is lstat/no-follow-opened before resolution/mutation; symlink-root external-sentinel fixture is integrated. |
| CONTEXT | D-01, D-02, D-03 | Blocking-first bounded research/snowballing | — | EXCLUDED | Owner forbids new research; completed evidence history remains unchanged. |
| CONTEXT | D-04 | Exact repository/ref/path/time/digest identity | 01-49, 01-50 | COVERED | Public identity and retained generations enforce exactness. |
| CONTEXT | D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12 | Acquisition, directional evidence, cache, external-write, missing-source, parallel-conflict policy | 01-49, 01-51, 01-53 | COVERED | Exact cache and conflict labels preserved; no acquisition/external write occurs. |
| CONTEXT | D-13 | Retain old identity/digest/replacement history | 01-50 | COVERED | Exact Git-reachable historic bytes bind fragments. |
| CONTEXT | D-14, D-15, D-16, D-17, D-18, D-19 | Evidence acceptance, human approval, fact/observation/inference/conflict separation | 01-49, 01-50, 01-53 | COVERED | Validators preserve fields; human gate cannot approve claims or ADRs. |
| CONTEXT | D-20 | Stale evidence remains history and blocks dependent approval | 01-50, 01-53 | COVERED | History accepted only as exact provenance; no automatic state promotion. |
| CONTEXT | D-21, D-22, D-24 | Disposable/live spike execution boundaries | — | EXCLUDED | No spike or live public probe is run; retained evidence only. |
| CONTEXT | D-23 | Retained spike recipe/measurement/source digests | 01-50 | COVERED | Fragment digests remain immutable and revalidatable. |
| CONTEXT | D-25, D-26, D-27, D-28 | ADR recommendation only, scoped blockers, explicit roles/sign-offs | 01-53 | COVERED | Human checkpoint is judgment-only; blockers and approval boundaries remain. |
| CONTEXT | D-29, D-30, D-31, D-32 | Stable IDs, canonical YAML/schema, relation vocabulary, migrations | — | EXCLUDED | No ID, YAML schema, relation vocabulary, or migration change. |
| CONTEXT | D-33, D-34, D-35 | Passed/blocked exit, reusable tracks, no waived gates | 01-53 | COVERED | `gaps_found`/0 completed phases retained; PRE118 remains non-passing. |
| CONTEXT | D-36 | Targeted refresh work instead of broad rerun | 01-50 | COVERED | Exact provenance seam only; no research expansion. |
| CONTEXT | D-37 | Chromium + Firefox baseline | 01-52, 01-53 | COVERED | Approved Chromium verifies scope; Firefox remains explicit blocker without workaround. |
| CONTEXT | D-38 | Exact environment/commands/hashes | all | COVERED | All Python/test/browser commands use `tools/phase1-python`. |
| CONTEXT | D-39 | Five samples for nondeterministic measurements | — | EXCLUDED | No new nondeterministic measurement is produced. |
| OWNER DEFERRAL | PRE118; CR-05–CR-07/SPK-G; WR-02; runtime/guest/native/portable; deploy/release; ADR/external/residual/transition; Firefox workaround | Non-passing deferred boundaries | 01-53 | EXCLUDED | Explicitly preserved; absence from repair plans is authorized, not a source gap. |

Audit result: all four active gaps and all six Phase 1 requirements are covered; remaining source items are explicit unchanged-history or owner-authorized exclusions. No unplanned item remains.
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| focused tests → phase-wide conclusion | Narrow green results can misrepresent a still-red retained suite. |
| full-suite output → allowed deferral | A loose classifier could suppress a new defect under PRE118. |
| automated/human verification → canonical phase authority | Test or visual approval must not mutate status or accept decisions. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-53-01 | Repudiation | full-suite attribution | high | mitigate | Require exact equality between the declared TestCase.id() allowlist and `result.failures + result.errors`, exact direct/wrapper diagnostics, a nonzero source-verifier probe with the precise PRE118 line, and at least one retained refusal. This HIGH threat blocks the human checkpoint on any mismatch. |
| T-01-53-02 | Elevation of Privilege | verification → phase/ADR authority | high | mitigate | Human approval authorizes only bounded visual/accessibility judgment. Explicitly bypass every standard STATE/ROADMAP/REQUIREMENTS mutation and metadata-commit step; Plan 01-53 may create one closeout commit whose sole path is `01-53-SUMMARY.md`, so automated or human green cannot transition phase/ADR authority. |
| T-01-53-03 | Tampering | canonical status and closeout evidence | high | mitigate | Record exact pre-task SHA-256 for all three of STATE.md, ROADMAP.md, and REQUIREMENTS.md; require all three values unchanged immediately before SUMMARY creation and again after the closeout commit; require both the staged NUL-parsed path set and committed diff-tree path set to equal only `01-53-SUMMARY.md`; block completion on any hash, path, or standard-metadata-step-bypass violation. |
| T-01-53-04 | Information Disclosure | local preview and ephemeral evidence | low | mitigate | Keep logs/manifest/result in anonymous mode-0600 link-count-zero descriptors, prove stable fstat identity and exact bytes through fsync, close all before checkpoint, create or transfer no private namespace entry, and expose preview loopback-only. |
| T-01-53-05 | Elevation of Privilege | stable hash mistaken for locked phase state | high | mitigate | Safely parse STATE before baseline, human gate, closeout, and post-commit; require gaps_found and integer completed_phases 0; deterministic passed/1 mutations must reject. |

All HIGH threats block progress until mitigations pass. No package-manager install occurs; supply-chain package legitimacy gate is not applicable.
</threat_model>

<verification>
Wave-4 Task 1 runs only after all eight dependencies and blocks on any anonymous-descriptor invariant, namespace-detachment/close proof, signal exit, state parse/mutation probe, scoped regression, PRE118 classifier, canonical hash, or close refusal. Task 2 is reachable only after cleanup=closed plus fresh STATE gaps_found/0 parse and matching hashes. Closeout repeats state parse/hashes after commit, requires sole commit path SUMMARY, and bypasses standard status/roadmap/requirements metadata. Any failed parse/probe/close/hash/path check blocks rather than warns.
</verification>

<success_criteria>
All authorized gaps and twelve accumulated external-review defects are closed; retained suite matches exact non-passing PRE118 contract; private artifacts remain anonymous, retain exact identity and payload through fsync, expose no replaceable namespace entry, and close cleanly; STATE safely parses as gaps_found/0 before baseline, human gate, closeout, and post-commit while passed/1 probes reject; bounded human judgment is recorded; post-closeout canonical hashes match; sole commit path is SUMMARY; standard metadata mutation remains bypassed. Any unmet gate blocks authorization.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-53-SUMMARY.md` when done.
</output>
