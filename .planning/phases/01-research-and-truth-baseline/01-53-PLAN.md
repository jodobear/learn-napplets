---
phase: 01-research-and-truth-baseline
plan: 53
type: execute
wave: 3
depends_on: [01-49, 01-50, 01-51, 01-52]
files_modified: []
autonomous: false
gap_closure: true
requirements: [EVID-01, EVID-02, EVID-03, EVID-04, OPER-01, OPER-03]
must_haves:
  truths:
    - "After wave-1 Plans 01-49, 01-50, and 01-51 and wave-2 Plan 01-52 complete, all four focused gap regressions pass together through tools/phase1-python, and no CR-08, IMP011, IMP016, WR-04, or reduced-motion replay failure remains."
    - statement: "The complete retained Phase 1 suite is rerun as a backstop; failure IDs equal the declared direct-ID set, error IDs equal the declared wrapper-ID set, their union equals the complete TestCase.id() allowlist, each direct traceback and the direct probe has its own exact parsed line set, each wrapper has its exact sole exception line, and skips, expected failures, unexpected successes, duplicate IDs, extra ERROR lines, and every other unexpected result category are rejected."
      verification: backstop
    - "Pre-task SHA-256 values for STATE.md, ROADMAP.md, and REQUIREMENTS.md remain exact through the human checkpoint and verification-only closeout; only 01-53-SUMMARY.md is created and committed, and the standard execute-plan state/roadmap/requirements mutation and metadata-commit steps are explicitly bypassed per D-25 and D-33 through D-35."
    - "Human visual/accessibility judgment occurs only after automated blockers close and can approve only the local static site's reading hierarchy, focus visibility, responsive layout, motion comfort, and evidence-label clarity."
    - "All Plan 01-53 ephemeral evidence lives inside one securely created private temporary directory that is non-symlink, owned by the executor UID, mode 0700, printed with its actual artifact paths for the human checkpoint, and removed only when recorded device/inode ownership still matches after checkpoint finalization."
  prohibitions:
    - requirement_id: EVID-02
      category: transparency
      status: unresolved
      verification: null
      statement: "MUST NOT collapse conflict, uncertainty, drift, or blocked evidence into settled upstream fact."
  flagged_assumptions:
    - "EVID-02 adjacency probe remains unresolved: the four scoped repairs do not define whether equal/touching conflict records merge, collide, or remain parallel."
    - "EVID-02 empty probe remains unresolved: this gap set does not redefine empty conflict/drift collection semantics."
    - "EVID-02 ordering probe remains unresolved: this gap set does not create a new stable-order contract for equal conflict records."
    - "OPER-03 adjacency probe remains unresolved: this gap set does not define how coincident approval/governance events compose."
    - "OPER-03 empty probe remains unresolved: absence of a required human approval remains non-passing, but no broader empty-governance model is inferred."
    - "OPER-03 ordering probe remains unresolved: existing role/date/order rules are retained without inventing a new tie-break contract."
  artifacts:
    - path: .planning/phases/01-research-and-truth-baseline/01-53-SUMMARY.md
      provides: "Focused pass matrix, exact category/line-set PRE118 attribution, three-file pre/post SHA-256 equality, exact summary-only closeout commit path, and explicit human visual/accessibility judgment outcome."
  key_links:
    - from: "Plans 01-49 through 01-52 focused regressions"
      to: "complete tests/phase1 retained-suite backstop"
      via: "Wave 3 begins only after all four dependencies; the integration tracer runs focused checks first, then requires exact equality between unittest TestCase.id() results and the declared PRE118 allowlist."
      pattern: "PRE118"
    - from: "automated site checks"
      to: "human visual/accessibility checkpoint"
      via: "The checkpoint is unreachable until the automated prerequisite command passes."
      pattern: "blocking"
    - from: "pre-task canonical-file digest manifest"
      to: "summary-only closeout commit"
      via: "A pre-closeout guard runs before normal closeout writes; the post-closeout gate repeats all three digests and requires the closeout commit's NUL-parsed path set to equal only 01-53-SUMMARY.md."
      pattern: "verification_only_closeout"
---

<objective>
Prove the four authorized gaps are closed together, preserve PRE118 as explicit deferred non-passing evidence, then obtain bounded human visual/accessibility judgment.

Purpose: Prevent a locally green focused fix from hiding an integration regression or crossing the owner-authorized phase/approval boundary.
Output: A summary-only evidence record containing focused/full-suite results, exact PRE118 attribution, unchanged canonical status, and the human judgment result.
</objective>

<execution_context>
@/home/at/.codex/gsd-core/workflows/execute-plan.md
@/home/at/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-RESEARCH.md
@.planning/phases/01-research-and-truth-baseline/01-FINAL-VERIFICATION.md
@.planning/phases/01-research-and-truth-baseline/deferred-items.md
@.planning/phases/01-research-and-truth-baseline/01-49-SUMMARY.md
@.planning/phases/01-research-and-truth-baseline/01-50-SUMMARY.md
@.planning/phases/01-research-and-truth-baseline/01-51-SUMMARY.md
@.planning/phases/01-research-and-truth-baseline/01-52-SUMMARY.md
</context>

<scope_boundary>
This is a verification-and-human-judgment plan. Do not modify production, test, research, spike, STATE.md, ROADMAP.md, REQUIREMENTS.md, review-binding, package, runtime, browser pin, ADR, deployment, release, or transition files. PRE118 must remain visible and deferred. The only persistent closeout artifact is 01-53-SUMMARY.md. CR-05 through CR-07/SPK-G, WR-02, package admission, runtime/guest execution, native-runtime qualification, portable targets, deployment/release, ADR acceptance, external action, residual-risk acceptance, Phase 2 transition, and Firefox/cross-browser workaround remain excluded.
</scope_boundary>

<assumption_delta_decision>
Generalized noun: Phase 1 evidence baseline. Decision: no-change. The detector's “choose” signal describes later human use of the baseline; this gap set introduces no alternate primary key, source of truth, or architecture identity.
</assumption_delta_decision>

<retained_failure_contract>
The source refusal is produced by `tools/phase1-python tools/validate-planning.py --verify-phase1-source-inputs --review .planning/phases/01-research-and-truth-baseline/01-REVIEWS.md` through `review_manifest_errors` in `tools/validate-planning.py`. Classification is category- and line-exact; broad PRE118 substring presence is insufficient.

For each direct traceback, parse `ERROR ` evidence by taking the suffix beginning at the first literal `ERROR ` on every traceback line that contains it. Preserve one occurrence per parsed line, then require both occurrence-count equality and set equality against this exact TestCase.id() map:
- `test_consolidation_recovery.ObservedRefreshPublicationTests.test_observed_refresh_rejects_tampered_or_incomplete_attestation_before_reader_entry` → {`ERROR OBS004: observed-refresh staged validation refused: reviewed acquisition provenance refused before observed-refresh snapshot: PRE118: review plan manifest is not the exact active plan set`}
- `test_consolidation_recovery.ObservedRefreshPublicationTests.test_observed_refresh_staged_validation_binds_exact_five_target_digests` → {`ERROR OBS004: observed-refresh staged validation refused: reviewed acquisition provenance refused before observed-refresh snapshot: PRE118: review plan manifest is not the exact active plan set`}
- `test_evidence.BaseSourceEvidenceTests.test_evidence_validation_uses_temporary_report_without_canonical_mutation` → {`ERROR IO001: reviewed acquisition provenance refused before compatibility snapshot: PRE118: review plan manifest is not the exact active plan set`}
- `test_evidence.SourceEvidenceTests.test_evidence_validation_uses_temporary_report_without_canonical_mutation` → {`ERROR IO001: reviewed acquisition provenance refused before compatibility snapshot: PRE118: review plan manifest is not the exact active plan set`}
- `test_governance.GovernanceValidationTests.test_phase_one_closeout_requires_governance_and_complete_evidence_contract` → {`ERROR PRE118: review plan manifest is not the exact active plan set`}

The direct probe must return nonzero, have empty stderr, and have stdout lines exactly equal in order and cardinality to:
- `bootstrap verification passed`
- `toolchain verification passed`
- `ERROR PRE118: review plan manifest is not the exact active plan set`
- `Planning validation failed: 1 error(s), 0 warning(s).`

For every wrapper traceback below, parse stripped lines beginning `phase1_terminal_validator.` and require the one-element sequence exactly [`phase1_terminal_validator.ValidationError: Plan 01-29 source-input verifier refused terminal evidence`]. Its parsed `ERROR ` line sequence must be empty:
- `test_closeout_commands.TerminalPublicationTests.test_terminal_validator_binds_staged_three_file_set_before_publication`
- `test_closeout_commands.TerminalPublicationTests.test_terminal_validator_preserves_closed_ledger_through_post_closure_verification`
- `test_closeout_commands.TerminalPublicationTests.test_terminal_validator_requires_canonical_probe_attempt_ledger`
- `test_closeout_commands.TerminalPublicationTests.test_terminal_validator_requires_exact_reviewed_source_input_binding`
- `test_closeout_commands.TerminalPublicationTests.test_terminal_validator_requires_separate_role_bound_records_but_allows_one_principal`
- `test_closeout_commands.TerminalPublicationTests.test_terminal_validator_uses_external_target_only_aggregate_without_self_reference`
- `test_closeout_commands.VerificationAdjudicationTests.test_fresh_verification_has_exact_review_finding_adjudication`

`result.failures` TestCase.id() values must equal the five direct IDs exactly; `result.errors` IDs must equal the seven wrapper IDs exactly; their union must equal the twelve-ID allowlist with no duplicates. `result.skipped`, `result.expectedFailures`, and `result.unexpectedSuccesses` must each be empty, and no other result category may contain an entry. Any extra parsed `ERROR ` line fails even when a permitted PRE118 line is also present. An empty retained-result set, a passing direct probe, any line-order/cardinality mismatch, category migration, missing ID, or unexpected ID fails verification. PRE118 remains deferred and non-passing; this contract never counts it as phase-pass evidence.
</retained_failure_contract>

<tasks>

<task type="tracer">
  <name>Task 1: Run the four-gap integration path and classify the complete retained suite</name>
  <read_first>Plans 01-49 through 01-52 summaries and focused test names; 01-FINAL-VERIFICATION behavioral checks; deferred-items.md PRE118 evidence; tools/validate-planning.py `_parse_review_manifest` and `review_manifest_errors`; tests/phase1/test_closeout_commands.py current test class/method IDs and terminal wrapper result; retained_failure_contract above; STATE.md frontmatter</read_first>
  <files>None — verification-only; write results only to 01-53-SUMMARY.md through normal executor output.</files>
  <behavior>
    - CR-08 positive/negative source identity tests pass with generated output current.
    - Historical impact provenance and all six consolidation safety regressions pass without fragment changes.
    - The WR-04 tracer and strict-root neighbors pass together.
    - Direct reduced/no-preference replay option tests and the complete site suites pass.
    - Full Phase 1 discovery runs; failures equal the five direct IDs, errors equal the seven wrapper IDs, traceback/probe line sequences match retained_failure_contract exactly, no duplicate/extra ERROR line or category migration occurs, and skipped/expected-failure/unexpected-success collections remain empty.
    - One private mode-0700 non-symlink temporary directory owned by the executor UID contains the pre-task digest manifest, focused log, full-suite log, and classifier summary; its actual paths and recorded device/inode are printed for checkpoint and closeout use.
    - STATE.md, ROADMAP.md, and REQUIREMENTS.md retain their pre-task exact SHA-256 values and locked status values remain present.
  </behavior>
  <action>Addresses review concerns: exact PRE118 category/line classification, canonical-file guarding before closeout, and safe temporary evidence ownership. Run only after all four dependencies complete. As Task 1's first operation, invoke a Python initializer that creates one directory with `tempfile.mkdtemp(prefix="learn-napplets-01-53-", dir="/tmp")`, records its initial UID/device/inode/type/mode, uses `os.lstat` to require one real non-symlink directory owned by `os.geteuid()` with mode exactly `0700`, writes the three-file SHA-256 manifest, and emits exactly one nonempty absolute `/tmp/learn-napplets-01-53-*` path on stdout. If validation or manifest creation fails after `mkdtemp`, the initializer must re-open `/tmp` by directory descriptor, re-stat the child without following symlinks, require exact equality with the recorded UID/device/inode/type/mode plus executor ownership, require `shutil.rmtree.avoids_symlink_attacks`, and remove that partially created child through dirfd-relative `shutil.rmtree` before propagating failure. In shell, assign the initializer output first, capture and check its status, validate its one-line path, then derive and validate a known identity tuple from a fresh lstat plus manifest; only afterward export `PLAN53_TMP`/identity and install the EXIT cleanup trap. Assignment and export must remain separate so command-substitution failure cannot be masked. Put every later ephemeral artifact inside the verified directory; no fixed `/tmp` filename is allowed. Before any artifact read, write, or `tee`, each Python classifier invocation must fresh-lstat the directory against the exported known UID/device/inode/type/mode and abort immediately on mismatch; only a passing identity gate may read the manifest or create logs. Run the builder check, focused gap/site groups, then complete `tests/phase1` discovery. Apply retained_failure_contract literally: exact failure-ID and error-ID sets separately, duplicate rejection, exact direct ERROR-line sequences, exact wrapper exception sequence with zero ERROR lines, exact direct-probe stdout/stderr sequences, and empty skipped/expectedFailures/unexpectedSuccesses; reject every missing, extra, or category-migrated result. Write focused output, complete output, and classifier status to the private directory and print actual directory, manifest, log, and summary paths plus recorded device/inode for the human checkpoint. On Task 1 failure, print diagnostics, then remove only the exact known directory after a fresh dirfd-relative lstat proves UID/device/inode/type/mode identity and executor ownership and `shutil.rmtree.avoids_symlink_attacks` is true; a mismatch fails closed without deletion. On success retain the owned directory through Task 2 and verification-only closeout. Never convert PRE118 into green or phase-pass evidence. Do not change deferred-items.md. Assert STATE.md still contains `status: gaps_found` and `completed_phases: 0`, compare all three canonical hashes to the manifest, and stop before human judgment on any focused failure, classifier mismatch, missing PRE118 refusal, temp-ownership failure, or canonical-file mismatch.</action>
  <verify>
    <automated>set -o pipefail
PLAN53_TMP="$(tools/phase1-python -c 'exec("""import hashlib
import json
import os
import pathlib
import shutil
import stat
import tempfile

path = None
created = None
try:
    path = pathlib.Path(tempfile.mkdtemp(prefix="learn-napplets-01-53-", dir="/tmp"))
    created = os.lstat(path)
    created_identity = (
        created.st_uid,
        created.st_dev,
        created.st_ino,
        stat.S_IFMT(created.st_mode),
        stat.S_IMODE(created.st_mode),
    )
    valid = (
        path.is_absolute()
        and path.parent == pathlib.Path("/tmp")
        and path.name.startswith("learn-napplets-01-53-")
        and stat.S_ISDIR(created.st_mode)
        and not stat.S_ISLNK(created.st_mode)
        and created.st_uid == os.geteuid()
        and stat.S_IMODE(created.st_mode) == 0o700
    )
    if not valid:
        raise RuntimeError("initializer private-temp validation failed")
    files = [
        pathlib.Path(".planning/STATE.md"),
        pathlib.Path(".planning/ROADMAP.md"),
        pathlib.Path(".planning/REQUIREMENTS.md"),
    ]
    hashes = {str(item): hashlib.sha256(item.read_bytes()).hexdigest() for item in files}
    manifest_path = path / "pre-task-sha256.json"
    manifest_path.write_text(
        json.dumps(
            {
                "ownerUid": created.st_uid,
                "device": created.st_dev,
                "inode": created.st_ino,
                "type": stat.S_IFMT(created.st_mode),
                "mode": stat.S_IMODE(created.st_mode),
                "sha256": hashes,
            },
            sort_keys=True,
        )
        + "\n",
        encoding="utf-8",
    )
    manifest_state = os.lstat(manifest_path)
    if (
        not stat.S_ISREG(manifest_state.st_mode)
        or stat.S_ISLNK(manifest_state.st_mode)
        or manifest_state.st_uid != os.geteuid()
    ):
        raise RuntimeError("initializer manifest validation failed")
    print(path)
except BaseException:
    if path is not None and created is not None:
        parent_fd = os.open("/tmp", os.O_RDONLY | os.O_DIRECTORY)
        try:
            fresh = os.stat(path.name, dir_fd=parent_fd, follow_symlinks=False)
            fresh_identity = (
                fresh.st_uid,
                fresh.st_dev,
                fresh.st_ino,
                stat.S_IFMT(fresh.st_mode),
                stat.S_IMODE(fresh.st_mode),
            )
            if (
                fresh_identity != created_identity
                or fresh.st_uid != os.geteuid()
                or not stat.S_ISDIR(fresh.st_mode)
                or stat.S_ISLNK(fresh.st_mode)
            ):
                raise RuntimeError("initializer cleanup identity mismatch")
            if not shutil.rmtree.avoids_symlink_attacks:
                raise RuntimeError("safe dirfd rmtree unavailable")
            shutil.rmtree(path.name, dir_fd=parent_fd)
        finally:
            os.close(parent_fd)
    raise
""")')"
initializer_status=$?
if [ "$initializer_status" -ne 0 ]; then
  exit "$initializer_status"
fi
PLAN53_PATH_LINES="$(printf '%s\n' "$PLAN53_TMP" | wc -l)"
if [ -z "$PLAN53_TMP" ] || [ "$PLAN53_PATH_LINES" -ne 1 ]; then
  printf 'Initializer must emit exactly one nonempty private-temp path\n' &gt;&amp;2
  exit 1
fi
case "$PLAN53_TMP" in
  /tmp/learn-napplets-01-53-*) ;;
  *)
    printf 'Initializer emitted invalid private-temp path: %s\n' "$PLAN53_TMP" &gt;&amp;2
    exit 1
    ;;
esac
PLAN53_IDENTITY="$(PLAN53_TMP="$PLAN53_TMP" tools/phase1-python -c 'import json,os,pathlib,stat,sys; temp=pathlib.Path(os.environ["PLAN53_TMP"]); state=os.lstat(temp); valid=temp.is_absolute() and temp.parent==pathlib.Path("/tmp") and temp.name.startswith("learn-napplets-01-53-") and stat.S_ISDIR(state.st_mode) and not stat.S_ISLNK(state.st_mode) and state.st_uid==os.geteuid() and stat.S_IMODE(state.st_mode)==0o700; sys.exit("private-temp identity validation failed before manifest read") if not valid else None; manifest=json.loads((temp/"pre-task-sha256.json").read_text(encoding="utf-8")); actual=(state.st_uid,state.st_dev,state.st_ino,stat.S_IFMT(state.st_mode),stat.S_IMODE(state.st_mode)); expected=(manifest["ownerUid"],manifest["device"],manifest["inode"],manifest["type"],manifest["mode"]); sys.exit("private-temp manifest identity mismatch") if actual!=expected else None; print(":".join(str(item) for item in actual))')"
identity_status=$?
if [ "$identity_status" -ne 0 ]; then
  exit "$identity_status"
fi
PLAN53_IDENTITY_LINES="$(printf '%s\n' "$PLAN53_IDENTITY" | wc -l)"
if [ -z "$PLAN53_IDENTITY" ] || [ "$PLAN53_IDENTITY_LINES" -ne 1 ]; then
  printf 'Identity gate must emit exactly one known identity tuple\n' &gt;&amp;2
  exit 1
fi
export PLAN53_TMP
export PLAN53_IDENTITY
cleanup_plan53_tmp() {
  task_status=$?
  trap - EXIT
  if [ "$task_status" -ne 0 ]; then
    tools/phase1-python -c 'import os,pathlib,shutil,stat,sys; temp=pathlib.Path(os.environ["PLAN53_TMP"]); expected=tuple(int(item) for item in os.environ["PLAN53_IDENTITY"].split(":")); valid=len(expected)==5 and temp.is_absolute() and temp.parent==pathlib.Path("/tmp") and temp.name.startswith("learn-napplets-01-53-"); sys.exit("private-temp cleanup path/identity tuple invalid") if not valid else None; parent_fd=os.open("/tmp",os.O_RDONLY|os.O_DIRECTORY)
try:
    state=os.stat(temp.name,dir_fd=parent_fd,follow_symlinks=False)
    actual=(state.st_uid,state.st_dev,state.st_ino,stat.S_IFMT(state.st_mode),stat.S_IMODE(state.st_mode))
    if actual!=expected or state.st_uid!=os.geteuid() or not stat.S_ISDIR(state.st_mode) or stat.S_ISLNK(state.st_mode) or stat.S_IMODE(state.st_mode)!=0o700:
        raise RuntimeError("private-temp cleanup identity mismatch")
    if not shutil.rmtree.avoids_symlink_attacks:
        raise RuntimeError("safe dirfd rmtree unavailable")
    shutil.rmtree(temp.name,dir_fd=parent_fd)
finally:
    os.close(parent_fd)'
    cleanup_status=$?
    if [ "$cleanup_status" -ne 0 ]; then
      printf 'Task 1 failed; private temp identity/cleanup gate also failed for %s\n' "$PLAN53_TMP" &gt;&amp;2
      exit "$cleanup_status"
    fi
  fi
  exit "$task_status"
}
trap cleanup_plan53_tmp EXIT
printf 'PRIVATE_TEMP_DIR=%s\n' "$PLAN53_TMP"
tools/phase1-python tools/build-site.py --check 2&gt;&amp;1 | tee "$PLAN53_TMP/focused.log" &amp;&amp; PYTHONPATH=tests/site:tests/phase1 tools/phase1-python -m unittest test_static_site test_site_browser test_evidence.BoundedCollectorTests.test_candidate_acquisition_tracer_blocks_incomplete_dimensions test_spike_consolidation.SpikeConsolidationTests.test_fragment_accepts_git_reachable_historical_registry_generation test_spike_consolidation.SpikeConsolidationTests.test_dry_run_accounts_for_all_spikes_and_preserves_canonical_targets test_spike_consolidation.SpikeConsolidationTests.test_duplicate_fragment_is_a_single_no_op_and_order_is_stable test_spike_consolidation.SpikeConsolidationTests.test_conflicting_stable_id_is_blocked_without_replacing_canonical_state test_spike_consolidation.SpikeConsolidationTests.test_consolidation_generation_recovers_after_each_publish_interruption test_spike_consolidation.SpikeConsolidationTests.test_consolidation_semantic_failure_does_not_enter_publication test_spike_consolidation.SpikeConsolidationTests.test_contention_and_injected_precommit_failure_preserve_byte_identical_state -v 2&gt;&amp;1 | tee -a "$PLAN53_TMP/focused.log" &amp;&amp; PYTHONPATH=tests/phase1 tools/phase1-python -c 'import hashlib,io,json,os,pathlib,stat,subprocess,sys,unittest; temp=pathlib.Path(os.environ["PLAN53_TMP"]); expected_identity=tuple(int(item) for item in os.environ["PLAN53_IDENTITY"].split(":")); state=os.lstat(temp); actual_identity=(state.st_uid,state.st_dev,state.st_ino,stat.S_IFMT(state.st_mode),stat.S_IMODE(state.st_mode)); owner_ok=len(expected_identity)==5 and actual_identity==expected_identity and stat.S_ISDIR(state.st_mode) and not stat.S_ISLNK(state.st_mode) and state.st_uid==os.geteuid() and stat.S_IMODE(state.st_mode)==0o700; sys.exit("private-temp classifier identity gate failed before artifact access") if not owner_ok else None; manifest=json.loads((temp/"pre-task-sha256.json").read_text(encoding="utf-8")); direct={"test_consolidation_recovery.ObservedRefreshPublicationTests.test_observed_refresh_rejects_tampered_or_incomplete_attestation_before_reader_entry":"ERROR OBS004: observed-refresh staged validation refused: reviewed acquisition provenance refused before observed-refresh snapshot: PRE118: review plan manifest is not the exact active plan set","test_consolidation_recovery.ObservedRefreshPublicationTests.test_observed_refresh_staged_validation_binds_exact_five_target_digests":"ERROR OBS004: observed-refresh staged validation refused: reviewed acquisition provenance refused before observed-refresh snapshot: PRE118: review plan manifest is not the exact active plan set","test_evidence.BaseSourceEvidenceTests.test_evidence_validation_uses_temporary_report_without_canonical_mutation":"ERROR IO001: reviewed acquisition provenance refused before compatibility snapshot: PRE118: review plan manifest is not the exact active plan set","test_evidence.SourceEvidenceTests.test_evidence_validation_uses_temporary_report_without_canonical_mutation":"ERROR IO001: reviewed acquisition provenance refused before compatibility snapshot: PRE118: review plan manifest is not the exact active plan set","test_governance.GovernanceValidationTests.test_phase_one_closeout_requires_governance_and_complete_evidence_contract":"ERROR PRE118: review plan manifest is not the exact active plan set"}; wrapped={"test_closeout_commands.TerminalPublicationTests.test_terminal_validator_binds_staged_three_file_set_before_publication","test_closeout_commands.TerminalPublicationTests.test_terminal_validator_preserves_closed_ledger_through_post_closure_verification","test_closeout_commands.TerminalPublicationTests.test_terminal_validator_requires_canonical_probe_attempt_ledger","test_closeout_commands.TerminalPublicationTests.test_terminal_validator_requires_exact_reviewed_source_input_binding","test_closeout_commands.TerminalPublicationTests.test_terminal_validator_requires_separate_role_bound_records_but_allows_one_principal","test_closeout_commands.TerminalPublicationTests.test_terminal_validator_uses_external_target_only_aggregate_without_self_reference","test_closeout_commands.VerificationAdjudicationTests.test_fresh_verification_has_exact_review_finding_adjudication"}; wrapper="phase1_terminal_validator.ValidationError: Plan 01-29 source-input verifier refused terminal evidence"; stream=io.StringIO(); result=unittest.TextTestRunner(stream=stream,verbosity=2).run(unittest.defaultTestLoader.discover("tests/phase1")); output=stream.getvalue(); (temp/"phase1-suite.log").write_text(output,encoding="utf-8"); failures={test.id():detail for test,detail in result.failures}; errors={test.id():detail for test,detail in result.errors}; failure_ids=[test.id() for test,_ in result.failures]; error_ids=[test.id() for test,_ in result.errors]; parse_error=lambda detail:[line[line.index("ERROR "):].strip() for line in detail.splitlines() if "ERROR " in line]; parse_wrapper=lambda detail:[line.strip() for line in detail.splitlines() if line.strip().startswith("phase1_terminal_validator.")]; direct_bad=sorted(identifier for identifier,expected in direct.items() if parse_error(failures.get(identifier,""))!=[expected]); wrapper_bad=sorted(identifier for identifier in wrapped if parse_wrapper(errors.get(identifier,""))!=[wrapper] or parse_error(errors.get(identifier,""))); probe=subprocess.run(["tools/phase1-python","tools/validate-planning.py","--verify-phase1-source-inputs","--review",".planning/phases/01-research-and-truth-baseline/01-REVIEWS.md"],text=True,capture_output=True,check=False); expected_probe=["bootstrap verification passed","toolchain verification passed","ERROR PRE118: review plan manifest is not the exact active plan set","Planning validation failed: 1 error(s), 0 warning(s)."]; canonical={str(path):hashlib.sha256(pathlib.Path(path).read_bytes()).hexdigest() for path in manifest["sha256"]}; duplicate=len(failure_ids+error_ids)!=len(set(failure_ids+error_ids)); categories_clear=not result.skipped and not result.expectedFailures and not result.unexpectedSuccesses; lines=["owner_ok="+str(owner_ok),"failure_ids="+repr(sorted(failure_ids)),"error_ids="+repr(sorted(error_ids)),"duplicate="+str(duplicate),"direct_bad="+repr(direct_bad),"wrapper_bad="+repr(wrapper_bad),"skipped="+repr(result.skipped),"expected_failures="+repr(result.expectedFailures),"unexpected_successes="+repr(result.unexpectedSuccesses),"probe_stdout="+repr(probe.stdout.splitlines()),"probe_stderr="+repr(probe.stderr.splitlines()),"canonical_match="+str(canonical==manifest["sha256"])]; summary=temp/"phase1-suite-summary.txt"; summary.write_text("\n".join(lines)+"\n",encoding="utf-8"); print(output,end=""); print("PRE_TASK_MANIFEST="+str(temp/"pre-task-sha256.json")); print("FOCUSED_LOG="+str(temp/"focused.log")); print("PHASE1_SUITE_LOG="+str(temp/"phase1-suite.log")); print("CLASSIFIER_SUMMARY="+str(summary)); ok=owner_ok and set(failure_ids)==set(direct) and len(failure_ids)==len(direct) and set(error_ids)==wrapped and len(error_ids)==len(wrapped) and not duplicate and not direct_bad and not wrapper_bad and categories_clear and probe.returncode!=0 and probe.stdout.splitlines()==expected_probe and probe.stderr.splitlines()==[] and canonical==manifest["sha256"]; sys.exit(0 if ok else 1)'</automated>
  </verify>
  <acceptance_criteria>All four gap groups and site suites pass; failure IDs, error IDs, direct ERROR-line sequences, wrapper exception lines, and direct-probe stdout/stderr equal the declared contract exactly; no skip/expected-failure/unexpected-success/duplicate/extra category remains; actual private artifact paths are printed; and all three canonical files match pre-task SHA-256.</acceptance_criteria>
  <done>Automated blockers owned by this gap set are closed, while PRE118 remains explicit deferred evidence and Phase 1 remains gaps_found.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Judge visual hierarchy, focus visibility, responsive reading, motion comfort, and evidence-label clarity</name>
  <read_first>Task 1 automated result plus the executor-printed actual private-temp manifest, focused log, full-suite log, and classifier-summary paths; site/README.md preview contract; generated home, learn, architecture, and sources pages</read_first>
  <files>None — human judgment is recorded only in 01-53-SUMMARY.md.</files>
  <what-built>Four scoped repairs with green focused/site checks and a PRE118-only retained-suite backstop.</what-built>
  <action>Only after Task 1 passes, re-lstat the executor-printed private directory and require the recorded UID/device/inode/mode before using its artifacts. Print the actual directory, manifest, focused-log, full-suite-log, and classifier-summary paths in the checkpoint together with the exact loopback URL. Start the generated site on a loopback-only port using the verified interpreter wrapper. Ask the human to inspect all four routes at wide and narrow widths, traverse by keyboard, emulate reduced motion, replay/reset the transcript, and read the source/status labels. This checkpoint approves visual/accessibility judgment only. It must not alter STATE.md, ROADMAP.md, or REQUIREMENTS.md or accept Phase 1, ADRs, residual risk, Firefox coverage, external action, release, or transition. Retain the owned private directory until verification-only closeout completes.</action>
  <how-to-verify>
    1. Open the executor-provided loopback URL and follow Home → Learn → Architecture → Sources at desktop and 320px-equivalent width; confirm reading hierarchy is clear and no horizontal page overflow appears.
    2. Use keyboard only from the skip link through primary navigation, calls to action, transcript disclosure, replay/reset, and source links; confirm focus is always visible and order is understandable.
    3. Enable reduced-motion emulation, click Replay transcript, and confirm no smooth animated movement or discomfort; confirm reset changes only the optional trace.
    4. Read several source/fact/blocker cards and confirm authority tier, evidence class, maturity, state, uncertainty, source identity, and refresh trigger make draft/implementation/blocked material visibly non-settled.
    5. Approve only if all four judgments pass; otherwise report exact route, viewport/preference, control/record ID, and observed defect.
  </how-to-verify>
  <verify>
    <automated>tools/phase1-python tools/build-site.py --check &amp;&amp; tools/phase1-python -m unittest discover -s tests/site -p 'test_static_site.py' -v &amp;&amp; tools/phase1-python -m unittest discover -s tests/site -p 'test_site_browser.py' -v</automated>
    <human-check>
      1. Open the executor-provided loopback URL and follow Home → Learn → Architecture → Sources at desktop and 320px-equivalent width; confirm reading hierarchy is clear and no horizontal page overflow appears.
      2. Use keyboard only from the skip link through primary navigation, calls to action, transcript disclosure, replay/reset, and source links; confirm focus is always visible and order is understandable.
      3. Enable reduced-motion emulation, click Replay transcript, and confirm no smooth animated movement or discomfort; confirm reset changes only the optional trace.
      4. Read several source/fact/blocker cards and confirm authority tier, evidence class, maturity, state, uncertainty, source identity, and refresh trigger make draft/implementation/blocked material visibly non-settled.
      5. Approve only if all four judgments pass; otherwise report exact route, viewport/preference, control/record ID, and observed defect.
    </human-check>
  </verify>
  <acceptance_criteria>Human explicitly approves all five bounded judgments after automated checks and after receiving actual private artifact paths; approval text states that Phase 1 and all retained deferred boundaries remain non-passing/unaccepted, while the three canonical files still match pre-task SHA-256.</acceptance_criteria>
  <done>Human visual/accessibility judgment is explicitly recorded without auto-accepting any phase, ADR, residual risk, browser workaround, release, or transition.</done>
  <resume-signal>Type "approved — visual/accessibility judgment only" or describe exact issues.</resume-signal>
</task>

</tasks>

<verification_only_closeout_override>
This plan intentionally overrides normal `execute-plan.md` closeout because its verified outcome is canonical non-mutation. After human approval, and before any normal closeout write, reload the executor-printed private manifest; re-lstat its directory and require the recorded executor UID, device, inode, real-directory type, and mode `0700`; then recompute exact SHA-256 for `.planning/STATE.md`, `.planning/ROADMAP.md`, and `.planning/REQUIREMENTS.md`. Any mismatch stops closeout before SUMMARY creation and prevents authorization.

When that guard passes, create only `.planning/phases/01-research-and-truth-baseline/01-53-SUMMARY.md` using the standard summary template. Record focused results, exact failure/error category and line-set equality, actual private evidence paths, human judgment, pre-closeout three-file hash equality, and the fact that PRE118 and Phase 1 remain non-passing. Stage exactly that SUMMARY path; parse `git diff --cached --name-only -z` and require its NUL-delimited path set to equal the one SUMMARY path before committing. Create one `docs(01-53): complete integrated gap verification plan` commit containing only that staged file.

For this plan, explicitly skip `execute-plan.md` steps `update_current_position`, `extract_decisions_and_issues`, `update_session_continuity`, `update_roadmap`, `update_requirements`, and `git_commit_metadata`; do not call `state.advance-plan`, `state.update-progress`, `state.record-metric`, `state.add-decision`, `state.add-blocker`, `state.record-session`, `roadmap.update-plan-progress`, `requirements.mark-complete`, or the standard metadata commit. Task 1 and Task 2 produce no per-task commit; the summary-only closeout commit is Plan 01-53's sole commit.

After that commit, run the final post-closeout gate: recompute all three canonical SHA-256 values and require equality with the pre-task manifest; obtain the closeout commit's changed paths with `git diff-tree --no-commit-id --name-only -z -r <closeout-commit>` and require NUL-parsed set equality with only `.planning/phases/01-research-and-truth-baseline/01-53-SUMMARY.md`. Failure of either check prevents completion/authorization and must be reported; it may not be converted to a warning. Only after both checks pass, re-lstat the private directory and remove it only when the ownership flag is true and UID/device/inode/type/mode still match the manifest. If identity changed, fail closed without deleting that path.
</verification_only_closeout_override>

## Artifacts this phase produces

| Path | New record | Contract |
|---|---|---|
| `.planning/phases/01-research-and-truth-baseline/01-53-SUMMARY.md` | Execution summary | Records focused checks, exact category/line-set PRE118 attribution, three-file SHA-256 equality, exact summary-only closeout path, and bounded human judgment. |
| `<executor-printed-private-temp>/pre-task-sha256.json` | Ephemeral ownership/hash manifest | Records UID/device/inode/mode and exact pre-task STATE/ROADMAP/REQUIREMENTS SHA-256 values; private and never committed. |
| `<executor-printed-private-temp>/focused.log` and `phase1-suite.log` | Ephemeral runner output | Retains complete local command evidence inside the verified private directory; never canonical or committed. |
| `<executor-printed-private-temp>/phase1-suite-summary.txt` | Ephemeral classifier result | Records exact failure/error IDs, direct ERROR-line and wrapper-exception mismatches, all unexpected result categories, exact probe lines, and canonical-hash equality. |

No production/test symbol, canonical path, schema/record field, CLI flag, package, external service, API integration, or ORM schema is created. Official API-coverage detector returned `detected:false`; ORM scan returned no schema file, so no coverage declaration or schema-push task applies.

<probe_coverage>
## Spec-less Edge Probe Disposition

| Requirement | Category | Disposition |
|---|---|---|
| EVID-01 | adjacency | Explicit: Plan 01-49 requires exact repository/commit/path equality; any mismatch separates into rejection. |
| EVID-01 | empty | Explicit: Plan 01-49 rejects null/empty identity, URL, digest, and retrieval fields. |
| EVID-01 | encoding | Flagged assumption in Plan 01-49; no Unicode/path normalization rule is invented. |
| EVID-01 | ordering | Flagged assumption in Plan 01-49; record order is outside CR-08 validity. |
| EVID-02 | adjacency, empty, ordering | Three explicit flagged assumptions in this plan; scoped repairs do not alter these semantics. |
| EVID-03 | unclassified | Explicit flagged assumption in Plan 01-51; fixture proves blocked dimensions only. |
| EVID-04 | unclassified | Explicit flagged assumption in Plan 01-50; exact historical provenance only. |
| OPER-01 | idempotency | Explicit: Plan 01-50 duplicate-fragment/order regression. |
| OPER-01 | concurrency | Explicit: Plan 01-50 contention/interruption/precommit regressions. |
| OPER-03 | adjacency, empty, ordering | Three explicit flagged assumptions in this plan; no governance tie-break model is invented. |

Coverage: 14 applicable; 4 explicit predicates, 10 explicitly unresolved/flagged assumptions, 0 silent drops. The retained full suite is a structured backstop and abstains wherever PRE118 prevents explicit proof.
</probe_coverage>

<prohibition_probe>
## Recall → Precision Result

Stage 1 considered silent authority inflation, provenance rebinding, compatibility qualification, conflict collapse, auto-acceptance, generic input attacks, and routine test hygiene for each Phase 1 requirement. Stage 2 dropped routine-engineering items and routed generic OWASP/path/input concerns to the ASVS threat registers rather than minting canon prohibitions. Six bespoke transparency/governance prohibitions remain descriptor-less and flagged-unverified across Plans 01-49 through 01-53—one for each EVID-01, EVID-02, EVID-03, EVID-04, OPER-01, and OPER-03. None is auto-dismissed; no `check_*` descriptor or fabricated canonical compliance item is authored.
</prohibition_probe>

<source_coverage_audit>
## Multi-Source Coverage Audit

| SOURCE | ID | Scoped feature/decision | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Dated immutable-source evidence remains reliable without presenting unsettled behavior as fact | 01-49, 01-50, 01-53 | COVERED | Public identity and retained spike provenance are repaired; final gate preserves unsettled/deferred labels. |
| REQ | EVID-01 | Immutable source traceability | 01-49, 01-53 | COVERED | CR-08 strict tuple/digest/time validation plus integrated proof. |
| REQ | EVID-02 | Conflict, uncertainty, drift, impact, refresh visibility | 01-53 | COVERED | Regression backstop and explicit unresolved edge assumptions; no record semantics changed. |
| REQ | EVID-03 | Complete-before-acceptance compatibility | 01-51, 01-53 | COVERED | WR-04 tracer reaches blocked-dimension assertions. |
| REQ | EVID-04 | Reproducible mandatory-spike evidence | 01-50, 01-53 | COVERED | Exact historical registry bytes restore impact validation/consolidation. |
| REQ | OPER-01 | Detect/map drift without silent rewrite | 01-50, 01-53 | COVERED | Historic provenance, idempotency, contention, conflict, and no-rebind assertions. |
| REQ | OPER-03 | Roles, exit evidence, GSD status, traceability | 01-52, 01-53 | COVERED | Direct browser evidence, explicit human gate, locked gaps_found state. |
| RESEARCH | immutable Git source pattern | Commit/blob/digest before claim | 01-49, 01-50 | COVERED | URL tuple and Git-object history both fail closed. |
| RESEARCH | refresh detects/routes | Retain history; never rewrite meaning automatically | 01-50 | COVERED | Historical fragments remain byte-unchanged. |
| RESEARCH | exact ignored cache | Repository-owned cache only | 01-51 | COVERED | Fixture conforms; production boundary untouched. |
| RESEARCH | browser/accessibility evidence | Approved Chromium, direct observation, reduced motion | 01-52, 01-53 | COVERED | Firefox blocker explicitly retained; no workaround. |
| RESEARCH | package legitimacy/toolchain | No new install; wrapper-only commands | all | EXCLUDED | Existing approved isolated tools reused; no package action. |
| CONTEXT | D-01, D-02, D-03 | Blocking-first bounded research/snowballing | — | EXCLUDED | Owner forbids new research; completed evidence history remains unchanged. |
| CONTEXT | D-04 | Exact repository/ref/path/time/digest identity | 01-49, 01-50 | COVERED | Public identity and retained generations enforce exactness. |
| CONTEXT | D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12 | Acquisition, directional evidence, cache, external-write, missing-source, parallel-conflict policy | 01-49, 01-51, 01-53 | COVERED | Exact cache and conflict labels preserved; no acquisition/external write occurs. |
| CONTEXT | D-13 | Retain old identity/digest/replacement history | 01-50 | COVERED | Exact Git-reachable historic bytes bind fragments. |
| CONTEXT | D-14, D-15, D-16, D-17, D-18, D-19 | Evidence acceptance, human approval, fact/observation/inference/conflict separation | 01-49, 01-50, 01-53 | COVERED | Validators preserve fields; human gate cannot approve claims or ADRs. |
| CONTEXT | D-20 | Stale evidence remains history and blocks dependent approval | 01-50, 01-53 | COVERED | History accepted only as exact provenance; no automatic state promotion. |
| CONTEXT | D-21, D-22, D-24 | Disposable/live spike execution boundaries | — | EXCLUDED | No spike or live public probe is run; retained evidence only. |
| CONTEXT | D-23 | Retained spike recipe/measurement/source digests | 01-50 | COVERED | Fragment digests remain immutable and revalidatable. |
| CONTEXT | D-25, D-26, D-27, D-28 | ADR recommendation only, scoped blockers, explicit roles/sign-offs | 01-53 | COVERED | Human checkpoint is judgment-only; blockers and approval boundaries remain. |
| CONTEXT | D-29, D-30, D-31, D-32 | Stable IDs, canonical YAML/schema, relation vocabulary, migrations | — | EXCLUDED | No ID, YAML schema, relation vocabulary, or migration change. |
| CONTEXT | D-33, D-34, D-35 | Passed/blocked exit, reusable tracks, no waived gates | 01-53 | COVERED | `gaps_found`/0 completed phases retained; PRE118 remains non-passing. |
| CONTEXT | D-36 | Targeted refresh work instead of broad rerun | 01-50 | COVERED | Exact provenance seam only; no research expansion. |
| CONTEXT | D-37 | Chromium + Firefox baseline | 01-52, 01-53 | COVERED | Approved Chromium verifies scope; Firefox remains explicit blocker without workaround. |
| CONTEXT | D-38 | Exact environment/commands/hashes | all | COVERED | All Python/test/browser commands use `tools/phase1-python`. |
| CONTEXT | D-39 | Five samples for nondeterministic measurements | — | EXCLUDED | No new nondeterministic measurement is produced. |
| OWNER DEFERRAL | PRE118; CR-05–CR-07/SPK-G; WR-02; runtime/guest/native/portable; deploy/release; ADR/external/residual/transition; Firefox workaround | Non-passing deferred boundaries | 01-53 | EXCLUDED | Explicitly preserved; absence from repair plans is authorized, not a source gap. |

Audit result: all four active gaps and all six Phase 1 requirements are covered; remaining source items are explicit unchanged-history or owner-authorized exclusions. No unplanned item remains.
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| focused tests → phase-wide conclusion | Narrow green results can misrepresent a still-red retained suite. |
| full-suite output → allowed deferral | A loose classifier could suppress a new defect under PRE118. |
| automated/human verification → canonical phase authority | Test or visual approval must not mutate status or accept decisions. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-53-01 | Repudiation | full-suite attribution | high | mitigate | Require exact equality between the declared TestCase.id() allowlist and `result.failures + result.errors`, exact direct/wrapper diagnostics, a nonzero source-verifier probe with the precise PRE118 line, and at least one retained refusal. This HIGH threat blocks the human checkpoint on any mismatch. |
| T-01-53-02 | Elevation of Privilege | verification → phase/ADR authority | high | mitigate | Human approval authorizes only bounded visual/accessibility judgment. Explicitly bypass every standard STATE/ROADMAP/REQUIREMENTS mutation and metadata-commit step; Plan 01-53 may create one closeout commit whose sole path is `01-53-SUMMARY.md`, so automated or human green cannot transition phase/ADR authority. |
| T-01-53-03 | Tampering | canonical status and closeout evidence | high | mitigate | Record exact pre-task SHA-256 for all three of STATE.md, ROADMAP.md, and REQUIREMENTS.md; require all three values unchanged immediately before SUMMARY creation and again after the closeout commit; require both the staged NUL-parsed path set and committed diff-tree path set to equal only `01-53-SUMMARY.md`; block completion on any hash, path, or standard-metadata-step-bypass violation. |
| T-01-53-04 | Information Disclosure | local preview and ephemeral evidence | low | mitigate | Store logs and manifests only in one securely created private mode-0700 directory owned by the executor UID; verify UID/device/inode/type/mode before every use and cleanup, and expose the generated preview only on loopback. |

All HIGH threats block progress until mitigations pass. No package-manager install occurs; supply-chain package legitimacy gate is not applicable.
</threat_model>

<verification>
Wave-3 Task 1 runs only after Plans 01-49 through 01-52 and blocks on any scoped regression, PRE118 ID/diagnostic mismatch, canonical-file mismatch, or private-directory identity failure. Task 2 is reachable only after that automated gate and is a blocking human judgment. Closeout then requires all three STATE.md/ROADMAP.md/REQUIREMENTS.md SHA-256 values to equal their pre-task values after the commit, the sole commit path to equal `.planning/phases/01-research-and-truth-baseline/01-53-SUMMARY.md`, and explicit bypass of standard status/roadmap/requirements metadata steps. The securely created private mode-0700 directory remains through human review and closeout, then is removed only after UID/device/inode/type/mode revalidation; any failed identity, cleanup, hash, or path check blocks completion rather than warning.
</verification>

<success_criteria>
All four scoped gaps are demonstrably closed; the complete retained suite matches the exact non-passing PRE118 ID/diagnostic contract with no missing or unexpected result; bounded human visual/accessibility judgment is recorded; post-closeout SHA-256 for STATE.md, ROADMAP.md, and REQUIREMENTS.md exactly equals the pre-task manifest; the one closeout commit changes only `.planning/phases/01-research-and-truth-baseline/01-53-SUMMARY.md`; standard metadata mutation/commit steps remain bypassed; and the private mode-0700 evidence directory is removed only after exact UID/device/inode/type/mode revalidation. Any unmet gate is a blocking failure, never authorization or a warning.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-53-SUMMARY.md` when done.
</output>
