---
phase: 01-research-and-truth-baseline
plan: 51
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-research-and-truth-baseline/01-GAP-CLOSURE-PRE-EXECUTION-PLAN-REVIEW.json
  - .planning/phases/01-research-and-truth-baseline/01-GAP-CLOSURE-PRE-EXECUTION-PLAN-REVIEW.md
autonomous: false
gap_closure: true
requirements: [EVID-01, EVID-02, EVID-03, EVID-04, OPER-01, OPER-03]
must_haves:
  truths:
    - "Every active Phase 1 plan is externally reviewed from one exact immutable commit before focused gap implementation begins."
    - "The pre-execution review identifies a nonempty external reviewer identity, exact 01-01 through 01-57 plan-byte map, reviewed commit, and zero-actionable convergence."
    - "The historical 45-plan review at 1a9449b is retained as audit history and cannot authorize execution of Plans 01-46 through 01-57."
  artifacts:
    - path: .planning/phases/01-research-and-truth-baseline/01-GAP-CLOSURE-PRE-EXECUTION-PLAN-REVIEW.json
      provides: "Current immutable pre-execution plan-review binding for every active Phase 1 plan."
    - path: .planning/phases/01-research-and-truth-baseline/01-GAP-CLOSURE-PRE-EXECUTION-PLAN-REVIEW.md
      provides: "Externally authored review transcript and convergence rationale."
  key_links:
    - from: 01-GAP-CLOSURE-PRE-EXECUTION-PLAN-REVIEW.json
      to: 01-46-PLAN.md through 01-57-PLAN.md
      via: "The exact reviewed commit and lexically ordered SHA-256 byte map bind the active execution set before any repair task runs."
---

<objective>
Obtain the required deep external review convergence of the entire active Phase 1 plan set before focused gap implementation begins.

Purpose: Replace the superseded 45-plan authorization with a current immutable review binding while keeping the historical review as audit-only evidence.
Output: Externally authored review transcript and machine-readable exact plan binding.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-REVIEWS.md
@.planning/phases/01-research-and-truth-baseline/01-FINAL-VERIFICATION.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Obtain external deep review of the exact active gap-closeout plan snapshot</name>
  <precondition>All 01-01 through 01-57 PLAN.md files are committed, the intended review worktree is detached and clean, and no gap implementation has begun.</precondition>
  <files>.planning/phases/01-research-and-truth-baseline/01-GAP-CLOSURE-PRE-EXECUTION-PLAN-REVIEW.json, .planning/phases/01-research-and-truth-baseline/01-GAP-CLOSURE-PRE-EXECUTION-PLAN-REVIEW.md</files>
  <action>Use an external reviewer in a read-only detached worktree at one full Git commit. The reviewer must inspect every active plan from 01-01 through 01-57, their dependency DAG and waves, all six requirement IDs, and D-01 through D-39. Persist the raw externally authored review in the Markdown artifact and a separate JSON binding with `reviewedCommit`, nonempty `reviewerIdentity`, reviewer tool/version, UTC timestamps, the lexical repository-relative plan path-to-SHA-256 map for exactly 57 regular Git blobs, aggregate algorithm and digest, review transcript SHA-256, and a convergence object declaring `CONVERGED; HIGH=0; actionable=0`. The JSON must state that `01-REVIEWS.md` at `1a9449b` covers only the historical 45-plan snapshot and cannot authorize this active set. Commit only the two review artifacts as a child of the reviewed commit so their Git record is immutable. Per D-15, D-25, D-28, D-33, and D-35, do not treat automation, elapsed time, a generic approval, or historical review output as a substitute for this external exact-plan review.</action>
  <what-built>No repair code has run. This gate produces the current external plan-review authorization required before the focused repair plans may execute.</what-built>
  <instructions>Run the external detached read-only review, persist both required artifacts, and commit only those artifacts as the direct child of the reviewed plan snapshot.</instructions>
  <verification>Confirm the exact active-plan map, external transcript digest, reviewer identity, reviewed commit, and zero-actionable convergence using the automated command below.</verification>
  <how-to-verify>1. Inspect the JSON in the committed review-record commit and confirm its parent is the full `reviewedCommit`. 2. Recompute every 01-01 through 01-57 plan SHA-256 from that parent and compare it with the exact sorted map and aggregate. 3. Confirm the transcript digest, external reviewer identity, review method, and zero-actionable convergence are present. 4. Confirm the historical 45-plan review is classified only as audit history.</how-to-verify>
  <verify>
    <automated>test -s .planning/phases/01-research-and-truth-baseline/01-GAP-CLOSURE-PRE-EXECUTION-PLAN-REVIEW.json && test -s .planning/phases/01-research-and-truth-baseline/01-GAP-CLOSURE-PRE-EXECUTION-PLAN-REVIEW.md && tools/phase1-python -c "import json, pathlib; p=pathlib.Path('.planning/phases/01-research-and-truth-baseline/01-GAP-CLOSURE-PRE-EXECUTION-PLAN-REVIEW.json'); d=json.loads(p.read_text()); assert d['convergence']['verdict']=='CONVERGED; HIGH=0; actionable=0'; assert d['convergence']['actionableCount']==0; assert d['reviewerIdentity'].strip(); assert len(d['activePlanSha256'])==57; assert d['historical45PlanReview']['authorization']=='audit-only'"</automated>
  </verify>
  <acceptance_criteria>An external reviewer binds every active plan byte to one immutable commit and records zero actionable findings; the record explicitly rejects the historical 45-plan review as authorization for the current closeout sequence.</acceptance_criteria>
  <done>The pre-execution deep review convergence required by the roadmap is recorded before Plans 01-46 and 01-47 can execute.</done>
  <resume-signal>Provide the committed external review binding with `CONVERGED; HIGH=0; actionable=0`, or provide its actionable findings for targeted replanning.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| mutable plan workspace → implementation authorization | Changed plan bytes could be executed under a historical or unrelated review. |
| external reviewer output → plan-review binding | A generic, self-authored, incomplete, or wrong-commit review could be represented as convergence. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-51-01 | Tampering | active plan set | high | mitigate | Task 1 requires an exact 57-plan regular-Git-blob map and aggregate from one detached reviewed commit. |
| T-51-02 | Spoofing | review authorization | high | mitigate | Task 1 requires a named external reviewer, transcript digest, immutable review-record commit, and zero-actionable convergence. |
| T-51-03 | Repudiation | historical review record | high | mitigate | Task 1 records that the 1a9449b 45-plan review is audit-only and cannot authorize the active set. |
</threat_model>

<verification>
Before any repair task, verify the committed pre-execution review record has one exact active-plan map, one reviewed commit, nonempty external reviewer identity, and zero actionable convergence.
</verification>

<success_criteria>
The Phase 1 focused gap sequence begins only after a current externally authored deep review has bound the entire active plan set to immutable bytes.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-51-SUMMARY.md` when done.
</output>
