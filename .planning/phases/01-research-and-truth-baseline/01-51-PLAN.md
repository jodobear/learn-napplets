---
phase: 01-research-and-truth-baseline
plan: 51
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phase1/test_evidence.py
autonomous: true
gap_closure: true
requirements: [EVID-03]
must_haves:
  truths:
    - "The candidate-acquisition compatibility tracer executes from collector.CACHE_ROOT and still proves incomplete compatibility dimensions remain blocked."
    - "The tracer uses repository `fixture-plan-01-51-tracer/candidate-acquisition`, commit `5101510151015101510151015101510151015101`, path `fixtures/candidate-acquisition.ts`, and source ID `SRC-FIXTURE-PLAN-0151-CANDIDATE-001`; before collection it exclusively claims `collector.CACHE_ROOT / 'fixture-plan-01-51-tracer'`, tracks ownership, cleans that child only when owned, and proves all unrelated or preexisting cache bytes remain identical per D-09."
    - "External, temporary, and symlink-mediated cache roots remain rejected before transport or writes; no production cache-confinement code is weakened."
  prohibitions:
    - requirement_id: EVID-03
      category: transparency
      status: unresolved
      verification: null
      statement: "MUST NOT present incomplete package, runtime, example, current-work, or conformance dimensions as qualified compatibility."
  flagged_assumptions:
    - "EVID-03 unclassified edge probe remains unresolved beyond this exact compatibility-tracer assertion; the fixture does not qualify any missing dimension or architecture."
  artifacts:
    - path: tests/phase1/test_evidence.py
      provides: "WR-04 tracer fixture aligned with exact repository cache confinement, exclusive child ownership, preexisting-sentinel refusal, and surgical cleanup."
      contains: "test_candidate_acquisition_tracer_blocks_incomplete_dimensions"
  key_links:
    - from: "tests/phase1/test_evidence.py::test_candidate_acquisition_tracer_blocks_incomplete_dimensions"
      to: "tools/acquire-sources.py::CACHE_ROOT"
      via: "The tracer passes the production-owned root, exclusively claims its fixed child before transport, and removes that child only when its ownership flag proves this invocation created it."
      pattern: "collector.CACHE_ROOT"
---

<objective>
Repair WR-04 without changing the exact repository cache-confinement boundary.

Purpose: Make the retained candidate-acquisition/compatibility tracer execute its intended EVID-03 assertions instead of failing during fixture setup.
Output: One surgical test-method repair and focused proof that strict cache policy remains unchanged.
</objective>

<execution_context>
@/home/at/.codex/gsd-core/workflows/execute-plan.md
@/home/at/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-FINAL-VERIFICATION.md
@.planning/phases/01-research-and-truth-baseline/01-REVIEW.md
@.planning/phases/01-research-and-truth-baseline/01-46-PLAN.md
@tools/acquire-sources.py
@tests/phase1/test_evidence.py
</context>

<scope_boundary>
Modify only `test_candidate_acquisition_tracer_blocks_incomplete_dimensions` in `tests/phase1/test_evidence.py`. Do not change `tools/acquire-sources.py`, `_confined_cache_root`, `CACHE_ROOT`, another test, source records, compatibility records, or any deferred package/runtime/review machinery.
</scope_boundary>

<assumption_delta_decision>
Generalized noun: acquisition cache root. Decision: no-change. The repaired fixture conforms to the one exact repository-owned root; it does not introduce an alternate cache identity.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Move the stale tracer fixture onto the exact collector cache root</name>
  <read_first>01-REVIEW.md WR-04; tests/phase1/test_evidence.py named tracer and nearby exact-root acceptance/rejection tests; tools/acquire-sources.py `CACHE_ROOT`, `_confined_cache_root`, `collect_immutable_candidate`, and `classify_compatibility`</read_first>
  <files>tests/phase1/test_evidence.py</files>
  <behavior>
    - The named tracer reaches source classification and validator assertions instead of raising exact-root admission error.
    - The literal fixture identity is repository `fixture-plan-01-51-tracer/candidate-acquisition`, commit `5101510151015101510151015101510151015101`, path `fixtures/candidate-acquisition.ts`, and source ID `SRC-FIXTURE-PLAN-0151-CANDIDATE-001`; its only test-owned top-level cache child is `collector.CACHE_ROOT / "fixture-plan-01-51-tracer"`.
    - The observed implementation remains unapproved and compatibility remains blocked with the six existing missing dimensions in fixed order.
    - Invalid host, mutable commit, escaping cache path, and normative-origin mutations still raise their intended errors.
    - A before/after snapshot of every unrelated regular cache file proves cleanup preserves sibling entries and bytes.
    - A preexisting fixed child containing a sentinel is refused before collection or transport; its path, type, and bytes remain exact, the collection-ownership flag stays false, and cleanup does not touch it.
  </behavior>
  <action>Addresses review concern: a fixed cache child needs exclusive ownership before collection. Inside only `test_candidate_acquisition_tracer_blocks_incomplete_dimensions`, replace the TemporaryDirectory cache root with `collector.CACHE_ROOT`. Set `repository = "fixture-plan-01-51-tracer/candidate-acquisition"`, `commit = "5101510151015101510151015101510151015101"`, `path = "fixtures/candidate-acquisition.ts"`, `source_id = "SRC-FIXTURE-PLAN-0151-CANDIDATE-001"`, and `test_owned_child = collector.CACHE_ROOT / "fixture-plan-01-51-tracer"`; use the allowlisted identity URL `https://api.github.com/repos/fixture-plan-01-51-tracer/candidate-acquisition`, matching `https://github.com/fixture-plan-01-51-tracer/candidate-acquisition` response, and matching FixtureTransport commit/blob keys. Before collection, snapshot relative paths and exact bytes for every regular non-symlink file under `collector.CACHE_ROOT` outside `test_owned_child`. Initialize an ownership flag false; claim the fixed child with `mkdir(exist_ok=False)` before invoking transport or collection and set the flag true only after that exact call succeeds. If the child already exists, fail before transport or any mutation and leave the ownership flag false. Add a preexisting-collision subcase under its own failure-safe `try/finally`: initialize a separate setup-ownership flag false, exclusively create the child, set that setup flag true only after creation succeeds, create and snapshot the sentinel's type/path/bytes, and assert the collection claim raises `FileExistsError` with zero transport calls and byte-identical sentinel state. The subcase's `finally` removes the sentinel child only when the separate setup-ownership flag proves this invocation created it, including when sentinel setup or any collision assertion fails; it never consults the collection-ownership flag or removes a preexisting child. Then run the real collection through a fresh exclusive claim. Wrap collection, validation, classification, and hostile-input assertions in `try/finally`; in `finally`, remove `test_owned_child` only when the collection-ownership flag is true, then assert the unrelated-file snapshot is unchanged. Never use `ignore_errors=True`, never delete a preexisting child, and remove no other top-level child. Keep `_confined_cache_root()`, all compatibility, allowlist, immutable-commit, cache-escape, and observed-versus-normative assertions unchanged in meaning per D-09 and D-16.</action>
  <verify>
    <automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_evidence.BoundedCollectorTests.test_candidate_acquisition_tracer_blocks_incomplete_dimensions -v</automated>
  </verify>
  <acceptance_criteria>The preexisting-sentinel subcase refuses collection before transport and preserves the sentinel byte-for-byte; the normal path acquires ownership exclusively, reaches final missingDimensions/affectedRequirements assertions, removes the fixed child only under a true ownership flag, and leaves the exact unrelated-cache snapshot unchanged.</acceptance_criteria>
  <done>WR-04 no longer masks the intended compatibility classification regression.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the fixture repair did not weaken cache confinement</name>
  <read_first>Task 1 diff; existing `test_collector_rejects_external_or_symlink_cache_root_before_transport`, `test_collector_accepts_exact_repository_cache_root`, and discovery regression</read_first>
  <files>tests/phase1/test_evidence.py</files>
  <behavior>
    - External and symlink roots still fail before transport.
    - Exact repository root still succeeds and cleans its own child.
    - The repaired candidate tracer passes in the same run with no inter-test cache collision.
    - A real preexisting fixed child makes the tracer fail without transport, mutation, or cleanup of that child.
    - The production collector file remains byte-unchanged by this plan.
  </behavior>
  <action>Run the complete four-test cache/tracer cluster together, including the named tracer's preexisting-sentinel refusal and normal owned-child path, then inspect the scoped diff and remove any edit outside the named tracer method. Do not compensate for fixture failure by changing production imports, monkeypatching `_confined_cache_root`, bypassing path validation, or broadening cleanup. Record the unchanged `tools/acquire-sources.py` blob/diff state and the sentinel byte-preservation result in the plan summary.</action>
  <verify>
    <automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_evidence.BoundedCollectorTests.test_collector_rejects_external_or_symlink_cache_root_before_transport test_evidence.BoundedCollectorTests.test_collector_accepts_exact_repository_cache_root test_evidence.BoundedCollectorTests.test_unittest_discovery_includes_non_allowlisted_ingress_once test_evidence.BoundedCollectorTests.test_candidate_acquisition_tracer_blocks_incomplete_dimensions -v &amp;&amp; git diff --check -- tests/phase1/test_evidence.py</automated>
  </verify>
  <acceptance_criteria>All four tests pass together, only the named tracer method changed, preexisting child bytes survive refusal without transport or cleanup, and exact-root production enforcement remains intact.</acceptance_criteria>
  <done>The retained suite exercises EVID-03 compatibility behavior without weakening CR-01.</done>
</task>

</tasks>

## Artifacts this phase produces

| Path | Modified symbol | Contract |
|---|---|---|
| `tests/phase1/test_evidence.py` | `BoundedCollectorTests.test_candidate_acquisition_tracer_blocks_incomplete_dimensions` | Uses the literal fixture tuple, claims `collector.CACHE_ROOT / "fixture-plan-01-51-tracer"` with `mkdir(exist_ok=False)`, deletes only under proven ownership, refuses and byte-preserves a preexisting sentinel, preserves unrelated cache bytes, and retains all compatibility assertions. |

No new symbol, path, field, CLI flag, package, external service, or production-code edit is produced.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| test fixture → repository-owned ignored cache | Test code may write into the real allowed root and must own/clean only a collision-free child. |
| regression repair → production confinement | A stale test must not be “fixed” by weakening the source-ingress boundary. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-51-01 | Tampering | cache-confinement regression | medium | mitigate | Restrict files_modified and action to one test method; run existing hostile-root and exact-root tests together. |
| T-01-51-02 | Denial of Service | fixture claim and cleanup | medium | mitigate | Exclusive `mkdir(exist_ok=False)` occurs before transport; a false-by-default ownership flag gates cleanup, while a preexisting-sentinel regression proves refusal and byte preservation. |
| T-01-51-03 | Repudiation | compatibility result | medium | mitigate | Retain exact missing-dimension order and unapproved observed-implementation assertions. |

No HIGH threat remains unmitigated. No package-manager install occurs; supply-chain package legitimacy gate is not applicable.
</threat_model>

<verification>
Run the named tracer alone, then the four-test cluster through `tools/phase1-python`. Confirm preexisting-sentinel refusal occurs before transport with exact byte preservation, production collector has no plan-owned edit, and successful normal runs leave no child they owned.
</verification>

<success_criteria>
WR-04 is repaired solely in its stale fixture; EVID-03 assertions execute, strict cache-root confinement stays exact, exclusive claim prevents collisions, and cleanup cannot remove preexisting or sibling data.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-51-SUMMARY.md` when done.
</output>
