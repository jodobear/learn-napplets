---
phase: 01-research-and-truth-baseline
plan: 32
type: execute
wave: 2
depends_on: [01-29]
files_modified:
  - tools/validate-planning.py
  - tests/phase1/test_preflight.py
autonomous: true
gap_closure: true
requirements: [EVID-01, OPER-03]
must_haves:
  truths:
    - "Every ordinary CLM citation reaches immutable source provenance validation, while malformed, embedded, unknown, and incomplete citations fail deterministically."
    - "Execution and completion reject vague convergence text, unresolved HIGH findings, ambient Python subprocesses, and any review record not bound to the exact active 43-plan set."
  artifacts:
    - path: tools/validate-planning.py
      provides: "Fail-closed citation, review-manifest preflight, and wrapper-only Phase 1 gate"
    - path: tests/phase1/test_preflight.py
      provides: "Exact citation, convergence, bound-review, and wrapper regression probes"
    - path: .planning/phases/01-research-and-truth-baseline/01-REVIEWS.md
      provides: "Independent review attestation naming every active plan and its digest"
  key_links:
    - from: citation-bearing catalogs, lessons, and ADRs
      to: claims.yaml and source-registry.yaml
      via: "decoded-code-point ASCII CLM token extraction followed by GATE016-GATE018"
    - from: phase completion gate
      to: tools/phase1-python
      via: "all Phase 1 child validator/test argv vectors start with approved wrapper"
  prohibitions:
    - statement: "Validation may not alter claims, source records, ADR states, approval fields, or phase transition status while inspecting citations and reviews."
      category: authority
      status: unresolved
      flagged: true
      verification: "Temporary-root tests and immutable input byte assertions cover inspection behavior."
---

<objective>
Repair Phase 1 citation provenance, strict review convergence, and child-process toolchain enforcement.

Purpose: Close CR-01, CR-02, and CR-03 at the gates that previously skipped ordinary citations and accepted ambient execution.
Output: A deterministic citation-to-provenance tracer and strict completion preflight.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-REVIEW.md
@.planning/phases/01-research-and-truth-baseline/01-REVIEWS.md
@.planning/phases/01-research-and-truth-baseline/01-VERIFICATION.md
@tools/validate-planning.py
@tools/phase1-python
@tests/phase1/test_preflight.py
@tests/phase1/test_lesson_evidence.py
@.planning/research/claims.yaml
@.planning/research/source-registry.yaml
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Trace citation adjacency, empty input, Unicode controls, and stable ordering to fail-closed provenance</name>
  <read_first>tools/validate-planning.py; tests/phase1/test_preflight.py; tests/phase1/test_lesson_evidence.py; claims.yaml; source-registry.yaml; 01-VERIFICATION.md; 01-REVIEW.md</read_first>
  <files>tools/validate-planning.py, tests/phase1/test_preflight.py</files>
  <behavior>
    - `test_citation_ascii_adjacency_extracts_exact_token` accepts a valid token beside Markdown and UTF-8 punctuation but rejects ASCII letter/digit/hyphen embedding.
    - `test_citation_empty_inventory_has_no_diagnostic` proves empty and absent citation input is quiet while unknown and incomplete records emit distinct GATE016 and GATE018 diagnostics.
    - `test_citation_tokens_are_ascii_decoded_codepoints_without_unicode_normalization` accepts NFC and NFD text plus curly-quote/em-dash punctuation around `CLM-POLICY-001`, rejects a combining-mark-adjacent token and Cyrillic-C lookalike, and performs no normalization.
    - `test_citation_diagnostics_stable_path_then_id` proves deterministic artifact-path then stable-ID ordering.
  </behavior>
  <action>Start RED in `Phase1ExecutionPreflightTests` with temporary planning roots and direct calls to a dependency-injectable `phase1_citation_and_inventory_errors`. Decode strict UTF-8 only. Extract the ASCII grammar `CLM-[A-Z0-9-]+` with a real word boundary, reject ASCII embedding and tokens adjacent to Unicode combining marks, and preserve exact extracted code points without NFC/NFD normalization. Read citation-bearing catalog, lesson, and ADR inputs deterministically, then retain separate GATE016 unknown-claim, GATE017 missing relation, and GATE018 incomplete immutable-source results. Assert the four named tests above before implementation and add the NFC `café`, NFD `cafe` plus combining mark, grapheme-adjacent, punctuation, and confusable controls explicitly. Preserve D-04, D-14, D-16, D-17, D-29, and D-31: source provenance is checked but no evidence classification or approval changes.</action>
  <verify><automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_preflight.Phase1ExecutionPreflightTests.test_citation_ascii_adjacency_extracts_exact_token && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_preflight.Phase1ExecutionPreflightTests.test_citation_empty_inventory_has_no_diagnostic && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_preflight.Phase1ExecutionPreflightTests.test_citation_tokens_are_ascii_decoded_codepoints_without_unicode_normalization && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_preflight.Phase1ExecutionPreflightTests.test_citation_diagnostics_stable_path_then_id</automated></verify>
  <acceptance_criteria>All four named citation probes pass through the wrapper, ordinary `CLM-*` citations are no longer skipped, control inputs cannot create a false claim token, and invalid provenance remains separately diagnosable and sorted.</acceptance_criteria>
  <done>Maintainers can rely on citation traversal to enumerate actual claim references and fail closed on incomplete provenance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind exact convergence grammar to an independent active-plan review manifest and wrapper-only completion children</name>
  <read_first>tools/validate-planning.py; tests/phase1/test_preflight.py; tools/phase1-python; 01-REVIEW.md; 01-REVIEWS.md; .planning/research/phase-governance.yaml</read_first>
  <files>tools/validate-planning.py, tests/phase1/test_preflight.py</files>
  <action>Build on, and do not defer, the Wave-1 manifest parser delivered by Plan 01-29. Add RED fixtures named `test_phase1_complete_requires_bound_preflight_and_executor_identity` and `test_phase1_child_processes_use_wrapper` to `Phase1ExecutionPreflightTests`. The first must prove `--phase-1-complete` invokes the already-established exact review-manifest preflight, requires nonempty `--executor-identity` supplied from `GSD_EXECUTOR_ID`, rejects reviewer/executor equality, and emits no completion success if that call is absent, malformed, or fails. Replace every `sys.executable` Phase 1 child route, including the lesson evidence suite, with argv beginning at `ROOT / 'tools' / 'phase1-python'`; preserve argv arrays and report wrapper failures as gate errors. Test that monkeypatched child calls cannot substitute an ambient interpreter. Keep the exact active-plan SHA-256, reviewed-commit-byte, RFC 3339 freshness, independent reviewer identity, and convergence grammar validation in Plan 01-29's Wave-1 helper; this task only makes every completion and child execution route consume it. Implement D-15, D-25, D-28, D-33, and D-35 by preparing review evidence only: this gate cannot accept an ADR, risk, or transition.</action>
  <verify><automated>test -n "$GSD_EXECUTOR_ID" && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_preflight.Phase1ExecutionPreflightTests.test_phase1_complete_requires_bound_preflight_and_executor_identity && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_preflight.Phase1ExecutionPreflightTests.test_phase1_child_processes_use_wrapper && GSD_EXECUTOR_ID="$GSD_EXECUTOR_ID" tools/phase1-python tools/validate-planning.py --phase-1-execution-preflight --executor-identity "$GSD_EXECUTOR_ID" && GSD_EXECUTOR_ID="$GSD_EXECUTOR_ID" tools/phase1-python tools/validate-planning.py --phase-1-complete --executor-identity "$GSD_EXECUTOR_ID"</automated></verify>
  <acceptance_criteria>The preflight accepts only a fresh independent review that exactly binds all active plan filenames, bytes, reviewed commit, timestamp, and reviewer identity; adversarial scope, digest, commit, freshness, and identity fixtures fail, and all Phase 1 subprocess invocations use the approved wrapper.</acceptance_criteria>
  <done>Execution and completion authorization cannot be obtained through stale plan review, vague review prose, unresolved HIGH findings, reviewer/executor self-certification, or ambient Python execution.</done>
</task>
</tasks>

## Artifacts this phase produces
- `tools/validate-planning.py` — `phase1_citation_and_inventory_errors`, strict preflight parser, and wrapper child launcher.
- `tests/phase1/test_preflight.py` — exact EVID-01 and OPER-03 regression probes.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| citation text → provenance gate | Citation text can hide unknown or malformed evidence references. |
| review record → execution authorization | Human-readable prose can be ambiguous or misleading. |
| completion gate → child process | Ambient interpreter state can alter evidence validation. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-32-01 | Tampering | citation parsing | high | mitigate | Task 1 tests ASCII grammar, code-point controls, provenance failures, and order. |
| T-32-02 | Spoofing | review convergence | high | mitigate | Task 2 accepts only bounded exact grammar and complete dispositions. |
| T-32-03 | Tampering | child interpreter | high | mitigate | Task 2 requires wrapper argv element zero. |
| T-32-04 | Repudiation | claim diagnostic order | medium | mitigate | Task 1 records stable path/ID diagnostics. |
| T-32-05 | Elevation of Privilege | downstream replay | high | mitigate | No replay is run here; Plan 01-33 removes manifest command authority. |
</threat_model>

<verification>
Run the named EVID-01 probes, strict preflight tests, and execution-preflight command through the approved wrapper.
</verification>

<success_criteria>
CR-01 through CR-03 receive focused behavior-level regression proof without altering canonical evidence or approval state.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-32-SUMMARY.md` when done.
</output>
