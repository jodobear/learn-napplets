---
phase: 01-research-and-truth-baseline
gsd_phase: 1
source_phase: 0
adr_status_limit: proposed
plan: 28
type: execute
wave: 10
depends_on: [01-09, 01-10, 01-11, 01-12, 01-13, 01-14, 01-15, 01-16, 01-17, 01-18, 01-19, 01-20]
files_modified:
  - tools/validate-research.py
  - tests/phase1/test_spike_consolidation.py
  - .planning/research/compatibility-matrix.yaml
  - .planning/research/drift-register.yaml
  - .planning/research/open-questions.yaml
  - .planning/research/security-egress-findings.md
  - .planning/research/reports/spike-consolidation.md
  - .planning/spikes/replay-manifest.yaml
autonomous: true
requirements: [EVID-02, EVID-03, EVID-04, OPER-01]
must_haves:
  truths:
    - "Every SPK-A through SPK-L report is explicitly accounted for before downstream ADR, summary, or closeout work reads consolidated evidence."
    - "SPK-C, SPK-D, SPK-G, and SPK-H immutable impact fragments are schema-validated and contribute compatibility, drift, open-question, or explicit blocked/uncertain outcomes through one serialized merge."
    - "Canonical compatibility, drift, open-question, security/egress, replay-manifest, and consolidation-audit outputs are staged from an immutable input snapshot under an exclusive lock and published as one lock-protected transaction; a contention loser exits or blocks deterministically and injected publication failure restores byte-identical prior canonical state."
    - "An exact rerun is byte-stable and a conflicting stable-ID proposal is blocked rather than overwritten."
    - "The required security/egress synthesis is derived from SPK-H evidence with upstream facts, browser observations, project policy, and unresolved questions kept separate, and every generated output claim links back to its validated fragment, report, and canonical source/claim record."
  artifacts:
    - path: tests/phase1/test_spike_consolidation.py
      provides: "Deterministic merge, conflict, rerun, and missing-input test fixtures"
    - path: .planning/research/reports/spike-consolidation.md
      provides: "All-spike report/fragment accounting, locked transactional merge, no-impact, conflict, and rerun audit"
    - path: .planning/spikes/replay-manifest.yaml
      provides: "Versioned replay inventory and freshness criteria for SPK-A through SPK-L"
    - path: .planning/research/security-egress-findings.md
      provides: "SPK-H-derived canonical security/egress synthesis"
  key_links:
    - from: .planning/spikes/spk-*/impact-fragment.yaml
      to: compatibility-matrix.yaml, drift-register.yaml, and open-questions.yaml
      via: "validate-research.py schema and stable-ID merge command with report and measurement digests"
    - from: .planning/spikes/spk-h-browser-egress/impact-fragment.yaml
      to: .planning/research/security-egress-findings.md
      via: "typed source, observation, policy, question, uncertainty, owner, and approval fields"
    - from: .planning/research/reports/spike-consolidation.md
      to: Plans 01-21 through 01-24
      via: "serialized canonical state and explicit all-spike accounting before ADR, summary, and phase-gate consumption"
---

<objective>
Serialize all completed spike evidence into canonical compatibility, drift, and open-question records without losing concurrent results or converting uncertainty into asserted fact.

Purpose: Make every recommendation and the Phase 1 closeout consume one validated, history-preserving evidence state after the parallel spike wave.
Output: Fragment schema and tests, deterministic consolidation tooling, three canonical record updates, a required security/egress synthesis, and an all-spike audit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-RESEARCH.md
@.planning/research/schemas/drift.schema.json
@.planning/research/schemas/open-question.schema.json
@.planning/research/schemas/compatibility.schema.json
@.planning/research/schemas/report-contract.yaml
@.planning/research/compatibility-matrix.yaml
@.planning/research/drift-register.yaml
@.planning/research/open-questions.yaml
@.planning/research/claims.yaml
@.planning/research/source-registry.yaml
@tools/validate-research.py
@tests/phase1/test_drift.py
@tests/phase1/test_compatibility.py
@tests/phase1/test_spikes.py
@.planning/spikes/spk-a-workspace/report.md
@.planning/spikes/spk-b-static-framework/report.md
@.planning/spikes/spk-c-boundary-harness/report.md
@.planning/spikes/spk-c-boundary-harness/impact-fragment.yaml
@.planning/spikes/spk-d-verified-loader/report.md
@.planning/spikes/spk-d-verified-loader/impact-fragment.yaml
@.planning/spikes/spk-e-content-rendering/report.md
@.planning/spikes/spk-f-course-workbench/report.md
@.planning/spikes/spk-g-package-conformance/report.md
@.planning/spikes/spk-g-package-conformance/impact-fragment.yaml
@.planning/spikes/spk-h-browser-egress/report.md
@.planning/spikes/spk-h-browser-egress/impact-fragment.yaml
@.planning/spikes/spk-i-diagram-motion/report.md
@.planning/spikes/spk-j-code-editing/report.md
@.planning/spikes/spk-k-deployment/report.md
@.planning/spikes/spk-l-source-freshness/report.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one immutable fragment-to-canonical merge path before full consolidation</name>
  <read_first>.planning/research/schemas/spike-impact-fragment.schema.json; .planning/research/schemas/report-contract.yaml; tools/validate-research.py; tests/phase1/test_spike_consolidation.py; .planning/spikes/spk-c-boundary-harness/impact-fragment.yaml; .planning/spikes/spk-h-browser-egress/impact-fragment.yaml; .planning/governance/evidence-policy.md</read_first>
  <files>tools/validate-research.py, tests/phase1/test_spike_consolidation.py</files>
  <behavior>
    - A SPK-C-shaped fragment with matching report, metadata, measurement, source IDs, requirement/phase impacts, uncertainty, and typed links passes a dry-run without modifying canonical YAML.
    - A duplicate fragment with the same stable ID and normalized content produces one planned result and no duplicate canonical history entry.
    - A proposal that reuses a stable canonical ID with incompatible immutable evidence is rejected, leaves the target record unchanged, and produces a deterministic blocked consolidation item.
    - Reordering input fragments does not change the dry-run output; a missing SPK-A through SPK-L report is a failed accounting result rather than an omitted input.
    - Two consolidation processes contend on one exclusive lock: one holds the transaction while the other blocks or exits with the documented bounded conflict diagnostic.
    - A failure injected after staging and before commit leaves every canonical target and audit byte-identical; a successful transaction publishes the complete canonical set and audit under the same lock.
    - A SPK-H output fixture with missing, altered, or mismatched source/observation/policy/question provenance fails validation before publication.
  </behavior>
  <action>Start with failing `test_spike_consolidation.py` fixtures, then consume the Plan 01-04 versioned `spike-impact-fragment.schema.json` and narrowly extend `validate-research.py`. Validate the immutable fragment contract with `schemaVersion`, stable `fragmentId`, `spikeId`, report/metadata/measurement paths plus SHA-256 values, `sourceIds`, `proposedOpenQuestions`, `proposedDriftRecords`, `compatibilityObservations`, `affectedRequirements`, `affectedPhases`, `uncertainty`, and typed `evidenceLinks`; require the SPK-H extension for its security/egress finding proposal and complete transitive `CLM-*` to immutable `SRC-*` provenance for every fact-bearing link. Implement `validate-impact-fragment <path>` and a deterministic `consolidate-spike-impacts --dry-run` path that inventories all twelve named SPK report paths, validates each discovered fragment before transforming it, sorts by stable ID, and emits an explicit blocked or no-impact result when evidence is unavailable or incompatible. Acquire an exclusive `fcntl.flock` lock before snapshotting, snapshot immutable input paths/digests into ignored same-filesystem staging, and make dry run write no canonical target. The tracer uses SPK-C input through validation, locked dry-run transformation, and audit output, establishing the same merge seam used in Task 2. Add two-process contention, injected pre-commit failure, post-commit success, and SPK-H output-provenance negative fixtures. Do not infer canonical facts from report prose; retain source observations and proposal status separately per D-12, D-16 through D-20, D-23, D-26, and D-29 through D-32. This addresses review concerns 3, 4, 10, and 15.</action>
  <verify><automated>tools/phase1-python -m unittest discover -s tests/phase1 -p 'test_spike_consolidation.py' && tools/phase1-python tools/validate-research.py validate-impact-fragment .planning/spikes/spk-c-boundary-harness/impact-fragment.yaml && tools/phase1-python tools/validate-research.py consolidate-spike-impacts --spikes .planning/spikes --research .planning/research --audit /tmp/spike-consolidation-dry-run.md --dry-run</automated></verify>
  <acceptance_criteria>The tracer validates SPK-C through the Plan 01-04 fragment contract and transitive source links, runs under an exclusive lock without changing canonical targets, and tests duplicate, collision, ordering, missing-report, contention, injected failure, successful transaction, and broken SPK-H output provenance cases.</acceptance_criteria>
  <done>The test suite proves one real fragment is validated and transformed through a deterministic dry run, while duplicate, collision, ordering, missing-report, lock, crash, and provenance failures remain visible and cannot alter canonical evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Consolidate all spike reports and fragments into history-preserving canonical evidence</name>
  <read_first>tools/validate-research.py; tests/phase1/test_spike_consolidation.py; .planning/research/schemas/spike-impact-fragment.schema.json; .planning/spikes/replay-manifest.yaml; .planning/spikes/spk-a-workspace/report.md; .planning/spikes/spk-h-browser-egress/{report.md,impact-fragment.yaml}; .planning/research/{claims.yaml,source-registry.yaml,compatibility-matrix.yaml,drift-register.yaml,open-questions.yaml}</read_first>
  <files>.planning/research/compatibility-matrix.yaml, .planning/research/drift-register.yaml, .planning/research/open-questions.yaml, .planning/research/security-egress-findings.md, .planning/research/reports/spike-consolidation.md, .planning/spikes/replay-manifest.yaml</files>
  <behavior>
    - The consolidation audit lists every SPK-A through SPK-L report, each available fragment digest, each validated merge or no-impact disposition, and each blocked/uncertain outcome.
    - A fragment with an exact prior stable ID and normalized payload is a no-op on rerun; canonical YAML and the audit remain byte-identical after a second execution.
    - An incompatible proposal cannot replace a canonical record; the audit and a linked deterministic `OQ-CONSOLIDATION-*` blocked record retain both evidence digests and impacts for human review.
    - The SPK-H-derived synthesis includes all ten canonical headings and keeps pinned-source facts, observed browser behavior, proposed CSP/project policy, and unresolved questions in separate sections.
  </behavior>
  <action>Run the normal consolidation command only after it has read every SPK-A through SPK-L `report.md` and every available spike-local impact fragment. Before transform, acquire the exclusive lock; capture an immutable input snapshot with path/digest inventory; stage every canonical target, `security-egress-findings.md`, `spike-consolidation.md`, and versioned `replay-manifest.yaml` below an ignored same-filesystem transaction directory; validate the complete staged set; and only then publish the full set under the held lock. The command must recover an injected pre-commit failure by restoring every target byte-for-byte and must make a contention loser block or return the documented bounded conflict without touching canonical files. Require matching SPK ID, report/metadata/measurement digests, complete transitive claim-to-source provenance, valid relation types, impacted requirement/phase IDs, and uncertainty before accepting a fragment proposal. Preserve existing canonical record history by appending attributable evidence observations rather than deleting or replacing prior values. Treat an identical stable-ID proposal as a sorted no-op; reject an incompatible stable-ID overwrite, leave the target record intact, and create a deterministic `OQ-CONSOLIDATION-*` blocked record with both evidence digests, requirement/phase impacts, resolution criterion, owner, and review requirement. When an expected report has no machine fragment, record `no-impact-fragment` in the audit instead of deriving a claim from prose. Generate `security-egress-findings.md` only from the validated SPK-H fragment and report, with the canonical headings and separate upstream-fact, browser-observation, project-policy, and upstream-question sections; validate every output claim's typed link back to the fragment, report, and canonical source/claim record. Generate `replay-manifest.yaml` for SPK-A through SPK-L with stable spike ID, report/metadata/measurement/fixture paths and digests, replay command through `tools/phase1-python`, applicability/blocked criteria, freshness threshold, and required outcome. Write a deterministic, stable-ID-sorted `spike-consolidation.md` that records the input snapshot, lock transaction, merge/no-op/no-impact/conflict result, canonical targets, blocked/uncertain items, replay-manifest digest, and rerun digest. Apply D-05, D-12, D-13, D-16 through D-20, D-23, D-26, and D-29 through D-32; no recommendation becomes an accepted ADR or a settled upstream fact. This addresses review concerns 3, 4, 10, 14, and 15.</action>
  <verify><automated>tools/phase1-python -m unittest discover -s tests/phase1 -p 'test_spike_consolidation.py' && tools/phase1-python tools/validate-research.py consolidate-spike-impacts --spikes .planning/spikes --research .planning/research --audit .planning/research/reports/spike-consolidation.md && tmp=$(mktemp) && sha256sum .planning/research/compatibility-matrix.yaml .planning/research/drift-register.yaml .planning/research/open-questions.yaml .planning/research/security-egress-findings.md .planning/research/reports/spike-consolidation.md > "$tmp" && tools/phase1-python tools/validate-research.py consolidate-spike-impacts --spikes .planning/spikes --research .planning/research --audit .planning/research/reports/spike-consolidation.md && sha256sum --check "$tmp" && rm "$tmp" && tools/phase1-python tools/validate-research.py validate --root .planning/research --report .planning/research/reports/validation.md && tools/phase1-python tools/validate-research.py validate-reports --root .planning && tools/phase1-python tools/validate-research.py replay-spikes --manifest .planning/spikes/replay-manifest.yaml --check</automated></verify>
  <acceptance_criteria>All SPK-A through SPK-L inputs appear in the immutable snapshot and replay manifest; the contention test has one deterministic loser, injected failure restores every target byte-identically, successful staging publishes the canonical set/audit/manifest under one lock, and broken SPK-H output provenance rejects before publication.</acceptance_criteria>
  <done>All spike inputs are accounted for and their supported impacts are serially merged, retained as explicit blocked/uncertain work, or recorded as no-impact; canonical records, required security synthesis, replay manifest, and audit are transactionally published and remain unchanged on an identical rerun.</done>
</task>

</tasks>

## Artifacts this phase produces

- `tests/phase1/test_spike_consolidation.py` — lock contention, staging/crash, rerun, duplicate, collision, ordering, missing-input, and output-provenance fixtures.
- `tools/validate-research.py` — Plan 01-04 fragment validation plus deterministic locked consolidation and replay-manifest commands.
- `.planning/research/{compatibility-matrix.yaml,drift-register.yaml,open-questions.yaml}` — history-preserving canonical outcomes from validated spike evidence.
- `.planning/research/security-egress-findings.md` — required SPK-H-derived source/observation/policy/question synthesis with typed output provenance.
- `.planning/research/reports/spike-consolidation.md` — all-SPK input snapshot, transaction, accounting, and canonical-merge audit.
- `.planning/spikes/replay-manifest.yaml` — versioned SPK-A through SPK-L replay inventory, digests, applicability, and thresholds.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parallel spike-local outputs → consolidation CLI | Independently produced results may be incomplete, malformed, duplicated, or contradictory. |
| validated fragment → canonical YAML | A proposed observation crosses into durable, downstream-consumed evidence records. |
| SPK-H egress observation → security synthesis | Browser behavior and project policy could be confused with upstream protocol fact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-28-01 | Tampering | fragment-to-canonical merge | high | mitigate | Validate schema, stable IDs, source links, relation types, and report/metadata/measurement digests before a sorted serialized merge. |
| T-28-02 | Repudiation | conflict and no-impact handling | high | mitigate | Audit every SPK report and available fragment, preserve conflicting digests, and create deterministic blocked records with owner and resolution criteria. |
| T-28-03 | Spoofing | security/egress conclusion | high | mitigate | Derive the synthesis from SPK-H evidence only and retain distinct upstream-fact, browser-observation, project-policy, and upstream-question sections. |
| T-28-04 | Denial of Service | malformed or missing spike input | medium | mitigate | Fail deterministic fragment validation and emit an explicit accounting/blocked disposition instead of omitting the input. |
| T-28-SC | Tampering | consolidation tooling | high | mitigate | Extend the reviewed Plan 01 Python toolchain and run deterministic local tests; install no new package. |
</threat_model>

<verification>
Run the consolidation unit suite, execute the all-spike merge twice while comparing canonical and audit SHA-256 values, then validate canonical research and all registered reports.
</verification>

<success_criteria>
Downstream ADR, summary, and phase-closeout plans consume one complete, validated, history-preserving evidence state in which no parallel spike fragment or incompatible outcome is silently lost.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-28-SUMMARY.md` when done.
</output>
