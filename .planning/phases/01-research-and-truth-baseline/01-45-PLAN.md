---
phase: 01-research-and-truth-baseline
plan: 45
type: execute
wave: 3
depends_on: [01-44]
files_modified:
  - tools/acquire-sources.py
  - tests/phase1/test_evidence.py
  - .planning/research/upstream-acquisition-queue.yaml
  - .planning/research/reports/upstream-acquisition-20260728.md
  - .planning/research/acquisition-log.yaml
  - .planning/research/candidate-source-manifest.yaml
autonomous: true
gap_closure: true
requirements: [EVID-01, EVID-03, OPER-03]
must_haves:
  truths:
    - "One fixture-backed public candidate path resolves official repository identity, immutable commit:path bytes, exact SHA-256, retrieval time, and observed-implementation classification, then validates a staged source record without promoting it to canonical authority."
    - "The compatibility classifier evaluates normative protocol, observed implementation, published package, runtime, example/fixture, current-work, and conformance dimensions; absent qualified dimensions produce an impact-scoped `blocked` result."
    - "The bounded acquisition queue and receipt retain the complete report-derived kehto/web, napplet/web, and napplet/naps observation window, old discovery pointers, and prior failure history as reviewable observed evidence."
    - "Queue entries and the collection receipt carry the exact reviewed commit and all four report-input SHA-256 values emitted by Plan 01-29's Git-blob-only source snapshot; absent, untracked, altered, or different-commit inputs block collection before any queue value is parsed."
    - "Public collection remains read-only, cache-confined, and non-normative until Plan 01-30's human authority decision and Plan 01-31's canonical intake guard apply it."
  artifacts:
    - path: tools/acquire-sources.py
      provides: "Read-only immutable identity/commit/blob collector and seven-dimension blocked-or-reviewable classifier"
    - path: .planning/research/upstream-acquisition-queue.yaml
      provides: "Bounded report-derived subjects, exact commit:path targets, evidence dimensions, impact IDs, refresh triggers, and reviewed-source input binding"
    - path: .planning/research/reports/upstream-acquisition-20260728.md
      provides: "Dated immutable byte, retrieval, uncertainty, non-normative collection receipt, and reviewed-source input binding"
    - path: .planning/research/acquisition-log.yaml
      provides: "Append-only retrieval, failure, and retry history"
  key_links:
    - from: queue candidate identity
      to: tools/acquire-sources.py
      via: "allowlisted public HTTPS identity response, immutable commit:path blob resolution, exact-byte SHA-256, and ignored cache confinement"
    - from: fixture collector tracer
      to: temporary source record validation
      via: "identity, commit, blob bytes, digest, retrieval time, and observed classification pass the canonical validator without writing a canonical source record"
    - from: Plan 01-29 reviewed Git-blob source snapshot
      to: acquisition queue and receipt
      via: "closed four-report SHA-256 map and reviewed commit are retained as `reviewedSourceInputBinding` before parser-derived candidates are collected"
    - from: acquisition receipt and history
      to: Plan 01-30 authority checkpoint and Plan 01-31 intake
      via: "exact queue/receipt records preserve evidence scope and do not imply authority, approval, or canonical ingestion"
  prohibitions:
    - statement: "This plan does not install or import packages, make external writes, verify a claim, accept an ADR, or promote merged implementation or release metadata to normative NIP/NAP authority."
      category: authority
      status: unresolved
      flagged: true
      verification: "The tracer emits observed or blocked classifications only, and canonical source intake is deferred to the authority-gated Plan 01-31."
---

<objective>
Trace immutable public source acquisition into a staged blocked-or-reviewable classification, then retain complete bounded acquisition history for human authority review.

Purpose: Split source ingress and history retention from authorization and dependency certification while preserving the review-ready evidence Plan 01-30 and Plan 01-31 require.
Output: A bounded read-only collector, fixture tracer, complete acquisition queue, immutable receipt, and additive history.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-research-and-truth-baseline/01-29-SUMMARY.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-RESEARCH.md
@.planning/research/reports/upstream-refresh-synthesis-2026-07-28.md
@.planning/research/reports/upstream-refresh-kehto-web-2026-07-28.md
@.planning/research/reports/upstream-refresh-napplet-web-2026-07-28.md
@.planning/research/reports/upstream-refresh-napplet-naps-2026-07-28.md
@.planning/research/candidate-source-manifest.yaml
@.planning/research/acquisition-log.yaml
@.planning/research/source-registry.yaml
@.planning/research/schemas/source.schema.json
@.planning/research/schemas/compatibility.schema.json
@tools/acquire-sources.py
@tools/validate-research.py
@tests/phase1/test_evidence.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one immutable public candidate to staged blocked compatibility evidence</name>
  <precondition>Plan 01-44's fresh wheelhouse certification and Plan 01-29's `--verify-phase1-source-inputs` check pass; no Plan 01-30 human authority determination has yet been applied to canonical source intake.</precondition>
  <read_first>01-29-SUMMARY.md; 01-REVIEWS.md; tools/validate-planning.py; tools/acquire-sources.py; tools/validate-research.py; .planning/research/schemas/source.schema.json; .planning/research/schemas/compatibility.schema.json; tests/phase1/test_evidence.py</read_first>
  <files>tools/acquire-sources.py, tests/phase1/test_evidence.py</files>
  <behavior>
    - `BoundedCollectorTests.test_candidate_acquisition_tracer_blocks_incomplete_dimensions` uses an in-process fixture transport for repository identity, commit/tree/blob, and package metadata; it proves one napplet/web commit:path resolves exact bytes, SHA-256, and observed-implementation classification, while a temporary source record passes canonical validation.
    - The tracer evaluates normative protocol, observed implementation, published package, runtime, example/fixture, current-work, and conformance dimensions, returning `blocked` with ordered missing dimensions and affected requirement IDs whenever qualified evidence is absent.
    - The collector rejects a non-allowlisted host, mutable-only identity, repository-escaping cache path, missing commit/blob, or attempt to treat observed implementation as normative authority.
    - `BoundedCollectorTests.test_acquisition_queue_uses_only_reviewed_git_blob_refresh_inputs` proves queue extraction uses the Plan 01-29 source snapshot's exact four report blobs and binding map; an altered worktree report, a missing/untracked report, an unlisted digest, or a different reviewed commit refuses before candidate parsing or network collection.
  </behavior>
  <action>Addresses Codex HIGH source-grounding finding: start RED in `tests/phase1/test_evidence.py` with an in-process fixture transport and a temporary Git fixture for `BoundedCollectorTests.test_acquisition_queue_uses_only_reviewed_git_blob_refresh_inputs`; unit tests must not contact a live host. Before parsing any upstream refresh material, call Plan 01-29's `--verify-phase1-source-inputs --review .planning/phases/01-research-and-truth-baseline/01-REVIEWS.md` contract and consume only its immutable `load_reviewed_phase1_source_snapshot()` mapping. Add a deterministic parser that accepts only the four bound report blobs, verifies the complete report-path/SHA-256 map and reviewed commit, extracts their candidate rows without consulting PLAN text, a working-tree report, a branch tip, or a live API response, and fails closed on an unrecognized, incomplete, duplicate, or digest-mismatched record. Extend `tools/acquire-sources.py` with a bounded read-only path that validates an allowlisted official HTTPS identity response, resolves a specified immutable commit:path blob, hashes exact bytes, records retrieval time, and confines cached bytes below ignored `.research/upstreams/`. Create the tracer from one napplet/web fixture-backed candidate through repository identity, commit, blob, digest, and an observed-implementation source classification, then validate the resulting source shape in a temporary research root rather than inserting it into canonical `source-registry.yaml`. Evaluate the seven compatibility dimensions—normative protocol, observed implementation, published package, runtime, example/fixture, current work, and conformance—and return a machine-readable `blocked` classification with precise missing dimensions and affected requirements whenever the qualified sources are absent. Apply D-04, D-05, D-06, D-10, D-14, D-16 through D-20, D-29, D-30, and D-31: public reads are allowed, but observed code and release metadata remain distinct from protocol authority and no automatic claim/ADR/source approval is possible.</action>
  <verify><automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_evidence.BoundedCollectorTests.test_candidate_acquisition_tracer_blocks_incomplete_dimensions && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_evidence.BoundedCollectorTests.test_acquisition_queue_uses_only_reviewed_git_blob_refresh_inputs && tools/phase1-python tools/validate-planning.py --verify-phase1-source-inputs --review .planning/phases/01-research-and-truth-baseline/01-REVIEWS.md && tools/phase1-python tools/acquire-sources.py --help</automated></verify>
  <acceptance_criteria>The fixture-backed tracer proves immutable-byte capture and temporary canonical validation, rejects collection boundary violations, and produces a seven-dimension blocked result with no approval field set to approved when qualified evidence is incomplete. The reviewed-input regression proves source candidate extraction cannot begin unless all four report bytes come from Plan 01-29's exact reviewed Git-blob snapshot and digest map.</acceptance_criteria>
  <done>A real collector path is executable through the certified wrapper but cannot falsely certify EVID-03 or mutate canonical source authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Collect the bounded public window and retain additive review-ready history</name>
  <read_first>01-29-SUMMARY.md; 01-REVIEWS.md; tools/validate-planning.py; tools/acquire-sources.py; .planning/research/candidate-source-manifest.yaml; .planning/research/acquisition-log.yaml; tests/phase1/test_evidence.py</read_first>
  <files>.planning/research/upstream-acquisition-queue.yaml, .planning/research/reports/upstream-acquisition-20260728.md, .planning/research/acquisition-log.yaml, .planning/research/candidate-source-manifest.yaml</files>
  <behavior>
    - A queue entry retains repository identity, default branch, selected immutable merge/default commit, exact commit:path target, expected evidence class, affected claims/drift/questions, and refresh trigger.
    - A collector failure appends a dated impact-scoped retry/failure outcome without removing `ACQ-FAIL-001`, old discovery pointers, or earlier candidate history.
    - The receipt preserves every `kehto/web`, `napplet/web`, and `napplet/naps` observation extracted from the exact four-report Git-blob snapshot, including the bounded zero-result observation, as observed implementation or release-history evidence only; its reviewed-source input binding is retained unchanged.
  </behavior>
  <action>Addresses Codex HIGH source-grounding finding: use the certified wrapper to first invoke `tools/validate-planning.py --verify-phase1-source-inputs --review .planning/phases/01-research-and-truth-baseline/01-REVIEWS.md`, then derive the bounded public queue exclusively from Plan 01-29's reviewed Git-blob source snapshot parser. Do not derive a merge window, repository identity, commit, tree, path, blob, content digest, result count, or selected candidate from this PLAN, a mutable report, a branch tip, or a live API response. Write the exact `reviewedSourceInputBinding` into both queue and receipt: reviewed commit, the four report paths/SHA-256 values, parser version, and deterministic parsed-record digest. The parser must emit the complete bounded observation set described by the four committed reports, including the zero-result observation, or fail before public collection. For every parser-emitted subject, record repository identity, default branch, selected immutable merge/default commits, exact commit:path targets, expected evidence class, affected claims/drift/questions, and refresh triggers. Execute only public HTTPS reads against those extracted immutable locators; a response that differs from its extracted identity or digest creates an impact-scoped failed receipt rather than a replacement candidate. Keep fetched bytes only in ignored cache and write the committed receipt with response digest, retrieval date, HTTP identity fields, immutable locator, collection result, uncertainty, and the unchanged reviewed-source binding. Add `validate-reviewed-acquisition --queue PATH --receipt PATH --review PATH` to `tools/acquire-sources.py`; it rechecks Plan 01-29's closed source-input set, requires queue/receipt bindings to equal the Git-blob snapshot's reviewed commit and four report digests, and refuses mismatched or unbound data before any downstream use. Append outcomes to `acquisition-log.yaml` without removing `ACQ-FAIL-001`; add corrected public identities as new candidates rather than rewriting `nap-ecosystem` discovery pointers. Classify all merged code, test, and release-PR material as observed implementation or release metadata, never normative NIP/NAP proof, per D-01 through D-10, D-12, D-13, and D-20. Preserve this digest-bound handoff for Plan 01-30's six-role human authority determination and Plan 01-31's exact-receipt source intake; do not create canonical source records in this task.</action>
  <verify><automated>tools/phase1-python tools/acquire-sources.py collect --queue .planning/research/upstream-acquisition-queue.yaml --receipt .planning/research/reports/upstream-acquisition-20260728.md --cache-root .research/upstreams && tools/phase1-python tools/acquire-sources.py validate-reviewed-acquisition --queue .planning/research/upstream-acquisition-queue.yaml --receipt .planning/research/reports/upstream-acquisition-20260728.md --review .planning/phases/01-research-and-truth-baseline/01-REVIEWS.md && tools/phase1-python tools/validate-research.py validate --root .planning/research --report /tmp/phase1-acquisition-validation.md</automated></verify>
  <acceptance_criteria>The dated receipt accounts for the complete bounded report-derived observation set and zero-result observation, records immutable bytes/digests for each selected material target, preserves prior discovery and failure history, and labels every collected implementation/release item non-normative pending human authority review. The queue and receipt retain an exact reviewed commit/four-report digest binding that `validate-reviewed-acquisition` rechecks before a downstream plan may consume either artifact.</acceptance_criteria>
  <done>Plans 01-30, 01-31, 01-32, 01-33, 01-35, and 01-43 can consume one certified, review-ready, reviewed-source-bound collection history without re-performing bootstrap, wheelhouse certification, or an unbounded source crawl.</done>
</task>

</tasks>

## Artifacts this phase produces

- `tools/acquire-sources.py` — bounded immutable identity/commit/blob collector and blocked-or-reviewable classifier.
- `tests/phase1/test_evidence.py` — fixture-based immutable candidate tracer and incomplete-dimension regression.
- `.planning/research/upstream-acquisition-queue.yaml` — bounded acquisition subjects, evidence dimensions, and reviewed-source Git-blob binding.
- `.planning/research/reports/upstream-acquisition-20260728.md` — dated immutable collection receipt with the same reviewed-source binding.
- `tools/acquire-sources.py validate-reviewed-acquisition` — read-only verification that a queue and receipt retain the exact Plan 01-29 review input set before downstream consumption.
- `.planning/research/acquisition-log.yaml` and `candidate-source-manifest.yaml` — additive candidate, retry, and failure history.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public HTTPS response → collected bytes | Repository identity, refs, paths, and bytes are untrusted until pinned and hashed. |
| observed merge/release material → source classification | Implementation and release observations must not become normative protocol or package evidence. |
| acquisition history → later authority decision | A new attempt must not erase prior discovery, failure, or blocked scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-45-01 | Spoofing | repository identity | high | mitigate | Task 1 allowlists official HTTPS identity, resolves immutable commit:path bytes, and retains exact locator/digest evidence. |
| T-45-02 | Tampering | cached source bytes | high | mitigate | Task 1 confines cache below ignored `.research/upstreams/` and hashes resolved immutable bytes before classification. |
| T-45-03 | Spoofing | normative/source classification | high | mitigate | Tasks 1–2 label merged code and release metadata as observed evidence only; qualified dimensions remain blocked until independent authority evidence exists. |
| T-45-04 | Repudiation | collection history | medium | mitigate | Task 2 appends dated receipt, failure, retry, impact, and refresh data while retaining old discovery IDs and ACQ-FAIL-001. |
| T-45-05 | Elevation of Privilege | collector operation | high | mitigate | The collector uses bounded public reads only, performs no package install/import or external write, and leaves canonical intake to Plan 01-31. |
| T-45-06 | Tampering | upstream refresh seed selection | high | mitigate | Tasks 1–2 derive candidates only from Plan 01-29's verified Git-blob snapshot, persist its reviewed commit/report-digest binding, and require `validate-reviewed-acquisition` before a consumer trusts queue or receipt values. |
</threat_model>

<verification>
Run the fixture-backed candidate tracer, execute the bounded collection through the certified wrapper, validate canonical research, and confirm the receipt retains observed—not normative—classification and additive history.
</verification>

<success_criteria>
The project has a bounded, immutable, review-ready acquisition history and a real staged source tracer that accurately blocks incomplete compatibility evidence without making an authority or phase decision.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-45-SUMMARY.md` when done.
</output>
