---
phase: 01-research-and-truth-baseline
plan: 29
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements-phase1-tools.txt
  - tools/verify-phase1-toolchain.py
  - tools/phase1-python
  - tools/acquire-sources.py
  - tests/phase1/test_evidence.py
  - .planning/research/upstream-acquisition-queue.yaml
  - .planning/research/reports/upstream-acquisition-20260728.md
  - .planning/research/acquisition-log.yaml
  - .planning/research/candidate-source-manifest.yaml
autonomous: true
gap_closure: true
requirements: [EVID-01, EVID-03, OPER-03]
must_haves:
  truths:
    - "The approved wrapper verifies the complete locked dependency closure and installed distribution RECORD hashes before any Phase 1 command runs."
    - "One real public candidate flow resolves repository identity and immutable commit:path bytes, validates staged canonical source data, and reports a substantive blocked eligibility result rather than a false baseline pass."
    - "The eight-merge refresh is retained as reviewable observed evidence while old discovery records remain historical inputs."
  artifacts:
    - path: requirements-phase1-tools.txt
      provides: "Full approved Phase 1 wheel closure with exact hashes"
    - path: tools/verify-phase1-toolchain.py
      provides: "Lock, installed-distribution, and RECORD-integrity verifier"
    - path: tools/acquire-sources.py
      provides: "Read-only immutable GitHub and registry metadata/byte collector"
    - path: .planning/research/upstream-acquisition-queue.yaml
      provides: "Bounded acquisition subjects and required evidence dimensions"
  key_links:
    - from: tools/phase1-python
      to: tools/verify-phase1-toolchain.py
      via: "pre-execution verifier invocation before forwarding argv"
    - from: tools/acquire-sources.py
      to: temporary canonical source and compatibility records
      via: "identity -> commit/path bytes/digest -> validate -> eligible or blocked classification tracer"
  prohibitions:
    - statement: "No package installation, registry write, upstream write, browser download, ADR acceptance, or production scaffold is authorized."
      category: safety
      status: unresolved
      flagged: true
      verification: "Final verifier checks only public read-only collection and proposed records."
---

<objective>
Establish dependency-integrity enforcement and prove the first end-to-end upstream-acquisition tracer without accepting an upstream claim.

Purpose: Repair CR-09 before wrapper-based certification and start the required source acquisition path for EVID-03 without treating merged code as protocol authority.
Output: A hash-locked wrapper, bounded collector, and dated refresh acquisition queue/receipt.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-RESEARCH.md
@.planning/phases/01-research-and-truth-baseline/01-REVIEW.md
@.planning/phases/01-research-and-truth-baseline/01-VERIFICATION.md
@.planning/research/reports/upstream-refresh-synthesis-2026-07-28.md
@.planning/research/reports/upstream-refresh-kehto-web-2026-07-28.md
@.planning/research/reports/upstream-refresh-napplet-web-2026-07-28.md
@.planning/research/reports/upstream-refresh-napplet-naps-2026-07-28.md
@.planning/research/toolchain-approval.yaml
@.planning/spikes/_shared/toolchain-environment.json
@tools/phase1-python
@tools/acquire-sources.py
@tests/phase1/test_evidence.py
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Trace one candidate from approved wrapper through immutable bytes to a blocked compatibility classification</name>
  <precondition>Before this plan starts, an independent reviewer has regenerated `01-REVIEWS.md` for the exact active 43-plan set with every active filename and SHA-256, the reviewed commit, RFC 3339 review timestamp/freshness, and reviewer identity; it must not be an executor-generated record.</precondition>
  <read_first>01-REVIEWS.md; requirements-phase1-tools.txt; .planning/spikes/_shared/toolchain-environment.json; tools/phase1-python; tools/acquire-sources.py; tools/validate-research.py; .planning/research/schemas/source.schema.json; .planning/research/schemas/compatibility.schema.json; tests/phase1/test_evidence.py; 01-RESEARCH.md §Upstream Merge Refresh</read_first>
  <reversibility rating="costly">The full locked wheel inventory and wrapper preflight become the certification boundary for every Phase 1 command; changing it requires synchronized environment, lock, and regression updates.</reversibility>
  <files>requirements-phase1-tools.txt, tools/verify-phase1-toolchain.py, tools/phase1-python, tools/acquire-sources.py, tests/phase1/test_evidence.py</files>
  <behavior>
    - A clean isolated environment passes `--verify-toolchain`; missing, undeclared, version-mismatched, lock-mismatched, or RECORD-altered distributions prevent argv forwarding.
    - A fixture-backed public-read response for one `napplet/web` commit:path resolves repository identity, commit, blob bytes, SHA-256, retrieval time, and observed-implementation classification; the staged source record passes the canonical validator.
    - The tracer's compatibility classifier evaluates normative protocol, observed implementation, published package, runtime, example/fixture, current-work, and conformance dimensions; missing qualified dimensions produce `blocked` with an impact map and cannot emit an eligible baseline.
  </behavior>
  <action>Start RED in `tests/phase1/test_evidence.py` with an in-process fixture transport for GitHub repository, commit/tree/blob, and npm metadata responses; do not contact a live host in unit tests. Add `SourceEvidenceTests.test_toolchain_integrity_blocks_untrusted_forwarding` and `BoundedCollectorTests.test_candidate_acquisition_tracer_blocks_incomplete_dimensions`; the latter runs the actual wrapper, collector, and `validate-research.py validate` against a temporary research root. Replace the top-level-only requirements with the complete already-approved PyYAML, jsonschema, playwright, attrs, greenlet, jsonschema-specifications, pyee, referencing, rpds-py, and typing_extensions closure; each exact version must carry its recorded wheel SHA-256. Implement `tools/verify-phase1-toolchain.py` to parse that lock, normalize names, compare `importlib.metadata.distributions()` against declared runtime plus bootstrap allowlist, verify every installed RECORD entry with a hash, and fail when the lock/installed inventory diverges. Invoke it from `tools/phase1-python` after its interpreter checks and before forwarding argv; support a non-forwarding `--verify-toolchain` mode. Extend the collector with a bounded read-only acquisition mode that validates an allowlisted official HTTPS identity response, resolves a specified immutable commit:path blob, hashes exact bytes, retains cache only below ignored `.research/upstreams/`, and returns a machine-readable eligibility classification. Use the seven required dimensions in that classifier and return `blocked` with missing dimensions and affected requirements whenever authoritative protocol or independently pinned package/runtime/example/conformance evidence is absent. Implement this per D-01 through D-11, D-14 through D-18, and D-29 through D-31; public reads are authorized by D-10, but neither the tracer nor its test can approve a claim, ADR, or source authority.</action>
  <verify><automated>tools/phase1-python --verify-toolchain && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_evidence.SourceEvidenceTests.test_toolchain_integrity_blocks_untrusted_forwarding && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_evidence.BoundedCollectorTests.test_candidate_acquisition_tracer_blocks_incomplete_dimensions</automated></verify>
  <acceptance_criteria>The test begins red, then proves lock/RECORD refusal before forwarding, exact immutable-byte capture, canonical validation of the staged source, and a seven-dimension `blocked` classification with no approval field set to approved.</acceptance_criteria>
  <done>The required acquisition-to-validation-to-classification tracer is runnable through an integrity-checked wrapper and cannot falsely certify EVID-03.</done>
</task>

<task type="auto">
  <name>Task 2: Run the bounded public acquisition and preserve review-ready history</name>
  <read_first>tools/acquire-sources.py; .planning/research/candidate-source-manifest.yaml; .planning/research/acquisition-log.yaml; .planning/research/source-registry.yaml; upstream-refresh-synthesis-2026-07-28.md; upstream-refresh-kehto-web-2026-07-28.md; upstream-refresh-napplet-web-2026-07-28.md; upstream-refresh-napplet-naps-2026-07-28.md</read_first>
  <files>.planning/research/upstream-acquisition-queue.yaml, .planning/research/reports/upstream-acquisition-20260728.md, .planning/research/acquisition-log.yaml, .planning/research/candidate-source-manifest.yaml</files>
  <action>Create an explicit bounded queue for `kehto/web`, `napplet/web`, and `napplet/naps`, recording repository ID, default branch, selected immutable merge/default commits, exact commit:path targets, expected evidence class, affected claims/drift/questions, and refresh triggers. Execute the collector through `tools/phase1-python` against public HTTPS endpoints only. Record the five `kehto/web` merges #204/#208/#209/#210/#211, three `napplet/web` merges #184/#186/#188, and the `napplet/naps` zero-merge window as repository-history observations; retain exact observed commit/path/blob/content digests for the material source targets described in the refresh reports. Store fetched bytes only in ignored cache and write a committed receipt with response digest, retrieval date, HTTP identity fields, immutable locator, collection result, and uncertainty. Append outcomes to `acquisition-log.yaml` without removing `ACQ-FAIL-001`, and add corrected public identities as additional candidates rather than rewriting the old `nap-ecosystem` discovery pointers. Classify every merged code/test/release-PR item as observed implementation or release metadata, never as normative NIP/NAP proof, per D-04 through D-09, D-12, D-13, and the refresh boundary.</action>
  <verify><automated>tools/phase1-python tools/acquire-sources.py collect --queue .planning/research/upstream-acquisition-queue.yaml --receipt .planning/research/reports/upstream-acquisition-20260728.md --cache-root .research/upstreams && tools/phase1-python tools/validate-research.py validate --root .planning/research --report /tmp/phase1-acquisition-validation.md</automated></verify>
  <acceptance_criteria>The receipt accounts for all eight merges and the zero-merge window, records immutable bytes/digests for each selected material target, preserves prior discovery and failure history, and labels all implementation/release evidence non-normative pending review.</acceptance_criteria>
  <done>Maintainers have an executable, read-only, review-ready acquisition record for the three official public identities without a false authority transition.</done>
</task>
</tasks>

## Artifacts this phase produces
- `tools/verify-phase1-toolchain.py` — `verify_lock_and_record_integrity()` and CLI verifier.
- `tools/acquire-sources.py` — `collect` acquisition command and seven-dimension eligibility classifier.
- `.planning/research/upstream-acquisition-queue.yaml` — bounded public acquisition subjects.
- `.planning/research/reports/upstream-acquisition-20260728.md` — dated immutable retrieval receipt.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| locked toolchain → validator execution | Altered dependencies can change evidence results. |
| public API response → retained source evidence | Network metadata and bytes are untrusted until identity, locator, and digest checks complete. |
| acquisition classification → evidence baseline | A complete-looking observed record could be mistaken for normative compatibility evidence. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-29-01 | Tampering | wrapper dependency closure | high | mitigate | Task 1 lock, inventory, and RECORD checks run before forwarded commands. |
| T-29-02 | Spoofing | upstream repository identity | high | mitigate | Task 2 captures public repository identity, immutable revision/path bytes, and scoped authority class. |
| T-29-03 | Tampering | source bytes and cache paths | high | mitigate | Task 1 confines cache and hashes resolved immutable bytes. |
| T-29-04 | Repudiation | acquisition history | medium | mitigate | Task 2 appends dated receipt and keeps prior discovery/failure records. |
| T-29-05 | Elevation of Privilege | package data | high | mitigate | Collection uses metadata reads only; it does not install or execute package code. |
| T-29-06 | Tampering | false compatibility acceptance | high | mitigate | Seven-dimension classifier returns blocked without qualified inputs and review. |
</threat_model>

<verification>
Run the wrapper verifier, focused tracer tests, public read-only collection, and canonical validation through `tools/phase1-python`.
</verification>

<success_criteria>
CR-09 is repaired before certification, and actual upstream acquisition has a reproducible path to an explicit blocked or review-ready evidence classification.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-29-SUMMARY.md` when done.
</output>
