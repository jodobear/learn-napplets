---
phase: 01-research-and-truth-baseline
plan: 29
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/phase1-bootstrap.py
  - tools/phase1-python
  - tools/validate-planning.py
  - tests/phase1/test_preflight.py
autonomous: true
gap_closure: true
requirements: [EVID-01, OPER-03]
must_haves:
  truths:
    - "Before any site-enabled Phase 1 command runs, the recorded interpreter executes a stdlib-only `-I -S` bootstrap self-test and rejects malicious startup-hook fixtures."
    - "An independent content-addressed review manifest exactly binds every active Phase 1 plan filename and SHA-256 value to reviewed commit bytes and a reviewer identity distinct from the executor; it is invalid only when a bound byte changes or an explicit supersession is recorded."
    - "Before execution authorization, the same independent reviewed commit contains exact regular-file blobs for `.planning/PROJECT.md`, `01-CONTEXT.md`, and all four 2026-07-28 upstream-refresh reports; the review record contains their closed sorted path-to-SHA-256 map and current checked-out bytes match it."
    - "The bootstrap/review wrapper refuses ordinary command forwarding until Plan 01-44 has certified a fresh wheelhouse-derived environment."
  artifacts:
    - path: tools/phase1-bootstrap.py
      provides: "Stdlib-only `--self-test` bootstrap run by repository-resolved `python.isolatedInterpreter` with `-I -S` before any site-enabled Phase 1 process"
    - path: tools/phase1-python
      provides: "Bootstrap-first wrapper that permits only certified forwarding after Plan 01-44"
    - path: tools/validate-planning.py
      provides: "Content-addressed active-plan and closed six-input source-grounding review authorization, plus a Git-blob-only source snapshot loader"
    - path: tests/phase1/test_preflight.py
      provides: "Bootstrap/startup-hook, review-manifest, source-grounding, duplicate-section, and missing-executor regressions"
  key_links:
    - from: `python.isolatedInterpreter` and `python.resolvedExecutable` in the toolchain environment
      to: tools/phase1-bootstrap.py
      via: "repository-root resolution plus direct `-I -S --self-test` invocation before any site-enabled child exists"
    - from: 01-REVIEWS.md
      to: tools/validate-planning.py
      via: "one-to-one current active-plan SHA-256 map, reviewed-commit bytes, independent reviewer identity, and explicit supersession state"
    - from: the closed six-path reviewed source-input map
      to: tools/validate-planning.py and Plan 01-45
      via: "regular Git blobs and current checked-out bytes match the same reviewed commit and SHA-256 map before the Git-blob-only snapshot loader returns report bytes"
    - from: tools/phase1-python
      to: Plan 01-44 certification artifacts
      via: "ordinary forwarding remains denied until the fresh wheelhouse-install attestation and verifier are available"
  prohibitions:
    - statement: "No package installation, upstream acquisition, browser download, ADR acceptance, external write, or production scaffold is authorized by this bootstrap/review plan."
      category: safety
      status: unresolved
      flagged: true
      verification: "The wrapper refuses ordinary forwarding before Plan 01-44 certification, and the direct bootstrap/preflight paths use only standard-library code."
---

<objective>
Establish an isolated-Python bootstrap and an independently bound review authorization before any site-enabled Phase 1 gap command can run.

Purpose: Separate authorization from dependency certification and acquisition, so a small Wave 1 trust boundary proves startup isolation and exact review binding before Plan 01-44 builds a fresh verified wheelhouse environment and Plan 01-45 performs public acquisition.
Output: A bootstrap-first, content-addressed authorization gate that ordinary wrapper forwarding cannot bypass.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-RESEARCH.md
@.planning/phases/01-research-and-truth-baseline/01-REVIEW.md
@.planning/phases/01-research-and-truth-baseline/01-VERIFICATION.md
@.planning/research/reports/upstream-refresh-synthesis-2026-07-28.md
@.planning/research/reports/upstream-refresh-kehto-web-2026-07-28.md
@.planning/research/reports/upstream-refresh-napplet-web-2026-07-28.md
@.planning/research/reports/upstream-refresh-napplet-naps-2026-07-28.md
@.planning/research/toolchain-approval.yaml
@.planning/spikes/_shared/toolchain-environment.json
@tools/phase1-python
@tools/acquire-sources.py
@tests/phase1/test_evidence.py
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Prove the stdlib-only bootstrap before the content-addressed review gate</name>
  <precondition>An independent reviewer has regenerated `01-REVIEWS.md` for the exact active Phase 1 plan set and its closed six-path source-grounding input map; `GSD_EXECUTOR_ID` is nonempty and differs from that reviewer identity.</precondition>
  <read_first>.planning/PROJECT.md; .planning/spikes/_shared/toolchain-environment.json; .planning/research/toolchain-approval.yaml; 01-REVIEWS.md; tools/phase1-python; tools/validate-planning.py; tests/phase1/test_preflight.py; 01-CONTEXT.md; upstream-refresh-synthesis-2026-07-28.md; upstream-refresh-kehto-web-2026-07-28.md; upstream-refresh-napplet-web-2026-07-28.md; upstream-refresh-napplet-naps-2026-07-28.md; STATE.md</read_first>
  <files>tools/phase1-bootstrap.py, tools/phase1-python, tools/validate-planning.py, tests/phase1/test_preflight.py</files>
  <behavior>
    - `tools/phase1-bootstrap.py --self-test`, launched by the manifest-selected `python.isolatedInterpreter` with `-I -S`, confirms its resolved path/version/SHA-256 against the committed environment record and rejects malicious `.pth`, `sitecustomize`, and `usercustomize` fixtures before any site-enabled child is allowed.
    - `test_preflight_rejects_review_not_bound_to_active_plan_manifest` rejects omitted, extra, duplicate, altered, malformed, or reviewed-commit-inconsistent entries in the exact sorted active-plan SHA-256 manifest.
    - `test_preflight_content_addressed_review_requires_bound_bytes_or_explicit_supersession` accepts an unchanged bound plan set regardless of elapsed calendar time and rejects a changed digest, changed reviewed commit, or explicit supersession until a new exact review is recorded.
    - `test_preflight_requires_exact_convergence_and_high_disposition` rejects non-converged review prose, unresolved HIGH findings, a missing reviewer identity, or a reviewer identity equal to the executor.
    - `test_preflight_rejects_duplicate_authorization_or_convergence_sections` rejects zero or more than one bounded authorization/convergence section even when the manifest entries themselves are unique.
    - `test_phase1_complete_rejects_missing_executor_identity_before_success_output` proves `--phase-1-complete` fails before emitting completion success when `--executor-identity` is absent or empty.
    - `test_preflight_rejects_absent_untracked_or_byte_mismatched_required_source_input` uses a temporary Git repository and rejects any missing, extra, duplicate, non-regular, untracked, reviewed-commit-absent, HEAD-divergent, or working-tree-byte-mismatched member of the closed six-path input set.
    - `test_preflight_loads_only_reviewed_git_blob_source_snapshot` proves the source-input loader returns only `git show` bytes from the review's exact commit after every path/digest/mode check, and never substitutes plan prose, a mutable working-tree report, or a later branch revision.
  </behavior>
  <action>Addresses the content-addressed review authorization repair and establishes the bootstrap predecessor for the wheelhouse repair in Plan 01-44. Start RED with a standard-library bootstrap self-test and the named review-gate tests, then create `tools/phase1-bootstrap.py` using only Python standard-library modules. In POSIX shell, extract `python.isolatedInterpreter` from the committed environment JSON; reject an absolute, empty, or repository-escaping value, resolve it below the repository root, and invoke that path as the first Python process with `-I -S`. The bootstrap compares that invocation against `python.resolvedExecutable`, version, executable SHA-256, approved-environment digest, and an allowable startup-file inventory before any site-enabled process exists. Its `--self-test` creates malicious `.pth`, `sitecustomize`, and `usercustomize` fixtures and proves that their payload cannot execute while the bootstrap rejects an unsafe inventory. Run the standard-library preflight path directly through this recorded interpreter after that bootstrap; do not use `tools/phase1-python` to forward an ordinary site-enabled command until Plan 01-44 has certified its fresh environment. Make `tools/phase1-python` always run the direct bootstrap, then refuse ordinary forwarding while the Plan 01-44 attestation/verifier pair is absent or invalid. Implement the review gate with exact `CONVERGED; HIGH=0; actionable=0`, dedicated dispositions for listed HIGH findings, a sorted one-to-one SHA-256 map for every active `01-*-PLAN.md`, a nonblank independent reviewer identity, and a recorded Git commit containing the exact reviewed plan bytes. Per D-15, D-25, D-28, D-33, and D-35, review validity is content-addressed: reject only a bound plan-byte or reviewed-commit mismatch, or an explicit supersession; elapsed time alone never invalidates unchanged bytes. Require `--executor-identity` from nonempty `GSD_EXECUTOR_ID`, reject equality with the independent reviewer, and route completion preflight through this gate before any completion output. Addresses Codex HIGH source-grounding finding: before returning authorization, require `01-REVIEWS.md` to declare exactly this lexically sorted source-input set: `.planning/PROJECT.md`; `.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md`; `.planning/research/reports/upstream-refresh-kehto-web-2026-07-28.md`; `.planning/research/reports/upstream-refresh-napplet-web-2026-07-28.md`; `.planning/research/reports/upstream-refresh-napplet-naps-2026-07-28.md`; and `.planning/research/reports/upstream-refresh-synthesis-2026-07-28.md`. Define the review-record fields as `reviewed_source_inputs` entries containing only repository-relative path, SHA-256, and Git blob identity, bound to the same `reviewed_commit` as the active-plan map. Add a standard-library `load_reviewed_phase1_source_snapshot()` interface in `tools/validate-planning.py` that first verifies the ordinary review manifest, then uses Git object queries to require every listed path to be a regular `100644` blob in the reviewed commit and unchanged at current `HEAD`; require the working-tree file to be tracked, regular, non-symlinked, and byte-identical to the declared digest. The loader returns an immutable in-memory mapping from direct Git blob bytes only after all six checks pass. Reject an omitted, extra, duplicated, malformed, untracked, mode-mismatched, absent, changed, or different-commit entry before downstream code can consume any report or canonical decision input. Expose `--verify-phase1-source-inputs --review PATH` as a read-only command that validates the same closed map and emits its reviewed commit plus sorted path/digest results for later plans; do not permit it to repair or rewrite inputs. Plan 01-45 must consume the loader rather than reconstructing its merge window, locators, commits, or digests from PLAN prose or mutable state; Plans 01-30, 01-31, 01-35, 01-43, and 01-40 must invoke the verifier or consume its resulting digest-bound receipt/snapshot. On bootstrap or review-gate failure halt without starting Plan 01-44 or Plan 01-45; this task authorizes evidence work only and cannot accept an ADR, risk, or phase transition.</action>
  <verify><automated>ROOT="$(git rev-parse --show-toplevel)" && RECORDED_RELATIVE="$(awk -F '"' '/"isolatedInterpreter"[[:space:]]*:/ {print $4; exit}' "$ROOT/.planning/spikes/_shared/toolchain-environment.json")" && case "$RECORDED_RELATIVE" in ''|/*|*'..'*) exit 1;; esac && RECORDED_PYTHON="$ROOT/$RECORDED_RELATIVE" && test -x "$RECORDED_PYTHON" && "$RECORDED_PYTHON" -I -S "$ROOT/tools/phase1-bootstrap.py" --environment "$ROOT/.planning/spikes/_shared/toolchain-environment.json" --self-test && test -n "$GSD_EXECUTOR_ID" && GSD_EXECUTOR_ID="$GSD_EXECUTOR_ID" "$RECORDED_PYTHON" -I -S "$ROOT/tools/validate-planning.py" --phase-1-execution-preflight --executor-identity "$GSD_EXECUTOR_ID" && "$RECORDED_PYTHON" -I -S "$ROOT/tests/phase1/test_preflight.py" Phase1ExecutionPreflightTests.test_preflight_rejects_duplicate_authorization_or_convergence_sections && "$RECORDED_PYTHON" -I -S "$ROOT/tests/phase1/test_preflight.py" Phase1ExecutionPreflightTests.test_preflight_content_addressed_review_requires_bound_bytes_or_explicit_supersession && "$RECORDED_PYTHON" -I -S "$ROOT/tests/phase1/test_preflight.py" Phase1ExecutionPreflightTests.test_preflight_rejects_review_not_bound_to_active_plan_manifest && "$RECORDED_PYTHON" -I -S "$ROOT/tests/phase1/test_preflight.py" Phase1ExecutionPreflightTests.test_preflight_requires_exact_convergence_and_high_disposition && "$RECORDED_PYTHON" -I -S "$ROOT/tests/phase1/test_preflight.py" Phase1ExecutionPreflightTests.test_phase1_complete_rejects_missing_executor_identity_before_success_output && "$RECORDED_PYTHON" -I -S "$ROOT/tests/phase1/test_preflight.py" Phase1ExecutionPreflightTests.test_preflight_rejects_absent_untracked_or_byte_mismatched_required_source_input && "$RECORDED_PYTHON" -I -S "$ROOT/tests/phase1/test_preflight.py" Phase1ExecutionPreflightTests.test_preflight_loads_only_reviewed_git_blob_source_snapshot && "$RECORDED_PYTHON" -I -S "$ROOT/tools/validate-planning.py" --verify-phase1-source-inputs --review "$ROOT/.planning/phases/01-research-and-truth-baseline/01-REVIEWS.md"</automated></verify>
  <acceptance_criteria>The repository-resolved `python.isolatedInterpreter` runs the stdlib-only `-I -S` bootstrap self-test before any site-enabled process and proves malicious startup fixtures do not execute. Preflight tests run after that boundary without an isolated `PYTHONPATH` dependency. No later gap task starts unless the exact active-plan bytes and reviewed commit bind to an independent reviewer; unchanged bytes do not expire merely with elapsed time, while changed or explicitly superseded bindings fail. The `--verify-phase1-source-inputs` command accepts only the six required regular Git blobs and matching tracked worktree bytes from that same reviewed commit, returns their sorted digest map, and fails closed on every absence or mismatch. A missing executor identity cannot reach completion output, and the wrapper cannot forward ordinary commands before Plan 01-44 certification.</acceptance_criteria>
  <done>The Wave-1 authorization boundary proves isolated Python startup and an independently operated content-addressed review gate before the separate fresh-wheelhouse certification plan begins.</done>
</task>

</tasks>

## Scope-sanity rationale

Plan 01-29 owns only the pre-site authorization seam: the manifest-selected stdlib bootstrap, active-plan review binding, executor/reviewer separation, and the wrapper's refusal to forward an uncertified environment. It is one four-path trust boundary with one end-to-end bootstrap-to-review-gate tracer. Plan 01-44 owns wheelhouse certification and Plan 01-45 owns source ingress plus retained queue/receipt history, so neither dependency provenance nor acquisition storage remains hidden inside this authorization plan.

## Artifacts this phase produces
- `tools/phase1-bootstrap.py` — stdlib-only `-I -S` startup self-test and recorded-interpreter identity check.
- `tools/phase1-python` — bootstrap-first wrapper that fails closed before Plan 01-44 certification.
- `tools/validate-planning.py` — active-plan exact review-manifest, closed six-input source-grounding verifier/Git-blob snapshot loader, reviewed-commit, and executor-identity gate.
- `tests/phase1/test_preflight.py` — bootstrap, active-plan binding, source-grounding, convergence, and executor-identity regressions.

## Created-symbol inventory
- `phase1-bootstrap.py --self-test` — direct recorded-interpreter check that rejects startup-hook execution before a site-enabled process.
- `--phase-1-execution-preflight --executor-identity` — content-addressed review gate over the active plan manifest and independent reviewer identity.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| recorded interpreter → bootstrap process | Startup hooks or a substituted executable could execute before the Phase 1 gate. |
| review record → authorization | A stale, self-reviewed, vague, or byte-mismatched review could otherwise authorize execution. |
| bootstrap result → wrapper forwarding | A wrapper could start site-enabled code before the separate fresh-environment certification exists. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-29-01 | Spoofing | review-manifest authorization | high | mitigate | Task 1 verifies the exact active-plan digest map, reviewed-commit bytes, explicit supersession state, and independent executor/reviewer identities; elapsed time never authorizes or invalidates unchanged bytes. |
| T-29-02 | Tampering | Python startup | high | mitigate | Task 1 resolves only a repository-contained recorded interpreter and executes the stdlib-only `-I -S --self-test` before any site-enabled process. |
| T-29-03 | Elevation of Privilege | wrapper forwarding | high | mitigate | Task 1 makes the wrapper refuse ordinary forwarding until Plan 01-44's fresh wheelhouse attestation and verifier succeed. |
| T-29-04 | Tampering | source-grounding inputs | high | mitigate | Task 1 requires the closed six-path review map, exact regular Git blobs from the same reviewed commit, matching tracked worktree bytes, and a Git-blob-only snapshot loader before downstream source use. |
</threat_model>

<verification>
Run the direct recorded-interpreter `-I -S` bootstrap self-test, then execute the content-addressed active-plan review preflight and named preflight regressions without ordinary wrapper forwarding.
</verification>

<success_criteria>
A malicious Python startup path, a nonconverged review, a changed plan byte, reviewer/executor identity reuse, or a missing executor identity blocks Phase 1 work before Plan 01-44 dependency certification or Plan 01-45 source collection begins.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-29-SUMMARY.md` when done.
</output>
