---
phase: 01-research-and-truth-baseline
plan: 39
type: execute
wave: 9
depends_on: [01-38]
files_modified:
  - tests/phase1/test_security_review.py
  - tools/validate-phase1-security.py
  - .planning/research/reports/asvs-reference-20260728.md
  - .planning/research/reports/asvs-5.0.0-l1-applicability.yaml
  - .planning/phases/01-research-and-truth-baseline/01-SECURITY-EVIDENCE.md
  - .planning/phases/01-research-and-truth-baseline/01-SECURITY.md
autonomous: false
gap_closure: true
requirements: [EVID-01, EVID-02, EVID-03, EVID-04, OPER-01, OPER-03]
must_haves:
  truths:
    - "The dedicated ASVS L1 review contains exactly one high-severity primary mitigation row for each required CR-01 through CR-09 and WR-01 through WR-03 finding, plus zero or more stable `SEC-*` findings discovered by the review."
    - "Every applicable ASVS control has a verification status, command or evidence reference, observed result, and nonempty linked evidence or finding references; every finding has threat, severity, disposition, command, result, source, and applicable-control links."
    - "A passed security status requires a human-authored, commit- and evidence-digest-bound security review, all required CR/WR findings mitigated, no unresolved HIGH CR/WR or SEC finding, and separate dated role-bound auditor and rechecker records. One human principal may hold multiple roles but neither human sign-off may reuse a generic approval or name the automation executor."
    - "Existing upstream, package, and Firefox evidence blockers remain visible as research blockers and are not restated as accepted residual risk."
  artifacts:
    - path: tests/phase1/test_security_review.py
      provides: "ASVS policy/reference, extensible finding-register, role-record, and disposition regression tests"
    - path: tools/validate-phase1-security.py
      provides: "Pre-checkpoint TDD-owned validator for ASVS controls, CR/WR-plus-SEC findings, role records, and human-authored 01-SECURITY.md"
    - path: .planning/research/reports/asvs-reference-20260728.md
      provides: "Immutable official OWASP ASVS 5.0.0 catalog retrieval, licensing, and digest evidence"
    - path: .planning/research/reports/asvs-5.0.0-l1-applicability.yaml
      provides: "Complete pinned ASVS 5.0.0 L1 applicability matrix with per-control results and evidence links"
    - path: .planning/phases/01-research-and-truth-baseline/01-SECURITY-EVIDENCE.md
      provides: "Automation-prepared immutable command, plan-review, and evidence-digest dossier"
    - path: .planning/phases/01-research-and-truth-baseline/01-SECURITY.md
      provides: "Human-authored and human-rechecked Phase 1 ASVS L1 remediation security review"
  key_links:
    - from: ASVS applicability controls and security finding register
      to: CR/WR and SEC regression tests and repaired sources
      via: "per-control status/evidence/result links and finding-specific command/result/source evidence"
    - from: role-bound security determinations
      to: reviewed commit, plan/evidence digests, and security status
      via: "separate dated digest-bound records that permit a shared human principal but prohibit generic reuse"
    - from: security status
      to: final verifier
      via: "zero unresolved high findings across CR/WR and SEC is a prerequisite, not transition approval"
  prohibitions:
    - statement: "A passed security review does not waive unresolved protocol/package/browser research blockers, accept residual risk, accept an ADR, or authorize a later phase."
      category: authority
      status: unresolved
      flagged: true
      verification: "Register validates remediation findings only; verifier retains research blocker impact map."
---

<objective>
Produce the dedicated Phase 1 ASVS L1 review with a test-enforced complete mitigation register.

Purpose: Establish that every trust-critical CR/WR remediation is independently reviewed and no unresolved HIGH security finding is hidden before fresh verification.
Output: `01-SECURITY.md` and parser-backed register validation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-research-and-truth-baseline/{01-REVIEW.md,01-VERIFICATION.md,01-VALIDATION.md}
@tools/{phase1-python,verify-phase1-toolchain.py,validate-planning.py,validate-research.py,refresh-sources.py}
@tests/phase1/{test_preflight.py,test_evidence.py,test_drift.py,test_compatibility.py,test_spikes.py,test_spike_consolidation.py,test_gap_closeout.py}
@.planning/research/{source-registry.yaml,compatibility-matrix.yaml,drift-register.yaml,open-questions.yaml}
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: TDD the ASVS validator and an extensible L1 finding register before review</name>
  <read_first>.planning/config.json; 01-RESEARCH.md §Applicable ASVS Categories; .planning/research/source-registry.yaml; tools/acquire-sources.py; 01-REVIEWS.md; 01-MANUAL-REVIEW.md; 01-REVIEW.md; 01-VALIDATION.md; tests/phase1/test_gap_closeout.py; tests/phase1/test_spikes.py; tests/phase1/test_spike_consolidation.py; tools/validate-planning.py; tools/validate-research.py</read_first>
  <files>tests/phase1/test_security_review.py, tools/validate-phase1-security.py, .planning/research/reports/asvs-reference-20260728.md, .planning/research/reports/asvs-5.0.0-l1-applicability.yaml</files>
  <behavior>
    - `test_asvs_scope_uses_config_and_pinned_official_reference` starts RED and requires `.planning/config.json` to select `security_asvs_level: 1` and `security_block_on: high`; it separately requires an immutable official standards reference with repository `OWASP/ASVS`, tag `v5.0.0_release`, commit `5cf9b032440be53ce345ab3c130fda46ba1ce7a2`, exact path `5.0/en/0x03-What-is-the-ASVS.md`, exact-byte SHA-256, retrieval date, authority `standards-reference`, evidence class, maturity, uncertainty, and refresh trigger.
    - `test_asvs_l1_applicability_matrix_is_complete_and_finding_bound` requires every pinned ASVS 5.0.0 L1 requirement exactly once and requires each applicable row to have a verification status, command or evidence reference, observed result, and nonempty linked evidence or finding references; every CR-01..CR-09 and WR-01..WR-03 finding maps to at least one applicable versioned ASVS ID.
    - `test_asvs_l1_register_requires_required_cr_wr_and_allows_sec_findings` requires the required CR-01..CR-09 and WR-01..WR-03 subset exactly once, permits zero or more unique stable `SEC-*` findings, and rejects duplicate IDs, malformed SEC IDs, blank threat/severity/disposition/command/result/source/control cells, or an optional SEC row with no applicable-control link.
    - `test_passed_security_review_rejects_unmitigated_high_dispositions` requires `asvs_level: L1`, `blocking_threshold: high`, `status: passed`, `open_high_count: 0`, all required CR/WR rows mitigated, and no unresolved high-severity CR/WR or SEC row; it also requires separate dated, role-bound, commit-and-digest-bound auditor and rechecker records while allowing one non-executor human principal to supply both roles.
    - `test_security_validator_rejects_generic_or_executor_role_reuse` directly runs `tools/validate-phase1-security.py` against temporary reviews and rejects one generic approval reused across roles, missing role/timestamp/commit/plan/evidence digest/rationale, or a human sign-off whose principal equals `GSD_EXECUTOR_ID`.
  </behavior>
  <action>Addresses review concern and human decisions 2, 4, and 5: begin with the five named RED tests, then create and test `tools/validate-phase1-security.py` in this pre-checkpoint task; no checkpoint may be the implementation owner of this validator. Use `.planning/config.json` only as project policy selecting ASVS level 1 and the high blocking threshold; do not treat configuration or `01-RESEARCH.md` as standards authority. Through the bounded read-only acquisition path, resolve official repository `OWASP/ASVS`, stable tag `v5.0.0_release`, immutable commit `5cf9b032440be53ce345ab3c130fda46ba1ce7a2`, the complete `5.0/en/` requirement catalog, and its license; retain exact bytes only in ignored cache, hash every catalog member, and write `.planning/research/reports/asvs-reference-20260728.md` with immutable locators, per-file digests, retrieval date, license, authority/evidence/maturity classes, uncertainty, affected review scope, and refresh trigger. Create `.planning/research/reports/asvs-5.0.0-l1-applicability.yaml` containing every ASVS 5.0.0 Level-1 requirement exactly once with its versioned ASVS ID, source path/digest, applicability (`applicable`, `not-applicable`, or `blocked`), rationale, verification status, command/evidence reference, observed result, and linked evidence/finding references. Require each applicable CR/WR mapping to name a versioned ASVS ID. The validator must require the exact CR-01..CR-09 and WR-01..WR-03 subset but allow extra stable `SEC-*` findings when the security review discovers them; every finding carries threat class, severity, primary coverage, disposition, command, result, source path, and applicable ASVS IDs. A passed review requires every required CR/WR mitigation and zero unresolved HIGH findings across both required and SEC rows. If identity, commit, path, or digest cannot be verified, record the acquisition as blocked and prohibit a passed security status. Require security YAML frontmatter fields `review_date`, `scope`, `asvs_level`, `blocking_threshold`, `reviewed_commits`, `reviewed_plan_manifest_sha256`, `reviewed_evidence_manifest_sha256`, `status`, and integer `open_high_count`. Require separate `securityAuditor` and `securityRechecker` records, each with one dated role, human principal, reviewed Git commit, active-plan digest, evidence digest, ASVS-reference digest, determination, and rationale; allow a single human principal across roles but reject generic/copy-reused approval records and any human sign-off by `GSD_EXECUTOR_ID`. Recompute command/evidence manifests and active-plan review digest, require the reviewed commit to contain those plan bytes, and validate all fields before a passed review is relied upon. These tests and validator enforce scope/reference/report integrity only; they do not make the standard a protocol authority or generate the human review.</action>
  <verify><automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_security_review.SecurityReviewTests.test_asvs_scope_uses_config_and_pinned_official_reference && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_security_review.SecurityReviewTests.test_asvs_l1_applicability_matrix_is_complete_and_finding_bound && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_security_review.SecurityReviewTests.test_asvs_l1_register_requires_required_cr_wr_and_allows_sec_findings && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_security_review.SecurityReviewTests.test_passed_security_review_rejects_unmitigated_high_dispositions && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_security_review.SecurityReviewTests.test_security_validator_rejects_generic_or_executor_role_reuse && tools/phase1-python tools/validate-phase1-security.py --help</automated></verify>
  <acceptance_criteria>The pre-checkpoint validator and every named TDD regression pass. Missing official-reference evidence, a malformed applicable-control row, an absent required CR/WR row, a malformed/duplicated SEC finding, unresolved HIGH finding, generic role reuse, executor sign-off, or unbound commit/digest condition blocks a passed review.</acceptance_criteria>
  <done>The security review has a TDD-owned ASVS validator and authority-bounded, extensible finding/role structure before a human checkpoint can use it.</done>
</task>

<task type="auto">
  <name>Task 2: Prepare a bound ASVS L1 remediation-evidence dossier without a security verdict</name>
  <read_first>tests/phase1/test_security_review.py; .planning/config.json; .planning/research/reports/asvs-reference-20260728.md; 01-REVIEWS.md; 01-REVIEW.md; 01-VALIDATION.md; Plan 01-37 full-gate evidence; Plan 01-38 probe matrix; tools/validate-planning.py; tools/validate-research.py; tools/verify-phase1-toolchain.py</read_first>
  <files>.planning/phases/01-research-and-truth-baseline/01-SECURITY-EVIDENCE.md</files>
  <action>Addresses review concern: automation prepares evidence but cannot self-certify security. Require nonempty `GSD_EXECUTOR_ID` and run the repaired gate/test evidence, including `GSD_EXECUTOR_ID="$GSD_EXECUTOR_ID" tools/phase1-python tools/validate-planning.py --phase-1-complete --executor-identity "$GSD_EXECUTOR_ID"`, and produce `01-SECURITY-EVIDENCE.md`, not a passed security review. Bind the dossier to the exact `01-REVIEWS.md` active-plan digest, reviewed commit, review timestamp, independent plan-review identity, executor identity, the ASVS-reference digest, and a deterministic command/evidence manifest digest. Include the twelve required CR/WR mappings exactly once: CR-01 citation adjacency; CR-02 convergence/HIGH parsing; CR-03 wrapper children; CR-04 retained output; CR-05 confined fragment; CR-06 fixed replay mapping; CR-07 recovery interruption; CR-08 parallel drift sides; CR-09 toolchain integrity; WR-01 retained output fixture; WR-02 temporary report isolation; WR-03 ambiguous refresh reduction. For each, record the wrapper command, exit result, repaired source path, threat class, and evidence locator. Record source/authenticity and package blockers as unaccepted research constraints outside the dossier mapping; do not call them waivers or residual risks, and do not claim Firefox was solved. Do not create, replace, or label `01-SECURITY.md` as passed in this task.</action>
  <verify><automated>test -n "$GSD_EXECUTOR_ID" && tools/phase1-python -m unittest discover -s tests/phase1 -p 'test_security_review.py' && tools/phase1-python -m unittest discover -s tests/phase1 && GSD_EXECUTOR_ID="$GSD_EXECUTOR_ID" tools/phase1-python tools/validate-planning.py --phase-1-complete --executor-identity "$GSD_EXECUTOR_ID" && test -s .planning/phases/01-research-and-truth-baseline/01-SECURITY-EVIDENCE.md</automated></verify>
  <acceptance_criteria>The dossier has one bound, populated evidence entry for each of the twelve CR/WR findings and has no security verdict, approval, waiver, or live-state transition.</acceptance_criteria>
  <done>An independent security auditor receives complete, reproducible, identity-bound remediation evidence without an automation-created security pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human security audit and recheck the bound dossier before any passed security record</name>
  <read_first>01-SECURITY-EVIDENCE.md; 01-REVIEWS.md; tests/phase1/test_security_review.py; .planning/research/reports/asvs-reference-20260728.md; 01-VALIDATION.md; Plan 01-37 command evidence</read_first>
  <files>.planning/phases/01-research-and-truth-baseline/01-SECURITY.md</files>
  <action>Addresses review concern and human decisions 2, 4, and 5: checkpoint approval cannot bypass the pre-checkpoint validator or validation of the human-authored security artifact. A human security auditor whose principal is not `GSD_EXECUTOR_ID` independently inspects the dossier, reruns selected cited wrapper commands, and authors `01-SECURITY.md`. Immediately after authoring it, run `tools/phase1-python tools/validate-phase1-security.py --security .planning/phases/01-research-and-truth-baseline/01-SECURITY.md --evidence .planning/phases/01-research-and-truth-baseline/01-SECURITY-EVIDENCE.md --asvs-matrix .planning/research/reports/asvs-5.0.0-l1-applicability.yaml --reviewed-commit HEAD`; it recomputes reviewed-commit and artifact digests, validates the complete versioned ASVS matrix, the exact required CR/WR subset, any stable SEC rows, and separate role-bound records before any passed security record is relied upon. Bind each role determination to the reviewed commit, active-plan manifest digest, evidence-manifest digest, ASVS-reference digest, timestamp, determination, and rationale. One human principal may supply both `securityAuditor` and `securityRechecker` records, but each must be separately dated and role-specific; do not reuse a generic record, and do not permit the automation executor as a human sign-off. The auditor may set `status: passed` and `open_high_count: 0` only after confirming every required CR-01..CR-09 and WR-01..WR-03 row has command/result/source equality to the dossier, every applicable ASVS control has status/evidence/result/links, every discovered SEC finding is included, and no HIGH CR/WR or SEC finding remains unresolved. Otherwise record a non-passed security result with concrete finding IDs; do not waive, accept risk, or update STATE.md.</action>
  <what-built>Automation has prepared immutable ASVS scope, applicable-control evidence, and the required twelve-entry remediation dossier; it has not created a security decision and cannot suppress an auditor-discovered `SEC-*` finding.</what-built>
  <how-to-verify>1. Confirm the all-43-plan review, reviewed commit, and every recorded digest match the current bound artifacts. 2. Independently rerun enough cited wrapper commands to validate the CR/WR mapping and applicable-control evidence. 3. Record separate dated, role-bound auditor and rechecker determinations with commit and digest bindings; the same human principal is permitted, but neither record may be generic or the executor's. 4. Add each newly discovered stable `SEC-*` finding with its control/evidence/result links. 5. Sign a passing security review only when every required register row and all HIGH-finding conditions are satisfied; otherwise record finding IDs and retain a non-passed result.</how-to-verify>
  <verify><automated>test -n "$GSD_EXECUTOR_ID" && tools/phase1-python -m unittest discover -s tests/phase1 -p 'test_security_review.py' && GSD_EXECUTOR_ID="$GSD_EXECUTOR_ID" tools/phase1-python tools/validate-phase1-security.py --security .planning/phases/01-research-and-truth-baseline/01-SECURITY.md --evidence .planning/phases/01-research-and-truth-baseline/01-SECURITY-EVIDENCE.md --asvs-matrix .planning/research/reports/asvs-5.0.0-l1-applicability.yaml --reviewed-commit HEAD</automated></verify>
  <acceptance_criteria>`01-SECURITY.md` can be passed only with separate dated role-bound non-executor records, current commit/digest binding, exact required CR/WR evidence equality, complete applicable-control evidence, stable optional SEC findings, and zero unresolved HIGH findings. A missing or mismatched condition leaves it non-passed.</acceptance_criteria>
  <done>Security status is human-reviewed and human-rechecked under the validator-owned policy; automation has not certified its own work.</done>
  <resume-signal>Type `security review accepted` after the independent auditor has authored the bound review, or describe the unresolved finding IDs.</resume-signal>
</task>
</tasks>

## Artifacts this phase produces
- `tests/phase1/test_security_review.py` — pinned ASVS scope/reference test plus exact-register and passed-disposition tests.
- `.planning/research/reports/asvs-reference-20260728.md` — official OWASP ASVS 5.0.0 immutable source locator, digest, retrieval, and authority boundary.
- `01-SECURITY-EVIDENCE.md` — automation-prepared bound remediation-evidence dossier with no verdict.
- `01-SECURITY.md` — independently authored and human-rechecked ASVS L1 trust-boundary and exact CR/WR mitigation register.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| remediation evidence → security finding disposition | A green test can be mismapped, incomplete, or conceal a high finding. |
| security report → final verifier | A report can falsely claim passing status while waiving high risk. |
| upstream/package evidence → remediation scope | Research uncertainty can be confused with a mitigated security conclusion. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-39-01 | Spoofing | finding register completeness | high | mitigate | Task 1 enforces the exact required CR/WR subset, stable optional SEC findings, and populated control/finding evidence cells. |
| T-39-02 | Repudiation | high disposition | high | mitigate | Task 1 rejects passed-review states with any unresolved HIGH CR/WR or SEC finding. |
| T-39-03 | Tampering | remediation command evidence | high | mitigate | Task 2 binds every finding to wrapper command, result, and source path. |
| T-39-04 | Elevation of Privilege | manifest and package execution | high | mitigate | Task 2 records fixed replay and package-gate controls. |
| T-39-05 | Denial of Service | crash publication/recovery | high | mitigate | Task 2 records hard-exit/recovery and contention mitigation evidence. |
| T-39-06 | Tampering | false evidence acceptance | high | mitigate | Task 2 separates unresolved research blockers from mitigation disposition. |
| T-39-07 | Repudiation | human security sign-off | high | mitigate | Task 1 validates separate dated role/digest records, permits a shared non-executor human principal, and rejects generic reuse. |
</threat_model>

<verification>
Run security-register tests, the full suite, and completion gate through the approved wrapper before treating the ASVS review as passed.
</verification>

<success_criteria>
The ASVS L1 artifact provides exact, evidence-backed remediation coverage for every CR/WR finding and exposes no HIGH finding as unresolved or waived.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-39-SUMMARY.md` when done.
</output>
