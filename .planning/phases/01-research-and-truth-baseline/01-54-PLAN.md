---
phase: 01-research-and-truth-baseline
plan: 54
type: execute
wave: 3
depends_on: [01-50, 01-56]
files_modified:
  - tools/validate-research.py
  - tools/canonical-recovery.py
  - tests/phase1/test_consolidation_recovery.py
  - .planning/.observed-refresh-staging/phase-01/claims.yaml
  - .planning/.observed-refresh-staging/phase-01/drift-register.yaml
  - .planning/.observed-refresh-staging/phase-01/open-questions.yaml
  - .planning/.observed-refresh-staging/phase-01/package-map.md
  - .planning/.observed-refresh-staging/phase-01/open-work-snapshot.json
  - .planning/.observed-refresh-staging/phase-01/observed-refresh-validation-attestation.json
autonomous: true
gap_closure: true
requirements: [EVID-01, EVID-02, OPER-01]
must_haves:
  truths:
    - "The retained observed-refresh attestation names SHA-256 values equal to all five current live canonical research artifacts, including the four artifacts changed by the later truth refresh."
    - "Every re-attested live target is a regular checkout file byte-equal to one exact mode 100644 Git blob read by object ID from a single recorded commit; dirty, symlinked, executable-mode, absent, duplicate-tree-entry, or mixed-generation input is refused per D-04 and D-13."
    - "The original four-report reviewed-source binding is revalidated from exact Git blobs at its recorded sourceSnapshotCommit, while PRE118 remains a separate non-passing terminal-review condition."
    - "One writer-exclusive canonical transaction spans recovery, the registered five-file live read, Git/worktree revalidation, attestation construction, and six-file staging-envelope publication; a concurrent observed refresh cannot publish between snapshot selection and attestation publication."
    - "All six staging-envelope files are published as one recoverable generation; injected failure leaves the complete old envelope or the complete new envelope, never a target-map mismatch."
    - "Staging-envelope replacements remain provisional with their complete old envelope recoverable until a final raw-filesystem live-source postcondition passes; mutation outside the advisory lock rolls staging back byte-for-byte, removes the journal only after rollback verification, and refuses re-attestation."
  artifacts:
    - path: tools/validate-research.py
      provides: "A narrow live-generation re-attestation route that binds canonical Git blobs, retained acquisition provenance, and one recoverable staging envelope."
      contains: "re_attest_observed_refresh_live"
    - path: tools/canonical-recovery.py
      provides: "A non-reentrant writer transaction API with provisional publication, source postcondition, and exact old-envelope rollback under one retained exclusive lock."
      contains: "publish_from_canonical_snapshot"
    - path: tests/phase1/test_consolidation_recovery.py
      provides: "Success, provenance-refusal, dirty/nonregular-input, and interruption-recovery regressions for re-attestation."
      contains: "test_re_attestation_binds_exact_git_blob_live_generation_and_preserves_provenance"
    - path: .planning/.observed-refresh-staging/phase-01/observed-refresh-validation-attestation.json
      provides: "Current five-target digests plus immutable live-generation and retained reviewed-source provenance."
      contains: "canonicalGitBinding"
  key_links:
    - from: "tools/validate-research.py::re_attest_observed_refresh_live"
      to: "tools/canonical-recovery.py::publish_from_canonical_snapshot"
      via: "One retained writer lock covers recovery, the registered five-file snapshot, revalidation, provisional staging publication, final source postcondition, and commit-or-exact-rollback without calling a second lock-taking API."
      pattern: "observed-refresh-validation"
    - from: "canonical Git blob map"
      to: ".planning/.observed-refresh-staging/phase-01/observed-refresh-validation-attestation.json"
      via: "Target SHA-256, blob object IDs, source commit, and target-only generation digest are serialized before recoverable staging-envelope publication."
      pattern: "canonicalGitBinding"
---

<objective>
Repair the stale observed-refresh validation attestation against the current canonical research generation without rewriting canonical evidence.

Purpose: Close the live attestation mismatch that currently stops `test_live_observed_refresh_generation_matches_attestation`, while preserving exact source provenance, immutable history, transaction recovery, and the distinct PRE118 refusal.
Output: A tested re-attestation route and one current six-file staging envelope whose target map equals the five live canonical artifacts.
</objective>

<execution_context>
@/home/at/.codex/gsd-core/workflows/execute-plan.md
@/home/at/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-43-PLAN.md
@.planning/phases/01-research-and-truth-baseline/01-47-SUMMARY.md
@.planning/phases/01-research-and-truth-baseline/01-50-SUMMARY.md
@.planning/phases/01-research-and-truth-baseline/01-56-SUMMARY.md
@tools/validate-research.py
@tools/canonical-recovery.py
@tools/acquire-sources.py
@tests/phase1/test_consolidation_recovery.py
@.planning/.observed-refresh-staging/phase-01/observed-refresh-validation-attestation.json
</context>

<scope_boundary>
Repair only the retained observed-refresh staging envelope and its validation/re-attestation tooling. Do not change the five live `.planning/research/` targets, source records, claims, drift meaning, open-question meaning, package status, review binding, PRE118 machinery, ADR state, STATE.md, ROADMAP.md, REQUIREMENTS.md, or any external system. Re-attestation certifies existing bytes; it does not accept evidence or authorize a phase transition.
</scope_boundary>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Bind one live canonical generation to exact Git blobs and retained source provenance</name>
  <read_first>tools/validate-research.py `OBSERVED_REFRESH_TARGETS`, `validate_observed_refresh_staged`, `_validate_observed_refresh_overlay`, and canonical-recovery loader; tools/acquire-sources.py `validate_reviewed_refresh_binding`; tools/canonical-recovery.py `_lock`, `_recover_locked`, `read_canonical_snapshot`, `generation_sha256`, `publish_generation`, and its lock-owned publication body; tests/phase1/test_consolidation_recovery.py ObservedRefreshPublicationTests; current attestation targetMap/reviewedSourceInputBinding</read_first>
  <files>tools/validate-research.py, tools/canonical-recovery.py, tests/phase1/test_consolidation_recovery.py</files>
  <behavior>
    - The exact registered five-file canonical snapshot equals five regular worktree files and five bytes read by accepted Git blob object IDs from one HEAD commit.
    - Each target resolves through exactly one NUL-delimited `git ls-tree -z --full-tree` record with mode `100644`, type `blob`, and its byte-exact fixed path; content is read only with `git cat-file blob`.
    - The old attestation's reviewedSourceInputBinding has exactly four report paths, matching report/source digest maps, a single sourceSnapshotCommit, complete candidate rows, and a parsed-record digest reproduced from exact report blobs at that commit.
    - PRE118 is neither called nor treated as passing evidence by this repair route; the route records that terminal review binding remains outside its authority.
    - Dirty checkout bytes, a target mode other than `100644`, symlink/nonregular input, absent/duplicate/malformed tree record, object/digest mismatch, mixed snapshot, altered reviewed-source binding, or Git error refuses without changing live or staging bytes.
    - A deterministic pause after live revalidation starts a concurrent valid observed-refresh publisher; that publisher cannot replace any live target until the re-attestation transaction has published and verified its six-file envelope, and the envelope binds exactly the transaction's five-file snapshot.
    - The transaction helper never reacquires `.canonical-recovery.lock`; lock ownership is passed only to private lock-required helpers, so the concurrency test also fails on reentrant deadlock or timeout.
    - A pause after all six staging replacements but before publication commit permits a raw non-locking replacement of one live source target; the final source postcondition detects it, restores all six old staging bytes from the retained journal/backups, verifies their exact digest map, removes the journal only afterward, and fails.
  </behavior>
  <action>Addresses external Codex review concerns: a released shared snapshot enabled stale publication, and the retained advisory writer lock cannot exclude raw filesystem/Git-checkout mutation; therefore committing/removing the staging journal before the final source rehash can leave a stale six-file envelope even when the command later fails. Start with copied-repository failing tests. Refactor `tools/canonical-recovery.py` so the publication body is a private lock-required helper and keep public `publish_generation` as its single-lock wrapper. Extend the lock-owned publication protocol with an explicit provisional phase: retain journal, complete old-envelope bytes/absence metadata, backups, and expected old digests after all target replacements and new-envelope validation. Accept a lock-local `source_postcondition` callback and invoke it only after all six staging targets are provisionally installed but before a committed marker, backup removal, or journal removal. If it raises, restore every old target or old absence from retained backups, fsync, rehash the complete old staging map, remove backups/journal only after exact rollback verification, and re-raise; startup recovery encountering a prepared/replacing/provisional journal must likewise restore the old envelope unless a durable committed marker proves the postcondition passed. Add `publish_from_canonical_snapshot(reader_id, target_paths, derive_generation, source_postcondition, *, root, timeout)` that validates the registered target set, acquires `.canonical-recovery.lock` exclusively once, recovers, reads the fixed regular source snapshot, derives/validates bytes, provisionally publishes without re-locking, and runs the final source postcondition as the last precommit gate. Callback code must not invoke any lock-taking API; deterministic reentry test must refuse without hanging. In `re-attest-observed-refresh-live`, resolve one HEAD commit, accept exactly one NUL-delimited `100644 blob` tree record per live target, compare object bytes, regular non-symlink worktree bytes, and transaction snapshot, and revalidate the retained four-report binding without PRE118 authority. Build/validate the five staged copies plus attestation with canonicalGitBinding, reviewedSourceInputBinding, SPK-G state, superseded digest, and five-target generationSha256. The final source postcondition must reopen all five live paths without following links, reread exact bytes, recheck file identities/modes and Git-object/worktree equality, and require equality with the initial transaction snapshot. Add two test-only pauses: one before provisional publication for participating-writer exclusion, and one after all staging replacements but before source postcondition. At the latter, raw-replace a live target without taking the canonical lock; require command refusal and exact restoration of every pre-task staging-envelope byte plus journal cleanup only after verified rollback. Retain interruption tests for every publication/rollback step. Apply D-04, D-13, D-20, and D-36; never modify live five-file generation in the production route.</action>
  <verify>
    <automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_consolidation_recovery.ObservedRefreshPublicationTests.test_re_attestation_binds_exact_git_blob_live_generation_and_preserves_provenance test_consolidation_recovery.ObservedRefreshPublicationTests.test_re_attestation_holds_one_writer_transaction_against_concurrent_refresh test_consolidation_recovery.ObservedRefreshPublicationTests.test_re_attestation_raw_live_mutation_rolls_back_provisional_staging_envelope test_consolidation_recovery.ObservedRefreshPublicationTests.test_re_attestation_rejects_reentrant_locking_without_hang test_consolidation_recovery.ObservedRefreshPublicationTests.test_re_attestation_rejects_dirty_nonregular_or_unbound_generation_without_staging_mutation test_consolidation_recovery.ObservedRefreshPublicationTests.test_re_attestation_generation_recovers_after_each_replacement_and_rollback -v</automated>
  </verify>
  <acceptance_criteria>The success fixture records one exact commit and five accepted `100644 blob` object IDs equal to the writer-transaction snapshot; participating refresh waits through verification; raw live replacement after provisional staging fails the source postcondition and restores the complete exact old six-file envelope before journal removal; lock reentry refuses without hanging; pre-publication provenance/filesystem defects make no staging change; every injected publication/rollback interruption recovers a complete old or committed-new generation.</acceptance_criteria>
  <done>One executable route can safely re-attest the current live observed-refresh generation without waiving PRE118 or rewriting canonical evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Publish and verify the current retained staging attestation</name>
  <read_first>Task 1 command and tests; current live five-target SHA-256 values; current six-file staging envelope; tests/phase1/test_consolidation_recovery.py `test_live_observed_refresh_generation_matches_attestation`</read_first>
  <files>.planning/.observed-refresh-staging/phase-01/claims.yaml, .planning/.observed-refresh-staging/phase-01/drift-register.yaml, .planning/.observed-refresh-staging/phase-01/open-questions.yaml, .planning/.observed-refresh-staging/phase-01/package-map.md, .planning/.observed-refresh-staging/phase-01/open-work-snapshot.json, .planning/.observed-refresh-staging/phase-01/observed-refresh-validation-attestation.json</files>
  <action>Record pre-task SHA-256 for the five live canonical targets and six staging-envelope files, execute the new re-attestation command through `tools/phase1-python`, recover any entered journal, and rerun the live-generation test. Require the staging target bytes and attestation targetMap to equal unchanged live bytes, canonicalGitBinding to rehash against recorded object IDs, four-report provenance exact, and no prepared/replacing/provisional journal or backup remaining after success. Recompute live SHA-256 after the command and fail on change. If the command refuses its final source postcondition, require all six pre-task staging hashes restored and do not commit. On success, commit the six staging-envelope paths together; never commit a partial/provisional envelope or an attestation whose target map differs from staged files.</action>
  <verify>
    <automated>tools/phase1-python tools/validate-research.py re-attest-observed-refresh-live --staged-root .planning/.observed-refresh-staging/phase-01 --attestation .planning/.observed-refresh-staging/phase-01/observed-refresh-validation-attestation.json &amp;&amp; PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_consolidation_recovery.ObservedRefreshPublicationTests.test_live_observed_refresh_generation_matches_attestation test_consolidation_recovery.ObservedRefreshPublicationTests.test_re_attestation_binds_exact_git_blob_live_generation_and_preserves_provenance -v</automated>
  </verify>
  <acceptance_criteria>The retained attestation equals all five live SHA-256 values, staging files equal corresponding live files, canonical live bytes remain unchanged, immutable source/Git provenance is reproducible, and no provisional journal/backup remains; any final source-postcondition refusal leaves all six pre-task staging bytes exact and produces no commit.</acceptance_criteria>
  <done>The live observed-refresh attestation mismatch is repaired before integrated closeout.</done>
</task>

</tasks>

## Artifacts this phase produces

| Path | New symbol/record | Contract |
|---|---|---|
| `tools/validate-research.py` | `re_attest_observed_refresh_live` plus CLI route | Re-attests only a complete lock-held live generation that is byte-equal to exact Git blobs and retained reviewed-source provenance. |
| `tools/canonical-recovery.py` | `publish_from_canonical_snapshot` plus provisional lock-required publication/rollback helper | Holds one writer exclusion across registered snapshot read, callback revalidation, provisional journaled publication, final raw-source postcondition, and commit-or-exact-rollback without reentrant lock acquisition. |
| `tests/phase1/test_consolidation_recovery.py` | Re-attestation, raw-mutation rollback, and interruption regressions | Proves success, fail-before-mutation, exact old-envelope rollback after provisional publication, and committed old-or-new recovery. |
| `.planning/.observed-refresh-staging/phase-01/{claims.yaml,drift-register.yaml,open-questions.yaml,package-map.md,open-work-snapshot.json}` | Current staged generation | Exact immutable copies of the five unchanged live canonical targets. |
| `.planning/.observed-refresh-staging/phase-01/observed-refresh-validation-attestation.json` | Re-attested generation record | Binds five target digests, generation digest, exact Git objects, retained four-report source provenance, SPK-G state, and superseded generation. |

No live canonical research record, schema, package, external resource, approval, or phase-status artifact is produced.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| live canonical snapshot → retained staging envelope | A participating or raw filesystem truth refresh can make a previously valid attestation stale before publication commits. |
| Git metadata → immutable canonical provenance | Path or digest claims are unsafe unless exact tree mode/type/object bytes agree. |
| re-attestation → terminal authority | Repairing target hashes must not waive PRE118 or accept evidence. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-54-01 | Tampering | live targetMap re-attestation | high | mitigate | Hold one writer transaction across recovery, registered snapshot, Git/worktree comparison, derivation, provisional six-file publication, and final raw-source postcondition; participating refresh waits, while non-locking mutation triggers exact old-envelope rollback before journal removal. |
| T-01-54-02 | Repudiation | retained reviewed-source binding | high | mitigate | Rehash exact four-report Git blobs at sourceSnapshotCommit and reproduce candidate/parser binding before emitting the new attestation. |
| T-01-54-03 | Denial of Service | six-file staging publication | medium | mitigate | Retain old bytes/backups through a durable provisional journal, inject publication/rollback interruptions, and finalize or remove the journal only after exact committed-new or restored-old verification. |
| T-01-54-04 | Elevation of Privilege | re-attestation versus terminal review | high | mitigate | Keep the route independent from and non-authoritative over PRE118; Plan 01-53 must still observe the exact non-passing PRE118 contract. |

No package-manager install occurs; supply-chain package legitimacy gate is not applicable.
</threat_model>

<verification>
Run copied-root re-attestation, participating-refresh exclusion, raw live-target replacement with exact provisional-envelope rollback, reentrant-deadlock refusal, and publication/rollback interruption recovery; execute the real bounded re-attestation; then require live targetMap comparison and exact pre/post live hashes.
</verification>

<success_criteria>
The retained observed-refresh staging envelope and attestation are derived and provisionally published within one writer transaction over the current five-file canonical generation, carry exact Git/source provenance, commit only after the final raw-source postcondition, restore the exact complete old envelope on out-of-lock mutation, recover publication/rollback interruptions, leave live evidence unchanged, and do not alter PRE118 or any human authority boundary.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-54-SUMMARY.md` when done.
</output>
