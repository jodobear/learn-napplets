---
phase: 01-research-and-truth-baseline
gsd_phase: 1
source_phase: 0
adr_status_limit: proposed
plan: 19
type: execute
wave: 8
depends_on: [01-07, 01-20, 01-26]
files_modified:
  - .planning/spikes/spk-k-deployment/metadata.yaml
  - .planning/spikes/spk-k-deployment/recipe.md
  - .planning/spikes/spk-k-deployment/fixture.md
  - .planning/spikes/spk-k-deployment/environment.json
  - .planning/spikes/spk-k-deployment/measurements.yaml
  - .planning/spikes/spk-k-deployment/report.md
autonomous: false
requirements: [EVID-04]
must_haves:
  truths:
    - "SPK-K measures local independent public-site assembly, lab artifact output, and optional portable output before any externally visible deployment/publication attempt."
    - "A public deployment or publication probe occurs only after explicit human authorization; declined authorization is recorded as scoped blocked evidence rather than silently performed."
  artifacts:
    - path: .planning/spikes/spk-k-deployment/metadata.yaml
      provides: "SPK-K deployment/publication experiment and authorization context"
    - path: .planning/spikes/spk-k-deployment/report.md
      provides: "Deployment/publication evidence for ADR 0003"
  key_links:
    - from: SPK-K output evidence
      to: ADR 0003 and portability/deployment decisions
      via: "local artifact digests, optional authorized external probe, and proposed/blocked report"
  prohibitions:
    - statement: "Phase 1 MUST NOT deploy, publish, or write to an external service without explicit approval, and MUST NOT treat a local build as successful public deployment."
      category: safety
      status: unresolved
      flagged: true
      verification: null
      reason: "External action remains a human-approved decision and local-only result is labeled accordingly."
---

<objective>
Produce deployment/publication evidence from local disposable artifacts and an explicitly authorized-or-blocked external probe.

Purpose: Inform ADR 0003 without publishing a product or bypassing external-action approval.
Output: SPK-K fixture, external-action decision gate, local/authorized measurements, and report.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/research/delivery-mode-recommendation.md
@.planning/research/teaching-scope.yaml
@.planning/research/compatibility-matrix.yaml
@.planning/research/schemas/spike.schema.json
@.planning/spikes/_shared/toolchain-environment.json
@.planning/governance/approval-matrix.md
@docs/learn-napplets-codex-pack-v3/docs/04-RESEARCH-PLAN.md section 9K
@docs/learn-napplets-codex-pack-v3/docs/10-DECISIONS-AND-ADRS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define local artifact and optional external deployment evidence</name>
  <read_first>.planning/research/delivery-mode-recommendation.md; .planning/research/teaching-scope.yaml; .planning/research/compatibility-matrix.yaml; .planning/research/schemas/spike.schema.json; .planning/governance/approval-matrix.md; docs/learn-napplets-codex-pack-v3/docs/04-RESEARCH-PLAN.md section 9K</read_first>
  <files>.planning/spikes/spk-k-deployment/metadata.yaml, .planning/spikes/spk-k-deployment/recipe.md, .planning/spikes/spk-k-deployment/fixture.md</files>
  <action>Create non-production `SPK-K` metadata, recipe, and fixture for local static-site artifact assembly, lab artifact output, and optional portable output assessment. Define actual artifact identity/digest, hosting/isolation/preview/rollback criteria, dry-run commands, success/failure/blocked outcomes, source/package baseline, and ADR 0003 impacts. Keep all local outputs below the spike directory. Include an external-probe authorization field whose default is `not-authorized`; no account, credential, remote configuration, publication, or deployment command appears in the executable recipe before an explicit decision. A sandbox-write exception must record a named target, target classification as disposable sandbox, exact command, cleanup procedure, retention period, owner, security/release sign-off fields, and result-digest policy; no unspecified environment may receive a write. This addresses review concern 14.</action>
  <verify><automated>tools/phase1-python tools/validate-research.py validate-spike .planning/spikes/spk-k-deployment --contract</automated></verify>
  <acceptance_criteria>SPK-K separates local artifact tests from any external probe and has deterministic blocked outcome for missing authorization.</acceptance_criteria>
  <done>The deployment decision is framed without performing an external write.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Decide whether SPK-K may perform an external sandbox deployment probe</name>
  <read_first>.planning/spikes/spk-k-deployment/metadata.yaml; .planning/spikes/spk-k-deployment/recipe.md; .planning/governance/approval-matrix.md; .planning/phases/01-research-and-truth-baseline/01-CONTEXT.md (D-10, D-25, D-28)</read_first>
  <files>.planning/spikes/spk-k-deployment/metadata.yaml</files>
  <decision>Authorize a named sandbox deployment/publication probe or preserve local-only deployment evidence as a scoped blocker.</decision>
  <context>External writes require explicit approval under D-10. The result informs ADR 0003 but does not authorize production release or ADR acceptance.</context>
  <options>
    <option id="local-only"><name>Record local-only evidence</name><pros>No external account, credential, or public artifact is created; preserves the Phase 1 boundary.</pros><cons>Deployment/publication conclusion remains scoped blocked pending a later authorized probe.</cons></option>
    <option id="sandbox-probe"><name>Authorize named sandbox probe</name><pros>Captures end-to-end preview/deployment evidence for the selected disposable artifact.</pros><cons>Requires an explicit target, credentials, and post-probe cleanup/recording; still cannot be production release.</cons></option>
  </options>
  <resume-signal>Select `local-only` or `sandbox-probe`; for sandbox-probe, provide the named target and approve the write explicitly.</resume-signal>
  <action>Record the selected decision, date, approver role, target if authorized, and any credential-handling limitation in SPK-K metadata. Do not infer authorization from available CLI credentials or execute a write command after a local-only decision.</action>
  <verify><automated>tools/phase1-python tools/validate-research.py validate-spike .planning/spikes/spk-k-deployment --contract</automated></verify>
  <acceptance_criteria>Metadata records either `local-only` or `sandbox-probe`; only the latter can unlock a named external write command.</acceptance_criteria>
  <done>External deployment permission is explicit, auditable, and separate from ADR acceptance.</done>
</task>

<task type="auto">
  <name>Task 3: Execute local SPK-K evidence and any authorized sandbox probe</name>
  <read_first>.planning/spikes/spk-k-deployment/metadata.yaml; .planning/spikes/spk-k-deployment/recipe.md; .planning/spikes/spk-k-deployment/fixture.md; .planning/spikes/_shared/toolchain-environment.json; .planning/governance/evidence-policy.md (## Reports, canonical heading authority); docs/learn-napplets-codex-pack-v3/templates/RESEARCH-REPORT-TEMPLATE.md (non-authoritative structural aid)</read_first>
  <files>.planning/spikes/spk-k-deployment/environment.json, .planning/spikes/spk-k-deployment/measurements.yaml, .planning/spikes/spk-k-deployment/report.md</files>
  <action>Run all declared local artifact/dry-run measurements, recording commands, environment, artifact digests, isolation/preview/rollback evidence, and deterministic replay. If and only if Task 2 selected `sandbox-probe`, run the explicitly named external sandbox command with credentials kept outside version control, capture target and result, then follow declared cleanup. If Task 2 selected `local-only`, record deployment/publication as blocked with affected ADR/phase mapping. In every case write `report.md` with the ordered headings `Research question`, `Sources and immutable revisions`, `Observations`, `Conflicts`, `Inference`, `Prototype or measurement`, `Recommendation`, `Uncertainty`, `Affected phases and requirements`, and `Owner and required approval`; cite matching `SPK-K` metadata and report observations, proposed ADR 0003 recommendation, uncertainty, and no production-release claim.</action>
  <verify><automated>tools/phase1-python tools/validate-research.py validate-spike .planning/spikes/spk-k-deployment --complete && tools/phase1-python tools/validate-planning.py</automated></verify>
  <acceptance_criteria>Report distinguishes local output from external probe state, contains artifact digests and replay result, and records a proposed/blocked deployment conclusion without publishing production artifacts.</acceptance_criteria>
  <done>ADR 0003 has local and, only when authorized, sandbox deployment evidence.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.planning/spikes/spk-k-deployment/{metadata.yaml,recipe.md,fixture.md}` — SPK-K local artifact criteria and explicit external-action gate.
- `.planning/spikes/spk-k-deployment/{environment.json,measurements.yaml,report.md}` — local/authorized-probe environment, results, digests, and ADR 0003 report.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local artifact → external hosting/publication | An external write can create public state and require credentials. |
| deployment evidence → ADR 0003 | Local assembly and a hosted preview are materially different claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Elevation of Privilege | deployment CLI/credentials | high | mitigate | Block on explicit human decision and keep credentials out of files/output. |
| T-19-02 | Tampering | published artifact identity | high | mitigate | Record artifact digest, source baseline, target, and result before report generation. |
| T-19-03 | Repudiation | external-action approval | high | mitigate | Persist selected option, approver/date, and target in SPK metadata. |
| T-19-SC | Tampering | deployment/package tooling | high | mitigate | Do not install or execute unapproved deployment tooling; use local-only blocker when unavailable. |
</threat_model>

<verification>
Validate SPK-K and planning gate; confirm metadata decision controls whether an external command is eligible and report distinguishes local versus external evidence.
</verification>

<success_criteria>
The deployment/publication recommendation is evidence-backed and explicit about any missing external authorization rather than silently performing public writes.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-19-SUMMARY.md` when done.
</output>
