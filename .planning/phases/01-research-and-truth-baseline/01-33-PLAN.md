---
phase: 01-research-and-truth-baseline
plan: 33
type: execute
wave: 4
depends_on: [01-45]
files_modified:
  - tools/validate-research.py
  - .planning/spikes/replay-manifest.yaml
  - tests/phase1/test_spikes.py
  - tools/refresh-sources.py
  - tests/phase1/test_drift.py
  - tests/phase1/test_evidence.py
autonomous: true
gap_closure: true
requirements: [EVID-01, EVID-04, OPER-01, OPER-03]
must_haves:
  truths:
    - "A completed spike proves every retained declared output is a confined regular file whose bytes match its declared digest and whose replay output is bound to exactly one retained output."
    - "Replay metadata cannot select an executable or arguments; every SPK-A through SPK-L replay derives fixed approved-wrapper argv from the stable spike mapping."
    - "Refresh produces one deterministic result per source and evidence tests cannot mutate canonical planning reports."
  artifacts:
    - path: tools/validate-research.py
      provides: "Retained-evidence resolver and fixed replay argv derivation"
    - path: .planning/spikes/replay-manifest.yaml
      provides: "Digest-pinned replay metadata without command authority"
    - path: tools/refresh-sources.py
      provides: "One-source/one-observation deterministic review-work reducer"
  key_links:
    - from: metadata rawOutputDigests and evidenceLinks
      to: spike-local retained bytes
      via: "relative path grammar, confinement, regular-file check, exact SHA-256, and replay binding"
    - from: replay manifest spikeId
      to: SPIKE_REPORTS
      via: "fixed wrapper argv generation rather than manifest command text"
  prohibitions:
    - statement: "Replay and refresh work may not execute arbitrary commands, install packages, write source/claim/ADR records, or mutate canonical reports from tests."
      category: safety
      status: unresolved
      flagged: true
      verification: "Negative fixtures prove command rejection and temporary report destinations."
---

<objective>
Bind reproducible spike evidence to real retained bytes, remove manifest command authority, and make refresh/test outputs deterministic and hermetic.

Purpose: Close CR-04, CR-06, WR-01, WR-02, and WR-03 before consolidation is trusted.
Output: Safe replay/evidence validation and deterministic review-work behavior.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-REVIEW.md
@.planning/phases/01-research-and-truth-baseline/01-VERIFICATION.md
@tools/validate-research.py
@.planning/spikes/replay-manifest.yaml
@tests/phase1/test_spikes.py
@tools/refresh-sources.py
@tests/phase1/test_drift.py
@tests/phase1/test_evidence.py
@.planning/spikes/spk-g-package-conformance/metadata.yaml
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Prove one retained output and fixed replay path end to end</name>
  <read_first>tools/validate-research.py; tests/phase1/test_spikes.py; replay-manifest.yaml; .planning/research/schemas/spike.schema.json; .planning/spikes/spk-g-package-conformance/metadata.yaml; 01-REVIEW.md</read_first>
  <files>tools/validate-research.py, .planning/spikes/replay-manifest.yaml, tests/phase1/test_spikes.py</files>
  <behavior>
    - `test_complete_spike_requires_retained_output_and_bound_replay_digest` passes with a real `raw.txt`, exact lowercase digest, matching evidence link, and replay digest; it rejects empty/null declarations, missing/altered bytes, absolute/traversing/NUL paths, directory/symlink escape, duplicate normalized paths, and an unbound output digest.
    - `test_replay_mapping_rejects_manifest_command_substitution` proves every SPK-A through SPK-L entry derives `[tools/phase1-python, tools/validate-research.py, validate-spike, .planning/spikes/<mapped-directory>, --complete]` and an arbitrary manifest command never starts.
  </behavior>
  <action>Start RED by fixing the WR-01 fixture to write `raw.txt` and calculate its actual digest. Add the two named tests and isolated temporary spike trees. Implement one reusable evidence resolver that accepts only nonempty repository-relative POSIX paths below the declared spike directory, rejects NUL/root/parent/normalization changes, resolves strictly, requires a regular non-symlink file, and returns bytes/digest. Apply it to all `rawOutputDigests`, typed evidence links, and replay output binding; reject duplicate normalized paths and require the replay digest to equal one validated retained output. Validate complete unique SPK-A through SPK-L manifest inventory and mapped report/metadata/measurement paths before any subprocess runs. Add `expected_replay_argv(spike_id, planning_root)` based solely on `SPIKE_REPORTS`; delete executable authority from the manifest or require legacy text to equal rendered argv exactly, and remove `shlex.split` execution. Preserve D-21 through D-24 and D-37 through D-39: this validates local evidence only, uses no package installation, browser workaround, or protocol conclusion.</action>
  <verify><automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_spikes.SpikeValidationTests.test_complete_spike_requires_retained_output_and_bound_replay_digest && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_spikes.SpikeValidationTests.test_replay_mapping_rejects_manifest_command_substitution && tools/phase1-python tools/validate-research.py replay-spikes --manifest .planning/spikes/replay-manifest.yaml --check</automated></verify>
  <acceptance_criteria>The valid retained-evidence tracer passes, every listed malformed/escape/substitution fixture fails before command launch, and all committed replays use fixed wrapper argv derived from stable IDs.</acceptance_criteria>
  <done>Completed spike evidence is real, digest-bound, and replayable without granting YAML command execution authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reduce duplicate refresh inputs and isolate canonical evidence-test output</name>
  <read_first>tools/refresh-sources.py; tests/phase1/test_drift.py; tests/phase1/test_evidence.py; source-registry.yaml; claims.yaml; drift-register.yaml; open-questions.yaml; 01-REVIEW.md</read_first>
  <files>tools/refresh-sources.py, tests/phase1/test_drift.py, tests/phase1/test_evidence.py</files>
  <action>Add RED tests `test_duplicate_refresh_observations_are_idempotent` and `test_conflicting_refresh_observations_are_one_ambiguous_result` to `RefreshComparisonTests`, and add `test_evidence_validation_uses_temporary_report_without_canonical_mutation` to `SourceEvidenceTests`. In `refresh-sources.py`, validate/normalize each comparison object, group by source ID before `compare`, sort normalized JSON bytes, collapse byte-identical observations to one stable result, and reduce nonidentical observations to one `ambiguous` result carrying sorted normalized-observation digests and one stable work ID. Preserve old/new pins and never rewrite source, claim, drift, question, or ADR content. Change traceability testing to put the report under `TemporaryDirectory` while reading the real research root, then byte-check the canonical report before and after. Apply D-13, D-20, D-33 through D-36; refresh routes review work only and cannot authorize any source or phase result.</action>
  <verify><automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_drift.RefreshComparisonTests.test_duplicate_refresh_observations_are_idempotent && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_drift.RefreshComparisonTests.test_conflicting_refresh_observations_are_one_ambiguous_result && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_evidence.SourceEvidenceTests.test_evidence_validation_uses_temporary_report_without_canonical_mutation</automated></verify>
  <acceptance_criteria>Identical duplicates collapse to byte-stable one-source output, contradictory inputs yield one explicit ambiguous work item, and evidence tests leave canonical report bytes unchanged.</acceptance_criteria>
  <done>Refresh is deterministic and evidence testing is hermetic with respect to canonical planning state.</done>
</task>
</tasks>

## Artifacts this phase produces
- `tools/validate-research.py` — confined retained-evidence resolver and `expected_replay_argv()`.
- `.planning/spikes/replay-manifest.yaml` — stable-ID evidence metadata with no executable authority.
- `tools/refresh-sources.py` — duplicate observation reducer.
- `tests/phase1/test_spikes.py`, `test_drift.py`, and `test_evidence.py` — CR/WR regression suites.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| spike metadata → retained output | Declarations can point at altered or outside files. |
| replay manifest → subprocess | Human-editable YAML previously selected a local executable. |
| refresh input → review output | Contradictory duplicate input can mislead reviewers. |
| test report path → canonical evidence | A test can otherwise alter shared review state. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-33-01 | Tampering | retained evidence bytes | high | mitigate | Task 1 confines files and verifies exact digests/binding. |
| T-33-02 | Elevation of Privilege | replay command | high | mitigate | Task 1 derives fixed argv and rejects command substitution. |
| T-33-03 | Tampering | path escape | high | mitigate | Task 1 rejects traversal, symlink, nonregular, and malformed paths. |
| T-33-04 | Repudiation | refresh work | high | mitigate | Task 2 emits one canonical ambiguous record with input digests. |
| T-33-05 | Tampering | test output | high | mitigate | Task 2 uses temporary outputs and byte-stability proof. |
</threat_model>

<verification>
Run named retained-output/replay, refresh, and evidence-isolation tests; then replay the committed manifest through the wrapper.
</verification>

<success_criteria>
CR-04, CR-06, and WR-01 through WR-03 are closed with behavior-level evidence and no new authority or execution surface.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-33-SUMMARY.md` when done.
</output>
