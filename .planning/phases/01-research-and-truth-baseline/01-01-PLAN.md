---
phase: 01-research-and-truth-baseline
gsd_phase: 1
source_phase: 0
adr_status_limit: proposed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - requirements-phase1-tools.txt
  - .planning/research/toolchain-approval.yaml
  - .planning/spikes/_shared/toolchain-environment.json
  - tools/phase1-python
  - tools/validate-planning.py
  - tests/phase1/test_preflight.py
autonomous: false
requirements: [EVID-04, OPER-03]
must_haves:
  truths:
    - "Every Phase 1 parser, schema validator, and browser runner install is approved by a human before its command runs, is pinned, and has provenance, license, and hash/source-decision fields recorded."
    - "The isolated Phase 1 tool environment records Python, package, browser-binary, OS/container, Node, and package-manager versions without creating a production dependency manifest."
  artifacts:
    - path: requirements-phase1-tools.txt
      provides: "Pinned Phase 1-only PyYAML, jsonschema, and Playwright tool requirements"
    - path: .planning/research/toolchain-approval.yaml
      provides: "Human supply-chain approval evidence"
    - path: .planning/spikes/_shared/toolchain-environment.json
      provides: "Reproducibility environment manifest"
  key_links:
    - from: requirements-phase1-tools.txt
      to: .planning/research/toolchain-approval.yaml
      via: "exact approved package versions and provenance fields"
    - from: .planning/research/toolchain-approval.yaml
      to: .planning/spikes/_shared/toolchain-environment.json
      via: "approved toolchain is recorded in the executable environment"
  prohibitions:
    - statement: "Phase 1 MUST NOT add a production application dependency manifest, production framework scaffold, or unreviewed package installation."
      category: safety
      status: unresolved
      flagged: true
      verification: null
      reason: "Phase 1 toolchain is isolated research infrastructure and requires human approval."
---

<objective>
Approve and install the isolated Phase 1 research toolchain without crossing the supply-chain or production-scaffold gates.

Purpose: Enable safe YAML/schema validation and reproducible browser evidence while preserving the human package-approval boundary.
Output: An ignored cache boundary, pinned research-only requirements, approval evidence, and an environment manifest.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-RESEARCH.md
@.planning/phases/01-research-and-truth-baseline/01-PATTERNS.md
@.planning/phases/01-research-and-truth-baseline/01-VALIDATION.md
@.planning/governance/approval-matrix.md
@.planning/validation/required-artifacts.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the fail-closed Phase 1 execution-convergence preflight</name>
  <read_first>.planning/phases/01-research-and-truth-baseline/01-REVIEWS.md; .planning/STATE.md; tools/validate-planning.py; .planning/governance/approval-matrix.md</read_first>
  <files>tools/validate-planning.py, tests/phase1/test_preflight.py</files>
  <behavior>
    - A review record with only failed, expired, or transport-error reviewer lanes fails execution preflight.
    - A current HIGH finding lacking an addressed, deferred, or rejected disposition with rationale fails execution preflight.
    - A record without an authorized dated convergence decision fails execution preflight; a successful reviewer plus complete finding dispositions and authorization passes.
  </behavior>
  <action>Write failing deterministic fixtures before implementation, then extend `tools/validate-planning.py` with `--phase-1-execution-preflight`. Parse the committed Phase 1 review record's explicit convergence block and fail closed unless it names at least one successful reviewer response, disposition rows for every current HIGH finding, and an authorized dated convergence decision. Treat an authentication/transport failure as neither success nor approval. Emit stable diagnostics naming the missing reviewer, finding disposition, or authorization; do not alter the review record or state file. This implements the required execution gate and addresses review concern 9.</action>
  <verify><automated>python3 -m unittest discover -s tests/phase1 -p 'test_preflight.py' && python3 tools/validate-planning.py --help</automated></verify>
  <acceptance_criteria>Fixtures independently prove failed reviewer lanes, undispositioned current HIGH findings, and missing convergence authorization prevent a pass; the command does not mutate planning state.</acceptance_criteria>
  <done>The machine-checkable convergence validator exists before any package review or research tool installation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Approve the Phase 1 research-only package and browser toolchain</name>
  <precondition>`python3 tools/validate-planning.py --phase-1-execution-preflight` exits 0 against the committed review record.</precondition>
  <read_first>@.planning/phases/01-research-and-truth-baseline/01-RESEARCH.md (Package Legitimacy Audit and Standard Stack); @.planning/phases/01-research-and-truth-baseline/01-VALIDATION.md; .gitignore; .planning/governance/approval-matrix.md</read_first>
  <files>requirements-phase1-tools.txt, .planning/research/toolchain-approval.yaml, .planning/spikes/_shared/toolchain-environment.json</files>
  <what-built>Package legitimacy audit results and the proposed isolated Phase 1 tooling set are ready for review.</what-built>
  <how-to-verify>Inspect the official source, version, license, available hash/source decision, and exact isolated installation command for PyYAML, jsonschema, Playwright, Chromium, and Firefox.</how-to-verify>
  <resume-signal>Reply with approved or blocked for PyYAML, jsonschema, Playwright, Chromium, and Firefox.</resume-signal>
  <action>Present the exact installation set `PyYAML==6.0.3`, `jsonschema==4.26.0`, and `playwright==1.61.0` for human package-legitimacy review before any package-manager command, per D-21 and D-23. The reviewer must record an approve-or-block decision for every SUS package and select one reviewed browser-runner route: direct driving of installed Chrome and Firefox, or Playwright-managed Chromium and Firefox binaries. Inspect official provenance, license, version, available hash/source decision, executable or binary provenance, and the exact commands. Do not install a package, acquire browser binaries, create `package.json`, or create any production path before the reviewer responds; a denial or no reviewed environment blocks all YAML/schema/browser-dependent tasks without a handwritten substitute.</action>
  <verify><automated>python3 tools/validate-planning.py</automated></verify>
  <acceptance_criteria>The checkpoint records an explicit approve-or-block response for all three named packages and the Chromium/Firefox browser-binary acquisition; no installation command precedes that response.</acceptance_criteria>
  <done>Human supply-chain decision is available to the next task.</done>
</task>

<task type="auto">
  <name>Task 3: Install the approved research toolchain, pin its wrapper, and capture the reproducibility baseline</name>
  <read_first>.gitignore; .planning/research/toolchain-approval.yaml; requirements-phase1-tools.txt (after creation); .planning/phases/01-research-and-truth-baseline/01-RESEARCH.md lines 122-169</read_first>
  <files>.gitignore, requirements-phase1-tools.txt, .planning/research/toolchain-approval.yaml, .planning/spikes/_shared/toolchain-environment.json, tools/phase1-python</files>
  <action>Create an ignored `.research/` boundary in `.gitignore` while preserving the existing archive rule, per D-09. Write `requirements-phase1-tools.txt` with exactly the human-approved pinned Python packages and write `.planning/research/toolchain-approval.yaml` with package name, version, release-artifact URL, registry/project URL, license decision, artifact SHA-256 or documented unavailable-hash reason, approver, date, and approved-or-blocked status. If every required entry and selected runner route is approved, create an isolated environment below `.research/phase1-tools/`, install only from the pinned requirements file, and either configure reviewed installed Chrome/Firefox executables or acquire only approved Playwright Chromium/Firefox binaries. Create executable `tools/phase1-python` as the stable Phase 1 interpreter wrapper; it resolves only the approved isolated interpreter, checks its recorded version and hash against the manifest, and refuses an ambient interpreter. Capture the manifest's Linux/OS or container identity, Python and package versions, Git executable version, commit OID/blob OID/revision:path acquisition convention, browser vendor/version or Playwright browser revision, Node/package-manager versions, browser flags, requirements-file SHA-256, and default five-sample median policy, per D-37 through D-39. All Plans 01-02 through 01-28 must use this wrapper for verification and replay. If any required approval is blocked, record the scoped blocker and do not substitute a handwritten YAML or JSON Schema implementation. This addresses review concern 1.</action>
  <verify><automated>test -x tools/phase1-python && test -f requirements-phase1-tools.txt && test -f .planning/research/toolchain-approval.yaml && tools/phase1-python -c "import json; d=json.load(open('.planning/spikes/_shared/toolchain-environment.json')); assert d['samplesRequired']==5 and d['summaryStatistic']=='median'; assert d['browsers'] and d['packages']" && python3 tools/validate-planning.py</automated></verify>
  <acceptance_criteria>Every approved package and browser route has release/provenance/license/hash evidence; `tools/phase1-python` rejects an ambient interpreter and runs the approved isolated one; the manifest records actual tool, Git, browser, and binary provenance. A blocked entry remains a deterministic blocker.</acceptance_criteria>
  <done>Approved Phase 1 tooling is isolated, provenance-complete, and invoked through one recorded interpreter wrapper.</done>
</task>

</tasks>

## Artifacts this phase produces

- `requirements-phase1-tools.txt` — pinned research-tool requirement set.
- `.planning/research/toolchain-approval.yaml` — package approval record fields for provenance, license, hash/source decision, approver, and status.
- `.planning/spikes/_shared/toolchain-environment.json` — reproducibility fields `browsers`, `packages`, `samplesRequired`, `summaryStatistic`, Git-object acquisition provenance, and browser/binary provenance.
- `tools/phase1-python` — stable checked interpreter wrapper for every post-bootstrap Phase 1 verification and replay command.
- `tests/phase1/test_preflight.py` — failing fixtures for review convergence execution gating.
- `.gitignore` — ignored `.research/` acquisition/tooling boundary.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human approval → package manager | Approved research dependencies and browser binaries enter the local environment. |
| ignored cache → committed evidence | Bulky executable dependencies must not be committed as evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | Python/browser package acquisition | high | mitigate | Blocking human review records provenance, version, license, and hash/source decision before installation. |
| T-01-02 | Information Disclosure | Toolchain manifest | medium | mitigate | Record versions and executable paths only; never write credentials, cookies, tokens, or transport logs. |
| T-01-03 | Elevation of Privilege | Research environment | high | mitigate | Constrain dependencies and browsers to ignored `.research/phase1-tools/` and prohibit production scaffolding. |
| T-01-SC | Tampering | pip installs and Playwright browser downloads | high | mitigate | Human checkpoint verifies all SUS packages before the exact pinned commands run. |
</threat_model>

<verification>
Run `python3 tools/validate-planning.py`; verify the human checkpoint precedes any package-manager or browser-binary command; inspect the isolated environment manifest with Python JSON parsing.
</verification>

<success_criteria>
Approved tools are pinned, isolated, reproducibly described, and unavailable or rejected tools yield an impact-scoped block rather than unsafe replacement tooling.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-01-SUMMARY.md` when done.
</output>
