---
phase: 01-research-and-truth-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - requirements-phase1-tools.txt
  - .planning/research/toolchain-approval.yaml
  - .planning/spikes/_shared/toolchain-environment.json
autonomous: false
requirements: [EVID-04, OPER-03]
must_haves:
  truths:
    - "Every Phase 1 parser, schema validator, and browser runner install is approved by a human before its command runs, is pinned, and has provenance, license, and hash/source-decision fields recorded."
    - "The isolated Phase 1 tool environment records Python, package, browser-binary, OS/container, Node, and package-manager versions without creating a production dependency manifest."
  artifacts:
    - path: requirements-phase1-tools.txt
      provides: "Pinned Phase 1-only PyYAML, jsonschema, and Playwright tool requirements"
    - path: .planning/research/toolchain-approval.yaml
      provides: "Human supply-chain approval evidence"
    - path: .planning/spikes/_shared/toolchain-environment.json
      provides: "Reproducibility environment manifest"
  key_links:
    - from: requirements-phase1-tools.txt
      to: .planning/research/toolchain-approval.yaml
      via: "exact approved package versions and provenance fields"
    - from: .planning/research/toolchain-approval.yaml
      to: .planning/spikes/_shared/toolchain-environment.json
      via: "approved toolchain is recorded in the executable environment"
  prohibitions:
    - statement: "Phase 1 MUST NOT add a production application dependency manifest, production framework scaffold, or unreviewed package installation."
      category: safety
      status: unresolved
      flagged: true
      verification: null
      reason: "Phase 1 toolchain is isolated research infrastructure and requires human approval."
---

<objective>
Approve and install the isolated Phase 1 research toolchain without crossing the supply-chain or production-scaffold gates.

Purpose: Enable safe YAML/schema validation and reproducible browser evidence while preserving the human package-approval boundary.
Output: An ignored cache boundary, pinned research-only requirements, approval evidence, and an environment manifest.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-RESEARCH.md
@.planning/phases/01-research-and-truth-baseline/01-PATTERNS.md
@.planning/phases/01-research-and-truth-baseline/01-VALIDATION.md
@.planning/governance/approval-matrix.md
@.planning/validation/required-artifacts.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve the Phase 1 research-only package and browser toolchain</name>
  <precondition>The GSD orchestrator has recorded successful deep plan-review convergence for Phase 1 before any execution begins.</precondition>
  <read_first>@.planning/phases/01-research-and-truth-baseline/01-RESEARCH.md (Package Legitimacy Audit and Standard Stack); @.planning/phases/01-research-and-truth-baseline/01-VALIDATION.md; .gitignore; .planning/governance/approval-matrix.md</read_first>
  <files>requirements-phase1-tools.txt, .planning/research/toolchain-approval.yaml, .planning/spikes/_shared/toolchain-environment.json</files>
  <what-built>Package legitimacy audit results and the proposed isolated Phase 1 tooling set are ready for review.</what-built>
  <how-to-verify>Inspect the official source, version, license, available hash/source decision, and exact isolated installation command for PyYAML, jsonschema, Playwright, Chromium, and Firefox.</how-to-verify>
  <resume-signal>Reply with approved or blocked for PyYAML, jsonschema, Playwright, Chromium, and Firefox.</resume-signal>
  <action>Present the exact installation set `PyYAML==6.0.3`, `jsonschema==4.26.0`, and `playwright==1.61.0` for human package-legitimacy review before any package-manager command, per D-21, D-23, and the SUS dispositions. The reviewer must inspect official registry/project provenance, license, version, available hash/source decision, and the two browser-binary commands; record an approval or scoped blocker for each package. Do not install a package, download a browser binary, create `package.json`, or create any production path before the reviewer responds.</action>
  <verify><automated>python3 tools/validate-planning.py</automated></verify>
  <acceptance_criteria>The checkpoint records an explicit approve-or-block response for all three named packages and the Chromium/Firefox browser-binary acquisition; no installation command precedes that response.</acceptance_criteria>
  <done>Human supply-chain decision is available to the next task.</done>
</task>

<task type="auto">
  <name>Task 2: Install only the approved, pinned research toolchain and record the approval</name>
  <read_first>.gitignore; .planning/research/toolchain-approval.yaml; requirements-phase1-tools.txt (after creation); .planning/phases/01-research-and-truth-baseline/01-RESEARCH.md lines 122-169</read_first>
  <files>.gitignore, requirements-phase1-tools.txt, .planning/research/toolchain-approval.yaml</files>
  <action>Create an ignored `.research/` boundary in `.gitignore` while preserving the existing archive rule, per D-09. Write `requirements-phase1-tools.txt` with exactly the human-approved pinned Python packages and write `.planning/research/toolchain-approval.yaml` with package name, version, registry/project URL, license decision, hash or documented unavailable-hash reason, approver, date, and approved-or-blocked status. If every required entry is approved, create an isolated environment below `.research/phase1-tools/`, install only from the pinned requirements file, then install Playwright Chromium and Firefox binaries; otherwise halt with the recorded scoped blocker and do not substitute a handwritten YAML or JSON Schema implementation.</action>
  <verify><automated>test -f requirements-phase1-tools.txt && test -f .planning/research/toolchain-approval.yaml && python3 tools/validate-planning.py</automated></verify>
  <acceptance_criteria>The three package entries are version-pinned and approval-backed; the installation is isolated under ignored `.research/`; a blocked entry leaves a deterministic blocker record rather than an alternative parser or validator.</acceptance_criteria>
  <done>Approved Phase 1 tooling is isolated and its provenance decision is version-controlled.</done>
</task>

<task type="auto">
  <name>Task 3: Capture the reproducibility baseline for all later spikes</name>
  <read_first>.planning/research/toolchain-approval.yaml; .planning/phases/01-research-and-truth-baseline/01-CONTEXT.md (D-37 through D-39); .planning/phases/01-research-and-truth-baseline/01-RESEARCH.md (Environment Availability)</read_first>
  <files>.planning/spikes/_shared/toolchain-environment.json</files>
  <action>Create `.planning/spikes/_shared/toolchain-environment.json` from the actual isolated environment. Record Linux/OS or container identity, Python and installed package versions, executable paths and versions for Chromium/Chrome and Firefox, Node and package-manager versions, relevant browser flags, approved package references, and a SHA-256 digest for the requirements file, per D-37 and D-38. Include a `samplesRequired` default of 5 and `summaryStatistic` value `median` so nondeterministic spikes retain all samples and report range plus median per D-39.</action>
  <verify><automated>python3 -c "import json; p='.planning/spikes/_shared/toolchain-environment.json'; d=json.load(open(p)); assert d['samplesRequired']==5 and d['summaryStatistic']=='median'; assert d['browsers'] and d['packages']"</automated></verify>
  <acceptance_criteria>The manifest contains actual tool versions and executable locations, not inferred version labels, and encodes the Linux Chromium/Firefox five-sample baseline.</acceptance_criteria>
  <done>Later spike recipes can reference one validated environment baseline.</done>
</task>

</tasks>

## Artifacts this phase produces

- `requirements-phase1-tools.txt` — pinned research-tool requirement set.
- `.planning/research/toolchain-approval.yaml` — package approval record fields for provenance, license, hash/source decision, approver, and status.
- `.planning/spikes/_shared/toolchain-environment.json` — reproducibility fields `browsers`, `packages`, `samplesRequired`, and `summaryStatistic`.
- `.gitignore` — ignored `.research/` acquisition/tooling boundary.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human approval → package manager | Approved research dependencies and browser binaries enter the local environment. |
| ignored cache → committed evidence | Bulky executable dependencies must not be committed as evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | Python/browser package acquisition | high | mitigate | Blocking human review records provenance, version, license, and hash/source decision before installation. |
| T-01-02 | Information Disclosure | Toolchain manifest | medium | mitigate | Record versions and executable paths only; never write credentials, cookies, tokens, or transport logs. |
| T-01-03 | Elevation of Privilege | Research environment | high | mitigate | Constrain dependencies and browsers to ignored `.research/phase1-tools/` and prohibit production scaffolding. |
| T-01-SC | Tampering | pip installs and Playwright browser downloads | high | mitigate | Human checkpoint verifies all SUS packages before the exact pinned commands run. |
</threat_model>

<verification>
Run `python3 tools/validate-planning.py`; verify the human checkpoint precedes any package-manager or browser-binary command; inspect the isolated environment manifest with Python JSON parsing.
</verification>

<success_criteria>
Approved tools are pinned, isolated, reproducibly described, and unavailable or rejected tools yield an impact-scoped block rather than unsafe replacement tooling.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-01-SUMMARY.md` when done.
</output>
