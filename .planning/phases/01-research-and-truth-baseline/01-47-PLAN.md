---
phase: 01-research-and-truth-baseline
plan: 47
type: execute
wave: 2
depends_on: [01-42, 01-51]
files_modified:
  - tools/canonical-recovery.py
  - tools/validate-research.py
  - tests/phase1/test_spike_consolidation.py
  - tests/phase1/test_consolidation_recovery.py
autonomous: true
gap_closure: true
requirements: [EVID-04, OPER-01]
must_haves:
  truths:
    - "Spike consolidation publishes its complete canonical target map as one journaled generation or leaves the old generation readable after every injected interruption."
    - "A malformed, regular-file, symlink, or multiple-entry transaction directory causes a bounded recovery refusal rather than a reader retry loop."
    - "P-OPER01-CONCURRENCY has a concrete interruption/recovery predicate: every reader sees only a complete old or complete new generation."
  artifacts:
    - path: tools/canonical-recovery.py
      provides: "Fail-closed recovery scanning for all transaction-directory entries."
      contains: "_recover_locked"
    - path: tools/validate-research.py
      provides: "Consolidation publication through the shared journaled generation primitive."
      contains: "consolidate_spike_impacts"
    - path: tests/phase1/test_spike_consolidation.py
      provides: "Consolidation replacement-interruption regression."
    - path: tests/phase1/test_consolidation_recovery.py
      provides: "Bounded malformed-transaction reader regression."
  key_links:
    - from: consolidate_spike_impacts
      to: canonical_recovery.publish_generation
      via: "The fully validated six-target byte map is journaled as one generation rather than sequentially replacing live files."
    - from: read_canonical_snapshot
      to: _recover_locked
      via: "Unexpected transaction entries cause RecoveryError before the reader retries or parses a canonical target."
---

<objective>
Repair durable canonical consolidation and fail-closed recovery of malformed transaction state.

Purpose: Close CR-02 and CR-04 at the shared publication boundary that all later evidence readers rely on.
Output: Journaled consolidation and bounded recovery refusal tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-FINAL-VERIFICATION.md
@.planning/phases/01-research-and-truth-baseline/01-REVIEW.md
@.planning/phases/01-research-and-truth-baseline/01-42-SUMMARY.md
@tools/canonical-recovery.py
@tools/validate-research.py
@tests/phase1/test_spike_consolidation.py
@tests/phase1/test_consolidation_recovery.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Consolidate one validated spike set through the durable generation publisher</name>
  <precondition>A fresh exact-plan review authorizes the current Plan 01-47 execution set, and `tools/phase1-python --verify-toolchain` exits 0.</precondition>
  <read_first>01-REVIEW.md CR-02; 01-FINAL-VERIFICATION.md gap 2; 01-42-SUMMARY.md; tools/validate-research.py `consolidate_spike_impacts`; tools/canonical-recovery.py `publish_generation`; tests/phase1/test_spike_consolidation.py consolidation and recovery cases</read_first>
  <files>tools/validate-research.py, tools/canonical-recovery.py, tests/phase1/test_spike_consolidation.py</files>
  <behavior>
    - A copied planning root receives a fully schema- and semantic-validated six-target consolidation candidate; injected interruption after every replacement position recovers to exactly the prior complete byte map or the complete new byte map.
    - A successful consolidation publishes the matching compatibility, drift, questions, security synthesis, audit, and replay-manifest bytes as one coherent generation.
    - A staged semantic validation failure invokes no publisher and leaves every live target byte-identical.
    - A concurrent reader of the target set receives an immutable complete mapping, never a mixed map.
  </behavior>
  <action>Start RED with a copied-root `SpikeConsolidationTests` case that records all six live consolidation targets, injects every `publish_generation()` replacement interruption point, calls recovery, and compares the returned snapshot with only the complete old or complete new map. Refactor `consolidate_spike_impacts()` so it first derives every output byte in private same-filesystem staging, performs all existing schema, replay-manifest, and semantic drift validation against that candidate map, and only then passes the complete root-relative target-to-bytes map to `canonical_recovery.publish_generation()`. Remove the direct per-target replacement and in-memory rollback loop; preserve deterministic target ordering, conflict-only behavior, the existing exclusive consolidation lock, source/claim validation, and cleanup of candidate staging material. The publisher must remain the Plan 01-42 shared primitive rather than a second transaction implementation. Apply D-21 through D-24, D-29 through D-32, D-35, and D-38: spike evidence remains isolated, reproducible, digest-bound, and unavailable for recommendation if full validation or publication cannot prove a coherent generation.</action>
  <verify>
    <automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_spike_consolidation.SpikeConsolidationTests.test_consolidation_generation_recovers_after_each_publish_interruption && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_spike_consolidation.SpikeConsolidationTests.test_consolidation_semantic_failure_does_not_enter_publication && tools/phase1-python tools/validate-research.py recover-consolidation --research .planning/research</automated>
  </verify>
  <acceptance_criteria>Every replacement interruption leaves recoverable journal data and resolves to one complete old or new six-target map; a candidate validation failure reaches no live replacement; a successful normal run retains the same fully validated artifacts.</acceptance_criteria>
  <done>CR-02 no longer has a serial canonical-publication path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refuse every unexpected transaction-directory entry without retrying</name>
  <read_first>01-REVIEW.md CR-04; tools/canonical-recovery.py `_recover_locked`, `read_canonical_snapshot`, and `TRANSACTION_DIRECTORY`; tests/phase1/test_consolidation_recovery.py recovery fixtures</read_first>
  <files>tools/canonical-recovery.py, tests/phase1/test_consolidation_recovery.py</files>
  <behavior>
    - A regular leftover file, symlinked entry, and more than one transaction directory each cause `RecoveryError` within the test timeout.
    - For each malformed entry, `read_canonical_snapshot()` returns no target bytes and does not perform a second recovery/read cycle.
    - A valid prepared or committed journal retains the existing old-or-new recovery behavior.
  </behavior>
  <action>Start RED with subprocess- or thread-bounded fixtures that place a regular file, a symlink, and multiple directories under `.canonical-transactions`, then call the registered reader. Change `_recover_locked()` to enumerate every entry and reject any symlink, non-directory, malformed directory, or ambiguous multiplicity before selecting a journal. Ensure recovery cannot return normally while transaction content remains that `read_canonical_snapshot()` would observe; propagate a stable `RecoveryError` before canonical parsing. Retain the valid single-journal prepared/publishing/committed state machine, digest checks, lock behavior, and recovery cleanup. This is a denial-of-service boundary repair under D-18, D-20, D-33, and D-36: it records/refuses uncertain evidence mechanics rather than silently treating a damaged generation as authoritative.</action>
  <verify>
    <automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_consolidation_recovery.CanonicalRecoveryTests.test_unexpected_transaction_entries_refuse_within_timeout && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_spike_consolidation.SpikeConsolidationTests.test_recovery_rejects_tampered_or_incomplete_journal_before_reader_entry && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_spike_consolidation.SpikeConsolidationTests.test_recovery_after_each_publish_interruption_is_coherent</automated>
  </verify>
  <acceptance_criteria>All malformed transaction-entry fixtures fail quickly with no canonical bytes returned, while valid interrupted transactions still recover deterministically to one complete generation.</acceptance_criteria>
  <done>CR-04 cannot turn stray filesystem state into an infinite reader retry.</done>
</task>

</tasks>

## Artifacts this phase produces
- `tools/validate-research.py::consolidate_spike_impacts` — candidate validation followed by one journaled multi-file publication call.
- `tools/canonical-recovery.py::_recover_locked` — exhaustive unsafe-entry rejection before recovery or snapshot retry.
- `tests/phase1/test_spike_consolidation.py::SpikeConsolidationTests.test_consolidation_generation_recovers_after_each_publish_interruption` — CR-02 all-replacement regression.
- `tests/phase1/test_consolidation_recovery.py::CanonicalRecoveryTests.test_unexpected_transaction_entries_refuse_within_timeout` — CR-04 bounded-refusal regression.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| staged consolidation bytes → canonical evidence | Interruption could expose a mixture of evidence generations. |
| transaction-directory entries → recovery reader | A hostile or malformed entry could hang a reader or bypass durable-state validation. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-47-01 | Tampering | consolidation publisher | high | mitigate | Task 1 validates the entire candidate map and delegates its one publication to the journaled shared primitive. |
| T-47-02 | Denial of Service | transaction recovery | high | mitigate | Task 2 rejects all unexpected transaction entries before retry and tests bounded refusal. |
| T-47-03 | Repudiation | spike evidence generation | medium | mitigate | Task 1 retains deterministic byte maps and recovery outcomes for every interruption fixture. |
</threat_model>

<verification>
Run the consolidation interruption, recovery-refusal, and existing registered-reader regressions through `tools/phase1-python`, then run explicit recovery against the canonical research root.
</verification>

<success_criteria>
Canonical spike consolidation either exposes one complete durable generation or fails closed, and malformed transaction residue cannot make a reader hang.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-47-SUMMARY.md` when done.
</output>
