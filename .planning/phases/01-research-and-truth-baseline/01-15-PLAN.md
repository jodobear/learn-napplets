---
phase: 01-research-and-truth-baseline
plan: 15
type: execute
wave: 8
depends_on: [01-07, 01-26]
files_modified:
  - .planning/spikes/spk-g-package-conformance/metadata.yaml
  - .planning/spikes/spk-g-package-conformance/recipe.md
  - .planning/spikes/spk-g-package-conformance/fixture.md
  - .planning/spikes/spk-g-package-conformance/environment.json
  - .planning/spikes/spk-g-package-conformance/measurements.yaml
  - .planning/spikes/spk-g-package-conformance/report.md
  - .planning/spikes/spk-g-package-conformance/impact-fragment.yaml
autonomous: false
requirements: [EVID-03, EVID-04]
must_haves:
  truths:
    - "SPK-G determines whether selected released public package exports and conformance surfaces can be consumed against their recorded source baseline."
    - "SPK-G excludes private monorepo imports and installs each candidate package only after human review of exact version/provenance/license/hash-source evidence."
    - "SPK-G retains proposed package compatibility, drift, and blocker impacts in one immutable local fragment until Plan 01-28 consolidates them."
  artifacts:
    - path: .planning/spikes/spk-g-package-conformance/metadata.yaml
      provides: "SPK-G package gate and public-export experiment contract"
    - path: .planning/spikes/spk-g-package-conformance/report.md
      provides: "Public package/conformance consumption evidence"
    - path: .planning/spikes/spk-g-package-conformance/impact-fragment.yaml
      provides: "Immutable SPK-G public-package impact proposals"
  key_links:
    - from: SPK-G impact fragment
      to: canonical compatibility, drift, and open-question records plus ADR 0005/0008/0010
      via: "Plan 01-28 serially validates public export, source baseline, observed conformance, disposition, uncertainty, and impact links"
  prohibitions:
    - statement: "Phase 1 MUST NOT import private upstream monorepo paths, bypass package legitimacy review, or treat compile success as protocol authority."
      category: safety
      status: unresolved
      flagged: true
      verification: null
      reason: "Only reviewed public releases are tested and results remain implementation evidence."
---

<objective>
Measure selected public package and conformance consumption without importing private upstream internals.

Purpose: Make package/runtime recommendations traceable to exact released exports, source revisions, and observed compatibility.
Output: SPK-G contract, human package gate, isolated consumer fixture, and public-surface report.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/research/package-map.md
@.planning/research/compatibility-matrix.yaml
@.planning/research/source-registry.yaml
@.planning/research/drift-register.yaml
@.planning/research/toolchain-approval.yaml
@.planning/spikes/_shared/toolchain-environment.json
@docs/learn-napplets-codex-pack-v3/docs/04-RESEARCH-PLAN.md section 9G
@docs/learn-napplets-codex-pack-v3/docs/06-TECHNICAL-ARCHITECTURE.md section 14
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define exact public-export package/conformance consumers</name>
  <read_first>.planning/research/package-map.md; .planning/research/compatibility-matrix.yaml; .planning/research/source-registry.yaml; .planning/research/drift-register.yaml; .planning/research/schemas/spike.schema.json; docs/learn-napplets-codex-pack-v3/docs/04-RESEARCH-PLAN.md section 9G; docs/learn-napplets-codex-pack-v3/docs/06-TECHNICAL-ARCHITECTURE.md section 14</read_first>
  <files>.planning/spikes/spk-g-package-conformance/metadata.yaml, .planning/spikes/spk-g-package-conformance/recipe.md, .planning/spikes/spk-g-package-conformance/fixture.md</files>
  <action>Create `SPK-G` metadata, recipe, and a minimal disposable consumer fixture. Select only package names/releases/public exports already cataloged and tied to source baseline IDs; list exact version, registry/project provenance, license, hash/source decision, browser support, claimed public export, conformance target, candidate disposition, and human approval field. Explicitly reject deep/private package paths in fixture rules. Predeclare compile/import/conformance measurements, expected result/error, replay input, success/failure/blocked thresholds, and linked compatibility/ADR impacts.</action>
  <verify><automated>python3 tools/validate-research.py validate-spike .planning/spikes/spk-g-package-conformance</automated></verify>
  <acceptance_criteria>Every candidate has exact public package/export/version fields and no fixture import points into a private monorepo path.</acceptance_criteria>
  <done>Package legitimacy reviewers have concrete candidate package operations to inspect.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Approve or block exact SPK-G package installation and conformance commands</name>
  <read_first>.planning/spikes/spk-g-package-conformance/metadata.yaml; .planning/spikes/spk-g-package-conformance/recipe.md; .planning/research/package-map.md; .planning/research/toolchain-approval.yaml</read_first>
  <files>.planning/spikes/spk-g-package-conformance/metadata.yaml</files>
  <what-built>SPK-G has exact public package/export consumers and approval fields before installation.</what-built>
  <how-to-verify>Inspect each public release’s provenance, license, version, hash/source decision, documented export, and isolated install location; record approve or block in metadata.</how-to-verify>
  <resume-signal>Reply with approved or blocked for every SPK-G package/version and conformance command.</resume-signal>
  <action>Require human review of each package/version and the conformance command: official registry/project provenance, public release status, license, available hash/source decision, documented public export, and isolated install location. Record approval or block per candidate before any package installation. A blocked candidate remains in the matrix with a blocker; do not use a private path or alternative unreviewed package to obtain a passing result.</action>
  <verify><automated>python3 tools/validate-research.py validate-spike .planning/spikes/spk-g-package-conformance</automated></verify>
  <acceptance_criteria>All installable candidate entries carry a dated approval status, and no candidate lacking approval is eligible for execution.</acceptance_criteria>
  <done>Public package consumption has a preserved supply-chain gate.</done>
</task>

<task type="auto">
  <name>Task 3: Execute SPK-G against approved public releases and preserve compatibility impacts</name>
  <read_first>.planning/spikes/spk-g-package-conformance/metadata.yaml; .planning/spikes/spk-g-package-conformance/recipe.md; .planning/spikes/spk-g-package-conformance/fixture.md; .planning/spikes/_shared/toolchain-environment.json; .planning/research/compatibility-matrix.yaml</read_first>
  <files>.planning/spikes/spk-g-package-conformance/environment.json, .planning/spikes/spk-g-package-conformance/measurements.yaml, .planning/spikes/spk-g-package-conformance/report.md, .planning/spikes/spk-g-package-conformance/impact-fragment.yaml</files>
  <action>Install approved releases only in ignored SPK-G-local storage and run the declared import/compile/conformance checks. Record exact commands, package lock/identity data, public exports exercised, source baseline, browser/runtime environment, all output digests/errors, replay outcome, and whether the disposition is consume, wrap, avoid, or blocked. Write `report.md` with the ordered canonical headings and cite matching `SPK-G` metadata. Write immutable `impact-fragment.yaml` beside the report with `schemaVersion`, stable `fragmentId` `SPK-G-IMPACT-001`, `spikeId`, report/metadata/measurement paths and SHA-256 values, source IDs, proposed `OQ-*` and `DRF-*` entries, compatibility observations, affected requirement and phase IDs, uncertainty, and typed evidence links. The fragment is the sole SPK-G hand-off for canonical compatibility and related impacts; Plan 01-28 validates the stable IDs and carries forward an explicit blocked or uncertain record when the result cannot be merged, per D-05, D-12, D-16 through D-20, D-23, D-26, and D-29 through D-31.</action>
  <verify><automated>python3 tools/validate-research.py validate-spike .planning/spikes/spk-g-package-conformance && python3 tools/validate-research.py validate-report .planning/spikes/spk-g-package-conformance/report.md && python3 -c "from pathlib import Path; t=Path('.planning/spikes/spk-g-package-conformance/impact-fragment.yaml').read_text(); assert all(k in t for k in ('schemaVersion:', 'fragmentId: SPK-G-IMPACT-001', 'sourceIds:', 'proposedOpenQuestions:', 'proposedDriftRecords:', 'compatibilityObservations:', 'affectedRequirements:', 'affectedPhases:', 'uncertainty:', 'evidenceLinks:'))"</automated></verify>
  <acceptance_criteria>Report values identify approved releases and public exports, carry an immutable source baseline, preserve compile/conformance errors as evidence, and the local fragment presents every proposed canonical impact for later serialized validation.</acceptance_criteria>
  <done>Package and conformance recommendations are source/version-compatible observations ready for Plan 01-28 consolidation and subsequent ADR review.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.planning/spikes/spk-g-package-conformance/{metadata.yaml,recipe.md,fixture.md}` — reviewed package/export inventory and disposable consumer contract.
- `.planning/spikes/spk-g-package-conformance/{environment.json,measurements.yaml,report.md}` — exact public-release test evidence and compatibility report.
- `.planning/spikes/spk-g-package-conformance/impact-fragment.yaml` — immutable public-package compatibility, drift, and blocker hand-off for Plan 01-28.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry → consumer fixture | External code enters the local experiment. |
| public package behavior → protocol/architecture claim | Compile or conformance behavior is implementation observation, not upstream authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Tampering | package acquisition | high | mitigate | Human review approves each exact public package/version and isolated install command. |
| T-15-02 | Elevation of Privilege | private import paths | high | mitigate | Fixture rules and review reject private monorepo paths; only public exports can be exercised. |
| T-15-03 | Spoofing | conformance interpretation | high | mitigate | Bind output to source baseline/compatibility IDs and label it implementation observation. |
| T-15-SC | Tampering | package installs | high | mitigate | Blocking human checkpoint records provenance/license/hash-source decisions before install. |
</threat_model>

<verification>
Validate SPK-G before and after the human gate, validate canonical research links, and inspect that fixture imports use only reviewed public exports.
</verification>

<success_criteria>
The project knows which public package surfaces can be consumed, wrapped, avoided, or are blocked at the recorded source baseline.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-15-SUMMARY.md` when done.
</output>
