---
phase: 01-research-and-truth-baseline
plan: 56
type: execute
wave: 2
depends_on: [01-51]
files_modified:
  - tools/acquire-sources.py
  - tests/phase1/test_evidence.py
autonomous: true
gap_closure: true
requirements: [EVID-01, EVID-03, OPER-01]
must_haves:
  truths:
    - "Raw commit:path bytes are cached or represented only when their canonical Git blob object hash equals the exact blobSha already validated in the reviewed commit tree per D-04."
    - "A raw-byte/blob-ID mismatch fails before cache directory creation, cache-file write, successful outcome creation, queue/receipt generation, or canonical evidence publication."
    - "Queue, receipt, acquisition-log history, and observed-candidate manifest are preflighted and published as one exclusive recoverable generation; injected failure exposes either all old bytes or all new bytes."
    - "A rerun after interruption safely recovers then publishes the complete generation, while a rerun after success validates the existing complete immutable generation and performs no transport or rewrite."
    - "The production reviewed-window route accepts only the exact canonical queue `.planning/research/upstream-acquisition-queue.yaml` and receipt `.planning/research/reports/upstream-acquisition-20260728.md`; STATE.md, another research record, aliases, and every other caller path are rejected before lock, transport, or write."
  artifacts:
    - path: tools/acquire-sources.py
      provides: "Canonical Git blob rehashing before cache admission and four-target recoverable acquisition publication."
      contains: "canonical_git_blob_sha1"
    - path: tests/phase1/test_evidence.py
      provides: "Blob mismatch/success, preflight-collision, interruption, recovery, and idempotent-rerun regressions."
      contains: "test_reviewed_collection_rejects_raw_bytes_whose_git_blob_id_mismatches_before_cache"
  key_links:
    - from: "tools/acquire-sources.py::_validate_tree_target"
      to: "tools/acquire-sources.py::collect_immutable_candidate"
      via: "The reviewed target's validated blobSha is passed into the raw-byte collector and rederived from the exact bytes before cache admission."
      pattern: "expected_blob_sha"
    - from: "staged queue/receipt/history/candidate bytes"
      to: "tools/canonical-recovery.py::publish_generation"
      via: "One exclusive acquisition lock protects preflight/snapshot/staging, then the shared journaled publisher exposes the fixed four-target map."
      pattern: "publish_generation"
---

<objective>
Bind reviewed public-source bytes to their validated Git blob IDs and publish all acquisition evidence as one recoverable generation.

Purpose: Prevent a raw endpoint from supplying different bytes for an otherwise valid commit/path and prevent failed/rerun collection from partially mutating immutable acquisition evidence.
Output: Hardened source ingress plus atomic queue/receipt/history/candidate publication with deterministic failure/rerun tests.
</objective>

<execution_context>
@/home/at/.codex/gsd-core/workflows/execute-plan.md
@/home/at/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-45-PLAN.md
@.planning/phases/01-research-and-truth-baseline/01-51-SUMMARY.md
@tools/acquire-sources.py
@tools/canonical-recovery.py
@tests/phase1/test_evidence.py
@.planning/research/upstream-acquisition-queue.yaml
@.planning/research/reports/upstream-acquisition-20260728.md
@.planning/research/acquisition-log.yaml
@.planning/research/candidate-source-manifest.yaml
</context>

<scope_boundary>
Repair only reviewed public-window ingress and its four canonical acquisition outputs. Preserve the exact Plan 01-51 cache root/ownership rules, seven reviewed targets, failed PR211 locator, zero-result NAPS outcome, observed-only authority, PRE118, source/claim records, package/runtime blockers, and no-external-write boundary. Tests use fixtures only; this plan does not perform a live collection or change canonical acquisition records.
</scope_boundary>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Rehash raw bytes as the reviewed Git blob before cache admission</name>
  <read_first>tools/acquire-sources.py `HttpsTransport.blob`, `FixtureTransport`, `collect_immutable_candidate`, `_validate_tree_target`, `REVIEWED_COLLECTION_TARGETS`, and `collect_reviewed_public_window`; tests/phase1/test_evidence.py exact-root collector and candidate tracer tests as repaired by Plan 01-51</read_first>
  <files>tools/acquire-sources.py, tests/phase1/test_evidence.py</files>
  <behavior>
    - Canonical Git object hashing computes SHA-1 over `blob `, the decimal byte length, one NUL byte, and the exact raw bytes.
    - Bytes whose computed object ID equals the reviewed tree entry's lowercase 40-hex blobSha proceed to SHA-256, cache write, and observed-only source record creation.
    - One-byte mutation, truncation, expansion, wrong expected object ID, malformed object ID, or non-bytes response fails before creating the candidate cache directory/file or returning a record.
    - The production reviewed-window caller passes the blobSha from the already validated fixed target; caller-supplied commit/path alone cannot authorize raw bytes.
    - Plan 01-51 exclusive cache-child ownership, unrelated-byte preservation, and collision refusal remain exact.
  </behavior>
  <action>Write mismatch and success tests first using FixtureTransport and a computed fixture blob object ID. Add `canonical_git_blob_sha1(content: bytes) -> str` using stdlib hashlib and the canonical Git blob header. Make `expected_blob_sha` a required keyword argument to `collect_immutable_candidate`; validate its exact lowercase 40-hex shape, rederive the object ID immediately after obtaining byte content, and refuse mismatch before calculating the cache target, creating directories, writing cache bytes, calculating the source SHA-256, or building a source record. Change `collect_reviewed_public_window` to pass each fixed target's blobSha only after `_validate_tree_target` has confirmed the exact commit tree entry. Update every deterministic fixture call, including the Plan 01-51 tracer, to supply the actual Git object ID of its bytes while preserving exact cache-root and cleanup semantics. Do not treat the SHA-1 object ID as a content-authority upgrade; SHA-256 remains the evidence content digest and observed-only classification remains unchanged.</action>
  <verify>
    <automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_evidence.BoundedCollectorTests.test_reviewed_collection_rejects_raw_bytes_whose_git_blob_id_mismatches_before_cache test_evidence.BoundedCollectorTests.test_reviewed_collection_accepts_exact_git_blob_bytes_before_cache test_evidence.BoundedCollectorTests.test_candidate_acquisition_tracer_blocks_incomplete_dimensions test_evidence.BoundedCollectorTests.test_collector_accepts_exact_repository_cache_root -v</automated>
  </verify>
  <acceptance_criteria>Exact object bytes succeed and retain their SHA-256; every object-ID mismatch fails before any candidate cache path or record exists; the repaired tracer and exact cache confinement pass unchanged.</acceptance_criteria>
  <done>Reviewed tree identity and collected raw bytes are cryptographically bound before any cache/source-record side effect.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish queue, receipt, history, and candidates as one recoverable generation</name>
  <read_first>Task 1 collector; tools/acquire-sources.py `_checked_output_path`, `_write_json`, `_append_history`, `_append_observed_candidates`, and `collect_reviewed_public_window`; tools/canonical-recovery.py `publish_generation` and `recover_canonical_generation`; current four acquisition output formats</read_first>
  <files>tools/acquire-sources.py, tests/phase1/test_evidence.py</files>
  <behavior>
    - Before transport, queue/receipt paths are distinct confined paths, cannot alias the fixed history/manifest targets, and every existing target/parent is regular and non-symlink.
    - Production CLI queue and receipt values must equal the two fixed repository-relative canonical paths byte-for-byte after POSIX lexical normalization; repository confinement alone is insufficient.
    - Copied-root/custom-path support exists only in a private direct-call test seam that requires an explicit test capability unavailable from CLI, config, or environment and refuses activation against the production repository root.
    - Passing `.planning/STATE.md`, `.planning/research/claims.yaml`, either canonical path in the wrong role, or any other regular confined path as queue/receipt makes zero transport calls and leaves every probed file byte-identical.
    - Existing complete queue/receipt output for the identical reviewed binding validates with its matching history/candidate generation and returns idempotent success with zero transport calls and byte-identical files.
    - Missing/partial output, a conflicting immutable receipt, duplicate/ambiguous candidate block, unsafe path, or lock contention refuses before transport or write.
    - First publication stages parseable queue/receipt plus deterministic additive history/candidate bytes, then journal-publishes exactly four target paths under one exclusive acquisition lock.
    - Failure before publication leaves all old bytes; interruption after every replacement recovers all old bytes; a retry publishes all new bytes; no reader-observable mixed four-file generation is accepted.
  </behavior>
  <action>Addresses external Codex review concern: arbitrary regular confined `--queue` and `--receipt` values could replace unrelated canonical files. Refactor JSON/history/candidate writers into pure byte renderers. Define immutable production constants for queue `.planning/research/upstream-acquisition-queue.yaml`, receipt `.planning/research/reports/upstream-acquisition-20260728.md`, history `.planning/research/acquisition-log.yaml`, and candidates `.planning/research/candidate-source-manifest.yaml`. At the production CLI boundary, POSIX-lexically normalize the two caller values without following symlinks and require exact equality with their role-specific constants before acquiring a lock, inspecting output contents, invoking transport, or rendering bytes; swapping queue/receipt or naming any other confined regular file is a hard refusal. Do not expose a root/path override through argparse, environment, or config. Preserve copied-root fixtures through a private direct-call helper that accepts a module-private test capability object, requires the supplied root not resolve to production ROOT, and is never called by the CLI route; label it test-only in name/docstring. Add rejection probes using `.planning/STATE.md`, `.planning/research/claims.yaml`, and swapped canonical queue/receipt values, record hashes/listings for all four canonical outputs plus each decoy, assert zero transport, and require exact byte equality. Add fixed four-target generation map from canonical queue/receipt plus fixed history/candidate paths. Acquire an fcntl exclusive lock at a fixed ignored `.research/upstreams` child after exact-path refusal and retain it through collision preflight, snapshot, bounded transport, staging validation, journaled publication, and final rehash. Preflight all four paths with lstat, require distinct regular non-symlink targets or allowed absent canonical queue/receipt, and refuse partial/conflicting existing generation before transport. If a complete existing generation carries identical reviewed binding and validates all cross-links, return byte-preserving idempotent success without transport. Otherwise collect outcomes, render complete generation in memory, reparse/validate queue and receipt, require deterministic unique history/candidate identities, and call `canonical_recovery.publish_generation(ROOT, generation)` once. Provide private test injection for each replacement; recover and require complete old digest map after interruption, complete new map after retry, then a no-op rerun. Never call old sequential mutation helpers from production. Apply D-09, D-13, D-20, and D-36; publication changes no evidence meaning or authority.</action>
  <verify>
    <automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_evidence.BoundedCollectorTests.test_reviewed_collection_production_route_requires_exact_canonical_queue_and_receipt test_evidence.BoundedCollectorTests.test_reviewed_collection_rejects_state_or_other_confined_output_before_transport test_evidence.BoundedCollectorTests.test_reviewed_collection_test_path_seam_cannot_target_production_root test_evidence.BoundedCollectorTests.test_reviewed_collection_preflights_collisions_before_transport test_evidence.BoundedCollectorTests.test_reviewed_collection_generation_recovers_after_each_replacement_and_rerun test_evidence.BoundedCollectorTests.test_reviewed_collection_success_and_idempotent_rerun_publish_one_complete_generation -v</automated>
  </verify>
  <acceptance_criteria>Only exact role-specific canonical queue/receipt paths reach production collection; STATE.md, claims.yaml, swapped-role, alias, and other confined paths make zero transport calls and preserve every byte; the copied-root seam cannot target production ROOT; collision/partial cases preserve bytes; every injected replacement failure recovers exact old generation; retry publishes all new files; post-success rerun performs no transport or rewrite.</acceptance_criteria>
  <done>Canonical acquisition evidence can no longer be partially updated by a failure or rerun.</done>
</task>

</tasks>

## Artifacts this phase produces

| Path | New symbol/test | Contract |
|---|---|---|
| `tools/acquire-sources.py` | `canonical_git_blob_sha1` and required `expected_blob_sha` ingress | Refuses raw bytes not belonging to the reviewed Git tree blob before cache/record creation. |
| `tools/acquire-sources.py` | Pure four-target renderer plus exclusive recoverable publication path | Preflights, stages, validates, journal-publishes, recovers, and rehashes queue/receipt/history/candidate bytes together. |
| `tools/acquire-sources.py` | Exact production output constants plus private copied-root test seam | Prevents arbitrary confined paths from becoming acquisition publication targets. |
| `tests/phase1/test_evidence.py` | Blob-binding regressions | Proves exact bytes succeed and mismatches have zero cache/record side effects. |
| `tests/phase1/test_evidence.py` | Generation failure/rerun regressions | Proves preflight refusal, old-or-new recovery, successful retry, and idempotent completed rerun. |
| `tests/phase1/test_evidence.py` | Canonical-path rejection probes | Proves STATE.md, another research record, swapped roles, and production-root test-seam use refuse before transport/write. |

No live public read, source/claim promotion, package action, schema change, review acceptance, or canonical acquisition-data edit is produced by this plan.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| reviewed Git tree metadata → raw HTTPS bytes | A commit/path endpoint can return bytes that do not match the reviewed blob object. |
| collected outcomes → four canonical acquisition files | Sequential writes can expose or retain a mixed immutable-evidence generation. |
| concurrent/rerun collector → existing immutable history | A second writer can collide, overwrite, duplicate, or delete a prior generation. |
| caller output arguments → canonical repository files | A merely confined regular path can redirect publication over unrelated project state. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-56-01 | Tampering | raw commit:path content | high | mitigate | Recompute canonical Git blob SHA-1 and require equality with the reviewed tree blob before any cache or record side effect. |
| T-01-56-02 | Tampering | four-file acquisition publication | high | mitigate | Render/validate in memory, journal one exact fixed target map, inject each replacement failure, and recover only a complete generation. |
| T-01-56-03 | Denial of Service | collision/concurrent collection | medium | mitigate | Exclusive lock plus pre-transport collision/idempotency checks prevent destructive retries and lost updates. |
| T-01-56-04 | Repudiation | additive acquisition history | high | mitigate | Require queue/receipt binding, outcomes, history entries, and candidate rows to cross-validate before publication and after recovery. |
| T-01-56-05 | Tampering | production queue/receipt selection | high | mitigate | Require exact role-specific canonical paths before lock/transport/write; keep copied-root override test-only and prove STATE.md/claims/swapped-role refusal with unchanged hashes. |

No package-manager install occurs; supply-chain package legitimacy gate is not applicable.
</threat_model>

<verification>
Run object-hash ingress tests, Plan 01-51 cache cluster, canonical queue/receipt rejection probes, then four-generation collision/interruption/retry/idempotency tests through the approved wrapper.
</verification>

<success_criteria>
Only raw bytes belonging to the reviewed Git blob enter cache/evidence; only exact canonical queue/receipt paths enter production publication; every rejected decoy remains unchanged with zero transport; and every accepted publication/rerun preserves one complete cross-validated queue/receipt/history/candidate generation.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-56-SUMMARY.md` when done.
</output>
