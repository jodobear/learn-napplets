---
phase: 01-research-and-truth-baseline
plan: 35
type: execute
wave: 7
depends_on: [01-34, 01-41]
files_modified:
  - tools/validate-research.py
  - .planning/research/schemas/compatibility.schema.json
  - .planning/research/compatibility-matrix.yaml
  - tests/phase1/test_compatibility.py
  - tools/migrate-phase1-records.py
  - .planning/research/schemas/legacy/compatibility.schema.v1.json
  - .planning/research/schemas/fixtures/compatibility-v1-legacy.yaml
  - .planning/research/schemas/fixtures/compatibility-v2-current.yaml
  - .planning/research/schemas/migration-notes.md
autonomous: true
gap_closure: true
requirements: [EVID-01, EVID-02, EVID-03, OPER-01]
must_haves:
  truths:
    - "Compatibility eligibility is evaluated substantively across normative protocol, observed implementation, published package, runtime, example/fixture, current-work, and conformance dimensions."
    - "A blocked row remains blocked when any qualified dimension or required reviewer approval is missing; structural field presence alone cannot produce an eligible baseline."
    - "The evaluator returns ordered substantive missing/invalid reasons and retains a blocked baseline when a qualified dimension or approval is absent."
  artifacts:
    - path: .planning/research/schemas/compatibility.schema.json
      provides: "Typed substantive compatibility-dimension contract"
    - path: .planning/research/compatibility-matrix.yaml
      provides: "CMP-BASELINE-001 plus observed-source/release-commit classification"
    - path: tools/validate-research.py
      provides: "Semantic eligibility evaluator"
  key_links:
    - from: CMP-BASELINE-001 dimensions
      to: SRC, CLM, package evidence, SPK-G, and OQ records
      via: "source-class-aware semantic evaluation rather than field presence"
    - from: baseline eligibility result
      to: Plan 01-36 SPK-G runner
      via: "ordered qualified-or-blocked result consumed before a package operation can be constructed"
  prohibitions:
    - statement: "Compatibility work may not label a project-policy/archive source, merged implementation, release PR, or local fixture as qualified normative protocol, package admission, runtime conformance, or architecture approval."
      category: evidence-separation
      status: unresolved
      flagged: true
      verification: "Eligibility tests assert blocked/no approval for every incomplete or wrong-authority dimension."
---

<objective>
Build the substantive compatibility schema and evaluator that refreshed source and package evidence require before append-only observed-refresh publication.

Purpose: Address the EVID-03 source-baseline gap with a real eligibility model instead of structural placeholders; Plan 01-43 independently publishes observed-refresh records.
Output: A dimensioned compatibility matrix, evaluator, and deterministic v1-to-v2 compatibility migration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-RESEARCH.md
@.planning/research/{source-registry.yaml,claims.yaml,compatibility-matrix.yaml,drift-register.yaml,open-questions.yaml,open-work-snapshot.json,package-evidence.yaml,package-map.md}
@.planning/research/reports/upstream-acquisition-20260728.md
@.planning/phases/01-research-and-truth-baseline/01-41-SUMMARY.md
@.planning/research/reports/upstream-refresh-synthesis-2026-07-28.md
@.planning/spikes/spk-g-package-conformance/{metadata.yaml,recipe.md,report.md}
@.planning/adr/0010-package-versioning.md
@tools/validate-research.py
@tests/phase1/test_compatibility.py
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Migrate compatibility v1 to v2 without evaluating eligibility</name>
  <read_first>compatibility.schema.json; compatibility-matrix.yaml; tests/phase1/test_compatibility.py; tools/migrate-phase1-records.py; schemas/migration-notes.md</read_first>
  <reversibility rating="costly">The version-2 compatibility shape becomes the stored contract consumed by later eligibility and Phase 2 planning, so later changes require another explicit migration.</reversibility>
  <files>.planning/research/schemas/compatibility.schema.json, .planning/research/compatibility-matrix.yaml, tests/phase1/test_compatibility.py, tools/migrate-phase1-records.py, .planning/research/schemas/legacy/compatibility.schema.v1.json, .planning/research/schemas/fixtures/compatibility-v1-legacy.yaml, .planning/research/schemas/fixtures/compatibility-v2-current.yaml, .planning/research/schemas/migration-notes.md</files>
  <behavior>
    - `test_compatibility_v1_to_v2_migration` starts RED and proves malformed v1 input creates no output, valid migration is byte-stable and idempotent, every CMP/SRC/CLM/DRF/OQ reference and historical observation is retained, and ordinary validation accepts only v2 after migration.
    - Migration creates closed named dimension records for `normativeProtocol`, `observedImplementation`, `publishedPackage`, `runtime`, `exampleFixture`, `currentWork`, and `conformance`, but assigns no qualified or approved eligibility result.
  </behavior>
  <action>Perform only schema/data migration. Per D-30 and D-32, retain `schemas/legacy/compatibility.schema.v1.json` plus immutable legacy/current fixture pairs and preserve every stable CMP/SRC/CLM/DRF/OQ reference and historical observation. Add `migrate_compatibility_v1_to_v2()` and CLI `tools/phase1-python tools/migrate-phase1-records.py migrate-compatibility --input <v1> --output <v2>`; reject malformed legacy input before output, require deterministic byte-stable idempotent output, and make ordinary validation reject v1 after migration. Extend the schema only enough to represent the seven closed dimension records with stable references, status, evidence-class/authority expectation, locator/digest relation, and explicit missing reason. Set migrated dimensions to truthful blocked/not-reviewed states from existing records; do not resolve references, evaluate authority, decide eligibility, or grant approval. Append exact `1 -> 2` command, fixture SHA-256 values, field-meaning change, compatibility, and rollback notes.</action>
  <verify><automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_compatibility.CompatibilitySchema.test_compatibility_v1_to_v2_migration && tools/phase1-python tools/migrate-phase1-records.py migrate-compatibility --input .planning/research/schemas/fixtures/compatibility-v1-legacy.yaml --output /tmp/compatibility-v2.yaml</automated></verify>
  <acceptance_criteria>The exact migration test exists and passes; a missing method fails the wrapper invocation; v2 output is deterministic, idempotent, complete, current-format-only, and carries no synthesized eligibility or approval.</acceptance_criteria>
  <done>The compatibility records have a retained v1-to-v2 migration independent of semantic eligibility policy.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Evaluate seven substantive dimensions to ordered eligible-or-blocked truth</name>
  <read_first>Task 1 migrated v2 fixtures and notes; compatibility.schema.json; compatibility-matrix.yaml; source-registry.yaml; claims.yaml; package-evidence.yaml; tools/validate-research.py; tests/phase1/test_compatibility.py; 01-RESEARCH.md §Upstream Merge Refresh</read_first>
  <reversibility rating="costly">Eligibility reason codes and authority rules become the Phase 1 decision contract consumed by package measurement and final verification.</reversibility>
  <files>tools/validate-research.py, .planning/research/compatibility-matrix.yaml, tests/phase1/test_compatibility.py</files>
  <behavior>
    - `test_baseline_eligibility_requires_all_substantive_dimensions_without_approval` starts RED and tests normative protocol, observed implementation, published package, runtime, example/fixture, current work, and conformance individually and as a complete set.
    - The evaluator rejects empty, duplicate, unknown, incomplete, wrong-authority, source-release-only, policy-only, stale, and pending-review dimensions; it returns ordered missing/invalid reason codes and `blocked` until every required condition is qualified.
    - A fully populated synthetic fixture may be structurally valid while remaining `approval: not-approved`; only a separate reviewed record can express eligibility, and neither result accepts architecture or an ADR.
  </behavior>
  <action>Start separate RED substantive-dimension fixtures against Task 1's migrated v2 contract; do not modify migration code or legacy fixtures. Add ordered `baselineEligibility` result/reasons. Implement `evaluate_compatibility_baseline()` in `tools/validate-research.py` to resolve every cited SRC/CLM/package/SPK/OQ ID, check exact pin agreement, reject policy/archive or observed implementation in the normative slot, reject release PR/source-only evidence in the package slot, require independent runtime/example/fixture/conformance evidence, and require the appropriate human review state before eligibility. Update `CMP-BASELINE-001` from actually collected records. Missing qualified inputs must yield `blocked` with exact dimension/reason/impact ordering; structural completion cannot override it. Implement D-05, D-12, D-14 through D-20, D-29 through D-31, and ADR-0010's release-versus-source rule.</action>
  <verify><automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_compatibility.CompatibilitySchema.test_baseline_eligibility_requires_all_substantive_dimensions_without_approval && tools/phase1-python tools/validate-research.py validate --root .planning/research --report /tmp/phase1-substantive-compatibility.md</automated></verify>
  <acceptance_criteria>The exact eligibility test proves each dimension is substantive, every negative mutation yields an ordered blocked reason, and `CMP-BASELINE-001` cannot become eligible merely because fields are populated.</acceptance_criteria>
  <done>A maintainer can inspect precisely why the migrated compatibility baseline is eligible or blocked across all seven evidence dimensions.</done>
</task>
</tasks>

## Artifacts this phase produces
- `tools/validate-research.py` — substantive `evaluate_compatibility_baseline()` semantic gate.
- `.planning/research/schemas/compatibility.schema.json` — exact seven-dimension compatibility contract.
- `.planning/research/compatibility-matrix.yaml` — evaluated baseline and observed-source/release-commit records.
- `tools/migrate-phase1-records.py` — `migrate_compatibility_v1_to_v2()` and deterministic `migrate-compatibility` CLI.
- `.planning/research/schemas/{legacy/compatibility.schema.v1.json,fixtures/compatibility-v1-legacy.yaml,fixtures/compatibility-v2-current.yaml,migration-notes.md}` — retained v1 contract, immutable fixtures, checksums, and change record.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| source/package evidence → compatibility dimension | Correct-looking metadata can carry wrong authority or incomplete eligibility. |
| compatibility status → architecture decision | A structurally complete matrix could otherwise falsely unblock architecture. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-35-01 | Spoofing | normative protocol dimension | high | mitigate | Task 2 requires qualified authoritative source and review, not implementation code. |
| T-35-02 | Tampering | published package dimension | high | mitigate | Task 2 rejects source-only release metadata and requires independent registry evidence. |
| T-35-03 | Tampering | compatibility eligibility | high | mitigate | Task 2 uses source-aware semantic evaluation and deterministic reason codes. |
| T-35-04 | Elevation of Privilege | package/conformance execution | high | mitigate | Task 2 requires an approved substantive result; Plan 01-36 retains the human approval gate and operation guard. |
| T-35-05 | Repudiation | architecture approval | high | mitigate | Task 2 keeps eligibility output separate from ADR/phase approval.
</threat_model>

<verification>
Run the exact named substantive eligibility and v1-to-v2 migration probes, the migration CLI, and canonical research validator through the wrapper.
</verification>

<success_criteria>
EVID-03 is measured against a substantive eligible-baseline model; any missing qualified source or evidence remains a traceable blocker rather than a placeholder pass, and Plan 01-43 publishes observed refresh history separately.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-35-SUMMARY.md` when done.
</output>
