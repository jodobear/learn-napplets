---
phase: 01-research-and-truth-baseline
plan: 37
type: execute
wave: 7
depends_on: [01-32, 01-33, 01-34, 01-35, 01-36, 01-42, 01-43]
files_modified:
  - tests/phase1/test_closeout_commands.py
autonomous: true
gap_closure: true
requirements: [EVID-01, EVID-02, EVID-03, EVID-04, OPER-01, OPER-03]
must_haves:
  truths:
    - "A fresh independent review bound to the exact active 43-plan manifest passes before the full Phase 1 suite, exact research validator, report validator, recovery/replay, and completion gate run through the approved wrapper with no ambient interpreter fallback."
    - "A final gate failure remains visible as a command-specific blocker and cannot be converted to a qualified pass."
  artifacts:
    - path: tests/phase1/test_closeout_commands.py
      provides: "Exact supported closeout command contract and nonzero propagation regression"
  key_links:
    - from: tests/phase1/test_closeout_commands.py
      to: tools/phase1-python and Phase 1 validators
      via: "exact argv contract for final command sequence"
  prohibitions:
    - statement: "Full-gate execution does not update verification status, accept residual risk, alter ADRs, or create a production scaffold."
      category: authority
      status: unresolved
      flagged: true
      verification: "Only Plan 01-40 may write verifier/state output after all gates are observed."
---

<objective>
Run the full repaired Phase 1 gate sequence and prove its exact wrapper-based command contract before Nyquist review.

Purpose: Detect integration failures across toolchain, provenance, replay, recovery, compatibility, and completion instead of relying on focused tests.
Output: One deterministic closeout-command test plus observed full-gate results.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-research-and-truth-baseline/01-VERIFICATION.md
@.planning/phases/01-research-and-truth-baseline/01-REVIEWS.md
@tools/phase1-python
@tools/verify-phase1-toolchain.py
@tools/validate-planning.py
@tools/validate-research.py
@tests/phase1/{test_preflight.py,test_evidence.py,test_drift.py,test_compatibility.py,test_spikes.py,test_spike_consolidation.py}
@.planning/research/compatibility-matrix.yaml
@.planning/spikes/replay-manifest.yaml
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Prove the exact bound-review-first wrapper-only closeout command sequence and failure propagation</name>
  <read_first>01-REVIEWS.md; tools/phase1-python; tools/verify-phase1-toolchain.py; tools/validate-planning.py; tools/validate-research.py; tests/phase1/test_preflight.py; tests/phase1/test_spike_consolidation.py</read_first>
  <files>tests/phase1/test_closeout_commands.py</files>
  <behavior>
    - `test_supported_closeout_sequence_uses_approved_wrapper` asserts the exact ordered commands begin with a bound-review `--phase-1-execution-preflight --executor-identity` invocation, then toolchain verification, full tests, `validate --root .planning/research --report /tmp/...`, `validate-reports --root .planning`, recovery, replay, and `--phase-1-complete --executor-identity <GSD_EXECUTOR_ID>`; every Python command begins with `tools/phase1-python`.
    - `test_closeout_sequence_stops_on_nonzero_stage` proves a failed command remains identified by stage and prevents later success/verification output.
  </behavior>
  <action>Addresses review concern: make the independent active-plan review a fail-closed prerequisite. Write the two named tests first. Model final commands as explicit argv arrays, not shell concatenation, and assert the first stage is `tools/phase1-python tools/validate-planning.py --phase-1-execution-preflight --executor-identity <GSD_EXECUTOR_ID>`, with a nonempty executor identity that differs from the manifest reviewer. Assert every subsequent Python stage begins with repository `tools/phase1-python`. Include the exact supported `validate --root .planning/research --report /tmp/phase1-final-validation.md` and `validate-reports --root .planning` forms. Use injected subprocess fakes to prove a stale/mismatched review or any nonzero stage yields the stable stage identifier and prevents later commands, while successful fake results preserve order. The test must not run a package manager, write canonical research artifacts, or update phase state. This establishes the full-gate tracer contract after CR-01 through CR-09 and WR-01 through WR-03 remediation.</action>
  <verify><automated>tools/phase1-python -m unittest discover -s tests/phase1 -p 'test_closeout_commands.py'</automated></verify>
  <acceptance_criteria>The exact supported command vector is captured in a passing test, no ambient executable is accepted, and failure propagation is deterministic and blocking.</acceptance_criteria>
  <done>The final gate has a testable, wrapper-enforced execution contract.</done>
</task>

<task type="auto">
  <name>Task 2: Execute all repaired Phase 1 closeout gates after the bound-review preflight</name>
  <precondition>`GSD_EXECUTOR_ID` is set to the executing identity and differs from the independent reviewer identity recorded in the fresh all-43-plan review manifest.</precondition>
  <read_first>01-REVIEWS.md; tests/phase1/test_closeout_commands.py; tools/phase1-python; tools/validate-planning.py; tools/validate-research.py; replay-manifest.yaml; 01-VALIDATION.md; 01-SECURITY.md if present</read_first>
  <files>none — command-only verification; write only /tmp/phase1-final-validation.md</files>
  <action>Addresses review concern: execute the fail-closed scope-bound authorization before any closeout evidence. Execute the successful sequence from Task 1 in this exact order: `tools/phase1-python tools/validate-planning.py --phase-1-execution-preflight --executor-identity "$GSD_EXECUTOR_ID"`; `tools/phase1-python --verify-toolchain`; `tools/phase1-python -m unittest discover -s tests/phase1`; `tools/phase1-python tools/validate-research.py validate --root .planning/research --report /tmp/phase1-final-validation.md`; `tools/phase1-python tools/validate-research.py validate-reports --root .planning`; `tools/phase1-python tools/validate-research.py recover-consolidation --research .planning/research`; `tools/phase1-python tools/validate-research.py replay-spikes --manifest .planning/spikes/replay-manifest.yaml --check`; and `GSD_EXECUTOR_ID="$GSD_EXECUTOR_ID" tools/phase1-python tools/validate-planning.py --phase-1-complete --executor-identity "$GSD_EXECUTOR_ID"`. Capture compact command/exit/result evidence for the later Nyquist and verification plans. On any nonzero result, stop, retain the precise stage and diagnostic as an unresolved blocker, and do not edit canonical evidence to make the command pass.</action>
  <verify><automated>test -n "$GSD_EXECUTOR_ID" && GSD_EXECUTOR_ID="$GSD_EXECUTOR_ID" tools/phase1-python tools/validate-planning.py --phase-1-execution-preflight --executor-identity "$GSD_EXECUTOR_ID" && tools/phase1-python --verify-toolchain && tools/phase1-python -m unittest discover -s tests/phase1 && tools/phase1-python tools/validate-research.py validate --root .planning/research --report /tmp/phase1-final-validation.md && tools/phase1-python tools/validate-research.py validate-reports --root .planning && tools/phase1-python tools/validate-research.py recover-consolidation --research .planning/research && tools/phase1-python tools/validate-research.py replay-spikes --manifest .planning/spikes/replay-manifest.yaml --check && GSD_EXECUTOR_ID="$GSD_EXECUTOR_ID" tools/phase1-python tools/validate-planning.py --phase-1-complete --executor-identity "$GSD_EXECUTOR_ID"</automated></verify>
  <acceptance_criteria>The independent manifest preflight completes before every other closeout command; every required command exits zero and its result is captured for the next plan, or the first nonzero command and exact impact are retained as blockers without an artificial success state.</acceptance_criteria>
  <done>The entire repaired closeout has executable integration evidence suitable for Nyquist analysis.</done>
</task>
</tasks>

## Artifacts this phase produces
- `tests/phase1/test_closeout_commands.py` — wrapper-only closeout order and failure-propagation contract.
- `/tmp/phase1-final-validation.md` — transient research-validator result used as execution evidence.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| individual remediation → integrated closeout | A focused fix can fail when composed with recovery, replay, or completion. |
| closeout command → interpreter/toolchain | Ambient execution can bypass integrity checks. |
| nonzero result → phase readiness | A failure can be hidden by continuing or recording a qualified pass. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-37-01 | Tampering | closeout execution environment | high | mitigate | Task 1 and Task 2 require wrapper argv for every stage. |
| T-37-02 | Denial of Service | recovery/replay integration | high | mitigate | Task 2 runs recovery and replay before completion and stops on failure. |
| T-37-03 | Repudiation | command result evidence | high | mitigate | Task 2 captures each stage's compact exit/result for downstream review. |
| T-37-04 | Spoofing | green closeout status | high | mitigate | Failure propagation test prevents later success claims after a failed stage. |
</threat_model>

<verification>
Run the command-contract test and then the exact complete command sequence shown in Task 2.
</verification>

<success_criteria>
The final toolchain is exercised as one supported pipeline, with all failures honestly preserved for Nyquist rather than bypassed.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-37-SUMMARY.md` when done.
</output>
