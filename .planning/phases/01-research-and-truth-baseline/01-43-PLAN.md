---
phase: 01-research-and-truth-baseline
plan: 43
type: execute
wave: 9
depends_on: [01-35, 01-36, 01-42]
files_modified:
  - tools/validate-research.py
  - tests/phase1/test_consolidation_recovery.py
  - .planning/research/claims.yaml
  - .planning/research/drift-register.yaml
  - .planning/research/open-questions.yaml
  - .planning/research/package-map.md
  - .planning/research/open-work-snapshot.json
  - .planning/.observed-refresh-staging/phase-01/claims.yaml
  - .planning/.observed-refresh-staging/phase-01/drift-register.yaml
  - .planning/.observed-refresh-staging/phase-01/open-questions.yaml
  - .planning/.observed-refresh-staging/phase-01/package-map.md
  - .planning/.observed-refresh-staging/phase-01/open-work-snapshot.json
  - .planning/.observed-refresh-staging/phase-01/observed-refresh-validation-attestation.json
autonomous: true
gap_closure: true
requirements: [EVID-01, EVID-02, EVID-03, OPER-01]
must_haves:
  truths:
    - "Reviewed observed upstream refresh evidence is appended with real immutable locators and uncertainty without replacing a normative side, discovery history, or old snapshot."
    - "The compatibility baseline and SPK-G remain blocked whenever qualified normative, package, runtime, example, fixture, conformance, or approval evidence is absent."
    - "The five related observed-refresh records become visible only as one validated digest-bound canonical generation, and every multi-file reader obtains one complete shared-lock snapshot of the old or new generation rather than a mixture."
    - "Before staging, Plan 01-45's queue/receipt must validate against Plan 01-29's reviewed source-input map; every appended observed relation preserves that reviewed commit/four-report digest binding and no mutable refresh report is parsed."
  artifacts:
    - path: .planning/research/claims.yaml
      provides: "Append-only observed source relations and retained blocking claim state"
    - path: .planning/research/drift-register.yaml
      provides: "Parallel observed implementation history with unchanged normative sides and OQ routing"
    - path: .planning/research/open-work-snapshot.json
      provides: "New dated current-work observation that retains OWS-001"
    - path: tools/validate-research.py
      provides: "Observed-refresh staged-set validation attestation bound to five exact target paths and SHA-256 values"
    - path: tests/phase1/test_consolidation_recovery.py
      provides: "Five-record hard-interruption, journal-tampering, staged-validation CLI, live-hash, and registered-reader recovery regressions"
    - path: .planning/.observed-refresh-staging/phase-01/observed-refresh-validation-attestation.json
      provides: "Digest-bound observed-refresh staged validation attestation, including reviewed-source input provenance, consumed by Plan 01-42's tested publisher CLI"
  key_links:
    - from: Plan 01-29/01-45 reviewed-source binding and Plan 01-35 evaluator
      to: claims, drift, OQ, package map, and snapshot records
      via: "validated receipt provenance plus append-only locator/digest/uncertainty history and explicit blocked compatibility routing"
    - from: staged observed-refresh validation attestation
      to: five live canonical observed-refresh records
      via: "Plan 01-42 `observed-refresh` profile rehashes the exact staged target map, journals old/new digests under writer lock, atomically publishes, and supplies every registered reader a shared-lock complete snapshot"
---

<objective>
Append reviewed observed-refresh records and retain every unresolved compatibility and conformance blocker after schema and recovery foundations pass.

Purpose: Publish the current upstream observations as one durable, recovery-safe canonical generation after the v2 evaluator and shared transaction boundary pass.
Output: Deterministically ordered append-only observed sides, blocker routing, package comparison, dated snapshot, staged validation attestation, and journaled publication.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-35-SUMMARY.md
@.planning/phases/01-research-and-truth-baseline/01-36-SUMMARY.md
@.planning/phases/01-research-and-truth-baseline/01-42-SUMMARY.md
@tools/canonical-recovery.py
@tests/phase1/test_consolidation_recovery.py
@.planning/research/{source-registry.yaml,claims.yaml,compatibility-matrix.yaml,drift-register.yaml,open-questions.yaml,package-evidence.yaml,package-map.md,open-work-snapshot.json,upstream-acquisition-queue.yaml}
@.planning/research/reports/upstream-acquisition-20260728.md
@tools/acquire-sources.py
@.planning/spikes/spk-g-package-conformance/{metadata.yaml,recipe.md,measurements.yaml,report.md,impact-fragment.yaml}
@.planning/adr/0010-package-versioning.md
@tools/validate-research.py
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Stage and atomically publish one reviewed observed-refresh generation while preserving blocked normative counterparts</name>
  <precondition>Plan 01-35 has produced a valid v2 compatibility evaluator, Plan 01-42 has completed recovery-before-read enforcement, and the Plan 01-45 queue/receipt passes reviewed-acquisition validation; reviewer classifications from Plan 01-30 remain available.</precondition>
  <read_first>01-29-SUMMARY.md; 01-35-SUMMARY.md; 01-36-SUMMARY.md; 01-42-SUMMARY.md; 01-REVIEWS.md; tools/canonical-recovery.py; tools/validate-research.py; tools/acquire-sources.py; tests/phase1/test_consolidation_recovery.py; claims.yaml; drift-register.yaml; open-questions.yaml; package-map.md; open-work-snapshot.json; source-registry.yaml; compatibility-matrix.yaml; package-evidence.yaml; upstream-acquisition-queue.yaml; upstream-acquisition-20260728.md; ADR 0010; SPK-G metadata; SPK-G measurements; SPK-G report; SPK-G impact fragment</read_first>
  <files>tools/validate-research.py, tests/phase1/test_consolidation_recovery.py, .planning/research/claims.yaml, .planning/research/drift-register.yaml, .planning/research/open-questions.yaml, .planning/research/package-map.md, .planning/research/open-work-snapshot.json, .planning/.observed-refresh-staging/phase-01/claims.yaml, .planning/.observed-refresh-staging/phase-01/drift-register.yaml, .planning/.observed-refresh-staging/phase-01/open-questions.yaml, .planning/.observed-refresh-staging/phase-01/package-map.md, .planning/.observed-refresh-staging/phase-01/open-work-snapshot.json, .planning/.observed-refresh-staging/phase-01/observed-refresh-validation-attestation.json</files>
  <scope_ownership>Task 1 owns one five-target atomic publication transaction. Each staged/live pair—staged `claims.yaml` to live `claims.yaml`, staged `drift-register.yaml` to live `drift-register.yaml`, staged `open-questions.yaml` to live `open-questions.yaml`, staged `package-map.md` to live `package-map.md`, and staged `open-work-snapshot.json` to live `open-work-snapshot.json`—is part of Plan 01-42's fixed `observed-refresh` profile. The staged overlay validates all five together against one shared-lock snapshot; the attestation binds all five final bytes; then the publisher rehashes, journals, and exposes the same five as one generation. Splitting staged validation, individual record updates, or publication into separate plans/commits would create an unattested partial generation and invalidate the proof that every reader saw one complete old-or-new snapshot.</scope_ownership>
  <behavior>
    - `test_observed_refresh_five_file_generation_recovers_after_each_replacement` stages a complete changed five-file observed-refresh set, injects a hard interruption after each replacement, and proves every subsequent registered canonical reader obtains either the complete digest-verified old generation or the complete digest-verified new generation.
    - `test_observed_refresh_reader_snapshot_waits_out_successful_publish` pauses a registered five-file reader after its first snapshot file, starts a successful observed-refresh publisher, proves the publisher waits for the shared reader lock, and proves the returned mapping is entirely old or entirely new.
    - `test_observed_refresh_rejects_tampered_or_incomplete_attestation_before_reader_entry` proves an extra, missing, aliased, altered, or journal-tampered staged set is refused before publication and before any registered reader parses canonical evidence.
    - `test_observed_refresh_staged_validation_binds_exact_five_target_digests` proves staged validation uses the ordinary schema/semantic checks against the five-file overlay and emits an attestation whose `observed-refresh` profile, sorted target paths, and SHA-256 values equal the staged regular-file bytes.
    - `test_observed_refresh_staged_validation_cli_contract` invokes `validate-observed-refresh-staged --help` and proves its required `--staged-root` and `--attestation` paths accept only the exact five regular non-symlink staged records, reject any missing/extra/aliased/changed file or failed semantic overlay, and create no attestation on refusal.
    - `test_live_observed_refresh_generation_matches_attestation` is read-only after the actual publisher invocation and proves each of the five live canonical paths has the SHA-256 attested for the staging-root target map.
    - `test_observed_refresh_rejects_unbound_reviewed_acquisition_provenance` proves a missing, altered, or differently committed Plan 01-45 receipt/queue binding refuses staged validation before a live canonical reader parses evidence or a publisher receives an attestation.
  </behavior>
  <action>Addresses Codex HIGH source-grounding finding and review finding H2 dependency propagation: before staging or parsing any refreshed observation, run `tools/acquire-sources.py validate-reviewed-acquisition --queue .planning/research/upstream-acquisition-queue.yaml --receipt .planning/research/reports/upstream-acquisition-20260728.md --review .planning/phases/01-research-and-truth-baseline/01-REVIEWS.md`; an absent, altered, or differently committed source-input binding returns nonzero before `read_canonical_snapshot()` or staged validation. Start RED in `tests/phase1/test_consolidation_recovery.py` with a copied planning root, a complete old generation, a complete changed staged generation, every deterministic replacement interruption point, journal-tampering fixtures, and `test_observed_refresh_rejects_unbound_reviewed_acquisition_provenance`. After Plan 01-35 semantic validation passes, consume Plan 01-36's validated SPK-G `measurements.yaml`, `report.md`, and `impact-fragment.yaml`, then derive all five changed records in the exact same-filesystem staging root `.planning/.observed-refresh-staging/phase-01/`: `claims.yaml`, `drift-register.yaml`, `open-questions.yaml`, `package-map.md`, and `open-work-snapshot.json`. The staging root contains exactly those five regular non-symlink files plus `observed-refresh-validation-attestation.json`; do not modify any live target directly or serially. Extend `tools/validate-research.py` with the owned `validate-observed-refresh-staged --staged-root PATH --attestation PATH` route; its help exposes both required `Path` arguments, it registers the route and acquires Plan 01-42's shared-lock `read_canonical_snapshot()` for every live canonical input before overlay, rejects missing/extra/aliased/non-regular staged targets before overlay, overlays only the exact five staged regular files on that complete in-memory canonical snapshot, applies the ordinary schema and semantic checks, and writes no attestation on a nonzero result. On success it emits the attestation at `PATH`, containing profile `observed-refresh`, the exact sorted five repository-relative targets, each staged SHA-256, the validated SPK-G outcome/impact-fragment digest bindings, and the unchanged Plan 01-29 reviewed commit/four-report digest map copied from the validated Plan 01-45 receipt. Execute the actual Plan 01-42 publisher after staged validation: `tools/phase1-python tools/canonical-recovery.py publish-validated-canonical-set --profile observed-refresh --staged-root .planning/.observed-refresh-staging/phase-01 --attestation .planning/.observed-refresh-staging/phase-01/observed-refresh-validation-attestation.json`. Its owned profile map must rehash, journal, lock, atomically publish, and interruption-recover the whole five-file generation; success requires every live hash to equal its attested staged hash. No canonical target is written before the staged validation attestation succeeds. The post-publication and recovery tests must invoke every registered canonical reader, pause an observed-refresh reader after its first snapshot file while a successful publisher waits, and prove every reader receives one complete old or new snapshot rather than a mixed generation; after the real publisher command succeeds, run recovery and ordinary validation against the live generation, and record that each live target hash equals the digest in the staging-root attestation. Malformed, incomplete, altered, or tampered journal inputs return nonzero before a reader receives canonical bytes. Within that transaction, append rather than replace observed relations for every material commit:path record emitted by the validated receipt/queue snapshot; retain its reviewed-source binding with each relation. Add parallel observed-implementation history to identity, manifest, metadata, intent, handshake, egress, artifact, and conformance DRF records; retain normative sides, states, historic discovery IDs, and all pending authority conditions until direct authoritative NIP/NAP records and role-specific review resolve them. Update `CLM-UPSTREAM-BASELINE-001`, `CLM-CMP-PACKAGE-001`, and runtime/example/fixture claims only with wording the observed records support. A repository identity or committed release metadata is observed evidence, not qualified package, runtime, or protocol proof. Add or refine OQ resolution criteria for unavailable protocol paths, registry-artifact gaps, runtime/example/fixture/conformance measurement, and outstanding approvals. Retain `OWS-001`, then append one new dated snapshot item. Update the package map to distinguish observed release commits from independently fetched published artifacts and retain SPK-G blocked unless the Plan 01-35 evaluator reports all qualified dimensions and a separate package decision is approved. Sort IDs, histories, observations, impacts, source lists, and snapshots deterministically. Apply D-05, D-12, D-13, D-14 through D-20, D-29 through D-32, and D-36; do not convert observations to upstream facts, package admission, ADR acceptance, residual-risk acceptance, or a Phase 2 transition.</action>
  <verify><automated>tools/phase1-python tools/acquire-sources.py validate-reviewed-acquisition --queue .planning/research/upstream-acquisition-queue.yaml --receipt .planning/research/reports/upstream-acquisition-20260728.md --review .planning/phases/01-research-and-truth-baseline/01-REVIEWS.md && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_consolidation_recovery.ObservedRefreshPublicationTests.test_observed_refresh_five_file_generation_recovers_after_each_replacement && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_consolidation_recovery.ObservedRefreshPublicationTests.test_observed_refresh_reader_snapshot_waits_out_successful_publish && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_consolidation_recovery.ObservedRefreshPublicationTests.test_observed_refresh_rejects_tampered_or_incomplete_attestation_before_reader_entry && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_consolidation_recovery.ObservedRefreshPublicationTests.test_observed_refresh_rejects_unbound_reviewed_acquisition_provenance && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_consolidation_recovery.ObservedRefreshPublicationTests.test_observed_refresh_staged_validation_binds_exact_five_target_digests && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_consolidation_recovery.ObservedRefreshPublicationTests.test_observed_refresh_staged_validation_cli_contract && tools/phase1-python tools/validate-research.py validate-observed-refresh-staged --staged-root .planning/.observed-refresh-staging/phase-01 --attestation .planning/.observed-refresh-staging/phase-01/observed-refresh-validation-attestation.json && tools/phase1-python tools/canonical-recovery.py publish-validated-canonical-set --profile observed-refresh --staged-root .planning/.observed-refresh-staging/phase-01 --attestation .planning/.observed-refresh-staging/phase-01/observed-refresh-validation-attestation.json && tools/phase1-python tools/validate-research.py recover-consolidation --research .planning/research && tools/phase1-python tools/validate-research.py validate --root .planning/research --report /tmp/phase1-observed-refresh.md && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_consolidation_recovery.ObservedRefreshPublicationTests.test_live_observed_refresh_generation_matches_attestation && tools/phase1-python -m unittest discover -s tests/phase1 -p 'test_compatibility.py'</automated></verify>
  <acceptance_criteria>The task-local automated sequence first validates the Plan 01-45 acquisition receipt against Plan 01-29's reviewed source-input map, then stages the exact five records, validates them through `validate-observed-refresh-staged`, creates the digest-bound attestation with that same source binding, invokes Plan 01-42's actual `publish-validated-canonical-set --profile observed-refresh` command, then recovers and validates the live generation. The read-only live-hash test proves each published target equals its attested staged digest. Every injected replacement interruption recovers to only the complete old or complete new five-file generation, the paused-reader test proves a successful publisher waits for the reader's shared lock, and every registered reader receives a complete old or new mapping. Tampered/incomplete/altered staged sets publish nothing and snapshot acquisition refuses before parsing. Canonical records preserve old discovery and OWS-001 history, append observed sides with exact locators/digests and explicit uncertainty, retain every normative/package/runtime/example/fixture/conformance blocker, and validate deterministically after recovery.</acceptance_criteria>
  <done>Reviewers can inspect current upstream observations without promotion to normative fact or package admission, and later full-gate work sees one durable journaled five-record canonical generation through transactional reader snapshots.</done>
</task>
</tasks>

## File-level atomicity map

| Path | Role | Atomicity and failure rule |
|---|---|---|
| `tools/validate-research.py` | Staged-overlay validator and publisher-invocation integration | It admits only the exact staged five-file map, verifies the reviewed acquisition binding, and writes no attestation on validation failure; it never writes a live target directly. |
| `tests/phase1/test_consolidation_recovery.py` | Regression contract for the validator and Plan 01-42 publisher profile | It changes with the staged-map contract so interruption, reader, attestation, and live-hash tests exercise the same five logical paths; it is not publication evidence. |
| `.planning/research/claims.yaml` | Live target 1 | It becomes visible only through Plan 01-42's `observed-refresh` old-or-new generation recovery. |
| `.planning/research/drift-register.yaml` | Live target 2 | It becomes visible only through that same generation; no independent record replacement is permitted. |
| `.planning/research/open-questions.yaml` | Live target 3 | It becomes visible only through that same generation; unresolved routing is retained with the old-or-new snapshot. |
| `.planning/research/package-map.md` | Live target 4 | It becomes visible only through that same generation; observed release data cannot be promoted independently. |
| `.planning/research/open-work-snapshot.json` | Live target 5 | It becomes visible only through that same generation while retaining `OWS-001`. |
| `.planning/.observed-refresh-staging/phase-01/claims.yaml` | Staged copy 1 | It is a candidate byte source only and is never read as canonical evidence before a successful attestation and publisher transaction. |
| `.planning/.observed-refresh-staging/phase-01/drift-register.yaml` | Staged copy 2 | It is a candidate byte source only and must remain paired with all other staged targets. |
| `.planning/.observed-refresh-staging/phase-01/open-questions.yaml` | Staged copy 3 | It is a candidate byte source only and must remain paired with all other staged targets. |
| `.planning/.observed-refresh-staging/phase-01/package-map.md` | Staged copy 4 | It is a candidate byte source only and must remain paired with all other staged targets. |
| `.planning/.observed-refresh-staging/phase-01/open-work-snapshot.json` | Staged copy 5 | It is a candidate byte source only and must remain paired with all other staged targets. |
| `.planning/.observed-refresh-staging/phase-01/observed-refresh-validation-attestation.json` | External staged attestation | It is emitted last by successful staged validation, binds all five staged digests and reviewed-source provenance, and is unusable after a failed/tampered transaction. |

The durable journal and atomic publisher implementation remain Plan 01-42-owned external transaction artifacts in `tools/canonical-recovery.py`; Plan 01-43 invokes that tested interface but must neither recreate the journal nor bypass its lock/recovery path. The validator, its regression contract, all five staged copies, and the external attestation stay in this plan because separating any of them permits a stale test or a staged set to drift from the exact profile that the publisher rehashes. On any failure before publisher success, discard the candidate staged generation and provisional attestation, preserve the preexisting live five-target generation, and do not offer any partial record to a canonical reader. Plan 01-42 alone performs interruption recovery for an entered publication transaction.

## Artifacts this phase produces
- `tools/validate-research.py` — transactional-snapshot staged observed-refresh validation and five-target digest attestation.
- `tests/phase1/test_consolidation_recovery.py` — full observed-refresh interruption, paused-reader/publisher concurrency, tampering, staged-validation CLI, live-hash, attestation, and registered-reader snapshot coverage.
- `.planning/.observed-refresh-staging/phase-01/{claims.yaml,drift-register.yaml,open-questions.yaml,package-map.md,open-work-snapshot.json,observed-refresh-validation-attestation.json}` — exact five-file staging generation and digest-bound validation attestation, including Plan 01-29/01-45 reviewed-source provenance, before publication.
- `.planning/research/{claims.yaml,drift-register.yaml,open-questions.yaml,package-map.md,open-work-snapshot.json}` — one journaled, append-only observed-refresh generation.

## Created-symbol inventory
- `ObservedRefreshPublicationTests.test_observed_refresh_reader_snapshot_waits_out_successful_publish` — task-local proof that a five-file reader and a successful publisher cannot expose a mixed generation.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| reviewed observed record → normative evidence | Merged implementation or release metadata can be mistaken for protocol authority. |
| observed release commit → published package artifact | Repository commits can differ from independently fetched package bytes. |
| refreshed record update → historic truth | An update can erase discovery, blocker, or snapshot history. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-43-01 | Spoofing | protocol authority | high | mitigate | Task 1 preserves observed classification and blocked normative sides until direct qualified sources and review exist. |
| T-43-02 | Tampering | package comparison | high | mitigate | Task 1 separates observed release commits from registry artifact evidence and retains SPK-G eligibility/approval gates. |
| T-43-03 | Repudiation | historical evidence | medium | mitigate | Task 1 appends deterministic dated history and retains discovery IDs and OWS-001. |
| T-43-04 | Elevation of Privilege | package/conformance execution | high | mitigate | Task 1 records only evidence and blockers; Plan 01-36 owns the separately approved operation. |
| T-43-05 | Tampering | observed-refresh canonical generation and multi-file reader race | high | mitigate | Task 1 stages, validates, digest-binds, and actually publishes the exact five targets through Plan 01-42's tested `observed-refresh` CLI, then proves live hashes, interruption recovery, tampered-journal refusal, and a paused reader's complete shared-lock snapshot before every registered reader parses data. |
| T-43-06 | Tampering | refreshed observation provenance | high | mitigate | Task 1 validates the Plan 01-45 receipt/queue against Plan 01-29's Git-blob reviewed-source map and carries that immutable binding into staged validation/attestation before publishing observed relations. |
</threat_model>

<verification>
Run the five-record interruption, tampered-attestation, staged-validation CLI, real staged validation/attestation/publication, live-hash, recovery, canonical validation, and focused compatibility checks through the approved wrapper.
</verification>

<success_criteria>
Observed refresh evidence is durable, append-only, and accurately scoped while all unresolved compatibility conditions remain actionable blockers.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-43-SUMMARY.md` when done.
</output>
