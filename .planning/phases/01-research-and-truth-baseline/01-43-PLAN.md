---
phase: 01-research-and-truth-baseline
plan: 43
type: execute
wave: 7
depends_on: [01-35, 01-36, 01-42]
files_modified:
  - tools/validate-research.py
  - tests/phase1/test_consolidation_recovery.py
  - .planning/research/claims.yaml
  - .planning/research/drift-register.yaml
  - .planning/research/open-questions.yaml
  - .planning/research/package-map.md
  - .planning/research/open-work-snapshot.json
  - .planning/.observed-refresh-staging/phase-01/claims.yaml
  - .planning/.observed-refresh-staging/phase-01/drift-register.yaml
  - .planning/.observed-refresh-staging/phase-01/open-questions.yaml
  - .planning/.observed-refresh-staging/phase-01/package-map.md
  - .planning/.observed-refresh-staging/phase-01/open-work-snapshot.json
  - .planning/.observed-refresh-staging/phase-01/observed-refresh-validation-attestation.json
autonomous: true
gap_closure: true
requirements: [EVID-01, EVID-02, EVID-03, OPER-01]
must_haves:
  truths:
    - "Reviewed observed upstream refresh evidence is appended with real immutable locators and uncertainty without replacing a normative side, discovery history, or old snapshot."
    - "The compatibility baseline and SPK-G remain blocked whenever qualified normative, package, runtime, example, fixture, conformance, or approval evidence is absent."
    - "The five related observed-refresh records become visible only as one validated digest-bound canonical generation or readers recover the complete prior generation after an interruption."
  artifacts:
    - path: .planning/research/claims.yaml
      provides: "Append-only observed source relations and retained blocking claim state"
    - path: .planning/research/drift-register.yaml
      provides: "Parallel observed implementation history with unchanged normative sides and OQ routing"
    - path: .planning/research/open-work-snapshot.json
      provides: "New dated current-work observation that retains OWS-001"
    - path: tools/validate-research.py
      provides: "Observed-refresh staged-set validation attestation bound to five exact target paths and SHA-256 values"
    - path: tests/phase1/test_consolidation_recovery.py
      provides: "Five-record hard-interruption, journal-tampering, staged-validation CLI, live-hash, and registered-reader recovery regressions"
    - path: .planning/.observed-refresh-staging/phase-01/observed-refresh-validation-attestation.json
      provides: "Digest-bound observed-refresh staged validation attestation consumed by Plan 01-42's tested publisher CLI"
  key_links:
    - from: reviewed observed source records and Plan 01-35 evaluator
      to: claims, drift, OQ, package map, and snapshot records
      via: "append-only locator/digest/uncertainty history and explicit blocked compatibility routing"
    - from: staged observed-refresh validation attestation
      to: five live canonical observed-refresh records
      via: "Plan 01-42 `observed-refresh` profile rehashes the exact staged target map, journals old/new digests under lock, atomically publishes, and recovers before every registered reader"
---

<objective>
Append reviewed observed-refresh records and retain every unresolved compatibility and conformance blocker after schema and recovery foundations pass.

Purpose: Publish the current upstream observations as one durable, recovery-safe canonical generation after the v2 evaluator and shared transaction boundary pass.
Output: Deterministically ordered append-only observed sides, blocker routing, package comparison, dated snapshot, staged validation attestation, and journaled publication.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-35-SUMMARY.md
@.planning/phases/01-research-and-truth-baseline/01-36-SUMMARY.md
@.planning/phases/01-research-and-truth-baseline/01-42-SUMMARY.md
@tools/canonical-recovery.py
@tests/phase1/test_consolidation_recovery.py
@.planning/research/{source-registry.yaml,claims.yaml,compatibility-matrix.yaml,drift-register.yaml,open-questions.yaml,package-evidence.yaml,package-map.md,open-work-snapshot.json}
@.planning/research/reports/upstream-refresh-synthesis-2026-07-28.md
@.planning/spikes/spk-g-package-conformance/{metadata.yaml,recipe.md,measurements.yaml,report.md,impact-fragment.yaml}
@.planning/adr/0010-package-versioning.md
@tools/validate-research.py
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Stage and atomically publish one reviewed observed-refresh generation while preserving blocked normative counterparts</name>
  <precondition>Plan 01-35 has produced a valid v2 compatibility evaluator and Plan 01-42 has completed recovery-before-read enforcement; reviewer classifications from Plan 01-30 remain available.</precondition>
  <read_first>01-35-SUMMARY.md; 01-36-SUMMARY.md; 01-42-SUMMARY.md; tools/canonical-recovery.py; tools/validate-research.py; tests/phase1/test_consolidation_recovery.py; claims.yaml; drift-register.yaml; open-questions.yaml; package-map.md; open-work-snapshot.json; source-registry.yaml; compatibility-matrix.yaml; package-evidence.yaml; upstream-refresh-synthesis-2026-07-28.md; ADR 0010; SPK-G metadata; SPK-G measurements; SPK-G report; SPK-G impact fragment</read_first>
  <files>tools/validate-research.py, tests/phase1/test_consolidation_recovery.py, .planning/research/claims.yaml, .planning/research/drift-register.yaml, .planning/research/open-questions.yaml, .planning/research/package-map.md, .planning/research/open-work-snapshot.json, .planning/.observed-refresh-staging/phase-01/claims.yaml, .planning/.observed-refresh-staging/phase-01/drift-register.yaml, .planning/.observed-refresh-staging/phase-01/open-questions.yaml, .planning/.observed-refresh-staging/phase-01/package-map.md, .planning/.observed-refresh-staging/phase-01/open-work-snapshot.json, .planning/.observed-refresh-staging/phase-01/observed-refresh-validation-attestation.json</files>
  <behavior>
    - `test_observed_refresh_five_file_generation_recovers_after_each_replacement` stages a complete changed five-file observed-refresh set, injects a hard interruption after each replacement, and proves every subsequent registered canonical reader obtains either the complete digest-verified old generation or the complete digest-verified new generation.
    - `test_observed_refresh_rejects_tampered_or_incomplete_attestation_before_reader_entry` proves an extra, missing, aliased, altered, or journal-tampered staged set is refused before publication and before any registered reader parses canonical evidence.
    - `test_observed_refresh_staged_validation_binds_exact_five_target_digests` proves staged validation uses the ordinary schema/semantic checks against the five-file overlay and emits an attestation whose `observed-refresh` profile, sorted target paths, and SHA-256 values equal the staged regular-file bytes.
    - `test_observed_refresh_staged_validation_cli_contract` invokes `validate-observed-refresh-staged --help` and proves its required `--staged-root` and `--attestation` paths accept only the exact five regular non-symlink staged records, reject any missing/extra/aliased/changed file or failed semantic overlay, and create no attestation on refusal.
    - `test_live_observed_refresh_generation_matches_attestation` is read-only after the actual publisher invocation and proves each of the five live canonical paths has the SHA-256 attested for the staging-root target map.
  </behavior>
  <action>Addresses H3: start RED in `tests/phase1/test_consolidation_recovery.py` with a copied planning root, a complete old generation, a complete changed staged generation, every deterministic replacement interruption point, and journal-tampering fixtures. After Plan 01-35 semantic validation passes, consume Plan 01-36's validated SPK-G `measurements.yaml`, `report.md`, and `impact-fragment.yaml`, then derive all five changed records in the exact same-filesystem staging root `.planning/.observed-refresh-staging/phase-01/`: `claims.yaml`, `drift-register.yaml`, `open-questions.yaml`, `package-map.md`, and `open-work-snapshot.json`. The staging root contains exactly those five regular non-symlink files plus `observed-refresh-validation-attestation.json`; do not modify any live target directly or serially. Extend `tools/validate-research.py` with the owned `validate-observed-refresh-staged --staged-root PATH --attestation PATH` route; its help exposes both required `Path` arguments, it first invokes the shared recovery guard, rejects missing/extra/aliased/non-regular staged targets before overlay, overlays only the exact five staged regular files on recovered read-only canonical inputs, applies the ordinary schema and semantic checks, and writes no attestation on a nonzero result. On success it emits the attestation at `PATH`, containing profile `observed-refresh`, the exact sorted five repository-relative targets, each staged SHA-256, and the validated SPK-G outcome/impact-fragment digest bindings. Execute the actual Plan 01-42 publisher after staged validation: `tools/phase1-python tools/canonical-recovery.py publish-validated-canonical-set --profile observed-refresh --staged-root .planning/.observed-refresh-staging/phase-01 --attestation .planning/.observed-refresh-staging/phase-01/observed-refresh-validation-attestation.json`. Its owned profile map must rehash, journal, lock, atomically publish, and interruption-recover the whole five-file generation; success requires every live hash to equal its attested staged hash. No canonical target is written before the staged validation attestation succeeds. The post-publication and recovery tests must invoke every registered canonical reader and prove no reader sees a mixed generation; after the real publisher command succeeds, run recovery and ordinary validation against the live generation, and record that each live target hash equals the digest in the staging-root attestation. Malformed, incomplete, altered, or tampered journal inputs return nonzero before a reader consumes canonical bytes. Within that transaction, append rather than replace reviewed observed relations for material `napplet/web` and `kehto/web` commit:path records. Add parallel observed-implementation history to identity, manifest, metadata, intent, handshake, egress, artifact, and conformance DRF records; retain normative sides, states, historic discovery IDs, and all pending authority conditions until direct authoritative NIP/NAP records and role-specific review resolve them. Update `CLM-UPSTREAM-BASELINE-001`, `CLM-CMP-PACKAGE-001`, and runtime/example/fixture claims only with wording the observed records support. A repository identity or committed release metadata is observed evidence, not qualified package, runtime, or protocol proof. Add or refine OQ resolution criteria for unavailable protocol paths, registry-artifact gaps, runtime/example/fixture/conformance measurement, and outstanding approvals. Retain `OWS-001`, then append one new dated snapshot item. Update the package map to distinguish observed release commits from independently fetched published artifacts and retain SPK-G blocked unless the Plan 01-35 evaluator reports all qualified dimensions and a separate package decision is approved. Sort IDs, histories, observations, impacts, source lists, and snapshots deterministically. Apply D-05, D-12, D-13, D-14 through D-20, D-29 through D-32, and D-36; do not convert observations to upstream facts, package admission, ADR acceptance, residual-risk acceptance, or a Phase 2 transition.</action>
  <verify><automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_consolidation_recovery.ObservedRefreshPublicationTests.test_observed_refresh_five_file_generation_recovers_after_each_replacement && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_consolidation_recovery.ObservedRefreshPublicationTests.test_observed_refresh_rejects_tampered_or_incomplete_attestation_before_reader_entry && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_consolidation_recovery.ObservedRefreshPublicationTests.test_observed_refresh_staged_validation_binds_exact_five_target_digests && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_consolidation_recovery.ObservedRefreshPublicationTests.test_observed_refresh_staged_validation_cli_contract && tools/phase1-python tools/validate-research.py validate-observed-refresh-staged --staged-root .planning/.observed-refresh-staging/phase-01 --attestation .planning/.observed-refresh-staging/phase-01/observed-refresh-validation-attestation.json && tools/phase1-python tools/canonical-recovery.py publish-validated-canonical-set --profile observed-refresh --staged-root .planning/.observed-refresh-staging/phase-01 --attestation .planning/.observed-refresh-staging/phase-01/observed-refresh-validation-attestation.json && tools/phase1-python tools/validate-research.py recover-consolidation --research .planning/research && tools/phase1-python tools/validate-research.py validate --root .planning/research --report /tmp/phase1-observed-refresh.md && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_consolidation_recovery.ObservedRefreshPublicationTests.test_live_observed_refresh_generation_matches_attestation && tools/phase1-python -m unittest discover -s tests/phase1 -p 'test_compatibility.py'</automated></verify>
  <acceptance_criteria>The task-local automated sequence stages the exact five records, validates them through `validate-observed-refresh-staged`, creates the digest-bound attestation, invokes Plan 01-42's actual `publish-validated-canonical-set --profile observed-refresh` command, then recovers and validates the live generation. The read-only live-hash test proves each published target equals its attested staged digest. Every injected replacement interruption recovers to only the complete old or complete new five-file generation, while tampered/incomplete/altered staged sets publish nothing and no registered reader consumes canonical bytes. Canonical records preserve old discovery and OWS-001 history, append observed sides with exact locators/digests and explicit uncertainty, retain every normative/package/runtime/example/fixture/conformance blocker, and validate deterministically after recovery.</acceptance_criteria>
  <done>Reviewers can inspect current upstream observations without promotion to normative fact or package admission, and later full-gate work sees one durable journaled five-record canonical generation.</done>
</task>
</tasks>

## Artifacts this phase produces
- `tools/validate-research.py` — recovery-guarded staged observed-refresh validation and five-target digest attestation.
- `tests/phase1/test_consolidation_recovery.py` — full observed-refresh interruption, tampering, staged-validation CLI, live-hash, attestation, and registered-reader recovery coverage.
- `.planning/.observed-refresh-staging/phase-01/{claims.yaml,drift-register.yaml,open-questions.yaml,package-map.md,open-work-snapshot.json,observed-refresh-validation-attestation.json}` — exact five-file staging generation and digest-bound validation attestation before publication.
- `.planning/research/{claims.yaml,drift-register.yaml,open-questions.yaml,package-map.md,open-work-snapshot.json}` — one journaled, append-only observed-refresh generation.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| reviewed observed record → normative evidence | Merged implementation or release metadata can be mistaken for protocol authority. |
| observed release commit → published package artifact | Repository commits can differ from independently fetched package bytes. |
| refreshed record update → historic truth | An update can erase discovery, blocker, or snapshot history. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-43-01 | Spoofing | protocol authority | high | mitigate | Task 1 preserves observed classification and blocked normative sides until direct qualified sources and review exist. |
| T-43-02 | Tampering | package comparison | high | mitigate | Task 1 separates observed release commits from registry artifact evidence and retains SPK-G eligibility/approval gates. |
| T-43-03 | Repudiation | historical evidence | medium | mitigate | Task 1 appends deterministic dated history and retains discovery IDs and OWS-001. |
| T-43-04 | Elevation of Privilege | package/conformance execution | high | mitigate | Task 1 records only evidence and blockers; Plan 01-36 owns the separately approved operation. |
| T-43-05 | Tampering | observed-refresh canonical generation | high | mitigate | Task 1 stages, validates, digest-binds, and actually publishes the exact five targets through Plan 01-42's tested `observed-refresh` CLI, then proves live hashes, interruption recovery, and tampered-journal refusal before every registered reader. |
</threat_model>

<verification>
Run the five-record interruption, tampered-attestation, staged-validation CLI, real staged validation/attestation/publication, live-hash, recovery, canonical validation, and focused compatibility checks through the approved wrapper.
</verification>

<success_criteria>
Observed refresh evidence is durable, append-only, and accurately scoped while all unresolved compatibility conditions remain actionable blockers.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-43-SUMMARY.md` when done.
</output>
