---
phase: 01-research-and-truth-baseline
plan: 46
type: execute
wave: 1
depends_on: [01-42, 01-45]
files_modified:
  - tools/acquire-sources.py
  - tools/canonical-recovery.py
  - tools/validate-research.py
  - tools/refresh-sources.py
  - tests/phase1/test_evidence.py
  - tests/phase1/test_spike_consolidation.py
  - tests/phase1/test_consolidation_recovery.py
  - tests/phase1/test_drift.py
  - tests/phase1/test_gap_closeout.py
autonomous: true
gap_closure: true
requirements: [EVID-01, EVID-02, EVID-04, OPER-01, OPER-03]
must_haves:
  truths:
    - "Immutable-source collection writes only beneath the exact repository-owned cache and rejects external or symlink-mediated roots before transport or filesystem side effects."
    - "Canonical multi-file evidence publication exposes one complete old or new generation; malformed transaction residue fails quickly instead of trapping readers in retry loops."
    - "Local observations remain non-normative, staged drift receives semantic validation, and refresh locking recovers automatically when a process exits."
    - "The focused regression set executes CR-01 through CR-04 plus WR-01 and WR-03 without pulling deferred SPK-G or migration-copy work back into scope."
  artifacts:
    - path: tools/acquire-sources.py
      provides: "Exact repository cache-root confinement before source transport or writes."
    - path: tools/canonical-recovery.py
      provides: "Journaled generation publication and bounded fail-closed recovery."
    - path: tools/validate-research.py
      provides: "Atomic consolidation and valid observed-local drift synthesis."
    - path: tools/refresh-sources.py
      provides: "Process-lifetime refresh locking."
    - path: tests/phase1/test_gap_closeout.py
      provides: "Focused composition coverage for the six retained findings."
  key_links:
    - from: consolidate_spike_impacts
      to: canonical_recovery.publish_generation
      via: "The complete validated target map is published as one journaled generation."
    - from: new_drift_record
      to: validate_drift
      via: "Observed-local candidates keep normative null and pass semantic validation before publication."
---

<objective>
Repair the evidence boundaries that directly protect site truth and source freshness.

Purpose: Close CR-01, CR-02, CR-03, CR-04, WR-01, and WR-03 with the smallest direct code and regression set required before refreshed evidence can feed a public static site.
Output: Repository-confined acquisition, durable canonical publication/recovery, honest drift synthesis, recoverable refresh locking, and focused tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-research-and-truth-baseline/01-FINAL-VERIFICATION.md
@.planning/phases/01-research-and-truth-baseline/01-REVIEW.md
@.planning/phases/01-research-and-truth-baseline/01-42-SUMMARY.md
@.planning/phases/01-research-and-truth-baseline/01-45-SUMMARY.md
</context>

<scope_boundary>
Implement only CR-01 through CR-04 plus WR-01 and WR-03. Defer CR-05 through CR-07, SPK-G/package execution and admission, WR-02 migration copy semantics, portable targets, native-runtime qualification, cross-browser runtime labs, and exact-review/terminal-publication machinery.
</scope_boundary>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Confine source ingress and restore its missing regression</name>
  <read_first>01-REVIEW.md CR-01 and WR-01; tools/acquire-sources.py `_confined_cache_root`, `collect_immutable_candidate`, and `CACHE_ROOT`; tests/phase1/test_evidence.py duplicate collector classes and allowlist cases</read_first>
  <files>tools/acquire-sources.py, tests/phase1/test_evidence.py</files>
  <behavior>
    - `/tmp/.research/upstreams`, traversal lookalikes, and symlink-mediated roots fail before transport, directory creation, or output writes.
    - The exact repository-owned ignored cache remains usable for an approved immutable fixture.
    - Unittest discovers the non-allowlisted ingress regression exactly once and that test proves zero output.
  </behavior>
  <action>Start with hostile-root and discovery tests. Strengthen `_confined_cache_root()` so lexical and resolved paths must match the one repository cache location, every traversed component is non-symlink, and validation occurs before transport or filesystem creation. Merge or rename duplicate test classes so the URL allowlist rejection test cannot be overwritten at module load. Preserve existing immutable-commit, path, digest, authority, and observed-only behavior.</action>
  <verify>
    <automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_evidence.BoundedCollectorTests.test_collector_rejects_external_or_symlink_cache_root_before_transport test_evidence.BoundedCollectorTests.test_collector_rejects_non_allowlisted_url_without_writing test_evidence.BoundedCollectorTests.test_unittest_discovery_includes_non_allowlisted_ingress_once</automated>
  </verify>
  <acceptance_criteria>External and symlink roots cannot trigger transport or writes, the canonical cache still works, and discovery executes the allowlist rejection exactly once.</acceptance_criteria>
  <done>CR-01 and WR-01 are closed by direct boundary tests.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Publish and recover canonical evidence as one bounded generation</name>
  <read_first>01-REVIEW.md CR-02 and CR-04; tools/validate-research.py `consolidate_spike_impacts`; tools/canonical-recovery.py `publish_generation`, `_recover_locked`, and `read_canonical_snapshot`; existing consolidation/recovery tests</read_first>
  <files>tools/canonical-recovery.py, tools/validate-research.py, tests/phase1/test_spike_consolidation.py, tests/phase1/test_consolidation_recovery.py</files>
  <behavior>
    - Consolidation validates the complete candidate map before entering publication.
    - Every injected replacement interruption recovers to the entire old or entire new byte map, never a mixture.
    - Regular files, symlinks, malformed journals, or ambiguous transaction entries raise `RecoveryError` within a bounded test timeout and return no canonical bytes.
  </behavior>
  <action>Route the complete validated consolidation target map through the existing Plan 01-42 journaled publisher; remove the serial live replacement and local rollback loop. Make recovery enumerate every transaction entry and reject any unrecognized, symlinked, non-directory, malformed, or ambiguous state before reader retry. Keep valid prepared/publishing/committed recovery behavior and deterministic target ordering.</action>
  <verify>
    <automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_spike_consolidation.SpikeConsolidationTests.test_consolidation_generation_recovers_after_each_publish_interruption test_spike_consolidation.SpikeConsolidationTests.test_consolidation_semantic_failure_does_not_enter_publication test_consolidation_recovery.CanonicalRecoveryTests.test_unexpected_transaction_entries_refuse_within_timeout</automated>
  </verify>
  <acceptance_criteria>Readers observe one coherent generation or a fast explicit refusal after every tested interruption or malformed transaction state.</acceptance_criteria>
  <done>CR-02 and CR-04 are closed at the shared publication boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preserve observed-local semantics and recover refresh availability</name>
  <read_first>01-REVIEW.md CR-03 and WR-03; tools/validate-research.py `new_drift_record`, staged consolidation validation, and `validate_drift`; tools/refresh-sources.py lock acquisition/release; tests/phase1/test_drift.py</read_first>
  <files>tools/validate-research.py, tools/refresh-sources.py, tests/phase1/test_drift.py, tests/phase1/test_gap_closeout.py</files>
  <behavior>
    - A local spike observation emits `observed-local` with `normative: null`, retained report/measurement provenance, uncertainty, impact, and refresh trigger.
    - A two-sided drift record requires genuinely distinct pinned source identities.
    - Semantic drift validation runs against staged bytes before any publication call.
    - Refresh locking is released by process death and concurrent refresh attempts serialize without permanent stale files.
    - A focused composition test covers only CR-01–CR-04, WR-01, and WR-03.
  </behavior>
  <action>Replace duplicated normative/observed synthesis with the blocked observed-local shape unless two distinct reviewed sides exist. Invoke semantic drift validation before publication. Replace the persistent `O_EXCL` lock with process-lifetime advisory locking using a held file descriptor, or an equivalently safe liveness-bound primitive. Add a subprocess-termination regression plus one focused composition test. Do not modify migration-copy behavior or any SPK-G path.</action>
  <verify>
    <automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_drift test_gap_closeout && tools/phase1-python -m unittest discover -s tests/phase1 -v</automated>
  </verify>
  <acceptance_criteria>Local evidence cannot become normative by duplication, invalid staged drift cannot publish, a dead refresh process cannot block future refreshes, and the complete Phase 1 suite remains green.</acceptance_criteria>
  <done>CR-03 and WR-03 are closed; all six retained findings have focused regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Failure | Mitigation |
|---|---|---|
| caller path → source cache | External or symlink-directed writes | Exact repository-root admission before transport or writes |
| staged bytes → canonical evidence | Mixed generations after interruption | Existing journaled all-or-nothing publisher |
| transaction residue → reader | Infinite retry or ambiguous recovery | Exhaustive bounded fail-closed entry validation |
| local observation → normative claim | Implementation behavior promoted to protocol fact | Observed-local/null-normative modeling plus semantic validation |
| refresh process → future maintainers | Dead process leaves permanent lock | Process-lifetime advisory lock |
</threat_model>

<verification>
Run all named boundary regressions, then the full `tests/phase1` suite through `tools/phase1-python`. This plan repairs implementation safety only; it does not mark Phase 1 passed, accept ADRs, admit packages, or authorize runtime labs.
</verification>

<success_criteria>
The repository can safely acquire, publish, read, classify, and refresh the evidence needed by Plan 01-47 without the six retained trust-boundary failures.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-46-SUMMARY.md` when done.
</output>
