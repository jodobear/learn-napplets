---
phase: 01-research-and-truth-baseline
plan: 46
type: execute
wave: 2
depends_on: [01-45, 01-51]
files_modified:
  - tools/acquire-sources.py
  - tests/phase1/test_evidence.py
autonomous: true
gap_closure: true
requirements: [EVID-01, OPER-03]
must_haves:
  truths:
    - "A bounded collector rejects an external or symlink-mediated cache root before it invokes a transport or creates a directory, while the repository-owned ignored cache remains usable."
    - "The non-allowlisted source-ingress regression is discovered by unittest and proves no output was written."
    - "P-EVID01-ADJ, P-EVID01-EMPTY, P-EVID01-ENCODING, and P-EVID01-ORDER retain their designated direct probes; their acceptance predicates are exact citation adjacency, an empty inventory with no diagnostic, ASCII-code-point decoding without normalization, and stable path-then-ID diagnostic ordering."
  artifacts:
    - path: tools/acquire-sources.py
      provides: "Repository-owned, non-symlink cache-root confinement for immutable source collection."
      contains: "_confined_cache_root"
    - path: tests/phase1/test_evidence.py
      provides: "Discovered hostile-cache and non-allowlisted-ingress regressions."
  key_links:
    - from: collect_immutable_candidate
      to: CACHE_ROOT
      via: "The exact repository cache root is accepted only after no-symlink and containment checks, before identity/blob transport or output writes."
---

<objective>
Repair source-ingress confinement and restore the silently excluded ingress regression.

Purpose: Close CR-01 and WR-01 so immutable source collection remains a repository-owned evidence operation rather than a path-controlled write primitive.
Output: A fail-closed cache-root implementation and discovered regression coverage.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-FINAL-VERIFICATION.md
@.planning/phases/01-research-and-truth-baseline/01-REVIEW.md
@.planning/phases/01-research-and-truth-baseline/01-45-SUMMARY.md
@tools/acquire-sources.py
@tests/phase1/test_evidence.py
</context>

<assumption_delta_decision>
  <noun>Phase 1 immutable evidence-record identity</noun>
  <decision>no-change</decision>
  <rationale>The detector matched goal language about choosing scope; this repair narrows a local cache boundary and does not alter evidence identity, required fields, or modeling anchors.</rationale>
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject one hostile cache root before an immutable candidate can write</name>
  <precondition>A fresh exact-plan review has authorized the current Plan 01-46 execution set, and `tools/phase1-python --verify-toolchain` exits 0.</precondition>
  <read_first>01-45-SUMMARY.md; 01-FINAL-VERIFICATION.md gap 1; 01-REVIEW.md CR-01; tools/acquire-sources.py `_confined_cache_root`, `collect_immutable_candidate`, and `CACHE_ROOT`; tests/phase1/test_evidence.py `BoundedCollectorTests`</read_first>
  <files>tools/acquire-sources.py, tests/phase1/test_evidence.py</files>
  <behavior>
    - A fixture call using `/tmp/.research/upstreams` raises `ValueError` before the fixture transport's identity method is called and before any external cache path exists.
    - A cache root reached through a symlink raises `ValueError`; the test asserts no file appears outside the fixture repository root.
    - The actual repository `CACHE_ROOT` remains accepted only when it is a non-symlink directory below `ROOT/.research/upstreams`; a collected fixture blob is written only beneath that exact root.
    - Existing allowlisted, immutable-commit, repository-path, and observed-only classification tests remain green.
  </behavior>
  <action>Start RED by adding an isolated temporary-repository test that calls `collect_immutable_candidate()` with an external lookalike cache path and an instrumented `FixtureTransport`; assert refusal occurs before identity, commit, blob, parent-directory creation, or file output. Add a separate symlink-root fixture that points outside the temporary repository. Change `_confined_cache_root()` to compare the caller-supplied lexical location and resolved location to the one canonical `CACHE_ROOT` under `ROOT`, reject a root or traversed ancestor that is a symlink, require a real directory under the repository, and return only the verified canonical root. Keep `cache_path()` behavior compatible for its direct CLI path, but route collection through the strengthened helper before any network-facing transport call. Do not widen collection scope, alter reviewed Git-blob authorization, alter source classifications, create canonical source records, or turn observed repository bytes into upstream authority. This implements D-04, D-09, D-10, D-14, D-16, and D-17: all retained bytes remain immutable, repository-confined, read-only-acquired observations with unchanged uncertainty and approval boundaries.</action>
  <verify>
    <automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_evidence.BoundedCollectorTests.test_collector_rejects_external_or_symlink_cache_root_before_transport && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_evidence.BoundedCollectorTests.test_candidate_acquisition_tracer_blocks_incomplete_dimensions && tools/phase1-python -m unittest discover -s tests/phase1 -p 'test_evidence.py'</automated>
  </verify>
  <acceptance_criteria>The hostile external-root and symlink-root fixtures both fail before a transport method or output write, while the repository-owned fixture root writes the exact immutable blob below its approved cache path and the existing observed-only result remains valid.</acceptance_criteria>
  <done>CR-01 has an end-to-end fail-closed collection path backed by a deterministic regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restore discovery of the non-allowlisted ingress regression</name>
  <read_first>01-REVIEW.md WR-01; tests/phase1/test_evidence.py class declarations and `test_collector_rejects_non_allowlisted_url_without_writing`; tools/acquire-sources.py `reject_unallowlisted` and CLI error handling</read_first>
  <files>tests/phase1/test_evidence.py</files>
  <behavior>
    - `unittest.TestLoader().loadTestsFromModule()` contains exactly one test with the non-allowlisted ingress method name.
    - Invoking that named test rejects the fixture URL, returns nonzero, and leaves its requested cache path absent.
    - The collector test class has no duplicate module-level declaration that can overwrite the source-ingress case.
  </behavior>
  <action>Merge the two `BoundedCollectorTests` declarations into one class or give their responsibilities unique class names so Python cannot replace the ingress test at module load. Preserve the existing fixture-only acquisition tests and add a discovery assertion that loads the module's test suite, enumerates test IDs, and proves the non-allowlisted ingress method appears once. Keep the test's temporary cache assertion and command-level nonzero check; do not replace it with a direct helper-only assertion. This re-verifies D-10's bounded public-read policy and D-35's non-waivable gate rule without changing acquisition behavior outside the regression seam.</action>
  <verify>
    <automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_evidence.BoundedCollectorTests.test_collector_rejects_non_allowlisted_url_without_writing && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_evidence.BoundedCollectorTests.test_unittest_discovery_includes_non_allowlisted_ingress_once</automated>
  </verify>
  <acceptance_criteria>Verbose unittest execution names the non-allowlisted ingress test exactly once, and its command proves the rejected URL leaves no cache output.</acceptance_criteria>
  <done>WR-01 can no longer be hidden by a duplicate Python class declaration.</done>
</task>

</tasks>

## Artifacts this phase produces
- `tools/acquire-sources.py::_confined_cache_root` — exact repository-owned, non-symlink cache admission check used before collection writes.
- `tests/phase1/test_evidence.py::BoundedCollectorTests.test_collector_rejects_external_or_symlink_cache_root_before_transport` — CR-01 transport/write-order regression.
- `tests/phase1/test_evidence.py::BoundedCollectorTests.test_unittest_discovery_includes_non_allowlisted_ingress_once` — WR-01 discovery regression.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| caller-controlled cache path → local filesystem | An acquisition argument can redirect untrusted source bytes outside the repository. |
| source URL → acquisition transport | A non-approved host can cause a collection attempt or cache side effect. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-46-01 | Tampering | `_confined_cache_root` | high | mitigate | Task 1 permits only the exact repository cache root after lexical, resolved-path, and symlink checks before transport or writes. |
| T-46-02 | Elevation of Privilege | collector output path | high | mitigate | Task 1 rejects external/symlink paths before directory creation and validates with an instrumented transport fixture. |
| T-46-03 | Repudiation | ingress test discovery | medium | mitigate | Task 2 asserts the named URL-rejection case is discovered exactly once and performs its no-write check. |
</threat_model>

<verification>
Run both named source-ingress regressions and the complete `test_evidence.py` module through the certified wrapper. The next review gate consumes their command evidence; this plan does not claim a human evidence approval.
</verification>

<success_criteria>
A maintainer cannot direct collection output outside the repository-owned cache, and the test suite demonstrably executes the public-source allowlist rejection path.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-46-SUMMARY.md` when done.
</output>
