---
phase: 01-research-and-truth-baseline
plan: 38
type: execute
wave: 11
depends_on: [01-37]
files_modified:
  - tests/phase1/test_gap_closeout.py
  - .planning/phases/01-research-and-truth-baseline/01-VALIDATION.md
  - .planning/phases/01-research-and-truth-baseline/01-POST-CLOSURE-VERIFICATION.md
autonomous: true
gap_closure: true
requirements: [EVID-01, EVID-02, EVID-03, EVID-04, OPER-01, OPER-03]
must_haves:
  truths:
    - "The validation contract maps all fourteen exact edge probes to executable named tests and records actual post-remediation evidence."
    - "EVID-03 and EVID-04 completeness tests prove substantive eligibility and retained evidence without treating a blocked label or policy placeholder as a successful baseline or approval."
    - "Nyquist status becomes true only when all required commands and probe mappings pass; otherwise the contract truthfully remains noncompliant with a gap map."
    - "The canonical probe ledger is a finite fourteen-entry pre-closure evidence-production set: its ordered direct-attempt payload is SHA-256-bound before finalization, while later validator and regression reruns are explicitly post-closure verification and cannot add, replace, or invalidate a canonical probe row."
  artifacts:
    - path: tests/phase1/test_gap_closeout.py
      provides: "OPER-03 and substantive EVID-03/EVID-04 Nyquist edge tests"
    - path: .planning/phases/01-research-and-truth-baseline/01-VALIDATION.md
      provides: "Current Phase 1 validation evidence, finite pre-closure probe ledger, probe map, and human-review boundaries"
    - path: .planning/phases/01-research-and-truth-baseline/01-POST-CLOSURE-VERIFICATION.md
      provides: "Append-only, digest-bound batches of deliberately retained post-closure validator or regression outcomes without a global command-count claim"
  key_links:
    - from: each probe ID
      to: one named test and one final verifier record
      via: "exactly one canonical evidence row backed by its designated successful direct wrapper attempt; discovery and reruns remain separate noncanonical command-attempt history"
    - from: validation status
      to: full-gate output from Plan 01-37
      via: "actual wrapper command/date/result rather than pending template fields"
    - from: the closed fourteen-row pre-closure ledger
      to: retained post-closure verification batches
      via: "ledger ID plus SHA-256 of the normalized direct-attempt payload; later replayable verifier outcomes bind to that digest without changing the closed payload or canonical matrix"
  prohibitions:
    - statement: "Nyquist coverage cannot approve an ADR, recommendation, residual risk, source authority, package admission, or phase transition."
      category: authority
      status: unresolved
      flagged: true
      verification: "Manual boundary section and non-approval assertions are required for EVID-03/EVID-04 probes."
---

<objective>
Complete the Phase 1 Nyquist contract with real edge probes and actual full-gate evidence.

Purpose: Replace the stale Wave 0 template and ensure every checker-required probe is a substantive executable test rather than a structural label.
Output: Fourteen-probe test map and truthful validation strategy.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-research-and-truth-baseline/01-VALIDATION.md
@.planning/phases/01-research-and-truth-baseline/01-POST-CLOSURE-VERIFICATION.md
@.planning/phases/01-research-and-truth-baseline/01-VERIFICATION.md
@.planning/phases/01-research-and-truth-baseline/01-REVIEW.md
@tests/phase1/{test_preflight.py,test_drift.py,test_compatibility.py,test_spikes.py,test_spike_consolidation.py,test_closeout_commands.py}
@.planning/research/{compatibility-matrix.yaml,source-registry.yaml,package-evidence.yaml,phase-governance.yaml}
@.planning/spikes/replay-manifest.yaml
@tools/{phase1-python,validate-planning.py,validate-research.py}
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Make all EVID-03/EVID-04 and OPER-03 edge probes executable and non-approving</name>
  <read_first>tests/phase1/test_compatibility.py; tests/phase1/test_spikes.py; tests/phase1/test_governance.py; compatibility-matrix.yaml; source-registry.yaml; package-evidence.yaml; replay-manifest.yaml; phase-governance.yaml; 01-VALIDATION.md</read_first>
  <files>tests/phase1/test_gap_closeout.py</files>
  <behavior>
    - `test_compatibility_completeness_requires_qualified_dimensions_without_approval` uses a full dimensioned row and proves missing normative protocol, package artifact, runtime, example/fixture, current-work, or conformance input is `blocked`, never complete/approved.
    - `test_mandatory_spike_completeness_requires_retained_evidence_without_approval` traverses all SPK-A through SPK-L report/metadata/measurement entries, raw-output/evidence-link/replay digest bindings, and a deterministic mutation; it reports concrete gaps and no approval state.
    - `test_governance_role_adjacency_requires_independence`, `test_governance_empty_null_single_required_fields_fail`, and `test_governance_ordering_is_stable` cover the three OPER-03 probes with stable IDs/diagnostics.
  </behavior>
  <action>Write `test_gap_closeout.GapCloseoutTests.test_compatibility_completeness_requires_qualified_dimensions_without_approval`, `test_gap_closeout.GapCloseoutTests.test_mandatory_spike_completeness_requires_retained_evidence_without_approval`, `test_gap_closeout.GapCloseoutTests.test_governance_role_adjacency_requires_independence`, `test_gap_closeout.GapCloseoutTests.test_governance_empty_null_single_required_fields_fail`, and `test_gap_closeout.GapCloseoutTests.test_governance_ordering_is_stable` before implementation. Reuse the substantive evaluator from Plan 01-35 rather than checking labels: build copied canonical fixtures for a complete qualified dimension set and mutations that remove each required qualified link, then assert `blocked`, ordered reason/impact evidence, and an explicit non-approval result. Traverse the actual replay manifest's exact twelve SPK directories in a temporary root; require report/metadata/measurements plus every raw-output/evidence-link/replay binding to validate, mutate one retained digest, and assert a concrete stable gap with no approval. Add governance fixtures for adjacent independent roles, empty/null/single role/evidence forms, and permutations of requirement/signoff/exit-evidence IDs; assert stable diagnostics. Do not mutate canonical records in any test. These tests make the EVID-03/EVID-04 probes substantive and satisfy D-14 through D-18, D-21 through D-24, D-27 through D-35, and the manual approval boundary.</action>
  <verify><automated>tools/phase1-python -m unittest discover -s tests/phase1 -p 'test_gap_closeout.py'</automated></verify>
  <acceptance_criteria>The five named tests pass through the approved wrapper; all required dimensions and retained outputs are materially inspected; missing/mutated inputs yield stable gaps; no test result can be approval or architecture acceptance.</acceptance_criteria>
  <done>Each previously specless EVID-03, EVID-04, and OPER-03 edge has real executable coverage with an explicit non-approval boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile the validation contract with the canonical fourteen-probe evidence ledger</name>
  <read_first>01-VALIDATION.md; 01-POST-CLOSURE-VERIFICATION.md; tests/phase1/test_gap_closeout.py; test_preflight.py; test_drift.py; test_compatibility.py; test_spikes.py; test_spike_consolidation.py; test_closeout_commands.py; Plan 01-37 gate evidence</read_first>
  <files>tests/phase1/test_gap_closeout.py, .planning/phases/01-research-and-truth-baseline/01-VALIDATION.md, .planning/phases/01-research-and-truth-baseline/01-POST-CLOSURE-VERIFICATION.md</files>
  <action>Addresses review concern: establish a finite command-attempt closure rather than asking a canonical ledger to absorb later validation. Start RED with `test_validation_probe_attempt_ledger_is_canonical_and_complete` and `test_validation_probe_attempt_ledger_is_closed_before_post_closure_verification`, using temporary Markdown and post-closure-audit fixtures that reject an absent or duplicate direct attempt ID, an omitted required direct attempt, a canonical row pointing to a non-direct history entry, non-wrapper or non-designated direct argv, nonzero canonical exit, absent or duplicate canonical selection, a canonical attempt/evidence locator mismatch, a missing closure marker, a changed closed-payload digest, a post-closure record bound to a different ledger digest, or a post-closure record that changes the closed canonical matrix. Require nonempty `GSD_EXECUTOR_ID`, run every named probe through `tools/phase1-python`, then replace only stale draft/pending assertions in `01-VALIDATION.md` with actual date, command, result, and manual-review boundary evidence. Define the canonical ledger as `P1-38-NYQUIST-DIRECT-14`: its complete scope is exactly fourteen designated direct evidence-production attempts, one for each fixed probe ID, and its closure boundary is after all fourteen rows have successful result/evidence locators and before any task-level validator or regression rerun. Give each direct row `P1-38-DIRECT-<probe-id>`, role `direct`, exactly one probe ID, exact wrapper argv, exit, compact result, and evidence locator. Compute `preClosureLedgerSha256` over the UTF-8 canonical serialization of the ledger ID, closure boundary, and lexically ordered fourteen-row payload, excluding the digest field itself; persist the closure timestamp and digest beside the matrix. Record the exact Plan 01-37 command sequence and results, including `GSD_EXECUTOR_ID="$GSD_EXECUTOR_ID" tools/phase1-python tools/validate-planning.py --phase-1-complete --executor-identity "$GSD_EXECUTOR_ID"`, in a distinct Prerequisite Gate Evidence table rather than as canonical probe-attempt rows. Execute every direct attempt required by the matrix: P-EVID01-ADJ to `test_citation_ascii_adjacency_extracts_exact_token`; P-EVID01-EMPTY to `test_citation_empty_inventory_has_no_diagnostic`; P-EVID01-ENCODING to `test_citation_tokens_are_ascii_decoded_codepoints_without_unicode_normalization`; P-EVID01-ORDER to `test_citation_diagnostics_stable_path_then_id`; P-EVID02-ADJ to `test_parallel_sides_are_distinct_and_adjacent`; P-EVID02-EMPTY to `test_drift_rejects_empty_null_and_single_sides`; P-EVID02-ORDER to `test_drift_records_sort_ids_observations_impacts_and_history`; P-EVID03-MANUAL to the named compatibility completeness test; P-EVID04-MANUAL to the named mandatory-spike completeness test; P-OPER01-IDEMPOTENCY to `test_duplicate_refresh_observations_are_idempotent`; P-OPER01-CONCURRENCY to `test_recovery_after_each_publish_interruption_is_coherent`; and the three OPER-03 rows to the three named governance tests. Add the exact fourteen-row canonical probe matrix only after those direct rows are complete; each row references one successful designated direct attempt ID and its matching evidence locator. Create `01-POST-CLOSURE-VERIFICATION.md` with append-only batches: each batch has a unique batch ID, the closed ledger ID and `preClosureLedgerSha256`, timestamp, normalized batch-payload SHA-256 excluding its digest field, and individually identified retained runs with purpose, exact wrapper argv, exit, compact result digest, and evidence locator. The task's `<verify>` invocations and every later validator, security, terminal, or regression rerun are post-closure verification: they may execute repeatedly, may be retained in additional batches, and must never create, replace, or affect the completeness of a canonical probe row. A post-closure batch records only deliberately retained outcomes; it is not an assertion that all process invocations have been counted. Require the RED tests to prove the completed validation file has the finite fourteen-row direct ledger, its matching digest, and a valid noncanonical post-closure binding; they must accept repeated post-closure argv in separate batches while preserving the same closed ledger digest and matrix. Set `nyquist_compliant: true` and `wave_0_complete: true` only if every canonical row, closed-ledger check, and prerequisite full command passed; otherwise keep both false and list precise failing probe IDs and attempt IDs. Add a Manual Evidence-Completeness Boundaries section stating that compatibility/spike classifications prepare human review and cannot accept an ADR, recommendation, residual risk, or transition. This implements D-14 through D-18, D-21 through D-24, D-27 through D-35, and the manual approval boundary.</action>
  <verify><automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_gap_closeout.GapCloseoutTests.test_validation_probe_attempt_ledger_is_canonical_and_complete && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_gap_closeout.GapCloseoutTests.test_validation_probe_attempt_ledger_is_closed_before_post_closure_verification && tools/phase1-python -m unittest discover -s tests/phase1 -p 'test_gap_closeout.py' && tools/phase1-python -m unittest discover -s tests/phase1 && test -n "$GSD_EXECUTOR_ID" && GSD_EXECUTOR_ID="$GSD_EXECUTOR_ID" tools/phase1-python tools/validate-planning.py --phase-1-complete --executor-identity "$GSD_EXECUTOR_ID"</automated></verify>
  <acceptance_criteria>The validation file has exactly fourteen canonical probe IDs mapped once each to real named tests/fixtures and a finalized `P1-38-NYQUIST-DIRECT-14` payload with a matching pre-closure digest. Every canonical row references one successful designated direct wrapper attempt with matching evidence. The automated full-suite and completion checks in this task run after ledger closure as post-closure verification, may repeat without a global execution-count requirement, and cannot change the ledger digest or canonical matrix; deliberately retained reruns bind to that digest in independent post-closure batches. Nyquist state accurately reflects validated command results without approval claims.</acceptance_criteria>
  <done>The Phase 1 validation contract has a bounded, evidence-backed canonical probe ledger and permits auditable post-closure verification without reopening Nyquist canonicality.</done>
</task>
</tasks>

## Artifacts this phase produces
- `tests/phase1/test_gap_closeout.py` — substantive EVID-03/EVID-04 and OPER-03 edge tests.
- `01-VALIDATION.md` — current full-gate prerequisite evidence, SHA-256-bound fourteen-row pre-closure direct-attempt ledger, and canonical probe matrix.
- `01-POST-CLOSURE-VERIFICATION.md` — append-only, ledger-digest-bound batches for deliberately retained validator and regression reruns that do not change canonical completeness.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| evidence completeness classifier → validation conclusion | A structural label could be mistaken for qualified evidence. |
| governance record → OPER-03 closeout | Incomplete/aliased role records could appear complete. |
| validation contract → final verifier | A stale template can falsely imply completed coverage. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-38-01 | Spoofing | compatibility completeness | high | mitigate | Task 1 demands qualified dimensions and rejects non-approval shortcuts. |
| T-38-02 | Tampering | mandatory spike evidence | high | mitigate | Task 1 traverses all retained outputs and replay bindings. |
| T-38-03 | Repudiation | governance evidence | high | mitigate | Task 1 checks independent roles, completeness, and stable ordering. |
| T-38-04 | Tampering | stale or misattributed validation status | high | mitigate | Task 2 finalizes a SHA-256-bound fourteen-entry direct-attempt ledger before post-closure verification; later retained reruns bind to its digest in separate batches and cannot alter canonical rows. |
| T-38-05 | Repudiation | human approval boundary | high | mitigate | Task 2 documents non-approval output for manual dimensions. |
</threat_model>

<verification>
Close the fourteen designated direct evidence-production attempts, validate their digest and matrix, then run closeout tests, the full Phase 1 test suite, and the complete planning gate through the wrapper as post-closure verification before marking Nyquist evidence current.
</verification>

<success_criteria>
All fourteen required probes map to actual substantive evidence through exactly one canonical row backed by one successful designated direct wrapper attempt in the finalized SHA-256-bound pre-closure ledger; later validator and regression reruns remain separately auditable post-closure verification and do not change canonical completeness.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-38-SUMMARY.md` when done.
</output>
