---
phase: 01-research-and-truth-baseline
plan: 38
type: execute
wave: 11
depends_on: [01-37]
files_modified:
  - tests/phase1/test_gap_closeout.py
  - .planning/phases/01-research-and-truth-baseline/01-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [EVID-01, EVID-02, EVID-03, EVID-04, OPER-01, OPER-03]
must_haves:
  truths:
    - "The validation contract maps all fourteen exact edge probes to executable named tests and records actual post-remediation evidence."
    - "EVID-03 and EVID-04 completeness tests prove substantive eligibility and retained evidence without treating a blocked label or policy placeholder as a successful baseline or approval."
    - "Nyquist status becomes true only when all required commands and probe mappings pass; otherwise the contract truthfully remains noncompliant with a gap map."
  artifacts:
    - path: tests/phase1/test_gap_closeout.py
      provides: "OPER-03 and substantive EVID-03/EVID-04 Nyquist edge tests"
    - path: .planning/phases/01-research-and-truth-baseline/01-VALIDATION.md
      provides: "Current Phase 1 validation evidence, probe map, and human-review boundaries"
  key_links:
    - from: each probe ID
      to: one named test and one final verifier record
      via: "exactly-once matrix mapping and command evidence"
    - from: validation status
      to: full-gate output from Plan 01-37
      via: "actual wrapper command/date/result rather than pending template fields"
  prohibitions:
    - statement: "Nyquist coverage cannot approve an ADR, recommendation, residual risk, source authority, package admission, or phase transition."
      category: authority
      status: unresolved
      flagged: true
      verification: "Manual boundary section and non-approval assertions are required for EVID-03/EVID-04 probes."
---

<objective>
Complete the Phase 1 Nyquist contract with real edge probes and actual full-gate evidence.

Purpose: Replace the stale Wave 0 template and ensure every checker-required probe is a substantive executable test rather than a structural label.
Output: Fourteen-probe test map and truthful validation strategy.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-research-and-truth-baseline/01-VALIDATION.md
@.planning/phases/01-research-and-truth-baseline/01-VERIFICATION.md
@.planning/phases/01-research-and-truth-baseline/01-REVIEW.md
@tests/phase1/{test_preflight.py,test_drift.py,test_compatibility.py,test_spikes.py,test_spike_consolidation.py,test_closeout_commands.py}
@.planning/research/{compatibility-matrix.yaml,source-registry.yaml,package-evidence.yaml,phase-governance.yaml}
@.planning/spikes/replay-manifest.yaml
@tools/{phase1-python,validate-planning.py,validate-research.py}
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Make all EVID-03/EVID-04 and OPER-03 edge probes executable and non-approving</name>
  <read_first>tests/phase1/test_compatibility.py; tests/phase1/test_spikes.py; tests/phase1/test_governance.py; compatibility-matrix.yaml; source-registry.yaml; package-evidence.yaml; replay-manifest.yaml; phase-governance.yaml; 01-VALIDATION.md</read_first>
  <files>tests/phase1/test_gap_closeout.py</files>
  <behavior>
    - `test_compatibility_completeness_requires_qualified_dimensions_without_approval` uses a full dimensioned row and proves missing normative protocol, package artifact, runtime, example/fixture, current-work, or conformance input is `blocked`, never complete/approved.
    - `test_mandatory_spike_completeness_requires_retained_evidence_without_approval` traverses all SPK-A through SPK-L report/metadata/measurement entries, raw-output/evidence-link/replay digest bindings, and a deterministic mutation; it reports concrete gaps and no approval state.
    - `test_governance_role_adjacency_requires_independence`, `test_governance_empty_null_single_required_fields_fail`, and `test_governance_ordering_is_stable` cover the three OPER-03 probes with stable IDs/diagnostics.
  </behavior>
  <action>Write `test_gap_closeout.GapCloseoutTests.test_compatibility_completeness_requires_qualified_dimensions_without_approval`, `test_gap_closeout.GapCloseoutTests.test_mandatory_spike_completeness_requires_retained_evidence_without_approval`, `test_gap_closeout.GapCloseoutTests.test_governance_role_adjacency_requires_independence`, `test_gap_closeout.GapCloseoutTests.test_governance_empty_null_single_required_fields_fail`, and `test_gap_closeout.GapCloseoutTests.test_governance_ordering_is_stable` before implementation. Reuse the substantive evaluator from Plan 01-35 rather than checking labels: build copied canonical fixtures for a complete qualified dimension set and mutations that remove each required qualified link, then assert `blocked`, ordered reason/impact evidence, and an explicit non-approval result. Traverse the actual replay manifest's exact twelve SPK directories in a temporary root; require report/metadata/measurements plus every raw-output/evidence-link/replay binding to validate, mutate one retained digest, and assert a concrete stable gap with no approval. Add governance fixtures for adjacent independent roles, empty/null/single role/evidence forms, and permutations of requirement/signoff/exit-evidence IDs; assert stable diagnostics. Do not mutate canonical records in any test. These tests make the EVID-03/EVID-04 probes substantive and satisfy D-14 through D-18, D-21 through D-24, D-27 through D-35, and the manual approval boundary.</action>
  <verify><automated>tools/phase1-python -m unittest discover -s tests/phase1 -p 'test_gap_closeout.py'</automated></verify>
  <acceptance_criteria>The five named tests pass through the approved wrapper; all required dimensions and retained outputs are materially inspected; missing/mutated inputs yield stable gaps; no test result can be approval or architecture acceptance.</acceptance_criteria>
  <done>Each previously specless EVID-03, EVID-04, and OPER-03 edge has real executable coverage with an explicit non-approval boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the validation contract with the exact fourteen-probe matrix</name>
  <read_first>01-VALIDATION.md; tests/phase1/test_gap_closeout.py; test_preflight.py; test_drift.py; test_compatibility.py; test_spikes.py; test_spike_consolidation.py; test_closeout_commands.py; Plan 01-37 gate evidence</read_first>
  <files>.planning/phases/01-research-and-truth-baseline/01-VALIDATION.md</files>
  <action>Require nonempty `GSD_EXECUTOR_ID`, run every named probe through `tools/phase1-python`, then replace only stale draft/pending assertions in `01-VALIDATION.md` with actual date, command, result, and manual-review boundary evidence. Add an exact fourteen-row probe matrix mapping: P-EVID01-ADJ to `test_citation_ascii_adjacency_extracts_exact_token`; P-EVID01-EMPTY to `test_citation_empty_inventory_has_no_diagnostic`; P-EVID01-ENCODING to `test_citation_tokens_are_ascii_decoded_codepoints_without_unicode_normalization`; P-EVID01-ORDER to `test_citation_diagnostics_stable_path_then_id`; P-EVID02-ADJ to `test_parallel_sides_are_distinct_and_adjacent`; P-EVID02-EMPTY to `test_drift_rejects_empty_null_and_single_sides`; P-EVID02-ORDER to `test_drift_records_sort_ids_observations_impacts_and_history`; P-EVID03-MANUAL to the named compatibility completeness test; P-EVID04-MANUAL to the named mandatory-spike completeness test; P-OPER01-IDEMPOTENCY to `test_duplicate_refresh_observations_are_idempotent`; P-OPER01-CONCURRENCY to `test_recovery_after_each_publish_interruption_is_coherent`; and the three OPER-03 rows to the three named governance tests. Record the exact Plan 01-37 command sequence and results, including `GSD_EXECUTOR_ID="$GSD_EXECUTOR_ID" tools/phase1-python tools/validate-planning.py --phase-1-complete --executor-identity "$GSD_EXECUTOR_ID"`. Set `nyquist_compliant: true` and `wave_0_complete: true` only if every row and full command passed; otherwise keep both false and list precise failing probe IDs. Add a Manual Evidence-Completeness Boundaries section stating that compatibility/spike classifications prepare human review and cannot accept an ADR, recommendation, residual risk, or transition.</action>
  <verify><automated>tools/phase1-python -m unittest discover -s tests/phase1 -p 'test_gap_closeout.py' && tools/phase1-python -m unittest discover -s tests/phase1 && test -n "$GSD_EXECUTOR_ID" && GSD_EXECUTOR_ID="$GSD_EXECUTOR_ID" tools/phase1-python tools/validate-planning.py --phase-1-complete --executor-identity "$GSD_EXECUTOR_ID"</automated></verify>
  <acceptance_criteria>The validation file has exactly fourteen probe IDs mapped once each to real named tests/fixtures, every mapping records wrapper command/result evidence, and its Nyquist state accurately reflects observed command results without approval claims.</acceptance_criteria>
  <done>The Phase 1 validation contract is current, evidence-backed, and Nyquist-compliant only when its complete probe set is actually green.</done>
</task>
</tasks>

## Artifacts this phase produces
- `tests/phase1/test_gap_closeout.py` — substantive EVID-03/EVID-04 and OPER-03 edge tests.
- `01-VALIDATION.md` — current full-gate evidence and exactly-once fourteen-probe matrix.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| evidence completeness classifier → validation conclusion | A structural label could be mistaken for qualified evidence. |
| governance record → OPER-03 closeout | Incomplete/aliased role records could appear complete. |
| validation contract → final verifier | A stale template can falsely imply completed coverage. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-38-01 | Spoofing | compatibility completeness | high | mitigate | Task 1 demands qualified dimensions and rejects non-approval shortcuts. |
| T-38-02 | Tampering | mandatory spike evidence | high | mitigate | Task 1 traverses all retained outputs and replay bindings. |
| T-38-03 | Repudiation | governance evidence | high | mitigate | Task 1 checks independent roles, completeness, and stable ordering. |
| T-38-04 | Tampering | stale validation status | high | mitigate | Task 2 writes status only from observed commands/probes. |
| T-38-05 | Repudiation | human approval boundary | high | mitigate | Task 2 documents non-approval output for manual dimensions. |
</threat_model>

<verification>
Run closeout tests, the full Phase 1 test suite, and the complete planning gate through the wrapper before marking Nyquist evidence current.
</verification>

<success_criteria>
All fourteen required probes map to actual, substantive, exactly-once test evidence; the validation strategy is no longer a pending template.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-38-SUMMARY.md` when done.
</output>
