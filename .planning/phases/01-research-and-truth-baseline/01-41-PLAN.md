---
phase: 01-research-and-truth-baseline
plan: 41
type: execute
wave: 6
depends_on: [01-31]
files_modified:
  - tools/collect-registry-evidence.py
  - .planning/research/reports/package-registry-fixture-receipt-20260728.yaml
  - .planning/research/reports/package-registry-live-or-blocker-receipt-20260728.yaml
  - .planning/research/package-evidence.yaml
  - .planning/research/package-map.md
  - .planning/research/ecosystem-inventory.yaml
  - .planning/research/open-work-snapshot.json
  - tests/phase1/test_compatibility.py
  - tests/phase1/fixtures/registry-response.json
autonomous: true
gap_closure: true
requirements: [EVID-03, EVID-04, OPER-01]
must_haves:
  truths:
    - "Registry artifact evidence is independently recorded by a bounded collector and immutable response receipt without package installation or import."
    - "Fixture collection always proves the collector and exact-byte receipt mechanism, while only a permitted bounded live collection can support a published-package observation."
    - "Unavailable live collection remains an impact-scoped package blocker rather than an inferred published-package fact."
    - "Incomplete package evidence remains an impact-scoped blocked admission result."
    - "Review finding M1 is resolved: fixture and live-or-blocker collection write separate immutable receipts, each is revalidated after collection, and evidence records retain both attributable attempt histories."
  artifacts:
    - path: tools/collect-registry-evidence.py
      provides: "Bounded fixture/live registry collector that never installs or imports a package"
    - path: .planning/research/reports/package-registry-fixture-receipt-20260728.yaml
      provides: "Immutable exact captured-byte fixture-collection receipt"
    - path: .planning/research/reports/package-registry-live-or-blocker-receipt-20260728.yaml
      provides: "Immutable live-collection or impact-scoped-blocker receipt distinct from fixture history"
    - path: .planning/research/package-evidence.yaml
      provides: "Registry artifact evidence distinct from repository release commits"
    - path: .planning/research/open-work-snapshot.json
      provides: "Dated package/current-work observation preserving prior IDs"
  key_links:
    - from: immutable fixture and live-or-blocker receipts
      to: package-evidence artifact records
      via: "separate attempt IDs, recomputed SHA-256 of exact captured bytes or blocker inputs, explicit transport classification, and post-collection receipt validation"
    - from: package-evidence artifact records
      to: package-map and SPK-G eligibility
      via: "integrity, provenance, license, root-export and release-versus-source evidence"
---

<objective>
Independently record public package registry evidence after authority-gated source/normative intake.

Purpose: Split dense package intake from Plan 01-31 while retaining D-05 through D-09, D-14, D-20, and D-24 boundaries.
Output: Package evidence or explicit scoped package blockers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-31-SUMMARY.md
@.planning/research/{package-map.md,package-evidence.yaml,ecosystem-inventory.yaml,open-work-snapshot.json}
@.planning/spikes/spk-g-package-conformance/metadata.yaml
@tests/phase1/test_compatibility.py
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Collect immutable fixture and live-or-blocker registry receipts without overwriting either history</name>
  <read_first>package-map.md; package-evidence.yaml; ecosystem-inventory.yaml; open-work-snapshot.json; SPK-G metadata.yaml; tests/phase1/test_compatibility.py; 0010-package-versioning.md; 01-CONTEXT.md</read_first>
  <files>tools/collect-registry-evidence.py, .planning/research/reports/package-registry-fixture-receipt-20260728.yaml, .planning/research/reports/package-registry-live-or-blocker-receipt-20260728.yaml, .planning/research/package-evidence.yaml, tests/phase1/test_compatibility.py, tests/phase1/fixtures/registry-response.json</files>
  <behavior>
    - `test_registry_collector_fixture_receipt_rehashes_exact_response_bytes` starts RED, runs the actual collector against a bounded checked-in transport fixture, and proves the fixture receipt SHA-256 is recomputed from the exact captured bytes; changing one captured byte, receipt hash, method, host, redirect, timeout, or size bound fails.
    - `test_registry_attempt_receipts_are_separate_immutable_and_revalidated` proves fixture collection and live-or-blocker collection create different attempt IDs and different receipt paths, each collector invocation refuses to overwrite an existing receipt, `validate-receipt` succeeds for both retained histories, and the live-or-blocker attempt cannot change the fixture receipt bytes or its evidence attribution.
    - `test_registry_live_unavailable_routes_impact_scoped_blocker` proves a denied, unavailable, or nonessential live collection yields a dated package blocker with affected EVID/ADR/OQ/DRF scope and refresh trigger in the live-or-blocker receipt, not fixture evidence promoted to a published-package fact.
    - Source-only release data and a missing integrity, provenance, license, or package-root-export field produce `approval: not-approved` with an impact-scoped blocker; no package command or import is attempted.
  </behavior>
  <action>Resolves review finding M1 (receipt overwrite/revalidation) and applies D-05 through D-10, D-14, D-20, and D-24. Start RED with deterministic fixture transports for success, disallowed host, redirect, timeout, oversized body, changed response bytes, and unavailable live access. Create `tools/collect-registry-evidence.py` with an explicit `GET` method, HTTPS allowlist for the selected official registry host, redirects disabled or limited to that allowlist, connect/read timeout, streaming response-size cap, and no credentials, proxy inheritance, package manager, Node, install, script, or import. Give each collection invocation a generated stable attempt ID and make the receipt writer create its target with exclusive creation semantics: an existing receipt path is a nonzero refusal, never a replacement. The task always executes bounded fixture mode through the collector, captures the response bytes, recomputes their SHA-256, and writes only `.planning/research/reports/package-registry-fixture-receipt-20260728.yaml` with attempt ID, request method, canonical URL, allowed-host/redirect decision, allowed response status/headers, retrieval timestamp, byte count, exact response-byte SHA-256, and explicit `transport: fixture` classification. Immediately run `validate-receipt` against that fixture receipt. Fixture success proves collector/receipt mechanics only; it must not create a published-package fact or package-admission approval. Implement `--live-if-permitted` as a separately bounded read-only mode that always writes only `.planning/research/reports/package-registry-live-or-blocker-receipt-20260728.yaml`: it runs a live request only if D-10 permits the public read and the declared package-evidence scope establishes D-24 essential, sandboxed, credential-free collection, using the same limits and receipt rehash. If those predicates are not met or collection is unavailable, write a valid live-or-blocker receipt with its own attempt ID, failure or policy reason, affected requirement/phase/ADR/OQ/DRF IDs, safe fallback, and refresh trigger. Immediately run `validate-receipt` against that live-or-blocker receipt too. A live receipt may record observed registry metadata but still cannot install or import the package. Require `package-evidence.yaml` to retain both receipt paths, IDs, SHA-256 values, and transport/outcome classifications; fixture evidence remains mechanism-only even after a live attempt. Use a qualifying live receipt plus official project metadata to record one selected `@napplet/*` or `@kehto/*` artifact's version, tarball URL, SRI/integrity and exposed SHA, provenance/attestation or explicit absence, license, documented root exports, retrieval time, and released-commit versus source-head comparison. Reject repository release PR data as an artifact substitute.</action>
  <verify><automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_compatibility.CompatibilitySchema.test_registry_collector_fixture_receipt_rehashes_exact_response_bytes && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_compatibility.CompatibilitySchema.test_registry_attempt_receipts_are_separate_immutable_and_revalidated && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_compatibility.CompatibilitySchema.test_registry_live_unavailable_routes_impact_scoped_blocker && test -s .planning/research/reports/package-registry-fixture-receipt-20260728.yaml && tools/phase1-python tools/collect-registry-evidence.py validate-receipt --receipt .planning/research/reports/package-registry-fixture-receipt-20260728.yaml && test -s .planning/research/reports/package-registry-live-or-blocker-receipt-20260728.yaml && tools/phase1-python tools/collect-registry-evidence.py validate-receipt --receipt .planning/research/reports/package-registry-live-or-blocker-receipt-20260728.yaml && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_compatibility.CompatibilitySchema.test_published_package_metadata_requires_independent_registry_artifact</automated></verify>
  <acceptance_criteria>The actual collector runs in fixture mode, receipt hashing is recomputed from captured bytes, and every malformed receipt/boundary fixture fails. Fixture and live-or-blocker receipts have distinct immutable paths and attempt IDs, both survive the second collection unchanged and pass their own post-collection validation. Fixture output is explicitly non-factual; permitted live output is observed package evidence only, while nonpermitted or unavailable live collection leaves a detailed impact-scoped blocker and never invokes package code.</acceptance_criteria>
  <done>Separate validated fixture and live-or-blocker histories preserve the provenance required to pin a registry artifact or accurately block admission before any conformance operation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve package history and package-to-SPK-G blocker routing</name>
  <read_first>package-evidence.yaml; package-map.md; ecosystem-inventory.yaml; open-work-snapshot.json; .planning/research/reports/package-registry-fixture-receipt-20260728.yaml; .planning/research/reports/package-registry-live-or-blocker-receipt-20260728.yaml; SPK-G metadata.yaml; 0010-package-versioning.md</read_first>
  <files>.planning/research/package-map.md, .planning/research/ecosystem-inventory.yaml, .planning/research/open-work-snapshot.json, tests/phase1/test_compatibility.py</files>
  <action>Addresses review finding M1: update package map and ecosystem inventory to distinguish repository identity, observed release commit, independently retrieved artifact, immutable fixture receipt, and immutable live-or-blocker receipt. Require package evidence/history records to retain both receipt paths, attempt IDs, SHA-256 values, and classifications rather than selecting one latest receipt. Add a dated current-work snapshot without overwriting `OWS-001` or historic unavailable records. Retain a blocked package-admission disposition with impacted EVID/ADR/OQ/DRF identifiers, safe fallback, and refresh trigger until every SPK-G eligibility item plus its separate approval exists. Add `CompatibilitySchema.test_package_metadata_preserves_history_and_blocker_routing` for deterministic ordering, two-receipt attribution, and history/ID preservation.</action>
  <verify><automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_compatibility.CompatibilitySchema.test_published_package_metadata_requires_independent_registry_artifact && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_compatibility.CompatibilitySchema.test_package_metadata_preserves_history_and_blocker_routing && tools/phase1-python tools/validate-research.py validate --root .planning/research --report /tmp/phase1-package-intake.md</automated></verify>
  <acceptance_criteria>Repository identity, observed release commits, independently retrieved registry artifacts, fixture receipts, and live-or-blocker receipts remain distinct. Both receipt histories remain attributable after the live/blocker attempt and are linked by path, attempt ID, digest, and classification. `OWS-001` and historical unavailable records remain byte-present while one dated snapshot is appended. Any incomplete eligibility retains an impact-scoped SPK-G blocker with EVID/ADR/OQ/DRF links, safe fallback, and refresh trigger. The deterministic ordering/history-preservation regression and canonical research validation both exit zero.</acceptance_criteria>
  <done>Package evidence is separate, receipt-history-preserving, deterministic, and blocked rather than inferred when incomplete.</done>
</task>
</tasks>

## Artifacts this phase produces
- `tools/collect-registry-evidence.py` — bounded GET-only fixture/live collector with receipt rehash validation.
- `.planning/research/reports/package-registry-fixture-receipt-20260728.yaml` — immutable exact captured-byte fixture-mechanism receipt.
- `.planning/research/reports/package-registry-live-or-blocker-receipt-20260728.yaml` — immutable observed-live or impact-scoped-blocker attempt receipt, validated after collection.
- `.planning/research/package-evidence.yaml` and package history records — separate registry evidence, both receipt bindings, and blocked-admission routing for SPK-G.
- `tests/phase1/test_compatibility.py` and `tests/phase1/fixtures/registry-response.json` — direct collector/receipt, separate-history, revalidation, and unavailable-live boundary regressions.

## Created-symbol inventory
- `receipt.attemptId` — immutable identifier separating fixture and live-or-blocker collection histories.
- `receipt.transport` and `receipt.outcome` — explicit fixture, observed-live, or impact-scoped-blocker classification consumed by receipt validation and package evidence.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| registry metadata → package eligibility | Fields can be absent or untrusted. |
| release source → published artifact | A release commit can diverge from published bytes. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-41-01 | Tampering | package artifact metadata | high | mitigate | Task 1 independently pins integrity/provenance/license/exports. |
| T-41-02 | Elevation of Privilege | package execution | high | mitigate | Both tasks prohibit package manager execution and imports. |
| T-41-03 | Repudiation | package history | medium | mitigate | Task 2 preserves IDs, snapshots, impacts, refresh triggers, and both immutable receipt references. |
| T-41-04 | Repudiation | fixture/live collection provenance | medium | mitigate | Review finding M1 is mitigated by exclusive-create fixture and live-or-blocker receipt paths, distinct attempt IDs, post-collection `validate-receipt` checks, and history records that retain both bindings. |
</threat_model>

<verification>Run fixture collector/receipt rehash, fixture receipt validation, policy-gated live-or-blocker collection, live-or-blocker receipt validation, focused package tests, and canonical validation through the approved wrapper.</verification>
<success_criteria>Review finding M1 is closed: package intake preserves and revalidates independently attributable fixture and live-or-blocker histories without executing package code or weakening authority, package, or SPK-G approval boundaries.</success_criteria>
<output>Create `.planning/phases/01-research-and-truth-baseline/01-41-SUMMARY.md` when done.</output>
