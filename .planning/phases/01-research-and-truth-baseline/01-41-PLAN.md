---
phase: 01-research-and-truth-baseline
plan: 41
type: execute
wave: 4
depends_on: [01-31]
files_modified:
  - tools/collect-registry-evidence.py
  - .planning/research/reports/package-registry-receipt-20260728.yaml
  - .planning/research/package-evidence.yaml
  - .planning/research/package-map.md
  - .planning/research/ecosystem-inventory.yaml
  - .planning/research/open-work-snapshot.json
  - tests/phase1/test_compatibility.py
  - tests/phase1/fixtures/registry-response.json
autonomous: true
gap_closure: true
requirements: [EVID-03, EVID-04, OPER-01]
must_haves:
  truths:
    - "Registry artifact evidence is independently recorded by a bounded collector and immutable response receipt without package installation or import."
    - "Fixture collection always proves the collector and exact-byte receipt mechanism, while only a permitted bounded live collection can support a published-package observation."
    - "Unavailable live collection remains an impact-scoped package blocker rather than an inferred published-package fact."
    - "Incomplete package evidence remains an impact-scoped blocked admission result."
  artifacts:
    - path: tools/collect-registry-evidence.py
      provides: "Bounded fixture/live registry collector that never installs or imports a package"
    - path: .planning/research/reports/package-registry-receipt-20260728.yaml
      provides: "Exact captured-response-byte SHA-256 receipt with fixture/live provenance"
    - path: .planning/research/package-evidence.yaml
      provides: "Registry artifact evidence distinct from repository release commits"
    - path: .planning/research/open-work-snapshot.json
      provides: "Dated package/current-work observation preserving prior IDs"
  key_links:
    - from: collect-registry-evidence receipt
      to: package-evidence artifact records
      via: "recomputed SHA-256 of the exact captured response bytes and explicit fixture/live classification"
    - from: package-evidence artifact records
      to: package-map and SPK-G eligibility
      via: "integrity, provenance, license, root-export and release-versus-source evidence"
---

<objective>
Independently record public package registry evidence after authority-gated source/normative intake.

Purpose: Split dense package intake from Plan 01-31 while retaining D-05 through D-09, D-14, D-20, and D-24 boundaries.
Output: Package evidence or explicit scoped package blockers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-31-SUMMARY.md
@.planning/research/{package-map.md,package-evidence.yaml,ecosystem-inventory.yaml,open-work-snapshot.json}
@.planning/spikes/spk-g-package-conformance/metadata.yaml
@tests/phase1/test_compatibility.py
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Collect one bounded registry fixture receipt and conditionally record a live package blocker</name>
  <read_first>package-map.md; package-evidence.yaml; ecosystem-inventory.yaml; open-work-snapshot.json; SPK-G metadata.yaml; tests/phase1/test_compatibility.py; 0010-package-versioning.md; 01-CONTEXT.md</read_first>
  <files>tools/collect-registry-evidence.py, .planning/research/reports/package-registry-receipt-20260728.yaml, .planning/research/package-evidence.yaml, tests/phase1/test_compatibility.py, tests/phase1/fixtures/registry-response.json</files>
  <behavior>
    - `test_registry_collector_fixture_receipt_rehashes_exact_response_bytes` starts RED, runs the actual collector against a bounded checked-in transport fixture, and proves the receipt SHA-256 is recomputed from the exact captured bytes; changing one captured byte, receipt hash, method, host, redirect, timeout, or size bound fails.
    - `test_registry_live_unavailable_routes_impact_scoped_blocker` proves a denied, unavailable, or nonessential live collection yields a dated package blocker with affected EVID/ADR/OQ/DRF scope and refresh trigger, not fixture evidence promoted to a published-package fact.
    - Source-only release data and a missing integrity, provenance, license, or package-root-export field produce `approval: not-approved` with an impact-scoped blocker; no package command or import is attempted.
  </behavior>
  <action>Addresses review concern and human decision 7: own the collector and receipt in this task. Start RED with deterministic fixture transports for success, disallowed host, redirect, timeout, oversized body, changed response bytes, and unavailable live access. Create `tools/collect-registry-evidence.py` with an explicit `GET` method, HTTPS allowlist for the selected official registry host, redirects disabled or limited to that allowlist, connect/read timeout, streaming response-size cap, and no credentials, proxy inheritance, package manager, Node, install, script, or import. The task always executes bounded fixture mode through the collector, captures the response bytes, recomputes their SHA-256, and writes `.planning/research/reports/package-registry-receipt-20260728.yaml` with request method, canonical URL, allowed-host/redirect decision, allowed response status/headers, retrieval timestamp, byte count, exact response-byte SHA-256, and explicit `transport: fixture` classification. Fixture success proves collector/receipt mechanics only; it must not create a published-package fact or package-admission approval. Implement `--live-if-permitted` as a separately bounded read-only mode: it runs only if D-10 permits the public read and the declared package-evidence scope establishes D-24 essential, sandboxed, credential-free live collection; it uses the same limits and receipt rehash. If those predicates are not met or collection is unavailable, persist a dated impact-scoped blocker with affected requirement, phase, ADR, OQ/DRF IDs, safe fallback, and refresh trigger. A live receipt may record observed registry metadata but still cannot install or import the package. Use a qualifying receipt plus official project metadata to record one selected `@napplet/*` or `@kehto/*` artifact's version, tarball URL, SRI/integrity and exposed SHA, provenance/attestation or explicit absence, license, documented root exports, retrieval time, and released-commit versus source-head comparison. Reject repository release PR data as an artifact substitute. Apply D-05 through D-10, D-14, D-20, and D-24.</action>
  <verify><automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_compatibility.CompatibilitySchema.test_registry_collector_fixture_receipt_rehashes_exact_response_bytes && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_compatibility.CompatibilitySchema.test_registry_live_unavailable_routes_impact_scoped_blocker && tools/phase1-python tools/collect-registry-evidence.py collect --fixture tests/phase1/fixtures/registry-response.json --receipt .planning/research/reports/package-registry-receipt-20260728.yaml && tools/phase1-python tools/collect-registry-evidence.py validate-receipt --receipt .planning/research/reports/package-registry-receipt-20260728.yaml && tools/phase1-python tools/collect-registry-evidence.py collect --live-if-permitted --receipt .planning/research/reports/package-registry-receipt-20260728.yaml && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_compatibility.CompatibilitySchema.test_published_package_metadata_requires_independent_registry_artifact</automated></verify>
  <acceptance_criteria>The actual collector runs in fixture mode, receipt hashing is recomputed from captured bytes, and every malformed receipt/boundary fixture fails. Fixture output is explicitly non-factual; permitted live output is observed package evidence only, while nonpermitted or unavailable live collection leaves a detailed impact-scoped blocker and never invokes package code.</acceptance_criteria>
  <done>A registry artifact is independently pinned from a direct receipt or accurately blocked before any admission or conformance operation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve package history and package-to-SPK-G blocker routing</name>
  <read_first>package-evidence.yaml; package-map.md; ecosystem-inventory.yaml; open-work-snapshot.json; SPK-G metadata.yaml; 0010-package-versioning.md</read_first>
  <files>.planning/research/package-map.md, .planning/research/ecosystem-inventory.yaml, .planning/research/open-work-snapshot.json, tests/phase1/test_compatibility.py</files>
  <action>Update package map and ecosystem inventory to distinguish repository identity, observed release commit, and independently retrieved artifact. Add a dated current-work snapshot without overwriting `OWS-001` or historic unavailable records. Retain a blocked package-admission disposition with impacted EVID/ADR/OQ/DRF identifiers, safe fallback, and refresh trigger until every SPK-G eligibility item plus its separate approval exists. Add `CompatibilitySchema.test_package_metadata_preserves_history_and_blocker_routing` for deterministic ordering and history/ID preservation.</action>
  <verify><automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_compatibility.CompatibilitySchema.test_published_package_metadata_requires_independent_registry_artifact && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_compatibility.CompatibilitySchema.test_package_metadata_preserves_history_and_blocker_routing && tools/phase1-python tools/validate-research.py validate --root .planning/research --report /tmp/phase1-package-intake.md</automated></verify>
  <acceptance_criteria>Repository identity, observed release commits, and independently retrieved registry artifacts remain distinct. `OWS-001` and historical unavailable records remain byte-present while one dated snapshot is appended. Any incomplete eligibility retains an impact-scoped SPK-G blocker with EVID/ADR/OQ/DRF links, safe fallback, and refresh trigger. The deterministic ordering/history-preservation regression and canonical research validation both exit zero.</acceptance_criteria>
  <done>Package evidence is separate, history-preserving, deterministic, and blocked rather than inferred when incomplete.</done>
</task>
</tasks>

## Artifacts this phase produces
- `tools/collect-registry-evidence.py` — bounded GET-only fixture/live collector with receipt rehash validation.
- `.planning/research/reports/package-registry-receipt-20260728.yaml` — exact captured-byte receipt tagged as fixture mechanism, observed live evidence, or impact-scoped blocker.
- `.planning/research/package-evidence.yaml` and package history records — separate registry evidence and blocked-admission routing for SPK-G.
- `tests/phase1/test_compatibility.py` and `tests/phase1/fixtures/registry-response.json` — direct collector/receipt and unavailable-live boundary regressions.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| registry metadata → package eligibility | Fields can be absent or untrusted. |
| release source → published artifact | A release commit can diverge from published bytes. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-41-01 | Tampering | package artifact metadata | high | mitigate | Task 1 independently pins integrity/provenance/license/exports. |
| T-41-02 | Elevation of Privilege | package execution | high | mitigate | Both tasks prohibit package manager execution and imports. |
| T-41-03 | Repudiation | package history | medium | mitigate | Task 2 preserves IDs, snapshots, impacts, and refresh triggers. |
</threat_model>

<verification>Run fixture collector/receipt rehash, policy-gated live-or-blocker collection, focused package tests, and canonical validation through the approved wrapper.</verification>
<success_criteria>Package intake is independently evidenced or impact-scoped blocked without executing package code, and cannot weaken the authority, package, or SPK-G approval boundaries.</success_criteria>
<output>Create `.planning/phases/01-research-and-truth-baseline/01-41-SUMMARY.md` when done.</output>
