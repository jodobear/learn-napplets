---
phase: 01-research-and-truth-baseline
plan: 52
type: execute
wave: 2
depends_on: [01-49]
files_modified:
  - site/assets/site.js
  - site/dist/assets/site.js
  - tests/site/test_site_browser.py
autonomous: true
gap_closure: true
requirements: [EVID-01, OPER-03]
non_owning_support: [A11Y-01]
must_haves:
  truths:
    - "Replay calls scrollIntoView with behavior `auto` whenever `matchMedia('(prefers-reduced-motion: reduce)').matches` is true."
    - "After Plan 01-49 completes, the builder copies the authored `site/assets/site.js` bytes into tracked `site/dist/assets/site.js`; an explicit non-check build regenerates that asset before `--check` proves no generated drift."
    - "A real approved Chromium test directly captures the options passed by replay; computed CSS alone is not accepted as evidence of JavaScript behavior."
    - "No-preference replay may retain smooth behavior, transcript focus returns with preventScroll, reset remains deterministic, and essential static content remains usable without JavaScript."
  prohibitions:
    - requirement_id: OPER-03
      category: transparency
      status: unresolved
      verification: null
      statement: "MUST NOT treat computed CSS or automated browser success as a substitute for direct behavior evidence and later human accessibility judgment."
  flagged_assumptions:
    - "This repair covers the approved Chromium replay path only; the existing Firefox/cross-browser blocker remains unresolved per D-37, with no workaround inferred."
  artifacts:
    - path: site/assets/site.js
      provides: "Reduced-motion-aware replay scroll selection."
      contains: "prefers-reduced-motion: reduce"
    - path: site/dist/assets/site.js
      provides: "Tracked generated replay script copied from the authored asset by tools/build-site.py."
      contains: "prefers-reduced-motion: reduce"
    - path: tests/site/test_site_browser.py
      provides: "Direct capture of replay scrollIntoView options under reduced motion."
      contains: "test_reduced_motion_replay_uses_auto_scroll"
  key_links:
    - from: "site/assets/site.js replay click handler"
      to: "window.matchMedia"
      via: "The media-query match selects `auto` before the scrollIntoView call."
      pattern: "matchMedia"
    - from: "site/assets/site.js"
      to: "site/dist/assets/site.js"
      via: "tools/build-site.py reads DEFAULT_SCRIPT, emits outputs['assets/site.js'], and the explicit non-check build atomically refreshes the tracked generated asset before --check."
      pattern: 'outputs\["assets/site\.js"\]'
    - from: "tests/site/test_site_browser.py::test_reduced_motion_replay_uses_auto_scroll"
      to: "Element.prototype.scrollIntoView"
      via: "The browser test replaces the method with an option recorder before clicking replay."
      pattern: "scrollIntoView"
---

<objective>
Make transcript replay honor reduced-motion preference and prove the exact JavaScript option in a real browser.

Purpose: Close the active Plan 01-48 accessibility defect without changing static content, CSS semantics, browser/toolchain pins, or the deferred Firefox boundary.
Output: A one-branch script repair and direct Chromium option-observation regression.
</objective>

<execution_context>
@/home/at/.codex/gsd-core/workflows/execute-plan.md
@/home/at/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-FINAL-VERIFICATION.md
@.planning/phases/01-research-and-truth-baseline/01-48-PLAN.md
@.planning/phases/01-research-and-truth-baseline/01-49-SUMMARY.md
@tools/build-site.py
@site/assets/site.js
@site/dist/assets/site.js
@site/assets/styles.css
@tests/site/test_site_browser.py
</context>

<scope_boundary>
Run only after Plan 01-49 because both plans use the builder and tracked `site/dist` generation. Change only replay scroll-behavior selection, its generated script copy, and its browser observation. Preserve static transcript/table content, CSS reduced-motion rules, reset/focus behavior, local-only requests, direct-installed approved Chromium, and all generated HTML pages. Do not change browser pins, add a Firefox workaround, add packages, introduce essential JavaScript content, or infer A11Y-01 completion for its owning phase.
</scope_boundary>

<assumption_delta_decision>
Generalized noun: replay motion preference. Decision: no-change. Reduced motion is an existing browser preference branch, not a new configurable identity or alternative rendering model.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Observe replay options and switch reduced-motion scrolling to auto</name>
  <read_first>site/assets/site.js replay/reset handlers; site/dist/assets/site.js generated copy; tools/build-site.py DEFAULT_SCRIPT, render_all, build, and --check paths; tests/site/test_site_browser.py setUpClass builder invocation, context factory, and current reduced-motion test; 01-FINAL-VERIFICATION.md direct replay observation</read_first>
  <files>site/assets/site.js, site/dist/assets/site.js, tests/site/test_site_browser.py</files>
  <behavior>
    - With reduced_motion=`reduce`, replay invokes scrollIntoView with exactly `{behavior: 'auto', block: 'start'}`.
    - Replay still opens the transcript, focuses its summary without a second scroll, and records the replay status.
    - The test observes the call options directly by instrumenting the browser method before clicking; it does not infer from CSS.
  </behavior>
  <action>First split the current browser test so a named regression instruments `Element.prototype.scrollIntoView`, stores the received options in a page-global observation, clicks replay, and asserts the exact reduced-motion option object plus transcript/focus behavior. In `site/assets/site.js`, evaluate `window.matchMedia('(prefers-reduced-motion: reduce)').matches` at replay time and select `auto` for a match, otherwise `smooth`; keep block `start`, focus `{preventScroll: true}`, and status text unchanged. Use feature availability defensively so the optional enhancement cannot break static content. Immediately after editing the authored script, run `tools/phase1-python tools/build-site.py` explicitly so `tools/build-site.py` copies the new bytes into tracked `site/dist/assets/site.js`, then run `tools/phase1-python tools/build-site.py --check`; browser `setUpClass` rebuilding is not accepted as the regeneration step. Preserve the direct-installed Chromium route and D-37 Firefox blocker.</action>
  <verify>
    <automated>tools/phase1-python tools/build-site.py &amp;&amp; tools/phase1-python tools/build-site.py --check &amp;&amp; PYTHONPATH=tests/site tools/phase1-python -m unittest test_site_browser.SiteBrowserTests.test_reduced_motion_replay_uses_auto_scroll -v</automated>
  </verify>
  <acceptance_criteria>The browser-captured replay options equal auto/start under reduce, transcript open/focus/status behavior passes, and explicit builder output leaves `site/dist/assets/site.js` byte-equal to `site/assets/site.js` before `--check` passes.</acceptance_criteria>
  <done>Reduced-motion replay no longer requests smooth scrolling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve optional enhancement and static-path behavior across motion modes</name>
  <read_first>Task 1 script/test; remaining SiteBrowserTests; tests/site/test_static_site.py static-equivalent and local-asset assertions</read_first>
  <files>site/assets/site.js, site/dist/assets/site.js, tests/site/test_site_browser.py</files>
  <behavior>
    - No-preference replay directly records smooth/start, preserving the intentional enhancement for users who allow motion.
    - Reset still clears optional events and leaves the static transcript unchanged.
    - JavaScript-disabled mobile architecture retains transcript, table, steps, and source links.
    - Every browser request stays loopback/local and all four routes return 200.
  </behavior>
  <action>Add a complementary direct option observation for no-preference mode within the existing browser module, reusing its bounded local server and approved Chrome process. Keep the reduced-motion and no-preference expectations in separate named tests so failures identify the active branch. If the authored script changes during this task, explicitly rerun the non-check builder before `--check`; never rely on browser `setUpClass` to refresh tracked output. Run the whole site static/browser suite; repair only script/test/generated-script behavior owned by this plan and do not alter CSS, generated HTML, source content, or preview infrastructure.</action>
  <verify>
    <automated>tools/phase1-python tools/build-site.py &amp;&amp; tools/phase1-python tools/build-site.py --check &amp;&amp; tools/phase1-python -m unittest discover -s tests/site -p 'test_static_site.py' -v &amp;&amp; tools/phase1-python -m unittest discover -s tests/site -p 'test_site_browser.py' -v</automated>
  </verify>
  <acceptance_criteria>Both direct motion-mode observations pass, reset/no-JS/local-route regressions remain green, and the generated assets are current.</acceptance_criteria>
  <done>The optional replay enhancement is motion-preference-correct without weakening the complete static learning path.</done>
</task>

</tasks>

## Artifacts this phase produces

| Path | New symbol/test | Contract |
|---|---|---|
| `site/assets/site.js` | Existing replay handler | Selects auto/start under reduced motion and smooth/start otherwise. |
| `site/dist/assets/site.js` | Tracked generated script | Is explicitly regenerated by `tools/build-site.py` from authored `site/assets/site.js` and passes `--check`. |
| `tests/site/test_site_browser.py` | `SiteBrowserTests.test_reduced_motion_replay_uses_auto_scroll` | Directly captures and asserts auto/start replay options under reduced motion. |
| `tests/site/test_site_browser.py` | `SiteBrowserTests.test_no_preference_replay_uses_smooth_scroll` | Directly captures and asserts smooth/start replay options when motion is allowed. |

`site/assets/site.js` adds no public symbol, field, path, or CLI flag; its existing replay handler gains the `matchMedia` branch and the existing builder refreshes the tracked generated copy. No package or external service is added.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| browser preference → optional replay | User agent motion preference crosses into local JavaScript behavior. |
| browser test → accessibility evidence | CSS state can create a false green unless the actual method call is observed. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-52-01 | Tampering | replay motion selection | medium | mitigate | Select at click time from browser preference and assert the exact call options in approved Chromium. |
| T-01-52-02 | Repudiation | browser evidence | medium | mitigate | Direct method instrumentation distinguishes JavaScript behavior from computed CSS evidence. |
| T-01-52-03 | Information Disclosure | browser requests | low | accept | Existing local-only request assertion remains in the complete browser suite; script adds no network call or data sink. |

No HIGH threat remains unmitigated. No package-manager install occurs; supply-chain package legitimacy gate is not applicable.
</threat_model>

<verification>
Run the named reduced-motion tracer first, then the complete site static and browser suites through `tools/phase1-python`. Automated green does not replace the later Plan 01-53 human visual/accessibility judgment.
</verification>

<success_criteria>
Replay directly honors reduced motion, retains allowed-motion/reset/static behavior, and is covered by observable browser-call evidence rather than CSS inference.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-52-SUMMARY.md` when done.
</output>
