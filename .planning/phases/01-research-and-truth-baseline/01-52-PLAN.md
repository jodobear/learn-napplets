---
phase: 01-research-and-truth-baseline
plan: 52
type: execute
wave: 5
depends_on: [01-50]
files_modified:
  - tools/validate-phase1-security.py
  - tests/phase1/test_security_review.py
  - .planning/phases/01-research-and-truth-baseline/01-GAP-CLOSURE-REVIEW-EVIDENCE.md
autonomous: true
gap_closure: true
requirements: [EVID-01, EVID-02, EVID-03, EVID-04, OPER-01, OPER-03]
must_haves:
  truths:
    - "The current CR-01 through CR-07 and WR-01 through WR-03 register is validated by exact set equality without weakening historical security-review validation."
    - "A current repair dossier binds every current finding to a wrapper command, repaired source/test path, result locator, ASVS L1 control, and one repair-review commit without declaring a verdict."
    - "No security or code-review outcome is self-authored by the implementation task."
  artifacts:
    - path: tools/validate-phase1-security.py
      provides: "Current-register-aware ASVS L1 validation that preserves historical default behavior."
    - path: tests/phase1/test_security_review.py
      provides: "Exact current-register and role/binding regressions."
    - path: .planning/phases/01-research-and-truth-baseline/01-GAP-CLOSURE-REVIEW-EVIDENCE.md
      provides: "Non-decisional current repair evidence dossier for external code and security review."
  key_links:
    - from: 01-GAP-CLOSURE-REVIEW-EVIDENCE.md
      to: validate-phase1-security.py
      via: "The external review in Plan 01-53 validates the exact ten-finding source/command/evidence map through the supplied required-finding set."
---

<objective>
Prepare an exact current finding-register validator and non-decisional evidence dossier for external review of the repaired trust boundaries.

Purpose: Make Plan 01-53's external code/security review about the current ten repairs, rather than the historical CR-01..CR-09 register or a self-authored pass.
Output: Backward-compatible security-validator support, regressions, and a current repair dossier.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-FINAL-VERIFICATION.md
@.planning/phases/01-research-and-truth-baseline/01-REVIEW.md
@.planning/phases/01-research-and-truth-baseline/01-GAP-CLOSURE-REGRESSION-MATRIX.md
@tools/validate-phase1-security.py
@tests/phase1/test_security_review.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Validate one current ten-finding repair register without changing historical mode</name>
  <precondition>Plan 01-50's cross-defect tests and matrix pass under the committed pre-execution exact-plan review binding from Plan 01-51.</precondition>
  <read_first>01-REVIEW.md CR-01 through CR-07 and WR-01 through WR-03; tools/validate-phase1-security.py `REQUIRED_FINDINGS`, `finding_map`, `validate`, and CLI; tests/phase1/test_security_review.py fixture helpers</read_first>
  <files>tools/validate-phase1-security.py, tests/phase1/test_security_review.py</files>
  <behavior>
    - Historical invocation without `--required-findings` still requires its existing CR-01..CR-09 and WR-01..WR-03 register.
    - `--required-findings CR-01,CR-02,CR-03,CR-04,CR-05,CR-06,CR-07,WR-01,WR-02,WR-03` requires exactly that current register in both dossier and human security review.
    - An omitted, duplicate, malformed, substituted, command-swapped, source-swapped, unresolved-high, or executor-signed current fixture is refused.
  </behavior>
  <action>Start RED with isolated current-register fixture variants. Add a `--required-findings` parser that accepts only a nonempty comma-separated ordered unique list of stable CR/WR IDs, passes that exact tuple through finding-set, review, and passed-status validation, and leaves the historical default unchanged when the option is absent. Require a passed current review to include exactly the requested required rows plus only well-formed `SEC-*` discoveries; preserve ASVS L1 high-severity blocking and separate dated securityAuditor/securityRechecker role records. Extend real-binding validation so a supplied dossier's reviewed commit, current source/test map, and evidence digest must agree with the review record before a passed result is trusted. This implements D-15, D-25 through D-28, D-33 through D-36: the validator checks human evidence but cannot create approval, accept an ADR, accept residual risk, or authorize Phase 2.</action>
  <verify>
    <automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_security_review.SecurityReviewTests.test_current_gap_finding_set_requires_exact_register && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_security_review.SecurityReviewTests.test_current_gap_finding_set_rejects_unbound_or_executor_review && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_security_review</automated>
  </verify>
  <acceptance_criteria>The current ten-row review cannot omit, exchange, or weaken a required finding, while the legacy register remains validated by its unchanged invocation.</acceptance_criteria>
  <done>The project has a tested exact-register contract for an externally authored current repair review.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble the current repair dossier without a review verdict</name>
  <precondition>Task 1 is committed and `git rev-parse --verify HEAD^{commit}` identifies the repair-review commit used by the dossier.</precondition>
  <files>.planning/phases/01-research-and-truth-baseline/01-GAP-CLOSURE-REVIEW-EVIDENCE.md</files>
  <action>After Task 1 is committed, generate the dossier from fresh wrapper runs at that commit. Its frontmatter must contain the full repair-review commit, the current pre-execution plan-review binding digest, certified toolchain digest, evidence-manifest digest, and the exact ordered ten IDs. Include one row per finding with CR/WR ID, threat category, high or warning severity, repaired source path, named test path, approved wrapper command, compact result digest, result locator, and applicable ASVS L1 control. Include a four-path regular-file SHA-256 map for the repair-review validator/source/tests and the repaired trust-boundary sources/tests named by Plans 01-46 through 01-50. Record retained browser, package, upstream, ADR, external-action, production-scaffold, and Phase 2 blockers outside the ten-row repair register. Do not use passed, approved, waived, accepted, transition, or security-verdict fields; this artifact is review input only, per D-14 through D-20 and D-25 through D-28.</action>
  <verify>
    <automated>test -s .planning/phases/01-research-and-truth-baseline/01-GAP-CLOSURE-REVIEW-EVIDENCE.md && tools/phase1-python -c "from pathlib import Path; t=Path('.planning/phases/01-research-and-truth-baseline/01-GAP-CLOSURE-REVIEW-EVIDENCE.md').read_text(); ids=['CR-01','CR-02','CR-03','CR-04','CR-05','CR-06','CR-07','WR-01','WR-02','WR-03']; assert all(t.count(i) >= 1 for i in ids); assert 'reviewed_commit:' in t"</automated>
  </verify>
  <acceptance_criteria>The dossier gives an external reviewer an exact current ten-finding map, fresh command evidence, immutable review commit, and retained blockers without declaring the repairs reviewed or secure.</acceptance_criteria>
  <done>Plan 01-53 can perform an independent code/security review without relying on historical finding evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| current repair evidence → security conclusion | The old CR/WR register or incomplete current set could be substituted for current remediation evidence. |
| implementation output → human review | Automation could present a dossier as a review verdict. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-52-01 | Tampering | required finding register | high | mitigate | Task 1 enforces the caller-supplied exact current set in dossier and human-review bindings. |
| T-52-02 | Repudiation | repair command evidence | high | mitigate | Task 2 records per-finding wrapper command, source/test map, result digest, and reviewed commit. |
| T-52-03 | Spoofing | security outcome | high | mitigate | Task 1 preserves non-executor role checks; Task 2 is explicitly non-decisional. |
</threat_model>

<verification>
Run the exact-register regressions and validate that the generated dossier names every current finding and no review verdict.
</verification>

<success_criteria>
The current repair set has machine-checkable review inputs while security and code-review determinations remain reserved for the next external gate.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-52-SUMMARY.md` when done.
</output>
