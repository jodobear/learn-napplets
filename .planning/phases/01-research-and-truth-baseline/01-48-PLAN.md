---
phase: 01-research-and-truth-baseline
plan: 48
type: execute
wave: 3
depends_on: [01-36, 01-47]
files_modified:
  - tools/measure-package-conformance.py
  - tools/run-spk-g-sandbox.py
  - tests/phase1/test_spikes.py
autonomous: true
gap_closure: true
requirements: [EVID-04, OPER-01]
must_haves:
  truths:
    - "A qualified SPK-G operation passes only a constructed OS-sandbox command to its executor; every unqualified or unavailable-sandbox route has an operation count of zero."
    - "The exact scoped npm package `@napplet/web` is accepted by strict package-name validation, while traversal, backslash, malformed scope, and non-root export forms are rejected."
    - "SPK-G measurements, report, and impact fragment publish as one no-follow journaled bundle or preserve all prior bytes after every injected retention failure."
  artifacts:
    - path: tools/measure-package-conformance.py
      provides: "Safe scoped npm-name validation, constructed sandbox execution, and journaled evidence retention."
    - path: tools/run-spk-g-sandbox.py
      provides: "Constructed bwrap command plus resource-limited execution boundary."
    - path: tests/phase1/test_spikes.py
      provides: "SPK-G sandbox-argv, scoped-name, and atomic-retention regressions."
  key_links:
    - from: run_spk_g
      to: construct_sandbox_argv
      via: "An eligible package argv is transformed into the verified no-network sandbox invocation before the injected runner receives it."
    - from: retain_validated_bundle
      to: canonical_recovery.publish_generation
      via: "Validated candidate bytes become one journaled three-file evidence generation."
---

<objective>
Repair the executable and retained-evidence SPK-G trust boundary.

Purpose: Close CR-05, CR-06, and CR-07 without turning an unqualified package fixture into a package admission or protocol conclusion.
Output: Constructed sandbox execution, scoped-name admission, and atomic bundle retention.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-FINAL-VERIFICATION.md
@.planning/phases/01-research-and-truth-baseline/01-REVIEW.md
@.planning/phases/01-research-and-truth-baseline/01-36-SUMMARY.md
@.planning/phases/01-research-and-truth-baseline/01-47-SUMMARY.md
@tools/measure-package-conformance.py
@tools/run-spk-g-sandbox.py
@tools/canonical-recovery.py
@tests/phase1/test_spikes.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Execute one qualified fixture only through a constructed sandbox command</name>
  <precondition>Plans 01-36 and 01-47 pass their named SPK-G and publication regressions, and a fresh exact-plan review authorizes the current Plan 01-48 execution set.</precondition>
  <read_first>01-REVIEW.md CR-05 and CR-06; 01-36-SUMMARY.md; tools/measure-package-conformance.py `fixed_operation` and `run_spk_g`; tools/run-spk-g-sandbox.py `SandboxContract` and `construct_sandbox_argv`; tests/phase1/test_spikes.py SPK-G fixtures</read_first>
  <files>tools/measure-package-conformance.py, tools/run-spk-g-sandbox.py, tests/phase1/test_spikes.py</files>
  <behavior>
    - A qualified synthetic `@napplet/web` artifact with its root export equal to `@napplet/web` produces a fixed package argv and an executor-visible command whose first program is the sandbox mechanism, includes network isolation and repository/workspace mounts, and contains the package argv only after the boundary delimiter.
    - `@scope/name` is accepted once; malformed scope segments, empty segments, traversal tokens, backslashes, uppercase-invalid forms, and root-export mismatches raise `ReceiptValidationError` before execution.
    - An unavailable contract, incomplete evidence, or failed resource-limit setup returns the existing stable blocked receipt and never invokes the injected runner.
    - The default path remains no-operation under the present blocked canonical evidence.
  </behavior>
  <action>Start RED with injected runners that record received argv without executing a package manager. Replace the slash blanket rejection in `fixed_operation()` with a strict npm-name validator that admits one lower-case scoped `@scope/name` form and an unscoped form, rejects malformed tokens and filesystem-significant content, and still requires exact equality with the documented root export. In `run_spk_g()`, preserve snapshot-before-parse, full eligibility, dated approval, and verified-contract checks; after they pass, create the isolated workspace and call `construct_sandbox_argv()` with the fixed package argv, repository root, and workspace. Add a `run_sandboxed_operation()` boundary in `run-spk-g-sandbox.py` that applies the declared wall-clock, CPU, memory, process-count, and file-size limits to the process invocation; the injected runner receives only the constructed sandbox argv and bounded workspace. A failure to construct or execute that boundary must return `SPK-G-BLOCKED-SANDBOX-UNAVAILABLE` before a package manager is called. Do not execute a real package install during tests, add a package dependency, use browser workarounds, alter source/package approval records, or reclassify a package result as protocol authority. This preserves D-21 through D-24, D-35, D-36, and D-37 through D-39.</action>
  <verify>
    <automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_spikes.SpikeValidationTests.test_spk_g_runner_receives_only_constructed_sandbox_argv && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_spikes.SpikeValidationTests.test_spk_g_accepts_scoped_package_root_and_rejects_unsafe_names && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_spikes.SpikeValidationTests.test_spk_g_sandbox_blocks_unavailable_or_escape_prone_execution</automated>
  </verify>
  <acceptance_criteria>The qualified fixture sends a constructed sandbox command, not a direct package-manager command, to the injected executor; safe scoped naming succeeds; every unsafe name or unavailable control fails before execution.</acceptance_criteria>
  <done>CR-05 and CR-06 cannot create an unbounded SPK-G operation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retain the three SPK-G evidence files as one no-follow generation</name>
  <read_first>01-REVIEW.md CR-07; tools/measure-package-conformance.py `retain_validated_bundle`; tools/canonical-recovery.py `publish_generation`; tests/phase1/test_spikes.py existing retained-bundle fixture; SPK-G measurements/report/impact-fragment paths</read_first>
  <files>tools/measure-package-conformance.py, tests/phase1/test_spikes.py</files>
  <behavior>
    - Candidate roots, candidate files, targets, and parent paths that are symlinks or non-regular entries are refused before reading or writing data.
    - For each injected publication failure position, measurements, report, and impact-fragment retain their identical old byte map and the candidate directory is removed.
    - A valid three-file candidate is validated first and then journal-published through the shared primitive as one complete map.
  </behavior>
  <action>Start RED with a temporary three-file retained bundle, a candidate symlink fixture, target-temporary symlink fixture, and an injected publisher that fails after each replacement position. Make `retain_validated_bundle()` require one private regular candidate directory, require its exact expected regular-file set, read candidate bytes only after validator success, and refuse symlinked/non-regular candidate or target paths. Require all retained targets to share one verified parent and invoke `canonical_recovery.publish_generation()` with that parent plus the exact three logical target names and bytes; do not create predictable sibling temporary names or replace targets in a local loop. Ensure any validation, confinement, publisher, or digest failure leaves the old target map unchanged and removes the candidate bundle. Keep a blocked receipt classified as blocked and preserve its safe fallback and impact routing. This implements D-09, D-18, D-20, D-23, D-29 through D-32, and D-36 without accepting package admission or external action.</action>
  <verify>
    <automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_spikes.SpikeValidationTests.test_spk_g_retained_bundle_refuses_symlink_paths && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_spikes.SpikeValidationTests.test_spk_g_retained_bundle_preserves_all_old_bytes_after_each_publish_failure && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_spikes.SpikeValidationTests.test_spk_g_retains_evidence_only_after_complete_result_validation</automated>
  </verify>
  <acceptance_criteria>A symlink cannot redirect candidate or target IO, each injected journal publication failure retains all three old files, and a valid receipt is retained only as one complete generation.</acceptance_criteria>
  <done>CR-07 no longer allows partial or symlink-following SPK-G evidence replacement.</done>
</task>

</tasks>

## Artifacts this phase produces
- `tools/measure-package-conformance.py::validate_npm_package_name` — strict scoped/unscoped package-name admission used before argv construction.
- `tools/run-spk-g-sandbox.py::run_sandboxed_operation` — resource-limited constructed sandbox invocation boundary.
- `tools/measure-package-conformance.py::retain_validated_bundle` — no-follow, shared-publisher retention of the three SPK-G evidence files.
- `tests/phase1/test_spikes.py::SpikeValidationTests.test_spk_g_runner_receives_only_constructed_sandbox_argv` — CR-05 regression.
- `tests/phase1/test_spikes.py::SpikeValidationTests.test_spk_g_accepts_scoped_package_root_and_rejects_unsafe_names` — CR-06 regression.
- `tests/phase1/test_spikes.py::SpikeValidationTests.test_spk_g_retained_bundle_preserves_all_old_bytes_after_each_publish_failure` — CR-07 regression.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| reviewed package evidence → process runner | An eligible-looking artifact could invoke a package manager outside its declared sandbox. |
| package identifier → fixed argv | A name parser could admit traversal or reject the intended scoped package. |
| candidate evidence bundle → retained evidence | Partial replacement or symlink redirection could alter evidence integrity. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-48-01 | Elevation of Privilege | SPK-G runner | high | mitigate | Task 1 constructs and limits the verified sandbox command before the runner can receive argv. |
| T-48-02 | Tampering | package-name parser | high | mitigate | Task 1 admits only strict package names and matching documented root exports. |
| T-48-03 | Tampering | retained evidence | high | mitigate | Task 2 validates private regular inputs and delegates one three-file publication to the journaled primitive. |
| T-48-04 | Repudiation | blocked measurement outcome | medium | mitigate | Tasks retain stable blocked receipts, source bindings, safe fallback, and impact routing. |
</threat_model>

<verification>
Run the six named SPK-G boundary regressions through the certified wrapper. A later independent review verifies that no real package operation occurred under the current blocked evidence state.
</verification>

<success_criteria>
SPK-G can only run a qualified fixture through the declared sandbox boundary and retains evidence atomically; otherwise it produces a precise no-operation blocker.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-48-SUMMARY.md` when done.
</output>
