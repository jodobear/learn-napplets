---
phase: 01-research-and-truth-baseline
plan: 55
type: execute
wave: 3
depends_on: [01-49, 01-52]
files_modified:
  - tools/build-site.py
  - tests/site/test_static_site.py
autonomous: true
gap_closure: true
requirements: [EVID-01, OPER-03]
must_haves:
  truths:
    - "A successful static build leaves the complete regular-file set under its output root exactly equal to `render_all()` output keys; retired routes and assets cannot remain previewable."
    - "`--check` fails when the output tree contains any missing, changed, or extra regular file, even when every expected file has correct bytes."
    - "Build mode removes only confined extra regular files after exact non-symlink inspection, prunes only empty confined directories, and refuses symlink or nonregular output entries without touching their referents."
    - "The caller-supplied output root is lstat-checked and opened with no-follow semantics before any resolution, inventory, write, or deletion; an output-root symlink is refused with its external target byte-identical."
    - "The production CLI maps omitted `--output` to the lexical repository-relative token `site/dist` and accepts only that same explicit normalized value; `.`, the repository root, absolute/alias forms, arbitrary `/tmp` paths, and every other output value refuse before inspection or mutation, while copied temporary roots remain reachable only through a capability-gated direct-call test seam that cannot target the production root."
    - "Two consecutive builds produce the same path set and byte digest tree, and stale route/asset requests return 404 after repair."
  artifacts:
    - path: tools/build-site.py
      provides: "Complete output-tree inventory, safe stale-file removal, and exact check-mode set equality."
      contains: "inventory_output_tree"
    - path: tests/site/test_static_site.py
      provides: "Stale route/asset, deterministic-tree, and symlink/nonregular refusal regressions."
      contains: "test_build_removes_stale_routes_and_assets_and_check_rejects_complete_tree"
  key_links:
    - from: "tools/build-site.py::render_all"
      to: "tools/build-site.py::inventory_output_tree"
      via: "The lexically sorted output-key set is the sole allowed regular-file set for build and check modes."
      pattern: "set(outputs)"
    - from: "tools/build-site.py::build"
      to: "site/dist preview root"
      via: "The production route first requires exact `site/dist`; a retained no-follow output-root descriptor then anchors every inventory, write, unlink, and empty-directory prune, while a private capability permits only non-production temporary fixture roots."
      pattern: "extra"
---

<objective>
Make the static builder own the complete output tree so stale routes and assets cannot survive a successful build or check.

Purpose: Ensure local preview serves only the deterministic files generated from current structured content and authored assets.
Output: Exact tree-set validation/removal in the stdlib builder and focused stale-output/security regressions.
</objective>

<execution_context>
@/home/at/.codex/gsd-core/workflows/execute-plan.md
@/home/at/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-48-PLAN.md
@.planning/phases/01-research-and-truth-baseline/01-49-SUMMARY.md
@.planning/phases/01-research-and-truth-baseline/01-52-SUMMARY.md
@tools/build-site.py
@tests/site/test_static_site.py
@tests/site/test_site_browser.py
@site/README.md
</context>

<scope_boundary>
Change only static output-tree ownership and its tests. Preserve rendered page/content meaning, source-identity validation, authored assets, reduced-motion behavior, local preview instructions, stdlib-only execution, and all four current routes. Do not add a framework, package, deployment, external request, route, canonical evidence field, or phase-status change.
</scope_boundary>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject extras in check mode and remove stale routes/assets in build mode</name>
  <read_first>tools/build-site.py `render_all`, `atomic_write`, `build`, `safe_route`, CLI `--output` parsing, and output-root safety check; tests/site/test_static_site.py complete-path-set and reproducibility tests; site/README.md preview command</read_first>
  <files>tools/build-site.py, tests/site/test_static_site.py</files>
  <behavior>
    - After a normal build, adding `retired/index.html` or `assets/retired.js` makes `--check` fail with both paths identified as extra.
    - A subsequent normal build removes both stale files and their now-empty directory while preserving every expected output byte.
    - The repaired tree's regular-file paths equal the output-key set exactly; rebuilding produces identical per-path SHA-256 values.
    - A stdlib preview of the repaired tree returns 404 for the retired route and asset while all four current routes and two current assets remain available.
    - Omitted `--output` and explicit lexical `site/dist` select the same production root; production build/check rejects `.`, the repository root, arbitrary `/tmp` roots, absolute/alias forms of `site/dist`, and every other caller value before lstat, inventory, creation, write, or deletion.
    - Copied temporary-output fixtures call a private helper with the module-private capability identity; that seam is absent from CLI/environment/config inputs, rejects the production repository and `site/dist` roots, and otherwise applies the same descriptor safety rules.
  </behavior>
  <action>Addresses external Codex review concerns: resolving a caller output root before lstat can follow a symlink, and accepting an arbitrary existing `--output` such as `.` lets complete-tree convergence inventory or delete repository files. Write failing production-route, stale-route/asset, and symlink-root tests first. Change the argparse default from the absolute `DEFAULT_OUTPUT` path to the lexical token `Path("site/dist")`, so omitted `--output` and explicit `--output site/dist` enter the same canonical production route. At that CLI boundary, preserve the raw argument, POSIX-lexically normalize without following any component, and require exact equality with repository-relative `site/dist` before lstat, diagnostic resolution, inventory, creation, write, or deletion. Explicitly reject `.`, the lexical/absolute repository root, `/tmp`, arbitrary `/tmp/...`, absolute or dot-component aliases of `site/dist`, and every other value; record repository listing/digest sentinels and prove rejection is mutation-free. Expose no output-root capability through argparse, environment, or config. Preserve copied temporary-root testing only through a module-private direct-call helper requiring identity equality with a module-private capability object; require its root to be an executor-created temporary fixture distinct from and outside the production repository root, and hard-refuse both the repository root and production `site/dist`. After route authorization, use `open_output_root(raw_output, create, capability)` without `resolve()` or any path API that follows its final component: walk existing parents with retained `O_DIRECTORY|O_NOFOLLOW` descriptors and lstat the final component relative to its parent. For an existing root, require a non-symlink directory, no-follow-open it, and require fstat device/inode/type/owner to match pre-open lstat before any mutation. For an absent authorized root, create only the final component beneath the validated parent; check mode must not create it. Retain the descriptor for `inventory_output_tree`, atomic writes, extra-file removal, and empty-directory pruning. Derive the allowed set only from confined `render_all()` keys. Check mode compares the complete regular-file set before bytes; build mode atomically writes expected output, removes only verified extra regular files, prunes only verified empty descendants, never removes the root, and re-inventories for exact equality. Never reopen the authorized root by pathname.</action>
  <verify>
    <automated>PYTHONPATH=tests/site tools/phase1-python -m unittest test_static_site.StaticSiteBuildTests.test_production_output_route_requires_exact_site_dist_before_mutation test_static_site.StaticSiteBuildTests.test_test_output_capability_accepts_temporary_root_and_refuses_production_root test_static_site.StaticSiteBuildTests.test_output_root_symlink_is_refused_before_resolution_or_mutation test_static_site.StaticSiteBuildTests.test_build_removes_stale_routes_and_assets_and_check_rejects_complete_tree test_static_site.StaticSiteBuildTests.test_build_is_byte_reproducible_and_check_detects_drift -v</automated>
  </verify>
  <acceptance_criteria>Omitted `--output` and explicit lexical `site/dist` both select only the canonical production root; `.`, repository-root, `/tmp`, arbitrary, absolute/dot alias, and symlink-root inputs refuse before mutation with sentinels exact; the private capability seam accepts a temporary fixture but rejects production roots; `--check` rejects both stale paths; normal production build removes them safely; the path/digest tree is deterministic; preview returns 404 for retired content and 200 for current routes/assets.</acceptance_criteria>
  <done>A successful build or check can no longer leave or accept stale regular files in the served output tree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refuse symlink and nonregular output entries without touching external data</name>
  <read_first>Task 1 inventory/deletion helpers and tests; existing output-path confinement checks; Python `os.stat(..., follow_symlinks=False)`, directory-descriptor unlink/rmdir APIs</read_first>
  <files>tools/build-site.py, tests/site/test_static_site.py</files>
  <behavior>
    - A symlink at an expected file, an extra file, or an intermediate output directory makes both build and check return nonzero before following or deleting the link.
    - A FIFO or other nonregular entry is refused and remains present.
    - An external sentinel referenced by a refused symlink remains byte-identical.
    - A capability-authorized temporary output-root symlink to an external directory makes build and check fail before creating, changing, or deleting any external entry; both an existing external sentinel and the external directory listing remain exact.
    - An ordinary stale regular file remains removable and the complete static suite stays green.
  </behavior>
  <action>Add table-driven copied-output fixtures for caller-root symlink, expected-path symlink, extra symlink, directory symlink, and one available nonregular entry type. Invoke every non-`site/dist` fixture only through the private capability-gated direct-call seam. The caller-root fixture creates an external directory with a sentinel and stable entry-name/digest inventory, supplies its symlink through the test seam, runs build and check, and requires refusal before any external listing or byte change. Separately invoke the production CLI with `.`, repository root, `/tmp`, and another arbitrary existing directory and prove exact rejection plus unchanged repository/external sentinels before the test seam is considered. Require inventory/deletion helpers to use lstat/fstatat-style non-following checks relative to the retained authorized root descriptor; do not use recursive deletion, resolved symlink targets, or broad cleanup. On refusal, report the confined relative entry and leave unsafe entries/referents exact. Run the complete static-site suite and regenerate checked-in `site/dist` only if the exact production route reports legitimate expected-file drift; no generated route/content change is expected.</action>
  <verify>
    <automated>PYTHONPATH=tests/site tools/phase1-python -m unittest test_static_site.StaticSiteBuildTests.test_production_output_route_requires_exact_site_dist_before_mutation test_static_site.StaticSiteBuildTests.test_test_output_capability_accepts_temporary_root_and_refuses_production_root test_static_site.StaticSiteBuildTests.test_output_root_symlink_is_refused_before_resolution_or_mutation test_static_site.StaticSiteBuildTests.test_builder_refuses_symlink_and_nonregular_output_entries_without_following test_static_site.StaticSiteBuildTests.test_build_removes_stale_routes_and_assets_and_check_rejects_complete_tree -v &amp;&amp; tools/phase1-python -m unittest discover -s tests/site -p 'test_static_site.py' -v &amp;&amp; tools/phase1-python tools/build-site.py --output site/dist --check</automated>
  </verify>
  <acceptance_criteria>Unsafe output entries fail closed with external bytes intact, ordinary extras are removed, all current outputs retain exact bytes, and checked-in `site/dist` passes complete-tree `--check`.</acceptance_criteria>
  <done>Complete output ownership cannot be redirected outside the served tree or weakened by a nonregular entry.</done>
</task>

</tasks>

## Artifacts this phase produces

| Path | New symbol/test | Contract |
|---|---|---|
| `tools/build-site.py` | Exact `site/dist` production gate, private temporary-root capability, `inventory_output_tree`, and confined stale-file cleanup | Prevents arbitrary-root inventory/deletion and treats `render_all()` keys as the complete served regular-file set. |
| `tests/site/test_static_site.py` | `test_production_output_route_requires_exact_site_dist_before_mutation` and capability-seam regression | Proves `.`, repository root, `/tmp`, arbitrary roots, and test-seam production-root use refuse before mutation while copied temporary fixtures remain testable. |
| `tests/site/test_static_site.py` | `test_build_removes_stale_routes_and_assets_and_check_rejects_complete_tree` | Proves stale paths fail check, disappear on build, return 404, and leave deterministic output. |
| `tests/site/test_static_site.py` | `test_builder_refuses_symlink_and_nonregular_output_entries_without_following` | Proves unsafe entries and external referents are not followed or removed. |
| `tests/site/test_static_site.py` | `test_output_root_symlink_is_refused_before_resolution_or_mutation` | Proves build/check never resolve a caller root symlink and leave its external sentinel and listing exact. |

No route, generated content field, dependency, service, or production framework is added.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| generated output set → preview server | Preview serves every retained file, not only files expected by the current builder. |
| output selection/cleanup → filesystem | An arbitrary existing root, unsafe traversal, or symlink could redirect complete-tree deletion outside `site/dist`. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-55-01 | Tampering | complete output tree | high | mitigate | Require exact set equality in check and post-build modes; cover stale route and asset with preview 404 assertions. |
| T-01-55-02 | Elevation of Privilege | output-root selection and stale-file deletion | high | mitigate | Require exact production `site/dist` before lstat/mutation; reject `.`, repository root, `/tmp`, arbitrary roots, and aliases with unchanged sentinels; confine temporary roots to a non-production capability seam; then retain a verified no-follow descriptor for every operation. |
| T-01-55-03 | Repudiation | deterministic build | medium | mitigate | Compare complete path set and per-path SHA-256 across consecutive builds. |

No package-manager install occurs; supply-chain package legitimacy gate is not applicable.
</threat_model>

<verification>
Run exact-production-root and capability-seam rejection probes first, then caller-root symlink, stale-output, unsafe-entry regressions, the complete static suite, and checked-in `site/dist` complete-tree check through `tools/phase1-python`.
</verification>

<success_criteria>
Production build/check accepts only exact `site/dist` and refuses `.`, repository root, `/tmp`, arbitrary roots, aliases, and caller-root symlinks before mutation; the capability seam preserves copied temporary-root testability but cannot target production; build safely converges the descriptor-bound served tree to exactly expected regular files, check rejects every extra, stale paths are not previewable, unsafe entries fail closed, sentinels remain exact, and repeated output trees are byte-deterministic.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-55-SUMMARY.md` when done.
</output>
