---
phase: 01-research-and-truth-baseline
plan: 42
type: execute
wave: 4
depends_on: [01-34]
files_modified:
  - tools/canonical-recovery.py
  - tools/validate-research.py
  - tools/validate-planning.py
  - tools/acquire-sources.py
  - tools/refresh-sources.py
  - tests/phase1/test_spike_consolidation.py
  - tests/phase1/test_preflight.py
autonomous: true
gap_closure: true
requirements: [EVID-01, EVID-02, EVID-04, OPER-01]
must_haves:
  truths:
    - "A hard interruption or journal tampering cannot leave a mixed consolidation generation consumable by any canonical reader."
    - "Every recovery path resolves to a verified complete pre-generation or verified complete post-generation, never an inferred or partial state."
    - "A terminal REVERIFICATION/STATE/ROADMAP generation stays staged until its exact staged bytes pass terminal validation; a journaled publisher then exposes all three or recovers the prior complete generation."
    - "The shared publisher accepts only named, fixed canonical target sets with exact validation attestations, so Plan 01-43 can atomically publish all five related observed-refresh records or recover the prior complete set."
  artifacts:
    - path: tools/canonical-recovery.py
      provides: "Shared durable publisher, pre-reader recovery guard, and allowlisted validated staged-set publication API for terminal and observed-refresh generations"
    - path: tools/validate-research.py
      provides: "Durable journaled consolidation publisher and pre-reader recovery guard"
    - path: tests/phase1/test_spike_consolidation.py
      provides: "Hard-interruption, tampered-journal, contention, direct-reader, all-reader, and staged-terminal-publication regression coverage"
  key_links:
    - from: consolidation or terminal journal
      to: all canonical publication targets
      via: "fsynced prepared/committed digest set, exclusive lock, and recovery before canonical reads"
    - from: Plan 01-40 terminal-validation attestation
      to: staged REVERIFICATION, STATE, and ROADMAP bytes
      via: "terminal target allowlist and SHA-256 binding checked again before journaled publication"
    - from: Plan 01-43 observed-refresh validation attestation
      to: staged claims, drift, open questions, package map, and current-work snapshot bytes
      via: "observed-refresh target allowlist, exact SHA-256 binding, locked journal, and interruption recovery before publication"
---

<objective>
Publish and recover one complete canonical evidence generation with a durable, fail-closed transaction.

Purpose: Close CR-07 durable crash-atomic publication and recovery after Plan 01-34 has closed CR-05 path/source confinement and CR-08 observed-local versus normative classification, without mixing schema repair with crash-recovery implementation.
Output: A journaled same-filesystem publisher, recovery-before-read guard, and interruption proof.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-34-SUMMARY.md
@tools/validate-research.py
@tests/phase1/test_spike_consolidation.py
@.planning/research/{claims.yaml,compatibility-matrix.yaml,drift-register.yaml,open-questions.yaml,package-map.md,open-work-snapshot.json}
@.planning/spikes/replay-manifest.yaml
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Recover one interrupted canonical generation before any reader consumes it</name>
  <precondition>Plan 01-34 has passed its v1-to-v2 drift migration and confined-fragment validation; the canonical target set is valid before the transaction begins.</precondition>
  <read_first>01-34-SUMMARY.md; tools/validate-research.py; tests/phase1/test_spike_consolidation.py; compatibility-matrix.yaml; drift-register.yaml; open-questions.yaml; replay-manifest.yaml</read_first>
  <files>tools/canonical-recovery.py, tools/validate-research.py, tools/validate-planning.py, tools/acquire-sources.py, tools/refresh-sources.py, tests/phase1/test_spike_consolidation.py, tests/phase1/test_preflight.py</files>
  <behavior>
    - `test_recovery_after_each_publish_interruption_is_coherent` injects an interruption after every replacement position and proves recovery exposes either the complete verified old generation or complete verified new generation.
    - `test_recovery_rejects_tampered_or_incomplete_journal_before_reader_entry` proves unknown state, modified journal bytes, missing backup, and digest mismatch fail before validate, report, fragment, replay, governance, or consolidation readers use canonical files.
    - `test_validate_planning_recovers_or_refuses_before_any_canonical_read` directly invokes `tools/validate-planning.py` after each injected interruption and journal-tampering fixture, proving it recovers a complete generation or returns nonzero before preflight/completion reads any canonical artifact.
    - `test_all_canonical_reader_entrypoints_guard_recovery` directly exercises every currently registered reader and proves missing, malformed, tampered, or incomplete journals stop that reader before canonical input is parsed; the registration API rejects duplicate or unguarded additions so Plan 01-36 can add and task-locally test its package-reader routes.
    - `test_validated_canonical_set_api_rejects_wrong_profile_or_attestation` proves the shared publisher accepts only the fixed terminal and observed-refresh target profiles, rejects an extra/missing/aliased target or altered staged digest before replacement, and retains the complete old set on every refusal.
    - `test_terminal_set_publishes_only_validated_staged_generation` proves a staged three-file terminal set whose validation attestation has exact target paths and SHA-256 values publishes only after validation; invalid, altered-after-validation, incomplete, or interrupted generations leave the live set at the complete old generation or recover it deterministically.
    - `test_publish_validated_canonical_set_cli_contract` invokes `canonical-recovery.py publish-validated-canonical-set --help`, publishes each valid terminal and observed-refresh copied-root fixture through the CLI, and proves unknown profile, missing or extra option, unsafe staged root, malformed or altered attestation, wrong target map, changed staged byte, or contention returns nonzero without changing live bytes.
    - Lock contention produces a stable nonzero result and does not publish bytes from either writer.
  </behavior>
  <action>Addresses H2, H3, review concerns 3 and 6, and human decisions 3 and 6: start RED in `SpikeConsolidationTests` and `Phase1ExecutionPreflightTests` with copied planning roots and deterministic interruption points. Replace in-memory rollback with a same-filesystem transaction directory and durable journal recording transaction ID, named target profile, ordered targets, old/new SHA-256, staged and backup paths, and prepared/committed state. Write and fsync staged bytes and backups, fsync transaction directories, atomically write and fsync the prepared journal, replace targets in a deterministic order with target and parent-directory fsync, verify each new digest, atomically record committed state, and remove transaction material only after terminal verification. Under the existing exclusive lock, recover a prepared or publishing journal to the fully verified old target set and verify a committed journal as the fully verified new target set. Reject unknown state, a malformed or modified journal, missing backup, digest mismatch, or unsafe transaction path with a nonzero result and no reader continuation. Expose one `publish_validated_canonical_set(profile, staged_targets, attestation)` API in `tools/canonical-recovery.py`. Its closed profile table contains only `terminal`—REVERIFICATION, STATE, ROADMAP—and `observed-refresh`—claims, drift register, open questions, package map, and current-work snapshot. It rejects extra, missing, aliased, symlinked, or unsafe staged targets; an attestation/profile mismatch; and any altered staged SHA-256. It rehashes each staged regular file, journals the exact old/new generation, and publishes only after its set-specific validation attestation and every staged byte validate; a failure leaves live bytes unchanged. Add the owned command `tools/canonical-recovery.py publish-validated-canonical-set --profile {terminal,observed-refresh} --staged-root PATH --attestation PATH`; its argparse help must expose precisely these required arguments, it must reject unknown/missing arguments and invalid profile values before reading a staged target, and it must resolve the fixed profile target map from the allowed staging-root layout rather than accepting caller-supplied target paths. The command calls the shared API, rehashes every published live target against the attestation before returning zero, emits a stable nonzero diagnostic for validation, lock, recovery, or publication failure, and never performs a direct sequential write. Plan 01-40 uses the terminal profile and Plan 01-43 uses the observed-refresh profile; neither path performs sequential canonical publication. Implement one shared `recover_before_canonical_read()` primitive and a closed registration API. Invoke the guard before the first canonical-file read in every route that exists in this plan—`tools/validate-research.py`, `tools/validate-planning.py`, `tools/acquire-sources.py`, and `tools/refresh-sources.py`—including validation, report validation, impact-fragment validation, replay validation, governance validation, consolidation, preflight, and terminal-completion routes. The registry must reject an unguarded or duplicate entry and its test exercises every then-registered reader. Plan 01-36, which depends on this plan, owns adding `tools/measure-package-conformance.py` routes and its task-local interruption/mixed-generation regression before any package operation. Preserve additive history and idempotency under D-13, D-20, D-29 through D-32, and D-36; recovery does not create upstream facts, accept risk, or change ADR state.</action>
  <verify><automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_spike_consolidation.SpikeConsolidationTests.test_recovery_after_each_publish_interruption_is_coherent && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_spike_consolidation.SpikeConsolidationTests.test_recovery_rejects_tampered_or_incomplete_journal_before_reader_entry && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_spike_consolidation.SpikeConsolidationTests.test_validate_planning_recovers_or_refuses_before_any_canonical_read && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_spike_consolidation.SpikeConsolidationTests.test_all_canonical_reader_entrypoints_guard_recovery && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_spike_consolidation.SpikeConsolidationTests.test_validated_canonical_set_api_rejects_wrong_profile_or_attestation && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_spike_consolidation.SpikeConsolidationTests.test_terminal_set_publishes_only_validated_staged_generation && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_spike_consolidation.SpikeConsolidationTests.test_publish_validated_canonical_set_cli_contract && tools/phase1-python tools/canonical-recovery.py publish-validated-canonical-set --help && tools/phase1-python tools/validate-research.py recover-consolidation --research .planning/research && tools/phase1-python tools/validate-research.py validate --root .planning/research --report /tmp/phase1-recovery-validation.md && tools/phase1-python tools/validate-research.py replay-spikes --manifest .planning/spikes/replay-manifest.yaml --check</automated></verify>
  <acceptance_criteria>Each injected interruption is recovered to a complete digest-verified generation; direct `validate-planning.py` recovery and every then-registered reader stop or recover before consuming canonical data; malformed journals and lock conflicts are nonpublishing. The owned `publish-validated-canonical-set --profile PROFILE --staged-root PATH --attestation PATH` CLI has passing help, positive terminal/observed-refresh fixture publication, and named nonzero refusal coverage. The profile-gate test rejects a wrong, incomplete, aliased, or altered terminal or observed-refresh staged set before replacement; a valid CLI publish rehashes live bytes against the attestation before returning zero.</acceptance_criteria>
  <done>CR-07 has a durable, fail-closed all-or-nothing publication boundary for canonical evidence and the later staged terminal set before closeout readers run.</done>
</task>
</tasks>

## Artifacts this phase produces
- `tools/canonical-recovery.py` — shared recovery guard, reader-registration API, profile-allowlisted journaled publisher, and the tested `publish-validated-canonical-set` CLI for terminal and observed-refresh canonical generations.
- `tools/validate-research.py` — recovery-guarded canonical reader and consolidation routes.
- `tools/validate-planning.py` — recovery-guarded direct preflight and terminal-completion reader routes.
- `tests/phase1/test_spike_consolidation.py` and `tests/phase1/test_preflight.py` — interruption, direct-reader, all-reader, and staged terminal-set regression coverage.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| staged bytes → canonical target set | A process interruption can leave different targets from different generations. |
| journal → recovery reader | A tampered journal can otherwise direct a reader to unsafe or mixed bytes. |
| concurrent publisher → canonical evidence | A second writer can race a partially completed transaction. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-42-01 | Denial of Service | interrupted publication | high | mitigate | Task 1 uses fsynced staged/backup bytes, deterministic replacement, and hard-interruption recovery tests. |
| T-42-02 | Tampering | journal and backup material | high | mitigate | Task 1 digest-binds journal state, rejects altered paths or bytes, and blocks reader entry. |
| T-42-03 | Tampering | concurrent publication | high | mitigate | Task 1 retains an exclusive lock and proves deterministic contention failure. |
| T-42-04 | Repudiation | evidence-generation lineage | medium | mitigate | Task 1 retains transaction ID, ordered targets, old/new digests, and terminal state until verification. |
| T-42-05 | Tampering | staged terminal publication | high | mitigate | Task 1 rehashes the exact attested REVERIFICATION/STATE/ROADMAP profile and journals it before all-or-nothing publication. |
| T-42-06 | Tampering | staged observed-refresh publication | high | mitigate | The tested profile-allowlisted publisher CLI requires exact staged digests and validation attestation before a locked, journaled, interruption-recoverable publication; Plan 01-43 supplies the profile-specific regression and real publication path. |
</threat_model>

<verification>
Run the direct-reader, registry, profile-attestation, and interruption probes through the wrapper, then explicit recovery, canonical validation, and fixed-manifest replay.
</verification>

<success_criteria>
CR-07 is closed with durable crash-atomic recovery evidence: no partial canonical generation is readable after interruption, tampering, or contention.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-42-SUMMARY.md` when done.
</output>
