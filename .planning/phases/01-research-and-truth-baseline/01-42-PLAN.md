---
phase: 01-research-and-truth-baseline
plan: 42
type: execute
wave: 4
depends_on: [01-34]
files_modified:
  - tools/canonical-recovery.py
  - tools/validate-research.py
  - tools/validate-planning.py
  - tools/acquire-sources.py
  - tools/refresh-sources.py
  - tools/measure-package-conformance.py
  - tests/phase1/test_spike_consolidation.py
  - tests/phase1/test_preflight.py
autonomous: true
gap_closure: true
requirements: [EVID-01, EVID-02, EVID-04, OPER-01]
must_haves:
  truths:
    - "A hard interruption or journal tampering cannot leave a mixed consolidation generation consumable by any canonical reader."
    - "Every recovery path resolves to a verified complete pre-generation or verified complete post-generation, never an inferred or partial state."
  artifacts:
    - path: tools/validate-research.py
      provides: "Durable journaled consolidation publisher and pre-reader recovery guard"
    - path: tests/phase1/test_spike_consolidation.py
      provides: "Hard-interruption, tampered-journal, contention, and reader-entry regression coverage"
  key_links:
    - from: consolidation journal
      to: all canonical publication targets
      via: "fsynced prepared/committed digest set, exclusive lock, and recovery before canonical reads"
---

<objective>
Publish and recover one complete canonical evidence generation with a durable, fail-closed transaction.

Purpose: Close CR-07 durable crash-atomic publication and recovery after Plan 01-34 has closed CR-05 path/source confinement and CR-08 observed-local versus normative classification, without mixing schema repair with crash-recovery implementation.
Output: A journaled same-filesystem publisher, recovery-before-read guard, and interruption proof.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-34-SUMMARY.md
@tools/validate-research.py
@tests/phase1/test_spike_consolidation.py
@.planning/research/{compatibility-matrix.yaml,drift-register.yaml,open-questions.yaml}
@.planning/spikes/replay-manifest.yaml
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Recover one interrupted canonical generation before any reader consumes it</name>
  <precondition>Plan 01-34 has passed its v1-to-v2 drift migration and confined-fragment validation; the canonical target set is valid before the transaction begins.</precondition>
  <read_first>01-34-SUMMARY.md; tools/validate-research.py; tests/phase1/test_spike_consolidation.py; compatibility-matrix.yaml; drift-register.yaml; open-questions.yaml; replay-manifest.yaml</read_first>
  <files>tools/canonical-recovery.py, tools/validate-research.py, tools/validate-planning.py, tools/acquire-sources.py, tools/refresh-sources.py, tools/measure-package-conformance.py, tests/phase1/test_spike_consolidation.py, tests/phase1/test_preflight.py</files>
  <behavior>
    - `test_recovery_after_each_publish_interruption_is_coherent` injects an interruption after every replacement position and proves recovery exposes either the complete verified old generation or complete verified new generation.
    - `test_recovery_rejects_tampered_or_incomplete_journal_before_reader_entry` proves unknown state, modified journal bytes, missing backup, and digest mismatch fail before validate, report, fragment, replay, governance, or consolidation readers use canonical files.
    - `test_validate_planning_recovers_or_refuses_before_any_canonical_read` directly invokes `tools/validate-planning.py` after each injected interruption and journal-tampering fixture, proving it recovers a complete generation or returns nonzero before preflight/completion reads any canonical artifact.
    - `test_all_canonical_reader_entrypoints_guard_recovery` directly exercises the declared reader registry and proves missing, malformed, tampered, or incomplete journals stop every reader before canonical input is parsed.
    - Lock contention produces a stable nonzero result and does not publish bytes from either writer.
  </behavior>
  <action>Start RED in `SpikeConsolidationTests` with copied planning roots and deterministic interruption points. Replace in-memory rollback with a same-filesystem transaction directory and durable journal recording transaction ID, ordered targets, old/new SHA-256, staged and backup paths, and prepared/committed state. Write and fsync staged bytes and backups, fsync transaction directories, atomically write and fsync the prepared journal, replace targets in a deterministic order with target and parent-directory fsync, verify each new digest, atomically record committed state, and remove transaction material only after terminal verification. Under the existing exclusive lock, recover a prepared or publishing journal to the fully verified old target set and verify a committed journal as the fully verified new target set. Reject unknown state, a malformed or modified journal, missing backup, digest mismatch, or unsafe transaction path with a nonzero result and no reader continuation. Addresses review concern: recovery must precede every canonical reader, including `tools/validate-planning.py` invoked directly. Implement one shared `recover_before_canonical_read()` primitive in `tools/canonical-recovery.py` and invoke it before the first canonical-file read in every route of `tools/validate-research.py`, `tools/validate-planning.py`, `tools/acquire-sources.py`, `tools/refresh-sources.py`, and `tools/measure-package-conformance.py`, including validation, report validation, impact-fragment validation, replay validation, governance validation, consolidation, preflight, and terminal-completion routes. Maintain a closed registry of reader entry points; tests must fail when an inventory member lacks the guard. Preserve additive history and idempotency under D-13, D-20, D-29 through D-32, and D-36; recovery does not create upstream facts, accept risk, or change ADR state.</action>
  <verify><automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_spike_consolidation.SpikeConsolidationTests.test_recovery_after_each_publish_interruption_is_coherent && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_spike_consolidation.SpikeConsolidationTests.test_recovery_rejects_tampered_or_incomplete_journal_before_reader_entry && tools/phase1-python tools/validate-research.py recover-consolidation --research .planning/research && tools/phase1-python tools/validate-research.py validate --root .planning/research --report /tmp/phase1-recovery-validation.md && tools/phase1-python tools/validate-research.py replay-spikes --manifest .planning/spikes/replay-manifest.yaml --check</automated></verify>
  <acceptance_criteria>Each injected interruption is recovered to a complete digest-verified generation; named recovery tests fail if absent; every malformed journal and lock-conflict path is nonzero and nonpublishing; canonical validation and fixed-manifest replay run only after successful recovery.</acceptance_criteria>
  <done>CR-07 has a durable, fail-closed all-or-nothing publication boundary before later closeout readers run.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| staged bytes → canonical target set | A process interruption can leave different targets from different generations. |
| journal → recovery reader | A tampered journal can otherwise direct a reader to unsafe or mixed bytes. |
| concurrent publisher → canonical evidence | A second writer can race a partially completed transaction. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-42-01 | Denial of Service | interrupted publication | high | mitigate | Task 1 uses fsynced staged/backup bytes, deterministic replacement, and hard-interruption recovery tests. |
| T-42-02 | Tampering | journal and backup material | high | mitigate | Task 1 digest-binds journal state, rejects altered paths or bytes, and blocks reader entry. |
| T-42-03 | Tampering | concurrent publication | high | mitigate | Task 1 retains an exclusive lock and proves deterministic contention failure. |
| T-42-04 | Repudiation | evidence-generation lineage | medium | mitigate | Task 1 retains transaction ID, ordered targets, old/new digests, and terminal state until verification. |
</threat_model>

<verification>
Run both exact named recovery probes through the wrapper, then explicit recovery, canonical validation, and fixed-manifest replay.
</verification>

<success_criteria>
CR-07 is closed with durable crash-atomic recovery evidence: no partial canonical generation is readable after interruption, tampering, or contention.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-42-SUMMARY.md` when done.
</output>
