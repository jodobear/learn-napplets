---
phase: 01-research-and-truth-baseline
plan: 49
type: execute
wave: 2
depends_on: [01-34, 01-43, 01-47]
files_modified:
  - tools/validate-research.py
  - tools/migrate-phase1-records.py
  - tools/refresh-sources.py
  - tests/phase1/test_drift.py
autonomous: true
gap_closure: true
requirements: [EVID-02, OPER-01]
must_haves:
  truths:
    - "New drift synthesized from a local spike uses an explicit observed-local side with `normative: null`, retained report and measurement provenance, a blocked state, and no upstream authority fields."
    - "Consolidation semantically validates staged drift before publication, so no invalid identical-side record can reach a canonical generation."
    - "Drift migration leaves the caller-provided nested input object byte-for-byte equivalent in meaning, and refresh can resume after an abnormal lock-holder termination."
    - "P-EVID02-ADJ, P-EVID02-EMPTY, and P-EVID02-ORDER retain concrete distinct-side, incomplete-side refusal, and stable ID/impact/history ordering predicates; P-OPER01-IDEMPOTENCY retains deterministic duplicate reduction."
  artifacts:
    - path: tools/validate-research.py
      provides: "Observed-local drift synthesis plus semantic staged-drift validation."
      contains: "new_drift_record"
    - path: tools/migrate-phase1-records.py
      provides: "Non-mutating drift v1-to-v2 migration."
      contains: "migrate_drift_v1_to_v2"
    - path: tools/refresh-sources.py
      provides: "Process-lifetime refresh report locking."
      contains: "acquire_lock"
    - path: tests/phase1/test_drift.py
      provides: "Drift classification, copy-semantics, idempotency, and stale-lock regressions."
  key_links:
    - from: new_drift_record
      to: validate_drift
      via: "New local evidence matches the existing exclusive observed-local semantic form before the publication map is admitted."
    - from: refresh report lock
      to: refresh process lifetime
      via: "An advisory flock releases automatically when the holder exits, allowing a later deterministic comparison run."
---

<objective>
Repair local-versus-normative drift synthesis and make drift maintenance safe after abnormal process conditions.

Purpose: Close CR-03, WR-02, and WR-03 while preserving parallel evidence, blocker routing, and deterministic review work.
Output: Valid observed-local synthesis, non-mutating migration, and recoverable refresh locking.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-FINAL-VERIFICATION.md
@.planning/phases/01-research-and-truth-baseline/01-REVIEW.md
@.planning/phases/01-research-and-truth-baseline/01-34-SUMMARY.md
@.planning/phases/01-research-and-truth-baseline/01-43-SUMMARY.md
@.planning/phases/01-research-and-truth-baseline/01-47-SUMMARY.md
@tools/validate-research.py
@tools/migrate-phase1-records.py
@tools/refresh-sources.py
@tests/phase1/test_drift.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Synthesize one local spike observation without inventing a normative side</name>
  <precondition>Plan 01-47's named journaled-consolidation and malformed-transaction regressions pass, and a fresh exact-plan review authorizes the current Plan 01-49 execution set.</precondition>
  <read_first>01-REVIEW.md CR-03; 01-FINAL-VERIFICATION.md gap 3; 01-34-SUMMARY.md observed-local contract; tools/validate-research.py `new_drift_record`, `merge_fragments`, `validate_drift`, and consolidation staging; tests/phase1/test_drift.py drift fixtures</read_first>
  <files>tools/validate-research.py, tests/phase1/test_drift.py</files>
  <behavior>
    - `new_drift_record()` for a local impact fragment returns `normative: null` and an `OBS-*` observed object carrying classification, report path/digest, measurement path/digest, statement, and fallback.
    - The observed-local object has no claim, source, commit, path, locator, content-digest, authority, maturity, or evidence-class fields.
    - `validate_drift()` accepts this generated record, rejects an identical upstream-side fixture, and a consolidation candidate with any semantic drift error does not call the publisher.
  </behavior>
  <action>Start RED with a realistic copied fragment that supplies its retained report and measurement paths/digests, then directly run `new_drift_record()` and the staged consolidation path. Replace the duplicate source-derived `normative` and `observed` side construction with the locked observed-local shape: `normative` is null; the observed object uses a stable `OBS-*` ID, classification `observed-local`, retained report/measurement provenance, a bounded local statement, and the existing safe fallback. Keep the generated drift record blocked, impact-scoped, review-pending, and routed to its dependent decision; do not source a claim or upstream authority field from local spike output. Before `consolidate_spike_impacts()` calls the Plan 01-47 publisher, run semantic `validate_drift()` against the staged documents and the staged source/claim indexes, propagating errors as a non-publication refusal. Apply D-12, D-14 through D-20, D-26, D-29 through D-32, and D-36: parallel upstream evidence remains parallel, local observations remain local, and unresolved meaning stays an explicit review task.</action>
  <verify>
    <automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_drift.DriftSchemas.test_new_drift_record_is_observed_local_and_semantically_valid && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_drift.DriftSchemas.test_parallel_sides_are_distinct_and_adjacent && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_drift.DriftSchemas.test_drift_rejects_empty_null_and_single_sides && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_spike_consolidation.SpikeConsolidationTests.test_consolidation_rejects_semantically_invalid_staged_drift_before_publish</automated>
  </verify>
  <acceptance_criteria>A generated local record passes the observed-local branch of semantic validation, cannot contain normative authority fields, and semantic invalidity prevents a journal publication from beginning.</acceptance_criteria>
  <done>CR-03 no longer conflates local observations with upstream evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve caller data and recover refresh work after process death</name>
  <read_first>01-REVIEW.md WR-02 and WR-03; tools/migrate-phase1-records.py `_canonical_record` and `migrate_drift_v1_to_v2`; tools/refresh-sources.py `acquire_lock` and report write path; tests/phase1/test_drift.py `RefreshComparisonTests`</read_first>
  <files>tools/migrate-phase1-records.py, tools/refresh-sources.py, tests/phase1/test_drift.py</files>
  <behavior>
    - A nested migration input with unsorted impact/history lists is deep-equal to its pre-call copy after `migrate_drift_v1_to_v2()` returns a canonical v2 output.
    - A child process acquires the report lock and is terminated; a subsequent refresh process obtains the lock and produces its deterministic report within the test timeout.
    - Normal duplicate and conflicting refresh tests retain their one-result idempotency and ambiguity behavior.
  </behavior>
  <action>Start RED with a migration fixture that deep-copies the caller document before invocation and compares every nested impacts/history value after return. Make `migrate_drift_v1_to_v2()` create its working document with `copy.deepcopy()` before `_canonical_record()` sorts any nested list; preserve its deterministic output and input-error behavior. Replace the persistent exclusive-create lock in `refresh-sources.py` with an advisory `fcntl.flock` context that holds an open descriptor through report staging and replacement, releases on normal completion, and is reclaimed by the OS on process termination. Keep the existing short bounded contention timeout and nonzero busy diagnostic; never delete a lock based solely on a guessed PID or elapsed clock. Add a subprocess termination fixture that waits for lock acquisition, terminates the holder, runs a second refresh, and asserts successful report generation. Apply D-13, D-20, D-33, D-34, and D-36: refresh stays comparison-only, preserves accepted claim bytes, produces deterministic review work, and makes no approval or ADR transition.</action>
  <verify>
    <automated>PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_drift.DriftSchemas.test_drift_migration_does_not_mutate_caller_input && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_drift.RefreshComparisonTests.test_refresh_recovers_after_lock_holder_termination && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_drift.RefreshComparisonTests.test_duplicate_refresh_observations_are_idempotent && PYTHONPATH=tests/phase1 tools/phase1-python -m unittest test_drift.RefreshComparisonTests.test_conflicting_refresh_observations_are_one_ambiguous_result</automated>
  </verify>
  <acceptance_criteria>The migration never mutates caller-owned nested values, and a terminated refresh holder cannot permanently block a subsequent deterministic comparison report.</acceptance_criteria>
  <done>WR-02 and WR-03 are covered by executable failure-path regressions.</done>
</task>

</tasks>

## Artifacts this phase produces
- `tools/validate-research.py::new_drift_record` — explicit `OBS-*` observed-local synthesis with null normative evidence.
- `tools/validate-research.py::consolidate_spike_impacts` — semantic staged-drift guard before shared publication.
- `tools/migrate-phase1-records.py::migrate_drift_v1_to_v2` — deep-copy migration ownership boundary.
- `tools/refresh-sources.py::acquire_lock` — advisory process-lifetime lock context.
- `tests/phase1/test_drift.py::DriftSchemas.test_new_drift_record_is_observed_local_and_semantically_valid` — CR-03 regression.
- `tests/phase1/test_drift.py::DriftSchemas.test_drift_migration_does_not_mutate_caller_input` — WR-02 regression.
- `tests/phase1/test_drift.py::RefreshComparisonTests.test_refresh_recovers_after_lock_holder_termination` — WR-03 regression.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| local spike output → drift record | A local observation could be published as a normative upstream source. |
| caller object → migration canonicalizer | In-place canonicalization could modify an upstream caller's state. |
| refresh process lifecycle → report lock | An abnormal exit could indefinitely deny later review work. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-49-01 | Spoofing | drift synthesis | high | mitigate | Task 1 uses the exclusive observed-local schema form and semantically rejects inherited authority fields. |
| T-49-02 | Tampering | staged drift publication | high | mitigate | Task 1 runs semantic drift validation before passing bytes to the shared publisher. |
| T-49-03 | Tampering | migration input | medium | mitigate | Task 2 deep-copies before sorting nested structures and proves caller preservation. |
| T-49-04 | Denial of Service | refresh locking | high | mitigate | Task 2 uses OS-lifetime advisory locking and a terminated-holder regression. |
</threat_model>

<verification>
Run the generated-local-drift, invalid-staged-drift, caller-copy, terminated-lock, duplicate, and ambiguity tests through the wrapper. Later review checks their mapping to the current CR/WR register.
</verification>

<success_criteria>
Volatile evidence keeps local and upstream provenance distinct, and drift operations remain deterministic and available after abnormal execution conditions.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-49-SUMMARY.md` when done.
</output>
