---
phase: 01-research-and-truth-baseline
plan: 49
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/build-site.py
  - tests/site/test_static_site.py
autonomous: true
gap_closure: true
requirements: [EVID-01]
must_haves:
  truths:
    - "The static-site builder accepts a source only when immutableUrl is a GitHub blob URL containing a lowercase 40–64 hexadecimal commit and sourceIdentity names the identical repository, commit, and normalized path per D-04."
    - "Every source digest is exactly 64 lowercase hexadecimal characters, and every retrievedAt value is an RFC3339 timestamp with Z or a numeric timezone offset."
    - "Empty, null, malformed, mutable branch/tag, and cross-field-mismatched source metadata fail before any public output is written."
  prohibitions:
    - requirement_id: EVID-01
      category: transparency
      status: unresolved
      verification: null
      statement: "MUST NOT label mutable or metadata-mismatched source input as immutable evidence."
  flagged_assumptions:
    - "EVID-01 encoding probe remains unresolved outside the deliberately ASCII repository/commit grammar and exact Unicode path comparison; no Unicode normalization or percent-decoding rule is inferred."
    - "EVID-01 ordering probe remains unresolved because source-record order is not part of CR-08 identity validity; this plan does not invent an ordering contract."
  artifacts:
    - path: tools/build-site.py
      provides: "Cross-field immutable GitHub source identity, SHA-256, and timezone-qualified retrieval validation."
      contains: "validate_source_identity"
    - path: tests/site/test_static_site.py
      provides: "Positive and negative source-identity boundary regressions."
      contains: "test_builder_rejects_mutable_branch_and_tag_source_urls"
  key_links:
    - from: "tools/build-site.py::validate_content"
      to: "tools/build-site.py::validate_source_identity"
      via: "Every sources.* record crosses the complete immutable-identity validator before render_all or atomic_write."
      pattern: "validate_source_identity"
---

<objective>
Close CR-08 by making the public source ledger reject every mutable, malformed, or internally inconsistent source identity.

Purpose: Restore the EVID-01 trust boundary promised by the generated sources page without changing canonical research records or weakening evidence labels.
Output: A strict stdlib-only source validator and exhaustive mutation regressions in the existing static-site test module.
</objective>

<execution_context>
@/home/at/.codex/gsd-core/workflows/execute-plan.md
@/home/at/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-research-and-truth-baseline/01-CONTEXT.md
@.planning/phases/01-research-and-truth-baseline/01-FINAL-VERIFICATION.md
@.planning/phases/01-research-and-truth-baseline/01-REVIEW.md
@tools/build-site.py
@tests/site/test_static_site.py
@site/content/site.json
</context>

<scope_boundary>
Repair only CR-08 validation and its tests. Preserve site content, generated page meaning, stdlib-only operation, all canonical source records, PRE118, package/runtime/browser blockers, and the no-production/no-transition boundaries. No external API, package, schema, deployment, publication, ADR, or state mutation is authorized.
</scope_boundary>

<assumption_delta_decision>
Generalized noun: immutable source identity. Decision: no-change. The detector fired on the roadmap verb “choose,” but CR-08 introduces no alternative identity model; it enforces the already-canonical repository + commit + path tuple from D-04.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Enforce one complete immutable-source identity before rendering</name>
  <read_first>tools/build-site.py constants, `text`, `validate_content`, `render_sources`, and `build`; site/content/site.json sourceIdentity/immutableUrl/digest/retrievedAt examples; 01-REVIEW.md CR-08</read_first>
  <files>tools/build-site.py, tests/site/test_static_site.py</files>
  <behavior>
    - A valid existing source record with `owner/repository@commit:path` and the matching GitHub `/blob/commit/path` URL builds successfully.
    - A branch or tag in the blob URL is rejected even when every other source field is nonempty.
    - A repository, commit, or path mismatch between sourceIdentity and immutableUrl is rejected before `render_all` and before output creation.
  </behavior>
  <action>Write the failing happy-path/mutable-path/mismatch tests first. Replace generic SAFE_URL acceptance with named full-match grammars `IMMUTABLE_GITHUB_BLOB_URL` and `SOURCE_IDENTITY`, then add `validate_source_identity(source, label)`. Parse both forms into repository, commit, and path; require lowercase 40–64 hexadecimal commits, two nonempty repository components, a nonempty normalized path with no empty, dot, dot-dot, backslash, query, fragment, or encoded-separator ambiguity, and exact component equality. Call this helper from `validate_content` for every source before rendering, per D-04 and D-12. Keep output escaping, fixed routes, and atomic writes unchanged.</action>
  <verify>
    <automated>PYTHONPATH=tests/site tools/phase1-python -m unittest test_static_site.StaticSiteBuildTests.test_build_emits_the_complete_four_page_learning_path_and_knowledge test_static_site.StaticSiteBuildTests.test_builder_rejects_mutable_branch_and_tag_source_urls test_static_site.StaticSiteBuildTests.test_builder_rejects_mismatched_source_identity_components -v</automated>
  </verify>
  <acceptance_criteria>Current source records build, while mutable refs and each repository/commit/path mismatch return nonzero before any output tree is created.</acceptance_criteria>
  <done>Public source URLs and sourceIdentity values form one exact immutable tuple instead of independent nonempty labels.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject malformed digest, retrieval time, and empty identity dimensions</name>
  <read_first>Task 1 validator and tests; Python stdlib `datetime.fromisoformat` behavior already available in the project interpreter; 01-FINAL-VERIFICATION.md CR-08 missing cases</read_first>
  <files>tools/build-site.py, tests/site/test_static_site.py</files>
  <behavior>
    - Digests shorter or longer than 64 characters, uppercase hexadecimal, or non-hexadecimal are rejected.
    - Retrieval timestamps without timezone, date-only values, invalid calendar/offset values, and arbitrary text are rejected; existing `Z` values pass.
    - Null or empty sourceIdentity, immutableUrl, digest, and retrievedAt values remain deterministic content errors.
    - Every negative mutation leaves the requested output directory absent or empty.
  </behavior>
  <action>Add `SHA256` and `RFC3339_WITH_ZONE` full-match grammars plus a `validate_retrieved_at` helper. Require the RFC3339 date-time separator, clock fields, optional fractional seconds, and either `Z` or `±HH:MM`; then parse the normalized value with stdlib datetime and require an aware offset to catch impossible dates and offsets. Expand the table-driven test mutations to cover malformed identity syntax, digest case/length/characters, timestamp timezone/shape/value, null/empty fields, and cross-field mismatches. Preserve D-18's separate evidence/status fields and do not rewrite input records.</action>
  <verify>
    <automated>PYTHONPATH=tests/site tools/phase1-python -m unittest test_static_site.StaticSiteBuildTests.test_builder_rejects_malformed_source_identity_digest_and_timestamp test_static_site.StaticSiteBuildTests.test_builder_rejects_mismatched_source_identity_components test_static_site.StaticSiteBuildTests.test_build_is_byte_reproducible_and_check_detects_drift -v &amp;&amp; tools/phase1-python tools/build-site.py --check</automated>
  </verify>
  <acceptance_criteria>All requested malformed metadata classes fail deterministically, valid checked-in content remains byte-reproducible, and the checked-in generated site passes `--check`.</acceptance_criteria>
  <done>CR-08 has direct positive, mutable-ref, malformed-field, empty-field, and mismatch regressions.</done>
</task>

</tasks>

## Artifacts this phase produces

| Path | New symbol/test | Contract |
|---|---|---|
| `tools/build-site.py` | `IMMUTABLE_GITHUB_BLOB_URL` | Captures exact GitHub repository, lowercase immutable commit, and normalized path. |
| `tools/build-site.py` | `SOURCE_IDENTITY` | Captures the canonical `repository@commit:path` tuple. |
| `tools/build-site.py` | `SHA256` | Accepts exactly 64 lowercase hexadecimal characters. |
| `tools/build-site.py` | `RFC3339_WITH_ZONE` | Accepts only date-time values with `Z` or numeric offset. |
| `tools/build-site.py` | `validate_source_identity` | Cross-checks URL and sourceIdentity components before rendering. |
| `tools/build-site.py` | `validate_retrieved_at` | Parses and rejects invalid or timezone-naive retrieval values. |
| `tests/site/test_static_site.py` | Three named CR-08 regressions from Tasks 1–2 | Proves mutable, malformed, empty, and mismatched inputs fail without output. |

No record field, file path, CLI flag, external dependency, or package is added.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| structured site content → public HTML | Untrusted or stale source metadata can be rendered as an immutable ledger claim. |
| sourceIdentity ↔ immutableUrl | Independently plausible values can spoof a coherent evidence identity unless cross-checked. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-49-01 | Tampering | `validate_content` source boundary | high | mitigate | Full-match immutable URL/identity/digest/time validation plus fail-before-write mutation tests; this HIGH threat blocks completion until all tests pass. |
| T-01-49-02 | Spoofing | sourceIdentity ↔ URL tuple | medium | mitigate | Parse both representations and require exact repository/commit/path equality. |
| T-01-49-03 | Repudiation | retrievedAt evidence label | medium | mitigate | Require and parse a timezone-qualified RFC3339 instant so undated or timezone-naive evidence cannot render. |

No package-manager install occurs; supply-chain package legitimacy gate is not applicable.
</threat_model>

<verification>
Run both named focused test groups and `tools/phase1-python tools/build-site.py --check`. Reject completion on any HIGH-threat mutation case, output side effect, or generated-output drift.
</verification>

<success_criteria>
The public ledger can no longer call a branch, tag, malformed tuple, bad digest, naive timestamp, or mismatched source record immutable; valid current output remains reproducible.
</success_criteria>

<output>
Create `.planning/phases/01-research-and-truth-baseline/01-49-SUMMARY.md` when done.
</output>
